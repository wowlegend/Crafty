# Crafty — Loop-Machinery Review (2026-06-15)

> Source: background workflow `crafty-loop-machinery-review` (wf_fe4d5418-763, 4 agents). Kevin asked (2026-06-15): "does the loop prompt + loop charter + anything loop-related need updating given the massive changes + big steers? run a workflow to deeply look into these." **Answer: YES** — the charter + kernel still describe the superseded "steady-state hold / one tiny self-validatable unit per tick" era; live work flipped to the multi-front, workflow-orchestrated mega-directive overhaul. This doc is the apply-list.

## Headline
Stop the kernel inlining per-slice done-flags + gate counts that demonstrably drift (point it at ACTIVE_PLAN + the plan doc for volatile state), and add the four first-class concepts the new mode runs on — **workflow-orchestration** (with a test-growth exemption + a T3 verify-claims gate), **batch re-baseline cadence**, the **required live-probe-and-LOOK verification axis**, and the **Kevin-confirmed Ember-Frontier direction** — while keeping the still-correct invariants verbatim (verify-before-assert, TDD ratchet, capture-determinism, the visual-gate re-capture hazard, anti-tunneling, no-AI-footer, ask-gates). Secondary-but-urgent: `KEVIN-REVIEW-BATCH.md` is in contract-drift.

## LOOP-CHARTER.md edits to apply (12) — apply next tick
1. **§0** add a CONFIRMED-DIRECTION clause: "Ember Frontier + grafted Blight-Heart climax" (decision-of-record; reversal = genuinely-Kevin, affirming a sub-direction = loop authority).
2. **§0 precedence chain** — name the 3 mega-directive docs as canonical artifacts (keep the mechanism verbatim).
3. **§1.3** retitle "PICK exactly ONE COMMITTABLE unit (multi-front aware)" — a unit = code slice OR Workflow-launch OR Workflow-artifact-integration; background workflows run in parallel, don't block the tick.
4. **§1.4** promote Workflows to a first-class activity (ultracode): when to spin background vs inline; test-growth exemption for a workflow-launch tick; **T3-trust caveat** — verify agent/workflow claims against LIVE code before acting (the audit corrected its own false "breaking already shatters" claim).
5. **§1.5** VERIFY — don't hardcode the gate count (read N live from ACTIVE_PLAN; 20 as of 2026-06-15); KEEP the iter-159 visual-gate-hazard paragraph verbatim; ADD the **LIVE-PROBE + LOOK required axis** (headless gates are blind to input/camera/feel/audio — shipped a dead mouse-look + dead touch cold-start green; gate = 6% pixelmatch NOT md5; swiftshader≈GPU but not a real finger/ear).
6. **§1.8** CONTINUE — three-state cadence: ~60-120s while committable units remain; await+integrate when a background workflow gates the next build; ~30min ONLY if fully Kevin-blocked. Never end the loop / idle-spin.
7. **§2** rewrite item (4): the active structural spine = the mega-directive overhaul (S3 de-monolith is largely-done/parked = a debt lane, not the spine); insert a NEW item (6) **Codebase-debt triage** ("treat ALL pre-masterplan code suspect") citing the audit's ranked P0 chain + the don't-double-tune sequencing note (audit ranks 1/7/13/14 co-sequence with the visual slices).
8. **§3** add a workflow-iteration exemption to the "unit count holds-or-grows" invariant (the destructive ratchet stays fully in force for code ticks).
9. **§4** add a BATCH re-baseline cadence addendum (accumulate before/after pairs across a campaign → one consolidated KEVIN-REVIEW taste review; each re-baseline still gets its HD self-eyeball + commit + rationale at ship time).
10. **§4 genuinely-Kevin list** add two categories: NEW-DIRECTION CONFIRMATION (a pick, distinct from a reversal) + adding any NEW dev dependency / test substrate / install.
11. **§6** supersede the stale 2026-06-10 seed list with pointers to the 2 new file:line-cited backlogs + the **ANTI-REDO** fact (ToneMapping already NEUTRAL + MOOD_GRADE already ships — do NOT re-tune).
12. **§5 process deltas** add the **capture-mode-lied** finding (capture disabled cast shadows + landmark emissive crowns → reviewed baselines were the flattest, beacon-less version, not real play → why the live-probe axis is load-bearing).

## Other loop-doc updates (apply next tick)
- **ACTIVE_PLAN.md** — demote the stale "🟢 ALL SELF-VALIDATABLE FRONTS DONE / steady-state ~45min hold" block + the "NEXT UNIT = OCEAN/COAST" blockquote to history (both superseded); keep it a VOLATILE RESUME POINTER (route open Kevin-decisions OUT to KEVIN-REVIEW, not the banner); update the live cursor (HEAD=192039e; S5+S4 shipped; next=S1/S2 visible-beauty).
- **KEVIN-REVIEW-BATCH.md** (contract-drift — charter §1.6 violation; predates this build session, zero mega-directive decisions): append the mega-directive decisions (direction=RESOLVED Ember Frontier; E2E installs=RESOLVED landed; re-baseline cadence=OPEN; the 4 debt decisions); fix the broken numbering (1→13 ascending then 40→14 descending); add a DONE/ARCHIVED partition + a single top "OPEN DECISIONS" index; add a BATCH-TASTE-REVIEW convention; use stable `git show <commit>:.../baseline/<frame>.png` refs never `current/`.
- **CLAUDE.md** — replace hardcoded "18/18 / 1182" style counts with "see ACTIVE_PLAN for live counts".

## Debatable — needs Kevin's call (surfaced 2026-06-15)
1. **Re-baseline cadence** (world-purpose #2, OPEN): (a) batch all into one taste review at ladder-close [recommended], (b) slice-by-slice, (c) pre-approve a re-baseline budget, (d) review only frames over a larger delta. — *Kevin answered earlier: BATCH. (a) adopted.*
2. **Test-growth ratchet exemption** for workflow/artifact ticks — OK, or should each non-code workflow tick flag a deliberate pause?
3. **Cadence floor while a workflow is in flight** — tight poll (more tokens) vs longer back-off (cheaper, slower integrate)?
4. **Gameplay-layer gating** (world-purpose #5): front-load S1-S6 visual beauty (direction-agnostic) before S7-S10 gameplay [default], or point-first?
5. **The 4 batched debt decisions:** procedural-music delete-vs-revive (muted ~300 LOC GCing every frame); spellUpgrades wire-vs-delete (player-facing lie today); brand "CRAFTY RPG" vs "Crafty"; iPad default tier low→med (iPad gets the flattest render today).
6. **"Lit/claimed region warms up" grade-flip** (world-purpose #3): dynamic warm-up (more re-baselines, strongest sense-of-place) vs static global grade?
7. **What still blocks the loop** vs parks-and-continues — confirm only real-device/spend/accounts/publishing/ear+taste/direction-reversal block; everything else (incl. deleting dead code, batched-async visual re-baselines) proceeds autonomously.

## Refreshed canonical /loop kernel (adopt verbatim; do NOT hand-edit per tick)
Stored as the loop's ScheduleWakeup prompt as of 2026-06-15. Reads all volatile state (slice cursor, gate/HEAD counts) from ACTIVE_PLAN + the plan doc rather than inlining it. Full text is the wakeup prompt; see the workflow result `wf_fe4d5418-763` / this commit's loop re-arm.
