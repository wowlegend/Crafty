# Kevin — Review / Decide Batch (Crafty SOTA master-plan autonomous run)

> **✅ W3 M-NPCS COMPLETE 2026-06-18 — on main `2b15e60`, auto-deployed.** The hub now has 4 living, named NPCs standing at their posts — **Bram the Trader** at the stall, **Mara the Smith** at the forge, **Old Pike the Warden** at the watchtower, **Sister Wren** at the cabin. They have floating nametags + gold minimap blips, and walking up + pressing **G** does something role-specific: the merchant opens the trade panel (now titled with his name), the smith opens crafting, the healer fully restores your health + mana, and the warden points you toward the frontier shrine. They never wander. Live-look-verified. NEXT autonomously: **M-AMBIENT** (ambient life — birds/critters/villager idle motion, the "max" living-world tier), then **M-NARRATIVE** (the quest chain — which also gives the warden his quest-log). That completes W3 (living-frontier); then W4 (polish).

> **✅ W3 M-HUB COMPLETE 2026-06-18 — on main `56cc0d3`, auto-deployed.** The Hearth is now a frontier OUTPOST: a forge, a market stall, a watchtower, and a healer's cabin (voxelKit toon buildings) ring the home plinth, each sitting flush on a small stone terrace, with the 4 NPC anchor spots ready for M-NPCS. I live-look-verified they sit flush (no floating). **Bonus catch:** while checking the outpost I found the home **lodge itself had been floating ~5 blocks** above the pad since W2-T7 (a stale render constant after we flushed the pad) — fixed, so the lodge now sits flush too. NEXT autonomously: **M-NPCS** (4 static interactive NPCs at the hub — merchant/smith/guide/healer with talk panels + nametags + minimap blips), then M-AMBIENT (ambient life) → M-NARRATIVE (the quest chain).

> **✅ W3 M-HUD COMPLETE 2026-06-18 — on main `e05f597`, auto-deployed. 1579 unit / 21 visual / build + eslint green.** The HUD is now a full classic-RPG kit: a cooldown **ability bar** (conic-sweep wedges; appears once you unlock an Aspect), a quiet bottom-left **combat-log** ticker, **nametags** floating over mobs/NPCs ("Bonejacker"/"Settler"…), a **target-frame** nameplate, a round **radial minimap** with destination + mob blips (the old square Orbitron minimap is gone), **decluttered chrome** (the always-on CONTROLS cheat-sheet now auto-fades after ~8s and toggles with H; the redundant top-center "Spell:" band + the creative-mode hunger pill are gone), and **far-LOD spawn beacons** (glowing light-shafts pointing to the nearest shrine + the Blight-Heart, visible from spawn). I live-look-verified every element myself. **DECIDE/FYI:** (1) **5 dependabot vulns** now flagged on the repo (1 critical / 1 high / 2 moderate / 1 low) — want a dependency-bump pass? (2) the ability bar sits just above the hotbar at the very bottom-center (desktop-only for now; touch placement is queued for M-AMBIENT) — flag if you want it moved. NEXT autonomously: **W3 M-HUB → M-NPCS → M-AMBIENT → M-NARRATIVE** (populate the Hearth outpost, NPCs, ambient life, the narrative quest chain).
>
> **⚠️ EXECUTION INCIDENT + LESSONS (2026-06-18, M-HUD):** the subagent-driven WORKFLOW that built M-HUD kept hitting transient **503 "Server is temporarily limiting requests" rate-limits** — root cause: it fans out **3 Opus-4.8-1M-context agents per task** (implement + spec-review + quality-review), and many concurrent 1M-context calls hit the capacity ceiling far more than single calls. Tasks .3/.4/.5/.6 failed those clean review passes; **.4 (combat log) never got built** and **.3 (ability bar) shipped with a freeze bug** because its review 503'd. Separately, after a /compaction I mis-read an empty TaskList + 0-byte output as "the workflow died" and started finishing M-HUD as controller — but it was still alive on the same `main`, so we briefly **double-worked** it (resolved harmlessly; git serialised the commits). I finished the tail CONTROLLER single-threaded (immune to the fan-out rate-limits), live-looking every element — which is how I caught the ability-bar freeze bug. **Takeaways (logged to memory):** for this kind of milestone, single-threaded controller execution is more robust to the current transient 503s than a 3×-1M fan-out; and a workflow's liveness must be positively confirmed before assuming it died.

> **W2 (THE LOOK) COMPLETE 2026-06-18 -- on main, auto-deployed.** The daytime game now looks materially different from what you reviewed: warm magic-hour lighting (glowy, form-readable), gloved hands (no more pink flesh-bars), 4 distinct spell shapes, per-element death bursts (green mob stays green), a continuous toon-Caribbean OCEAN (the checkerboard is gone), a cinematic 3D title screen (the childish one is gone), and spawn now reads as a CONTINENT not an island-peak. Every slice was live-look-verified by me before push. **DECIDE WHEN CONVENIENT:** (1) ocean perf -- CPU wave-plane ~4.2ms/frame; want a GPU-shader migration for phone-smoothness? (2) biome variety -- the 9-biome data model is in but the 6 grass-biomes render identically until a terrain-worker wiring pass; want that pass? (3) minor: the title bottom controls-hints are still a muddy token (one-line fix available). NEXT autonomously: W3 (living-frontier -- populate the world: hub, NPCs, nametags, combat log, real ability bar/minimap).

> **TITLE TAGLINE polish (W2-G3, minor):** the new cinematic title screen is live + a big upgrade, but the tagline 'Build - Craft - Cast Spells - Explore' overlaps the 3D hero's robe (low contrast in the middle). Wordmark + CTA are crisp. Quick fix queued (drop the tagline lower or shrink the hero ~5%); flag if you want it prioritized.

> **OCEAN PERF (W2-G2, your call):** the new toon-Caribbean ocean is a CPU-displaced Gerstner plane costing ~4.2ms/frame on the main thread -- fine on desktop, a real cost on iPad/phone (~25% of a 60fps budget). I went CPU-first per our fork. Want me to migrate the wave displacement to a GPU vertex shader (phone-smooth) now, or leave CPU + the cheap interim win (hoist static per-wave normalization)? Non-blocking; default = cheap interim win, revisit GPU if a device probe shows jank.

> **✅ 2026-06-17 — W1 (Purge & Fix) COMPLETE — on main `8b6e3a4`, auto-deployed. 1479 unit / build / eslint green.** Shipped: the dead `gameStarted` flag fixed (ESC=pause + relock guards resurrected; the **CRAFTY-RPG flash is GONE** — live-look-verified); legacy CRAFTY-RPG overlay + the dead **auth subsystem** (localhost:8001 boot-blocker + per-load console error) + the legacy **pet/tame system** (T-key freed) all DELETED; **Mana Potion restores mana** again; **ALL spatial SFX now route through the master limiter**; unique-key console error fixed; boss-reward legendaries registered; spell upgrades charge leveled mana; KeyF resolved to melee-only; bound allies no longer red-eyed; prod console spam DEV-gated; **M-key now opens a real Magic panel** (live-look-verified); dead combat-config / ECS-exports / economy / CSS purged.
> **YOUR EAR/EYE CHECKS (non-blocking — verify on the live site when convenient):** (1) the SFX-volume slider + Mute-All now actually attenuate in-game sound (audibility = your ear); (2) spell-upgrade mana balance now that leveled mana is charged (**xpCost left unwired** — no XP-debit sink exists; confirm OK, or you want me to add one); (3) bound-ally eye color (jade vs red); (4) the M-key panel is a **static spell-reference (wire-not-rip)** — confirm that's the wanted call vs the W3 live cooldown ability bar.
> **W1 FOLLOW-UPS (I'll fold into W2):** wrap the M-key MagicSystem panel in the shared `Modal` primitive (a11y parity — it's the one reachable panel missing #52's focus-trap); **REBUILD a broader brand-conformance gate** (App+MenuSystem; W1 removed the auth-only one); trim 2 stale pet comments; reconcile package-lock devDep drift.

> **✅ 2026-06-17 SESSION SUMMARY — your two greenlit items are DONE (on main, auto-deployed; 1459 unit / 21-state visual / build+eslint clean).**
> - **#51 spell-upgrade pillar REVIVED end-to-end** (`b953477` + predecessors): the upgrade system was dead-wired (buttons existed but did nothing). Now upgrading a spell actually raises its cast damage, the levels PERSIST across sessions, and the panel is reachable (Progression -> Spell Mastery, with per-spell rank pips + Upgrade buttons + a next-rank preview; new 21st `progression-open` capture state). Balance is conservative-safe: L1 of every spell equals the current base damage exactly, mana left flat, so upgrades are strictly-better, never a nerf.
> - **#52 modal a11y COMPLETE** (`4e8b0a4` S1 + `b62580d` S2): all 9 panels were hand-rolled backdrop divs with no screen-reader semantics or focus management. Built ONE shared bold-flat `Modal` primitive (role=dialog + aria-modal + aria-label + a Tab focus-trap + focus-restore, all capture-gated so it's pixel-invisible) and rolled it across every modal. The 3 captured panels stayed **byte-identical (zero re-baseline)** — purely an invisible accessibility win.
> - **OPEN — awaiting your word (non-blocking, I won't build these without a steer):** (1) **#51 balance residuals** — the free-vs-spend economy + mana ramp + damage curve are tuning calls only you can taste-judge in-play; (2) **#52 optional focus-ring** — if you want a *visible* focus outline on the captured panels I'd add it + re-baseline those 3 frames (deliberate visual change, your call); (3) **AuthModal** — the pre-game auth screen is a 9th modal; want it folded into the same a11y treatment? (4) the standing levers: **#44 holistic playtest** of `crafty-sand.vercel.app`, **#10 day-phase dial taste**, and the Kevin-gated audit items below. **I'm now in idle-watch (~30min cadence); I'll only build on your steer or a genuinely-new verified gap.**

> **🔄 REFRESHED 2026-06-10 (pre-loop clean line). LIVE items below; EVERYTHING under "HISTORICAL" further
> down is pre-2026-06-10 — its statuses are superseded (WILDHEART shipped+merged; the look reference was
> locked + built; VOIDHAND designed + M1 shipped; the loot-showcase capture state shipped; music policy is
> now LOOP-OWNED per the charter). Under the autonomous loop this file is the ASYNC review surface: the
> loop appends decisions-of-record + before/after eyeballs here and keeps building; nothing here blocks it
> except the physically-Kevin items.**
>
> **📌 2026-06-16 SOTA-AUDIT AUTONOMOUS BUILD (M1-M5) -- on main; 1380 unit / 20-state visual / build+eslint clean.** Shipped + pushed behind green gates: **M1** game-feel (trauma shake + weight-tiered hitstop + juiceIntensity dial) - **M2** combat readability (directional impact #9 + attack telegraphs #4 [dodgeable mob windup pose+glow + boss lava forming-ring] + death/victory beats #7 [mob dissolve + boss slow-mo + token overlays]) - **M3** settings/a11y (feedback-intensity slider + prefers-reduced-motion + audio SFX/music/master-mute + touch dodge) - **M4** living world (wind-grass REVIVED #5 + Blight-Heart monolith #8) - **M5** systems depth (inventory lost-buy bug FIXED #15 [the lone real autonomous bug]; #13 mining-feel + #11 element-zone-VFX/beast-verbs + #19 squad-depth all VERIFY-DONE = core already built, residuals Kevin-gated).
> - **META-FINDING (please act):** the 16-agent audit OVER-COUNTED gaps -- 3 of 4 M4 items were ALREADY built (shrines had silhouettes+beacons since S8d; far-LOD fog/haze is live in Atmosphere.jsx; biomes are distinct w/ per-biome flora). Only the wind-grass was truly dead. So **M5/M6 likely have far less NEW work than the 20-item backlog implies.** RECOMMEND: do the #44 holistic redeploy + playtest of `crafty-sand.vercel.app` to judge what ACTUALLY still needs work, then re-confirm M5/M6 priorities -- rather than me grinding verify-and-document ticks on already-built items.
> - **🔑 UPDATE — M5 CLOSED 2026-06-16, the pattern HELD throughout (STRONG recommendation now):** every M5 item confirmed the M4 finding -- **#11** (element-zone VFX + all 4 beast verbs WILDHEART/VOIDHAND/SOULBIND/ELEMANCER) and **#19** (squad CORE: follow-ring/engage/attack/leash + fusion) were ALREADY fully wired with SFX+VFX at their execution sites; **#13** mining-direction + **#19** squad-command-layer are DIRECTION calls, NOT gaps. Net: across M1-M5 the autonomous backlog produced exactly ONE real bug-fix (#15 lost-buy) + ONE genuinely-dead system revived (#5 grass) -- everything else was already-built or a Kevin-direction/taste residual. **M1-M5 autonomous build units are now effectively EXHAUSTED; M6 (#14 lighting-bounce / #10 HUD-aliveness / #16 adaptive-audio / #17 pre-game-brand / #20 repo-health) is mostly Kevin-taste/playtest/device-gated.** I've LENGTHENED the loop cadence to ~30min (no more no-op verify ticks at 120s). **The genuine next lever is YOURS: do the #44 holistic redeploy + full-run playtest, then tell me which M6 items (if any) you want built vs which are taste you'll judge in-play.**
> - **New Kevin-scoped (surfaced, NOT built -- design/perf/taste):** **#18 far-landmark visibility** (Blight-Heart readable from afar) needs a bigger camera far-plane [now 500] / less tuned fog / a far-render ring = perf+look call. **#12 biome EXPANSION** (6-9 Whittaker biomes + blending + biome spawn-tables) = a multi-slice worldgen feature + a design call (which biomes fit Ember+Blight). **#50** also now carries the M2 telegraph + death-beat + grass density/size FEEL knobs, **PLUS the M5 residuals:** #11 verb/zone FEEL magnitudes (roar charge-glow / grab phantom-block / bind jade-halo / imbue zone-breath rate); #13 mining DIRECTION (hold-to-mine + crack overlay + per-block hardness = slow survival-mine vs the current fast creative-mine that's good for building); #19 squad COMMAND layer (send/hold/focus stance + formations = commandable RTS-lite vs the current fire-and-forget auto-pack).
>
> **2026-06-16 (post re-grant -- 'keep going autonomously, you decide what is best'):**
> - **#10 day-phase dial -- BUILT + shipped (autonomous, lock-safe).** A persistent sun/moon clock (top-right, below the coin readout) to read the day-night SIEGE cycle at a glance: a bold-flat sky disc + horizon line, an amber sun arcing sunrise-left -> noon-top -> sunset-right, a blue moon below at night, + a DUSK/NIGHT label (warn-colored at dusk). Pure dayPhase() kernel (13 unit tests), capture-suppressed (gate 20/20, NO re-baseline), Game-Loop-isolated (1s interval + ref writes). LOOK-verified via scripts/visual/dayphase-probe.mjs. YOUR CALL (taste, trivial): final corner -- it briefly overlaps the transient DuskWarning/siege toast during night transitions (low-harm: the toast says the same then; the dial's value is the persistent DAY read) + keep-on-touch vs desktop-only (renders on both now).
> - **#51 (NEW high-value finding) -- the spell-upgrade RPG pillar is built-but-DEAD-wired.** world/spellUpgrades.js has a fully-authored level table (Fireball/Iceball/Lightning/Arcane x3: damage/manaCost/level-gate) + a mounted hook wiring getSpellStats/upgradeSpell into the store -- BUT (1) castSpell uses static BASE damage (ignores level), (2) NO UI calls upgradeSpell (unreachable), (3) the hook clobbers a loaded save's spellLevels with all-1s. So upgrades do nothing in combat + the player cannot upgrade. Reviving = a real progression win, mostly autonomous wiring (L1 == base, balance-safe; L2/L3 are level-gated unlocks, NOT an XP-spend). ONE design question: it was extracted from the DELETED AdvancedGameFeatures in the S3 de-monolith -- cut on purpose, or just unfinished? If you want it, I will build cast-wire + an upgrade UI section + the restore-fix with conservative defaults. Say the word.
> - **#51 NOW BUILDING (you greenlit 'do all you decide best').** Shipping the 3 slices (cast-wire / restore-fix / upgrade UI) with conservative defaults. THREE balance-TASTE residuals for your later word (none block): (a) **economy** -- upgrades are FREE + level-gated only (the `xpCost` field is NOT spent; every spell maxes by character Lv5); the gate ladder (xpCost<=100?Lv2:<=200?Lv3:Lv5) is an inconsistent magic-number map. Intended (free+gated) or should it be a real XP/coin spend? (b) **mana** -- shipped FLAT (upgrades are strictly-better: more damage, same mana). Want the per-level mana ramp (a damage/cost tradeoff; the table has a manaCost column)? one-line flip. (c) **damage curve** -- L2/L3 ramps ~2.1-2.5x at max (standard ARPG); flag for taste.
> - **#52 (surfaced) -- modal a11y needs a shared Modal primitive.** The 9 panel modals (Inventory/Magic/Settings/Building/Crafting/Credits/Trading/SpellUpgrade/Chest) lack role=dialog/aria-modal + focus-management (initial-focus / focus-trap / Esc). Doing it RIGHT = a shared Modal wrapper adopted by all panels -- a focused refactor touching 9 capture-adjacent panels (re-baseline risk on inventory-open/achievements-open). I deferred it rather than sprinkle attributes across 9 files (incomplete + eslint-risky). Want the shared Modal primitive + panel migration (with careful re-baselining)? Clean a11y win, just bigger than a single autonomous slice.


> **📌 CURRENT OPEN DECISIONS — 2026-06-15 session snapshot (HEAD `05d33b0`; gates: 1271 unit · 20-state visual · build clean).**
> This session shipped end-to-end + pushed to main: the VISUAL overhaul (AO · de-tile · richer textures · ocean/coast · tamed terrain), the S7-S10 PURPOSE arc (distance-tier stakes · shrine destinations · Blight-Heart climax + win-state · goal onboarding), the SFX OVERHAUL (master-bus limiter · SFX-work-with-music-off fix · victory sting), S6 ORES-BY-DEPTH (dig → depth-banded ores → mine → craft), and S8c-bis SHRINE CHESTS (reach a shrine → a frontier-scaled reward chest). All are CODE-COMPLETE behind green gates; what remains is YOUR call (none block the loop):
> - **#44 — ONE holistic full-run playtest** (Vercel AUTO-DEPLOYS on push, so everything is ALREADY live on `crafty-sand.vercel.app` -- no manual redeploy step). This SUBSUMES the older "needs the redeploy" notes (#37/#38/#39/#40 — camera/mouse-look/touch/music/ocean). Then judge how the whole loop FEELS.
> - **Playtest-tunable curves** (one-line each; I adjust on your word): #41 frontier difficulty · #43 boss-lair distance (~1280) · #46 ore rarity/depth (`world/oreGen.js`) · #47 shrine-chest reward + 12-block radius (`QuestSystem.openChest`) · #50 combat impact FEEL (M2 #9: directional flinch-lean + spark-cone + camera-lurch now follow the hit vector — magnitudes `tilt 0.22` in `game/mobHitFx.js flinchTilt`, cone `0.55` in `GPUSparkSystem`/`biasAlong`, shake bias in `Components`; all sensible defaults, tunable) · #48 de-island = how much sea near spawn (`world/oceanProfile.js OCEAN_CONTINENT_THRESHOLD`; note: world is infinite continents, not a literal island).
> - **Ear / taste** (no headless judgement): #45 SFX mix + the per-voice tuning checklist · #42 shrine look · S10b menu-tagline · #4 shrine-theming.
> - **Standing locks (default KEEP unless you say otherwise):** bold-flat / no-PBR · BLOOM≥0.85 · AO_MIN=0.55 · NEUTRAL tonemap · #3 grade-flip deferred · the 4 debt calls · i18n #73 BLOCKED (needs an id/display-name decouple refactor first).
> The numbered log below is the full historical record (newest first, #48→#1); the four bullets above are the live asks.**
>
> **LIVE — needs Kevin (when convenient; none block the loop):**
> 1. **🎬 Beast-frame eyeball (#64):** the 4 punchy roster frames (`frontend/tests/visual/current/beast-*.png`)
>    still await your ratify/drop call; the capture-reliability fix comes first (the frames flake — tracked).
> 2. **📱 M2 iPad FPS run — READY (2026-06-10, one-tap, ~5 min, bundles #63):** the harness SHIPPED and the
>    desktop gate PASSED (C−B 0.00ms median / +0.10ms p95 vs the 1.5/3.0ms budget, ANGLE Metal M3 Max —
>    full verdict + numbers in `memory/S2B2-M2-PERF.md`). Your part: Mac `cd /Users/kz/Code/Crafty/frontend
>    && npx vite --host` → iPad Safari `http://<mac-LAN-ip>:5173/?perf=B` (wait for the on-screen DONE
>    numbers) → then `?perf=C` → compare medians vs the budget (no Safari inspector needed). **Same session:
>    the #63 WILDHEART golem check** per `memory/S2B1-M2-PERF.md` §4. Does NOT block M3 — the loop proceeds.
>    *M2 decisions-of-record (loop, charter §5; reversal = plan-doc/PhantomBlockSystem edits):* budget pinned
>    at the STATE-REVIEW rec (1.5ms median / 3ms p95, C−B) · phantom pointLight moved to a fixed light pool
>    (always-mounted, intensity-gated — D-scenario-verified no edge hitch) · probe SM-skip in Components
>    (dev-only) · desktop runner = headless-new + Metal (headed Chrome is occlusion-suspended from agent
>    contexts; renderer string stamped in every report).
> 3. **🕹️ Playtest eyeballs queued:** **VOIDHAND FPV-FEEL (2026-06-10, the spec's M3 human gate):** press V
>    near a block → the phantom orbits → LEFT-click HURLS it at a mob (ballistic arc, element spark at
>    impact) → or RIGHT-click SLAMS a 3m AoE at the phantom's current orbit position (time the orbit = the
>    aim skill — a loop taste-decision, flag if it reads as random instead of skillful). Question to answer:
>    "does the aim feel learnable?" **M4 additions to the same playtest:** unlock Kinetic Grasp in the
>    Voidhand tree → day-kills fill the violet bar → V costs 25 → **hurl a mob INTO YOUR WALL = 3× (the
>    anvil — does the bonus READ?)**. *M4 decisions-of-record (reversal = one-liners): HUD bar renders only
>    when the talent is unlocked (no meter for a locked ability; keeps baselines); gold "WALL HIT!" label +
>    wall flash deferred to M7; walls PRISTINE on anvil hits (Decision #3 rec); kinetic = the phantom-rim
>    violet #B36BFF (one identity color per Aspect).* **M7 LOOK lock (2026-06-10, judged in-world):**
>    BEFORE frames at `.superpowers/s2b2-voidhand-m7-refs/held-*.png` — the night phantom already reads as
>    ink-silhouette + crisp violet rim (kept); building: faint emissive face-lift (night identity), impact
>    core-flash, gold WALL HIT! label. Element held-tint = NO (spec rec confirmed). **M7 SHIPPED (iters
>    26-30): AFTER frames are in beside the BEFOREs** (`after-t1-locked-1.png` = the night grass-block
>    identity restored; `flash-slam-v2-*` = the impact glint+bloom; the full flash envelope is numeric —
>    see the design doc). In-playtest: hurl a mob into your wall and watch for the gold WALL HIT! + the
>    bigger flash — that's the 3x landing. **AUDIO (2026-06-10, the interleave unit): the game now SOUNDS its
>    Aspects** — the roar finally roars (low feral sweep), grab chirps a whole-tone shimmer (the voidhand
>    motif), hurl whooshes, slam thumps with the camera kick, anvil hits ping gold. All synthesized in-engine
>    (#74 loop decision: ALL-SYNTH — external gen rejected v1, reversal = S4 side-by-side). EAR CHECK: do the
>    five read as one coherent family with the existing pad/arp? Tunables = the generate*Sound builders.
>    **SOULBIND SNARE (2026-06-10, M4): the third Aspect's verb plays** — unlock Soul Snare in the talent
>    tree, bank Soul on day kills, weaken a hostile to <=30% (its health bar turns JADE = bindable), hold X
>    aimed at it ~1.1s (a jade tether tightens) -> the bind chime + the creature joins you re-tinted jade.
>    UPDATE (M6 shipped): FUSE is live — bind two creatures (spider+zombie / cow+skeleton /
>    skeleton+spider), stand by them, hold X: a jade thread braids them into ONE hybrid (Dreadweaver /
>    Bonehide Bulwark / Marrowspinner). The Soul bar (jade) reads under the Kinetic bar once Soul Snare
>    is unlocked. ****🎼 ASPECT STINGERS (music-motif v2):** each Aspect now SINGS at its signature moment — transform
>    (a primal saw rise), slam (a falling gravity phrase), FUSE (a warm major arp), the day's first
>    zone (a lydian shimmer). All rare by design. EAR CHECK: do they feel premium or intrusive?
>
> **🦴 NEW MOBS (the content interleave):** three new night-siege types — the SKITTERLING (tiny
>    violet spider-legged swarm), the DUSKHOUND (dark-plum fast quad), the MOSS BRUTE (huge slow tank,
>    spawns rare via the new weighted table). Duskhound+skeleton FUSE into the GRIMHOUND. Card:
>    .superpowers/mob-variety-refs/mobs-card-1.png — eyeball IN-GAME at night: do the three read
>    distinct mid-siege? Is the brute scary enough?
>
> **🏆 THE SPINE IS COMPLETE — the ELEMANCER playtest protocol (~4 min):** unlock Elemental Focus →
>    Elemental Imbue in the talent panel (the HOW IT PLAYS card is right above). Build by day — watch
>    the white-gold bar fill from mining/placing. Press Z (the ring haloes the crosshair), cast at the
>    ground: the zone paints + its voice plays. Try: 1 (fire — the burn zone chars the ground when it
>    dies), 2 onto a fire zone (ice — STEAM, both vanish), 4 then 1 onto the rune (arcane — the next
>    zone comes out BIGGER), 3 near water (lightning pulses). Mobs crossing burn; frozen mobs crawl.
>
> **🜔 ELEMANCER M6 (the look) eyeball cue:** zones now RENDER — element-colored breathing rings on
>    ink skirts (fire orange/ice blue/lightning yellow/arcane violet), char scorch where fires burned
>    out, the four synth voices at spawn, the white-gold Resonance bar. The judge card verified
>    color/geometry; the EMISSIVE pop is night-context — eyeball one night siege with zones down:
>    do the rings read premium against dark terrain? (.superpowers/s2b4-elemancer-refs/zones-card-1.png)
>
> **🜂 YOUR ASPECT-UX ASK — SHIPPED:** the talent panel (the Aspects panel) now opens every column
>    with a HOW IT PLAYS card: the key (R/V/X/Z), what banks the meter, and the loop in 3-4 steps
>    (e.g. SOULBIND: weaken to a third [jade bar] → hold X → it fights for you → two together + X =
>    FUSE). Header retitled — the four-powers/four-keys model is stated in one line. EYEBALL: open the
>    talent panel; is the loop clear enough that Marcus could run each Aspect from the card alone?
>
> **🛠️ YOUR 2026-06-10 PLAYTEST FIXES — ALL SIX SHIPPED (re-verify protocol, ~3 min):**
>    (1) RESPAWN: die FAR from spawn (>100 blocks out — the bug only triggered there), respawn → you
>    should land and MOVE immediately; (2) ESC in-game → the PAUSE/settings menu (not the title screen);
>    close it → you're back in control; (3) open/close EVERY panel (inventory/crafting/magic/build/
>    trading/achievements/talents) → the cursor never vanishes mid-menu, the lock restores on close;
>    (4) die → the DEATH SCREEN is unoccluded (no title menu over it), Respawn is clickable immediately;
>    (5) you can NOT fire while dead. Root causes + the 6-commit trail in
>    docs/superpowers/plans/2026-06-11-crafty-kevin-respawn-pointerlock-cleanup.md. FOLLOW-UPS FLAGGED:
>    real world-pause while menus are open (mobs currently keep moving); the 4 panel flags → store.
>
> EYEBALL ASK (2 min, now with the showcase shortcut — `window.__craftyTest.call('soulbindShowcase')` in the dev console renders the 5-creature judge card):** the loop's headless judges verified FUNCTION (mesh census:
>    allies render; fusion births hybrids live) but a clean AESTHETIC frame defeated 4 instruments — in
>    YOUR playtest please eyeball: the jade family (tether/tint/bar) reads as ONE Aspect? the 3 hybrid
>    silhouettes read distinct? (The showcase fixture shipped — the console shortcut above.) **FEEL PASS (post-Aspect): allies now
>    visibly SWING (a squash-stretch pulse on each attack) and binding/fusion lands a jade halo ceremony —
>    watch for both in the siege.)** Also: cool-rim ice/lightning in live play · punchy glow in-world · the FPV
>    beast-form interim treatment once #71 ships · mob knockback/feel after the #68 15Hz-AI change ·
>    **#72 click-feel (2026-06-10):** every click now routes to exactly ONE verb (swinging at mobs can't
>    erode your walls; casts can't place blocks; chest-open is occlusion-correct) — verify mine/place/
>    melee/cast all FEEL unchanged in normal play. **Heads-up: mining/placing had been SILENTLY BROKEN at
>    HEAD** (a rapier field rename made the old listener compute NaN coordinates — sounds played, no real
>    edit) — the router work found + fixed it, so block-editing genuinely works again; if building feels
>    "suddenly different", that's why. *Decision-of-record (loop): target-priority routing, no lanes/modes;
>    a hotbar hand-slot lane model is the recorded reversal path (revisit w/ #71 hotbar honesty).*
> 4. **🔗 SOULBIND (B3) design batch (2026-06-10 — the loop proceeds on the recs, reversals recorded):**
>    the design-of-record is `docs/superpowers/specs/2026-06-10-crafty-s2b3-soulbind-design.md`. Your four
>    calls (§8): (1) v1 = "player stays the tank" (enemies never target allies until the v2 worker-faction
>    milestone — rec YES; the alternative was the schedule risk every design lens flagged); (2) pets vs
>    squad — two companion systems would coexist; rec: keep pets cosmetic, fold tame into SOULBIND at v2;
>    (3) the 3-hybrid roster taste (Dreadweaver/Bonehide Bulwark/Marrowspinner) + spectral jade #3DFFB0;
>    (4) squad cap 2+1-talent, costs 35/50. Also locked en route: kill-bus killer-attribution ships FIRST
>    (closes an ally-AFK-farm exploit that exists the moment any ally can kill).
> 5. **🜔 ELEMANCER (B4) design batch (2026-06-10 — the loop proceeds on the recs):** the design-of-record
>    is `docs/superpowers/specs/2026-06-10-crafty-s2b4-elemancer-design.md`. The headline: v1 is an
>    OVERLAY-ONLY chemistry Aspect (zero voxel edits — your iPad perf gate stays respected; real terrain
>    mutation is a designed v2 seam). Your calls (§8): (1) the fantasy honesty — pristine trees DON'T burn
>    in v1; fire spreads along YOUR built wood + reagent blocks (rec YES: building becomes the fuel);
>    (2) the Resonance meter banks from BUILD verbs, not kills (rec YES — the novel economy);
>    (3) white-gold #F5D76E identity + the four zone looks (judged at M6's showcase card); (4) the tunables.
> 6. **🜔 ELEMANCER M5 playtest cue (2026-06-10):** the verb PLAYS — mine/place by day to bank
>    Resonance (watch it on the save), unlock Elemental Imbue (after Elemental Focus), press Z (the
>    white-gold IMBUE ring appears at screen center), cast at the ground: the element's zone paints
>    in. Fire onto a frozen zone = both vanish (steam); cast onto an arcane rune = the zone comes
>    out bigger. (Zones are logic-only until M6 paints them — judge by mob behavior: burn ticks,
>    frozen crawl, the rune draws aggro.)
> 7. **💰 Standing S4 decisions (unchanged, parked by design):** monetization model · multiplayer scope.
> 8. **📜 Master-plan v2 rewrite (2026-06-10, under your authority grant) — async ratify:** SOTA-INITIATIVE.md
>    fully rewritten as a LIVING plan (v1 preserved at `git show ddfdf96:SOTA-INITIATIVE.md`). One NEW
>    loop-PROPOSED policy inside it needs your eventual yes/no: **S3 early-entry relaxation** — recorded
>    decision stays "S3 after ALL 4 Aspects"; proposed escape hatch = enter at ≥3 Aspects IF a forcing event
>    lands (touch becomes load-bearing externally, or a hard perf wall). Reversal = delete the clause.
> 9. **🧰 Two small items the HISTORICAL sweep would otherwise bury:** #32 vitest 3→4 security bump (dev-only
>    exposure — confirm transfer to loop, or dismiss) · the forced-med/low tier baselines from S2-A-M4b
>    (ratify into the gate, or hand the call to the loop).
>    **UPDATE 2026-06-13 (dependabot #29, now concrete):** `npm audit` = 6 vulns (1 critical + 5 high), ALL in
>    the build/test toolchain (esbuild 0.17–0.28 → vite → vitest/plugin-react/vite-node; GHSA-gv7w-rqvm-qjhr —
>    esbuild Deno-module integrity RCE via `NPM_CONFIG_REGISTRY`). **Dev/build-time ONLY — esbuild/vite are not
>    in the shipped game bundle, so no end-user exposure.** The clean fix is `vite@8` = a 2-major breaking jump
>    from the current Vite 6 (touches build + the vitest config + the visual-capture pipeline) → the loop will
>    schedule it as its OWN migration milestone (folds with the #32 vitest bump above), not a casual fix. No
>    action needed from you; flag if you want it prioritized ahead of the S3 stream.
> 10. **👁️ UX-legibility controls panel — DELIBERATE re-baseline eyeball (2026-06-13, loop iter 98):** the
>    always-on top-right CONTROLS panel was redesigned (renders from the new `game/keyMap.js` SoT; grouped
>    MOVE/COMBAT/ASPECTS/PANELS with keycap-badge chips) so it now TEACHES the four signature Aspect verbs
>    R/V/X/Z (the game identity, previously taught nowhere) + the honest M key. This shifted 2 gated baselines
>    (`explore-day` +8.98%, `explore-night` +8.63%) + the 3 forced-tier explore variants — a DELIBERATE
>    re-baseline (HD self-eyeballed at 2× crop, day + dusk both legible/premium; the other 11 states verified
>    UNAFFECTED via the gate). Before/after: `git show HEAD~?:…/baseline/explore-day.png` vs current. Taste call
>    I made (per the spec): always-visible + tight (curated the 4 secondary keys — Wheel/G/T/Tab — out of the
>    resting panel via a `panelHide` flag; they stay in the SoT; the full reference lives behind U). If you'd
>    prefer collapse-by-default or a different density, it's a one-line change — flag it.
> 11. **🌍 WORLD-DESIGN DIRECTION — ✅ RESOLVED (Kevin 2026-06-13): HYBRID (option C), scheduled as the NEXT UNIT AFTER the boss finale (S3-M4 close).** Kevin picked the hybrid (infinite wilds + crafted home anchor + signature landmarks + biome distinctness + oceans-as-a-place) and the timing (finish the in-flight AGF dissolve first, ~1 iter, then world-design ahead of S3-M5/the interleave). The loop will run a §4 design-gate workflow → committed spec → plan(s) → build. Original framing kept below for context. ⏎
> _(original, now resolved):_ **a recorded plan item that SLIPPED + needed steer (raised loop iter 99):** the world ENGINE is shipped + real (infinite procedural voxel terrain, camera-keyed chunk streaming/culling, water/oceans with bioluminescent wave shaders, biomes, greedy mesher). But the deliberate **"world-design / landmarks pass"** the master plan scheduled for *late-S2 (after B3, before/with B4)* NEVER happened — the loop went S2-B-complete → S3 de-monolith directly; the content interleaves were mob-variety/audio/feel/UX, not world. There is **NO world/terrain/ocean/biome design spec anywhere**. So it's an un-owned, overdue item living only as scattered backlog (§4 world-design/landmarks pass · §5 VISUAL biome/landmark silhouettes · §5 CONTENT biome distinctness · S3 gen-system perf). **Not a hard-block** per charter §4 (no money/publish/account, no reversal of a recorded decision) → the loop CAN self-gate it (design workflow → spec → your async ratify). **THE DIRECTIONAL FORK I need a steer on (or I'll proceed on the loop recommendation at the next interleave slot):** (A) **endless procedural wilds** (current engine; maximal explore, weak memorability), (B) **finite crafted world / island** (signature landmarks, authored oceans + coastlines, strong identity; caps the explore loop), or (C) **HYBRID — loop recommendation:** keep the infinite procedural wilds for the survival/explore loop BUT add a crafted "home" anchor + a handful of signature silhouette landmarks + genuine biome distinctness + oceans that are a *place* (depth, beaches, something out there) not just blue blocks — memorable structure without losing endless-explore. Also undecided: world EXTENT/scale intent, named regions, the ocean's gameplay role. **Want me to pull a world-design design-gate forward as the next interleave (and which of A/B/C)?** Until you say, the loop continues the standard priority (S3 de-monolith → next interleave); I'll self-gate the world pass on the hybrid (C) at its interleave slot unless you redirect.
>
> 12. **🛠️ HARDEN THE VISUAL GATE — fail-loud on a mount-crash / stale current/ (fast-follow, loop iter 105):** the S3-M4 boss-finale adversarial review exposed that the app had been CRASHING at mount since iter 101 (an orphaned `SPELL_UPGRADES` const) — and the visual gate SILENTLY KEPT REPORTING 13/13 for ~4 iterations because `npm run visual:capture` timed out (the app never mounted) yet `diff.test` happily compared the STALE pre-crash `current/` PNGs. Build + unit gates were real but a runtime ReferenceError sails past them. The crash is FIXED + the gate is now LIVE again, but the GATE INFRASTRUCTURE has a hole: it should FAIL LOUD when `current/` is missing/stale or when capture errors/times out (e.g. capture writes a fresh-run sentinel + diff.test asserts current/ is newer than the run start, OR capture exits non-zero on a page error and the visual script aborts). Loop will implement this as a quality-debt fast-follow. No action needed from you; flagged so the regression class is on record.
> 13. **🌍 WORLD-DESIGN — 2 player-facing defaults the loop chose (within your ratified HYBRID; reversible — flag if you want different):** the design-gate spec (`docs/superpowers/specs/2026-06-13-crafty-world-design-hybrid.md`) is committed + the build is laddered (M1 = the Home Anchor next). Two choices have the most player-facing weight, decided on the loop's defaults so it can proceed: **(a) the home anchor = a quiet solo "Hearth"** (lodge+brazier+pier, NO NPCs) rather than an inhabited hamlet (villager mob + trading) — lower risk, ships M1 fastest; a hamlet is a clean fast-follow if you want life there. **(b) deep oceans stay friendly-explorable — NO drowning/oxygen timer** (matches the broad kids→adults pillar); a stakes mechanic is a separate gameplay milestone if you want it. (Two other defaults — SEAM-A visual-only landmarks; make the 3 biomes sharp before adding new flavors — are lower-stakes, loop-decided.) No action needed; the loop builds on these unless you redirect.
> 40. **🎵 MUSIC OVERHAUL SHIPPED — real tracks replace the procedural loops (2026-06-14/15):** added ElevenLabs-generated day/night/boss tracks (`frontend/public/music/*.mp3`) with a smooth crossfade via `src/ui/MusicPlayer.jsx`; the procedural pad/arp is now MUTED (SoundManager PROC_MUSIC_GAIN=0); the biome wind-bed (item #19) is kept. EAR-CHECK owed (the music item batched in #37/#38): load the redeployed build and confirm the day/night/boss transitions + volume read well. Tunables: the crossfade in MusicPlayer.jsx + PROC_MUSIC_GAIN in SoundManager.
> 39. **🖱️ MOUSE-LOOK FIX (desktop) SHIPPED + needs the redeploy (2026-06-14/15):** desktop mouse-look was DEAD — drei PointerLockControls was element-fragile and untestable in the pinned-camera capture harness. Replaced with own `src/input/pointerLook.js` (applyMouseLook/attachPointerLook) + the project's FIRST live-input gate `npm run test:look` (scripts/visual/look-e2e.mjs, real-browser). This is the camera fix referenced in #37/#38 as needing the redeploy. NOTE: distinct from the older per-verb CAMERA-KICK juice (recoil effect, lower in this file).
> 50. **🔬 TOTAL SOTA AUDIT done — your instinct was exactly right; here's the verdict + the few calls only YOU can make (2026-06-16):** I ran a 16-agent deep audit (researched the June-2026 SOTA bar via web search, then scored EVERY element of the game against it). **Verdict: Crafty is a deep, well-tested ENGINE wearing a half-finished COSTUME — ~55-60% genuinely SOTA, ~40-45% still pre-master-plan.** The good half is real (renderer/atmosphere/VFX/Aspect-architecture/audio-synth/test-infra). The pre-master-plan half is exactly what you felt, and it clusters into 4 themes: (1) **works-but-flat/floaty** — flat hitstop, random (non-directional) screenshake, no coyote-time/input-buffer, enemies attack with ZERO wind-up telegraphs, instant pop-out deaths, the wind-grass shader is DEAD CODE so the world has no ground vegetation, landmarks are inert cubes; (2) **built-but-never-wired** — crafting is silent (dead sound), boss kills bank no Aspect meter, ore debris shatters white, and you literally can't mute the game (no audio UI); (3) **off-brand first/last impressions** — the login, loading splashes, and the DEATH/VICTORY screens are raw un-styled text; (4) **below-2026 accessibility** — no reduced-motion/intensity options, and touch has NO dodge button (a core verb unreachable on iPad). **The encouraging part: ~everything is fixable within your locks and I can build it autonomously — I'm starting now** (M1 = a real game-feel core: weight-tiered hitstop + trauma screenshake + a feedback-intensity dial, plus the verified one-line bug fixes). **The handful of calls that are YOURS (I'll ship sensible defaults + surface each):** (a) the render "glow vs restrained" lock — the ONE thing the bold-flat/BLOOM≥0.85 locks constrain; (b) FEEL timing/difficulty (telegraph windups, hitstop ms, shake size) needs your playtest to tune — I'll give you dials; (c) title-screen LOOK; (d) keep-or-delete the muted adaptive-music brain; (e) real-device perf. Full ranked backlog (20 items + 10 quick wins, milestoned M1-M6) in `docs/superpowers/2026-06-16-SOTA-TOTAL-AUDIT.md`. No action blocks the loop — I'm building.
> 49. **🧭 SPAWN NOW SHOWS YOU WHERE TO GO — persistent objective + direction cue (2026-06-16):** you reported "I spawn with no guide/direction; where do I go?" — and you were right. I verified (5-agent sweep) that a RETURNING player had NO persistent direction: the goal toast was one-time (localStorage), the compass shrine/Blight ticks only show when you happen to FACE them, the quest text has no bearing, the minimap plots only mobs. Fixed: a new always-on ObjectiveTracker banner under the compass that NAMES the current goal ("Reach the frontier shrine" -> "Shatter the Blight Heart" once you reach one) and points to it with an arrow + live distance that works no matter which way you're facing. All gated green (1277 tests), capture-safe (zero baseline change). **For YOU (playtest, when convenient): redeploy + spawn** and confirm the banner reads clearly + the arrow points you the right way; flag if the WORDING or PLACEMENT (it sits top-center just under the compass, near the Spell label) wants tuning — both are one-liners in HUD.jsx ObjectiveTracker. **Also: make sure the build you're playing is the LATEST** — everything this session (purpose arc, SFX, ores, shrine chests, this cue) is on main; if crafty-sand.vercel.app is stale you'd see none of it. No action blocks the loop.
> 48. **🏝️ "ON AN ISLAND" — it's actually infinite continents; the fix is one taste dial (your call) (2026-06-15):** I grounded your "seemingly on an island" note before changing anything and found the world is NOT a finite island — it's infinite procedural continents (`continent = noise2D(x*0.002, z*0.002)`, no edge/falloff). So there's nothing to "de-island"; what you're feeling is the LAND/OCEAN RATIO near spawn, set by one dial `OCEAN_CONTINENT_THRESHOLD = -0.15` in `world/oceanProfile.js` (ocean wherever the continent noise dips below it). Lowering it (e.g. -0.15 -> -0.25) shrinks the seas + grows contiguous land so spawn reads as mainland, not an isle — BUT it directly trades against the ocean/coast work you asked for (less ocean to see) + re-shapes the ratified "oceans-as-a-place" hybrid + re-baselines the ocean capture frames. That's a **"how much sea do you want near spawn" taste/scale call**, so I'm NOT re-shaping the world unilaterally. **Tell me which you want and I'll ship it as a re-baseline:** (a) keep as-is (oceans frequent, island-ish), (b) modestly more land (-0.20ish), or (c) clearly mainland (-0.28ish, oceans become occasional seas). One-line change either way. No action blocks the loop.
> 47. **🗝️ SHRINES NOW REWARD YOU — a treasure chest at every shrine, richer on the frontier; playtest it (2026-06-15):** the S8 destination loop now pays off tangibly. Reaching a shrine (within 12 blocks) spawns a guaranteed reward chest at its base, once per shrine; opening it gives the normal chest loot PLUS `(1 + zoneTier)` extra rolls biased to the rarer items — so a far-frontier shrine pays meaningfully more than a near one (the S7 risk/reward curve applied to destinations). Built inside the existing chest system with zero new components + no cross-hook plumbing; all gated green (1271). So the full loop is: see a spire on the horizon -> compass cue -> journey out (riskier + more rewarding the farther you go) -> claim the chest + complete the Pilgrimage quest. **For YOU (async playtest, when convenient): redeploy + walk to a shrine** and tell me if the reward feels worth the trek — too stingy/too generous, the 12-block trigger radius right, the frontier-scaling noticeable? Tunables: the `1 + tier` rolls + the rarer-half bias in `QuestSystem.jsx openChest`, and the 12-block radius in the shrine-chest spawner. No action blocks the loop.
> 46. **⛏️ MINING NOW HAS A PAYOFF — ores generate by depth; playtest the rarity curve (2026-06-15):** the reality-audit flagged that ores were defined + craftable + mob-dropped but NEVER generated in the world, so digging had no reward. Fixed end-to-end across 3 slices (all gated green, bold-flat + NO-RE-MESH preserved): **(gen)** the worker now seeds deep stone with depth-banded ores — coal shallow/common, iron mid, gold deep/rare, diamond deepest/rarest (deterministic by position, capture-safe); **(render)** 4 new bold-flat ore tiles in the voxel atlas (stone base + ore-color nuggets); **(loop)** mining an ore drops its lowercase block key (`coal`/`iron`/`gold`/`diamond`) which the existing recipes already craft with (Iron/Diamond/Gold gear). So the loop is real: dig down -> find ores that get rarer + more valuable with depth -> mine -> craft. **For YOU (async playtest, when convenient): redeploy + dig** and tell me if finding ore feels rewarding — are ores too rare/too common, too shallow/too deep, the depth progression satisfying? Every number is a one-line tunable in `world/oreGen.js` (the per-ore probabilities + min-depths). I can't feel the dig-pacing headless. No action blocks the loop.
> 45. **🔊 SFX OVERHAUL UNDERWAY — a master bus + limiter now governs the whole SFX mix; EAR-CHECK owed (2026-06-15):** starting your mega-directive point (1) "a thorough review + enhancement of all sound effects." The verified GAP: every WebAudio voice (SFX + ambient + the now-muted procedural music) connected STRAIGHT to the speakers, so when many sounds fired at once their peaks SUMMED with no global headroom and could clip/crackle. Slice 1 built a pure `createMasterBus` (gain -> brickwall-ish limiter -> destination, unit-tested); Slice 2 routed all 5 WebAudio voice-sites through it, so the SFX + ambient-wind-bed mix is now limited before the speakers (one headroom point, no clip when a siege of sounds hits at once). Locked by a static gate so a future edit can't re-open a direct-to-speaker voice; 1251 tests green, build clean, visual gate 20/20 (audio is runtime, not pixel-tested). **SCOPE note:** this governs the SFX/ambient mix — the ElevenLabs MUSIC is a separate HTML-`<audio>` path with its own volume (no clip risk), so it's intentionally not in this limiter. **EAR-CHECK owed (I can't hear headless):** in the redeployed build, trigger a busy moment (a night siege with multiple mobs snarling/hitting + UI clicks + a fanfare) and confirm the mix sounds FULLER and never crackles/distorts when many sounds stack. Next slices: autoplay-resume robustness (3) + a per-SFX level/timbre balance review + missing-SFX (4, ear-gated — I'll surface a tuning checklist). All gains/timbres are one-line tunables in `audio/synthVoices.js`; the limiter params in `audio/masterBus.js`. **UPDATE — Slice 3 shipped a real bug-fix (autonomously verified, but worth an ear-confirm):** SFX used to be SILENTLY DROPPED if you played with MUSIC OFF — the AudioContext only ever resumed inside the music-start path, which skips when music is disabled, so a suspended ctx ate every sound effect. Now a dedicated idempotent `resumeAudio()` fires on the entry gesture regardless of the music toggle. **Quick confirm:** turn music OFF in Settings, play, and check SFX are now audible (hits/clicks/fanfares). **UPDATE — Slice 4 + 🔊 SFX OVERHAUL COMPLETE:** found + filled the last real gap — the VICTORY climax (slaying the Blight Heart -> the win screen, the single biggest beat) was SILENT; added a dedicated triumphant `victory` sting (an ascending major run resolving into a held chord, grander than the quick reward fanfare) fired when the win screen appears. So the overhaul is done end-to-end: a master-bus limiter on the whole SFX mix · SFX work with music off · a victory sting on the climax. **What's left is your EAR (all one-line tunables in `audio/synthVoices.js`, the limiter in `audio/masterBus.js`) — a tuning checklist when you redeploy + play:** (1) the **victory** sting — triumphant + earned, or too long/short? (`makeVictorySound` `* 0.28` gain + 1.3s); (2) the **mix** under a busy siege — fuller + no crackle? (limiter `threshold -3`/`ratio 20` in masterBus.js); (3) **footstep** — fires often, too loud/repetitive? (`makeNoise` 0.05 + the play rate); (4) **roar / aggroGrowl** combat snarls — menacing but not fatiguing?; (5) **fanfare** (quest/level-up) vs **victory** — distinct enough?; (6) **siegeHorn / dawnChime** transition stings — balanced under music?; (7) **uiOpen/uiClose** menu foley — subtle? None of this blocks the loop — flag any that read wrong and I'll tune. No action blocks the loop.
> 44. **🏆 THE GAME NOW HAS A POINT, END-TO-END — S7+S8+S9 complete; please REDEPLOY + playtest (2026-06-15):** your core complaint ("what is the point? the character wanders aimlessly on an island") is now answered as a full arc: **(stakes)** distance from spawn ramps danger + loot, so going out is a real risk/reward choice; **(destinations)** tall shrines you see on the horizon + a compass cue + a Pilgrimage quest pull you outward; **(climax)** the Blight Heart — a fixed lair compass-marked from spawn that you journey to and where the dragon awakens; **(victory)** slaying it now shows a real WIN screen ("The Blight Heart is shattered -- the frontier is yours") with a "Keep exploring" handoff so the sandbox continues. All shipped behind 1243 green tests, bold-flat preserved, nothing in the visual baselines disturbed. **For YOU (one combined async session, when convenient): REDEPLOY `crafty-sand.vercel.app`** (gets EVERYTHING this session — AO, de-tile, richer textures, the tier risk/reward, shrines, the climax + win) **then play a run** and tell me how the whole loop FEELS — is the journey-out compelling, the frontier difficulty fair (#41), the shrines/Blight-Heart visible + worth reaching (#42/#43), the victory beat satisfying? That holistic playtest is the one thing I can't do headless. Next I build S10: onboarding that STATES the goal at game start so a brand-new player instantly gets the point.
> 43. **🐉 S9 (in progress) — the boss is now a foreshadowed CLIMAX you journey to (playtest the distance) (2026-06-15):** the dragon was a context-free ambush that auto-spawned 25 blocks from you at level 5 — no destination, no ending. Now it's the Blight Heart: a FIXED far-frontier lair (deep tier 4) that's compass-marked from spawn ("BLIGHT HEART (Nm)", ominous violet) so you see the end-goal on the horizon all game, and the dragon only AWAKENS when a level-5+ player REACHES the lair (~24 blocks). So the boss is the END of the journey, not a random interruption. Shipped: the fixed-site kernel, the compass marker, and the relocation (all gated green, the boss-closeup capture fixture preserved). **For YOU (async playtest, when convenient): the lair is at ~1280 blocks from spawn — is that the right distance for a climax, or too far (want it closer, or a fast-travel/teleport-to-marked-shrine)?** It's one constant (`BLIGHT_RADIUS` in `world/blightHeart.js`). I can't feel the trek headless. **Still building S9c: the WIN-STATE** — a victory beat + "keep exploring" handoff when you slay it (today defeating the boss just gives loot + a notification, no actual ending). After S9c the whole "give it a point" arc is complete: journey stakes -> destinations -> climax -> victory.
> 42. **🗺️ S8 SHIPPED — shrines are now real destinations; the game has a "go there" (2026-06-15):** the wayfinding loop that directly answers your "what is the point? the character wanders aimlessly" — it's now: SEE a tall glowing spire on the horizon -> a "SHRINE (Nm)" cue appears on your compass -> walk there -> the Pilgrimage quest completes (XP), with S7's tier-loot making far shrines worth the trek. Four pieces shipped: a deterministic nearest-shrine finder, the HUD compass marker (cyan, capture-suppressed + gate-locked), the Pilgrimage `reach_shrine` quest (which REPLACED a dead Travel-500 quest that literally never progressed), and taller landmark silhouettes + brighter beacons so shrines read from afar (landmark.png re-baselined; the spire is now an unmistakable tower). All gated green (1235 tests), bold-flat preserved. **For YOU (async, non-blocking): (a) REDEPLOY** `crafty-sand.vercel.app` to get S7+S8 (+ all the visual work: AO, de-tile, richer textures) onto the live build, then **walk out and confirm the loop reads** (spire visible from afar? compass cue clear? reaching it satisfying?). **(b) Taste:** the shrine tower height (`top` in `world/Terrain.jsx` LandmarksRender) + beacon brightness (the `Emissive` intensities) — too tall/garish or just-right? Still bonus-queued: a reward CHEST at the shrine base (deferred — needs a small cross-hook bridge). Next I build S9: a fixed, foreshadowed Blight-Heart climax + a WIN-STATE (today the boss is an aimless ambush with no ending).
> 41. **⚔️ S7 SHIPPED — the outward journey now has STAKES + REWARD (playtest the curve when convenient) (2026-06-15):** the first gameplay-POINT system is in: a distance `zoneTier` (concentric rings from spawn) now ramps BOTH danger (far = more + more-hostile mobs, even by day) AND loot (far = the rare/epic rows drop up to 2x more often). So venturing out is a deliberate risk/reward choice instead of aimless wandering — the core Ember-Frontier loop. It's all data/logic (no new AI, no persistence), tier-0 near spawn is byte-identical to before (no regression), and it's invisible to the visual gate (runtime-only). **What's for YOU (async, non-blocking): a playtest of the difficulty/reward CURVE** — is the ramp fun, or too punishing / too generous as you walk out? Every number is a one-line tunable: siege ramp in `game/dayNight.js` (`SIEGE_MOBS_PER_TIER` 2, `SIEGE_HOSTILE_PER_TIER` 0.04, caps), loot boost in `game/lootTier.js` (`RARITY_BONUS_PER_TIER` 0.25, cap 2x), ring width in `world/zoneTier.js` (`TIER_RING` 256 blocks, `MAX_TIER` 4). I can't feel the pacing headless — tell me if spawn feels too safe, the frontier too brutal, or the loot too stingy/swingy. Next I build S8 (shrines as actual destinations to walk toward), which is what makes the journey legible.
> 40. **🧭 PIVOT TO "THE POINT" — visuals are solid, now giving the game a purpose (2026-06-15):** the surface-stunning half of your mega-directive is largely DONE — vertex AO (form/depth), de-tile (kills the stamped-tile look), richer block tiles (premium snow/sand/stone), plus the earlier ocean + tamed terrain. The world now LOOKS good. So I'm pivoting to your BIGGER complaint — *"what is the point? the character wanders aimlessly on an island."* Next I build the gameplay POINT from the already-written plan (`docs/superpowers/plans/2026-06-15-crafty-world-purpose-sota.md`, your confirmed Ember-Frontier + Blight-Heart-climax direction): **S7** distance-tier scalar (outward journey gains meaning — biomes/loot/danger ramp with distance) → **S8** landmark shrines as real destinations with reward chests (the "see-it-go-to-it" payoff) → **S9** a fixed, foreshadowed Blight-Heart climax + a WIN-STATE (today the boss is an aimless ambush, no end) → **S10** onboarding that states a purpose. **Two things for you (async, non-blocking):** (a) this is the **ordering** open-decision from the plan (#5: visual-polish-first vs purpose-first) — I'm proceeding purpose-first since visuals are solid; flag if you'd rather I finish minor visual polish (ores/de-island) first. (b) **A real-play LOOK finding:** in non-capture pov shots, cast-shadows read WEAK and the cyan landmark wayfinding-beacons are FAINT — I'm folding "make destinations visible from afar" into S8 (shrine silhouettes + brighter beacons) rather than a one-off lighting tweak. (Note: I also retired a queued task — "un-gate shadows/beacons in capture mode" — as misguided: the gate's clean frames are correct for regression, and pov-probe already shows real play, so that would've broken the gate to no benefit.)
> 39. **🪨 WORLD VISUAL — STRUCTURAL DEPTH PASS: vertex ambient occlusion + the "flat/dim" investigation (2026-06-15):** chasing your "world reads dim/flat/pointless" complaint, I learned our bold-flat NEUTRAL-tonemap pipeline is ROBUST to gentle GLOBAL knobs — 4 attempts (distance aerial-haze, a lighting fill, bloom-threshold, exposure) all came in sub-perceptual (and bloom is S1-B-LOCKED at >=0.85 — lowering it for a glowier world is a YOUR-taste reversal of a locked decision; default = keep the restraint). So the real lever is STRUCTURAL: I baked **per-corner vertex ambient occlusion** into the voxel terrain — concave corners / crevices / cliff-bases now carry soft contact-shadow, so the world reads with 3D FORM (planted trees, grounded steps, recessed-face depth) instead of flat-shaded-per-face, while lit/flat areas stay bright (no dimming). Verified end-to-end with a hard diagnostic + a logic harness (the gate alone couldn't measure it — gentle AO is sub-6%). **TASTE SIGN-OFF (async, non-blocking): redeploy + walk the world; tell me if the AO depth is too subtle / too strong / muddy** — it's one constant (`AO_MIN = 0.55` in `world/Terrain.jsx`; lower = deeper shadow, higher = subtler). And the standing call: GLOWIER (cross the bloom lock) vs keep the restrained bold-flat — default = keep. Next structural lever = surface-texture fidelity, and grounding it surfaced a FORK for you: the audit's "128px + PBR normal maps" would CROSS the bold-flat / anti-AI-slop LOCK (our tiles are deliberate crisp pixel-art, NearestFilter, no mipmaps). So I'm building the ON-BRAND version — kill the repeated-identical-tile monotony via subtle per-cell colour variation + richer hand-crafted stylized patterns (the *correct* SOTA lever for stylized voxel a la Townscaper / Monument Valley / Bad North, which win on colour + silhouette, not surface PBR) — and treating PBR normal-maps / photoreal surfaces as YOUR taste call (**default: keep bold-flat, NO normal maps**). Say the word if you actually want the richer-realistic look. No action blocks the loop.
> 38. **🌊 OCEAN/COAST MILESTONE SHIPPED — your #1 ask, "why is the ocean still looking unchanged" (2026-06-14):** the ocean was a flat dark-blue; it's now a SOTA stylized sea, built plan-driven over 4 slices: **(S1)** a brighter tropical-teal surface + a sun sheen (the depth-tint only reached the underwater faces, so the top read flat — fixed); **(S2)** SHORE FOAM — a crisp white foam line at every coast (the "reads-as-ocean" signature; needed un-merging the greedy mesher's water-top faces to bake per-cell foam); **(S3)** depth-graded top surface (bright-teal shallows -> deep-navy offshore, read from above); **(S4)** a new `ocean-coast` capture state so it's pixel-gated (brings the visual gate to 20 states; 1182 unit tests, build clean). I reference-locked a SOTA stylized-water stack first + validated every slice myself with a new headless ocean-probe (HUD-hidden coast/surface/underwater views). **TASTE SIGN-OFF (async, non-blocking):** load the deployed build, walk to a coast, and tell me if you want the foam STRONGER/softer, the teal more tropical/moodier, or the depth gradient deeper. All tunable one-liners in `world/Terrain.jsx`. swiftshader ≈ GPU but the final color/foam taste is your call. **Now all self-validatable fronts (camera, music, terrain+sun, touch, ocean) are DONE — what's left is your-device/your-ears/your-taste:** redeploy + real-device touch feel + music ear-check + this ocean taste. I've lengthened the loop cadence accordingly.
> 37. **📱 TOUCH FRONT COMPLETE (probe-exhausted) + 🌊 OCEAN MILESTONE STARTING (2026-06-14):** the self-validatable touch work is DONE and verified on an emulated iPhone — cold-start (#36), QUESTS-panel declutter (collapses on a phone), spell-label wrap fix, a top-right LV/XP readout (touch had no level visibility), and a hardened deterministic touch probe. I judged + **skipped a touch minimap** (Crafty already has incoming-hit direction arrows + the boss compass marker, so a tiny phone mob-radar is incremental clutter). **What's left on touch needs YOUR real device** (batched): (a) does Start Adventure → play feel right; (b) thumb walk/look speed; (c) verb/jump button reachability + size; (d) the QUESTS chip / general ergonomics. **Also still owed (carried): redeploy `crafty-sand.vercel.app`** to ship the camera + all touch fixes; **music ear-check** (day/night/boss transitions + volume). **NEXT I'm starting the OCEAN/COAST milestone** — your "why is the ocean still looking unchanged" ask. I looked at the surface/coast myself: the water reads as flat, dark blue blocks (your complaint has merit). It's in the ratified hybrid world spec (oceans-as-a-place + beaches + coast landmarks), so I'll build it plan-driven (reference-lock a SOTA stylized voxel ocean → plan → slices with re-baseline). **Flag if you want a specific ocean look** (tropical/bright vs deep/moody, foam intensity, beach width) — else I proceed on the spec + a SOTA reference. No action blocks the loop.
> 36. **📱 TOUCH COLD-START FIXED — the game is now PLAYABLE on iPad/iPhone (2026-06-14):** closes the long-standing §0 hard gap flagged in item #239 ("UNPLAYABLE on its stated iPad/mobile target; Pointer-Lock-gated, which iOS Safari lacks"). The in-game touch surface (M0-M3a joystick/drag-look/verb tray) was already wired and fine — but the **menu→play ENTRY** was never bridged: the title menu hides only when input is `active`, and every menu button (incl. "Start Adventure") opened that gate via `requestPointerLock()`, which doesn't exist on iOS — so on a real device tapping Start Adventure did nothing and you were stuck on the title screen. Fixed with a touch-aware `enterPlay()` (desktop = pointer-lock; touch = open the gate directly), proven by a new real-browser probe (`scripts/visual/touch-probe.mjs`: emulates iPhone 13 + deletes the Pointer-Lock API to be iOS-faithful → Start Adventure → playable, joystick + drag-look + verb taps all green; 3 were FAIL before). Desktop mouse-look not regressed. **REAL-DEVICE CHECK OWED (only thing I can't do headless):** open the deployed build on an actual iPad/iPhone and confirm (a) Start Adventure drops you straight into play, (b) the left thumb walks + right thumb looks at a comfortable speed, (c) the verb/jump buttons are reachable + sized right for a finger. Polish I can see needs work: the QUESTS panel currently eats ~40% of a phone's width (cluttered) — on the touch-polish backlog. **⚠️ Also redeploy `crafty-sand.vercel.app`** to get this + the earlier camera fix (both are on `main`, not yet on the deploy). No action blocks the loop.
> 35. **⚔️ DECISION NEEDED — combat-depth milestones are PLAYTEST-GATED (2026-06-14, loop iter 187):** the next-levers quick-win backlog is harvested (place-puff, reward-audio, dead-level-up-sound fix, coin-sink, HOME compass marker, endless bounty quests, numbered siege ladder all shipped + 4 items were already-done/redundant). The remaining HIGH-VALUE work is 4 MILESTONE levers (in `docs/superpowers/specs/2026-06-14-crafty-next-levers-backlog.md`): **(1) a recurring apex/boss spine** (top retention — the boss is a one-shot L5 event; the back half has no escalating challenge), **(2) build-identity talents** (capstones + mutually-exclusive choices + respec — currently flat stat-sticks), **(3) enemy attack telegraphs + archetypes** (the #1 combat-DEPTH lever — hostiles attack with zero windup so the dodge i-frames have nothing to react to; combat is tank-trade), **(4) a cinematic-beat pass** (boss entrance + dawn/dusk sky payoff). **The loop is NOT building these autonomously** because their crux is combat FEEL / balance, which it can't validate headless (AI is frozen in capture, there's no mob-attack capture frame, and there's no in-world eyeball). **Want me to (a) author plan docs + build them for you to playtest-tune, (b) prioritize one, or (c) keep shipping smaller validatable polish/content?** Until you steer, the loop continues with validatable units (logic/data/capture-safe visuals/audio-with-ear-check). No action blocks the loop.
> 34. **🎉 REWARD-BEAT AUDIO SHIPPED + EAR-CHECK + a BUG FIXED (2026-06-14, loop iter 183):** leveling up was SILENT — a dead `window.playLevelUpSound` reference (never assigned) — now fixed; and achievement/quest-complete now play a triumphant brass FANFARE (were silent text toasts). **EAR-CHECK owed (I can't hear audio in headless):** level up + complete a quest and confirm the fanfare reads as a rewarding flourish (not too long/brash over the music). Tunable: `audio/synthVoices.makeFanfare` (the notes + the `* 0.3` gain). Capture-safe. **FYI (no action) — the holistic next-levers review was over-optimistic:** 4 of its top items were already-done or redundant (spell-upgrades duplicate the intellect-attribute scaling; break-debris, incoming-hit flash/shake already exist). I'm verifying each backlog item against live code before building. No action needed.
> 33. **🎺 DAY/NIGHT TRANSITION STINGS SHIPPED + EAR-CHECK (2026-06-14, loop iter 179):** the night siege + dawn had a SurvivalWarning banner + a gradual music shift but NO one-shot sting — the siege onset (the beat the onboarding nudge promises) began silently. Now a SIEGE HORN (ominous war-horn call) sounds at nightfall + a DAWN CHIME (bright ascending bells) at daybreak. **EAR-CHECK owed (I can't hear audio in headless):** survive a day→night→dawn cycle and confirm the horn reads as ominous-but-not-jarring + the chime as a relieved "you survived", both balanced under the music. Tunables (one-liners): `audio/synthVoices.makeSiegeHorn` / `makeDawnChime` (timbre + the `* 0.34` / `* 0.3` gains). Capture-safe (clock frozen → never fires in capture). No action to proceed; flag if either is too loud / wrong-character.
> 32. **❤️‍🩹 LOW-HEALTH DANGER FEEDBACK SHIPPED — vignette + heartbeat (2026-06-14, loop iters 173-174):** critical health (< 35% of max) now reads continuously — a red edge VIGNETTE that pulses + a soft low HEARTBEAT that quickens toward 0 HP (seen + heard). **EAR-CHECK owed (I can't hear audio in headless):** drop to low HP and confirm the heartbeat is dread-under-the-music, not an alarm — tunables (one-liners): `audio/synthVoices.makeHeartbeat` (timbre) + `game/lowHealth.heartbeatPeriod` (1.3s→0.55s cadence) + the vignette opacity in `ui/LowHealthVignette.jsx`. Both are null at safe HP (no baseline impact). **i18n #73 FINDING (no action — for your awareness):** the owed zh-CN game-content pass is BLOCKED by an architecture issue — item NAMES are used as identity/keys across inventory/crafting/equipment (`name.includes('Potion')`, `NAME_TO_ID`, etc.), so translating names would break logic; it needs an id/display-name decouple refactor first (invasive). Deferred until that's scoped; the en default is unaffected. No action needed.
>
> 31. **🐉 BOSS SILHOUETTE FEATURES SHIPPED + eyeball (2026-06-14, loop iter 172):** the Shadow Dragon (a blocky winged box) now reads as a DRAGON — swept-back horns + tapering tail + back-ridge spikes (the mob-distinctness treatment for the apex creature). **Eyeball: `frontend/tests/visual/baseline/boss-closeup.png`** (front-on; the horns read on top of the head). The tail + back-ridge render in-world from gameplay angles but are hidden in the front-on card — the fixture stays front-on+level because a 3/4 reveal angle catches the distant terrain (clutters the clean sky card); flag if you'd like a dedicated 3/4 boss-showcase fixture to gate the tail/ridge too. **Decisions-of-record (reversible — `src/render/BossEntity.jsx` DRAGON_FEATURES):** static bold-flat boxes, torso material (flash/phase together) + boss outline. No action needed; flag if the horn size/sweep reads wrong.
>
> 30. **🎨 MOB BESTIARY DISTINCTNESS SHIPPED + eyeball (2026-06-14, loop iters 169-171):** the 9 mob types were generic colored boxes (the master-plan-forbidden generic-voxel look); each FEATURED type now reads by SILHOUETTE via bold-flat box features — skeleton ribs, duskhound ears+tail, moss_brute shoulder-slabs+moss-crown, skitterling antennae, cow horns. **Eyeball: `frontend/tests/visual/baseline/mob-bestiary.png`** (a new studio-row fixture of the 5 featured types). **Decisions-of-record (reversible — `src/game/mobFeatures.js` per-type box specs + `src/render/MobModel.jsx`):** features are static boxes in the body-local frame, same toon material + outline as the body, `tone` shading (bone/dark); positions scale with body dims. Pig/zombie/villager/spider intentionally plain (spider's leg-sprawl already distinct). T4 (color accents / zombie head-tilt) deferred — the silhouette read landed without it. No action needed; flag if any species read is wrong or you want OTHER mobs/bosses given the same treatment (a clean fast-follow).
>
> 29. **🌙 TWILIGHT NIGHT SKY SHIPPED + eyeball (2026-06-14, loop iter 165):** the everyday-night (dusk) sky now has a clean cool MOON disc + a sparse twilight STARFIELD (it was gradient + sun-glow only). **Eyeball: `frontend/tests/visual/baseline/explore-night.png`** (moon upper-right + stars over the sunset-lit landscape) vs the prior plain dusk sky. Gated to dusk only — the day sky + the crimson boss sky are untouched. **Decisions-of-record (reversible — `src/render/Atmosphere.jsx` fragment shader + `src/render/nightSky.js`):** moon = a defined smoothstep disc (chosen over a soft glow, which washed out in the bright sunset) positioned upper-right opposite the sun; stars = sparse hash-grid points, ~1.5% density, cool-white; both scale by `starIntensity(mood)` (peak at dusk). **Finding for your steer (non-blocking):** the game's "night" is a bright SUNSET/dusk, not a dark night — so the moon/stars contrast is gentle by nature. If you'd prefer a genuinely DARK starry night (more dramatic celestial read, but changes night visibility/feel), that's a dusk-palette re-grade I can design — flag if you want it. No action needed; flag if the moon placement/size or star density reads wrong.
>
> 28. **✨ LOOT RARITY AURA SHIPPED + eyeball (2026-06-14, loop iter 163):** loot drops now carry a soft bloom-softened glow-shell around the gem, tier-scaled (legendary biggest/brightest) — the up-close "it glows" tell that reads from ANY angle (the vertical beam stays the across-map tell). **Eyeball: `frontend/tests/visual/current/loot-showcase.png`** (4 drops L→R: common grey faint → rare blue → epic purple halo → legendary gold bright halo) vs `baseline/loot-showcase.png` (pre-aura: gems + thin beams, no halos). Uses the established spellVfx outer-glow-shell + Bloom technique (coherent). **Decisions-of-record (reversible one-liners in `game/lootJuice.js`):** auraRadius 0.42→0.74 by tier; auraOpacity capped 0.38 (a glow, never a solid ball). The aura is <6% so it ships in CODE (every in-game drop) with NO re-baseline. **Eyeball update (iter 164): the loot-showcase is now RE-BASELINED CLEAN** (`baseline/loot-showcase.png` = the aura card, no cube) — the aura is locked in the gate. **The grey-cube leak is RESOLVED (iter 164):** it was the force-spawned boss-closeup DRAGON (box torso + outline, grey under studio light) persisting into loot-showcase — a hook can't despawn the boss (`bossActive` is bossSystem-local useState), so I reordered capture.mjs to run loot-showcase BEFORE the boss/spell/beast fixtures (no leak; all other frames byte-identical). **Decisions-of-record (reversible one-liners in `game/lootJuice.js`):** auraRadius 0.42→0.74 by tier; auraOpacity capped 0.38 (a glow, never a solid ball). **Remaining FYI (non-blocking):** a pre-existing non-fatal React "unique key" warning at the loot-showcase capture stage. No action needed; flag if the aura is too strong/subtle (tune the caps).
>
> 27. **🔴→🟢 BROKEN-MAIN CRASH FIXED + a loop verification hole closed (2026-06-13, loop iter 159) — FYI, no action:** heads-up that the game was crashing on load (ErrorBoundary, unplayable) on main for ~5 iters — the iter-154 "Look Sensitivity" change put a store selector in the wrong React component scope (`lookSensitivity` undefined in GameScene). FIXED (`c5c51ef`). It hid because the loop's "visual 18/18" checks were diffing STALE capture frames (the diff command doesn't re-render); I've corrected the loop's verify protocol (LOOP-CHARTER §1.5: must re-capture for render changes) + made the capture harness degrade gracefully instead of aborting under load. A clean capture-verify is the next loop unit. No action needed — flagging because main was briefly broken.
> 26. **📖 ASPECT-UNLOCK TEACHING TOAST SHIPPED (2026-06-13, loop iter 157):** unlocking a signature Aspect verb (R/V/X/Z) now pops a 5s toast teaching the key + verb ("WILDHEART unlocked — press R to roar"), sourced from the KEY_MAP binding SoT (can't drift). **In-play check:** spend a talent point on an Aspect verb-talent + confirm the toast reads well + auto-dismisses. No action needed; flag if the wording/timing is off.
> 25. **✨ MOB-DEATH SPARK FINISHER SHIPPED (2026-06-13, loop iter 156):** killing a mob was visually silent (it just vanished); now a kill fires a bigger, mob-coloured spark burst (count scales with the mob's xp — a moss_brute bursts harder than a pig). Reuses the proven GPU spark pool (so the look is pre-validated). **In-play check:** kill a few different mobs + confirm the burst reads as a satisfying finisher (not too sparse/busy). Tunable: `game/mobHitFx.js` `deathBurst` (the `40+xp` count + clamp). No action needed; flag if it's over/under-the-top.
> 24. **🔊 UI PANEL FOLEY SHIPPED + EAR-CHECK (2026-06-13, loop iter 155):** opening/closing a core panel (inventory/craft/build/magic via key, button, or the touch tray) now plays a soft open/close click — the menu chrome was silent. **I CANNOT hear audio in headless** — ear-verified by you: open + close a panel and confirm the click is pleasant + subtle (not intrusive over the music). Tunables (one-liners in `audio/synthVoices.js` `makeUIOpen`/`makeUIClose`): pitch sweep + the ~0.2 gain. No action needed; flag if it's too loud / annoying / wrong-character.
> 23. **⚙️ LOOK SENSITIVITY setting SHIPPED (2026-06-13, loop iter 154):** a "Look Sensitivity" slider in Settings now tunes look speed for BOTH mouse (PointerLockControls `pointerSpeed`) and touch (the drag-look). **Desktop-testable now:** open Settings → drag the slider → mouse-look speed changes live (0.3–2.5, default 1). Touch look-speed needs a real device for feel. No action needed; flag if the range/default feels wrong.
> 22. **💥 DAMAGE-DIRECTION CUES SHIPPED + eyeball (2026-06-13, loop iters 149-150):** on top of the existing red damage-flash + screen-shake, a red glow now appears at the screen EDGE pointing toward the attacker (combat legibility — you turn to the threat). **Eyeball:** in-play (it's a transient ~0.8s flash, not a static frame) — take a hit and confirm the red glow reads on the correct side + isn't too strong/weak. Tunables (one-liners in `ui/DamageDirection.jsx`): the gradient alpha/extent + the 0.8s fade. **Decisions-of-record (reversible):** only the mob-melee source is threaded so far (the most common); projectile + boss sources are a follow-up (the projectile stepper returns a hit count, not positions). The cue is capture-null in normal play (no baseline regression risk), so no permanent gate fixture yet. No action needed; flag if the cue is distracting / wrong-side / the wrong intensity.
> 21. **📱 TOUCH PANEL-ACCESS TRAY SHIPPED + eyeball (2026-06-13, loop iters 146-147):** touch players couldn't open ANY panel after M2a (the desktop E/C/B/M openers were hidden). Added a left-edge **tray**: a grid icon → a column of 4 gold-glyph openers (Inventory/Craft/Build/Magic). **Eyeball: `frontend/tests/visual/baseline/mobile.png`** (the gold-glyph tray as a left-edge column, clear of the quest tracker + joystick). Two real bugs fixed via the HD self-eyeball: (a) the touch glyphs had been rendering DARK since M2a (iter-137) — `color: var(...)` renders inconsistently on the headless-capture fixed-overlay SVGs → switched to a literal `#C9A86A`, so the sword/zap/jump cluster glyphs are now GOLD too; (b) a ~4% flaky slam-sound clip. **Decisions-of-record (reversible one-liners):** tray = the 4 STORE-backed panels only (quests/achievements/stats are InputManager-local — a follow-up); opening a panel yields touch control (`setActive(false)`) + the move/look listeners gate on `getInput().active`. **Still M2b (deeper touch focus polish, queued — none blocks the loop):** a VISIBLE pause overlay, Settings touch-sensitivity, the `resumeControl` relock refactor, gating `<PointerLockControls>` on `!isTouchDevice`. Real-device iPad confirmation of the tap-flow is the one Kevin-physical item. No action needed; flag if the tray placement/icons read wrong.
> 20. **🔊 MOB AGGRO-SNARL SHIPPED + EAR-CHECK (2026-06-13, loop iter 145):** mobs were silent until they hit/killed you; now a mob SNARLS spatially the moment it goes aggro (notices you) — a threat cue BEFORE contact, distinct from the heroic WILDHEART roar (shorter/higher/nastier). **I CANNOT hear audio in headless** — ear-verified by you: aggro a mob (walk into its sight) and confirm the snarl fires once + reads as menacing + isn't spammy when a group turns (a 2.5s global cooldown guards a turning siege). The voice (`makeAggroGrowl` in `audio/synthVoices.js`) is characterization-tested + the edge wiring gate-locked; only the SOUND BALANCE (timbre / vol 0.9 / range 22 / cooldown 2.5s) needs your ear — all one-liners. No action to proceed; flag if too loud / too sparse / wrong-character.
> 19. **🔊 BIOME-AMBIENT AUDIO SHIPPED + EAR-CHECK (2026-06-13, loop iter 122):** a looping ambient "wind bed" under the music now shifts character with your biome (snow = bright airy / desert = dry / stone = hollow / plains = soft / underwater = muffled). **I CANNOT hear audio in headless** — ear-verified by you: walk grass→sand→snow and dive, and confirm the ambient wind shifts per biome + stays a SUBTLE under-layer (not intrusive over the music). The map (`audio/biomeAmbience.js`) is unit-tested + the wiring gate-locked; only the SOUND BALANCE (cutoff/gain per biome, the 8s update lag) needs your ear — all one-liners to tune. No action needed; flag if it's too loud / laggy / wrong-character.
> 18. **🏆 WORLD-DESIGN LADDER COMPLETE — M6 signature landmarks SHIPPED + eyeball (2026-06-13, loop iter 120):** the infinite wilds now have tall wayfinding silhouettes (a glowing Spire + a Sky-arch monument, deterministically placed, sparse). **Eyeball: `frontend/tests/visual/baseline/landmark.png`** (the Sky-arch rising clear above a forest). **This closes the world-design pass you twice asked about** — built iters 106–120: M1 crafted Hearth spawn · M2 divable oceans · M3 biome-table · M4a biome foliage (snow pines) · M5a ocean depth-tint · M6 landmarks, + surface-keyed footstep audio. The world now LOOKS/FEELS/SOUNDS dramatically more distinctive than the undifferentiated-noise starting point. **Deferred (loop will pick): a biome-ambient audio pass (recommended next — `climate.js` already unlocks it) · a new-blocks batch (richer biome palettes + layered seabed) · biome-aware topography · then the Components/NPC de-monolith.** No action needed; flag if you want a different next direction, or want to eyeball the whole world pass before more (the `hearth`/`biome-snow`/`ocean-depth`/`landmark` baselines are the tour).
> 17. **🌊 WORLD-M5a — ocean water depth-tint SHIPPED + eyeball (2026-06-13, loop iter 118):** the M2 divable basins now darken toward deep-navy with depth (a shader tint by world-Y). **Eyeball: `frontend/tests/visual/baseline/ocean-depth.png`** (a new underwater fixture — the navy-deepening water column over a sandy seabed). Note it's **invisible from above** (the surface is all one depth) — the payoff is when you DIVE; best judged in-game by swimming down a basin. `explore-day` was re-baselined (its distant water edges darkened ~6%). Decision-of-record (reversible): the deep color `vec3(0.015,0.09,0.20)` + the `*0.82` strength + the `/22` depth range are one-liners in `Terrain.jsx` compileShader. The LAYERED seabed (sand→gravel→stone, M5b) is deferred — it needs new blocks, folding with M4b. No action needed; flag if the water's too dark/murky or you want the seabed materials sooner.
> 16. **🔊 LOCOMOTION AUDIO — surface-keyed footsteps SHIPPED + EAR-CHECK (2026-06-13, loop iter 116):** walking was silent; it now plays footsteps (grass/sand/snow/stone, pitch-varied per surface — so each biome SOUNDS distinct), a landing thud, and a jump cue. **I CANNOT hear audio in headless** — so this is ear-verified by you: walk over different biome surfaces (grass plains → sand beach → snowy highland → stone) and confirm the footsteps fire in stride + sound surface-distinct, and that jump/land cues read right (volume/pitch). The mechanism is gate-locked (wired, surface-keyed, throttled) + the climate sampler is characterization-tested; only the SOUND DESIGN (timbre/volume balance) needs your ear. Reversible: the rate-by-surface map + stride cadence are one-liners in `Components.jsx`. No action needed to proceed; flag if the footstep audio is annoying / too sparse / wrong-feeling.
> 15. **🌲 WORLD-M4a — biome FOLIAGE distinctness (snow pines) SHIPPED + eyeball (2026-06-13, loop iter 114):** the snow biome was barren; it now grows conical evergreen PINES. **Eyeball: `frontend/tests/visual/baseline/biome-snow.png`** (a new capture fixture over a probed snowfield — evergreens on a snowy highland, water beyond). Every biome now has signature flora (plains=round trees, desert=cacti, snow=pines). Decision-of-record (reversible): M4 was SPLIT — M4a foliage ✅, M4b palette + M4c topography deferred as incremental (topography last because a discrete per-biome height curve cliffs at biome borders). Also flagged: **the experience interleave is now due** (4 world milestones since UX@98) — the loop will likely do a biome-ambient audio pass next (per-biome wind/beds) before resuming the world ladder at M5 (ocean visual). No action needed; flag if you want the world ladder finished before any audio, or a different biome look.
> 14. **🏠 WORLD-M1 — the HOME ANCHOR ("Hearth") SHIPPED + eyeball (2026-06-13, loop iter 108):** the player now spawns on a crafted origin plinth — a flat raised stone pad with a toon lodge + brazier + planters. **Eyeball: `frontend/tests/visual/baseline/hearth.png`** (a dedicated new capture fixture; the diorama camera frames the distant vista so the Hearth needed its own pose). It reads as a clean home base; **the lodge is intentionally minimal for M1** — a *richness polish* (bigger/detailed lodge, pad material variety, the "short pier toward water" deferred until M2 lands coastlines near origin) is a flagged fast-follow, not an M1 blocker. **Decision-of-record (reversible): `HEARTH_Y=56`** — the spec draft's "~Y30 origin" was wrong (real ≈Y50, computed against the worldgen formula); 56 = a gently-raised pad over the real grade (else it carved a pit). No action needed; flag if you want the Hearth richer/relocated or a different identity.
>
> **Decisions TRANSFERRED to the loop (charter §5, Kevin 2026-06-10):** music tooling + per-Aspect motif
> policy (#74) · loot-beam punchiness · hotbar honesty approach · coin sinks · named regions · deep-night
> obsidian · taste-tunables previously listed as "pending Kevin" — the loop decides on evidence, logs the
> decision + reversal path HERE, and proceeds.
>
> ---
>
> ## HISTORICAL (pre-2026-06-10 — statuses superseded EXCEPT where re-listed in LIVE above; kept for the record)

> **🐾 PLAYABLE — S2-B1 WILDHEART M0-M5 built (7/8 milestones, 2026-06-07).** Bank Ferocity by day-kills → roar in the night siege → become an element-beast that now FIGHTS + MOVES distinctly (M5 combat + locomotion re-skin). Each milestone TDD-gated + adversarial-reviewed (the M5 review caught a real spark/form-desync — a lightning-hawk could throw fire sparks after a spell-switch — fixed pre-merge by deriving the spark from the LOCKED form). **M5 taste-tunables (all reversible — proceeding on my defaults unless you redirect; full table + in-app motion check in `memory/S2B1-M5-MOTION.md`):** the per-form damage/cooldown/move/gravity/jump numbers; **`turnRate` OMITTED** (camera-relative pointer-lock = no turn-rate seam; a tank-turn would be an S3 controller change, not a table field); the **dodge-roll + ledge-vault are deliberately form-INVARIANT** (i-frame fairness + traversal reliability — flag if you want a comet-far / golem-short dodge); the mob-layer hit-spray threshold now tracks form-multiplied damage (heavy forms spray more — intended). **NEXT = M6** (signature talent nodes incl. the `wildheart_roar` unlock) → M7-M8 (the LOOK — still gated on the ONE blocking look-reference decision below).

> **🐾 HARD GATE — S2-B1 WILDHEART design spec ready for your review (2026-06-07).** The LEAD Aspect (beast-transform). DESIGN-OF-RECORD at `docs/superpowers/specs/2026-06-07-crafty-s2b1-wildheart-design.md`, produced by an 11-agent design-workflow (5 code-seam mappers + 2 live-research lanes → synth → 3 adversarial reviewers) then reconciled by me. **The review earned its keep — it caught 4 BLOCKING defects I'd otherwise have built:** death-restore wired to the wrong transition (would strand a kid in beast-form through the death screen), ferocity accrual stomping the already-owned `onMobKill` slot (would silently break quests), an unlock-node crash in `foldTalentEffects`, and a fake input-abstraction. All corrected in v2. **CONCEPT (locked in the S2 spec):** hold-roar → become an element-beast (the loaded spell-element picks the form); your 2 attack intents re-skin per beast (zero new menu); a Ferocity meter banks in the day, unleashes in the night siege. **TWO-LAYER:** the mechanics are blind-buildable + TDD-gated; **the LOOK (the morph VFX + the 4 beast forms) is a reference-LOCK + in-world decision (the VFX discipline) — I will NOT blind-build it.**
> &nbsp;&nbsp;**THE ONE GENUINE BLOCKING DECISION (spec §8 #1, blocks only the look milestones M7/M8):** lock the beast-look REFERENCE direction — options I researched: **Bayonetta Origins** (crisp-not-blobby north star + literal roar), **Brawl Stars** (closest bright/kid-safe comp), **LoL transform-ults**, **Genshin elemental bursts**, **Hades** (or a blend). I'll prototype in-world (real Caribbean context, grayscale-judged), not a sky-studio frame.
> &nbsp;&nbsp;**The rest of §8 are reversible taste-tunables — I'll PROCEED on the spec's recommendations unless you redirect:** ferocity tuning (kills-only + per-tier scaling; bleed-to-zero at dawn), beast roster + the **counter-intuitive element map** (fire→comet / **ice→bull** / lightning→hawk / arcane→golem — flag if you'd rather fire→the heavy charger), talent taxonomy, per-beast combat-differentiation depth + collider/locomotion numbers, the bull's debris-shove (FPS-gated), the Ferocity HUD "feral" color token, and the roar keybinding.
> &nbsp;&nbsp;**Ask:** approve the design to start the build (M1 = the transactional collider-swap + restore-invariant, the de-risk-first item — decision-independent), and lock (or defer) the look reference whenever — it only gates M7/M8. The mechanics M1-M6 + M3.5 proceed without it.

> **🪙 SHIPPED — S2-A-M3c (loot juice) merged → ALL of M3 (the stakes loop) done.** Drop-beams now **tier by rarity** (taller/brighter for legendary, off the locked palette) + a rarity-tinted **pickup pop** + the pickup sound. **EYEBALL GAP (player-experience lens):** the beam/pop LOOK is NOT covered by the visual gate — loot drops need mob kills, which don't happen in the 12 capture states, so no automated test renders them. Whether it reads well per rarity / the pop feels right is **unverified**. Want me to (a) add a dev "loot-showcase" capture state (a fixture drop, so the gate + your eyeball both see it), (b) record a short clip, or (c) leave it for your next playtest? Tunable look constants live in `frontend/src/game/lootJuice.js`.

> **🎵 MUSIC — you asked how I'll treat/enhance it + flagged your ElevenLabs API key (CLI access).** My plan + the tooling question are answered in-thread (this turn). TL;DR: the engine side (adaptive layering/transitions) is the real upgrade and is tool-agnostic; for GENERATING the stems, ElevenLabs Music is a strong SOTA option (others: Suno/Udio higher-fidelity-songs but licensing/API maturity varies; Stable Audio for loopable game stems). **Decide later** (per you) — when we hit the music pass (folded into S2-B per the master plan). Not blocking.

> **🌗 SHIPPED — S2-A-M3a (day/night clock) + M3b (night siege + survive-to-dawn reward) merged (2026-06-03).** Day↔night now auto-cycles (your "still day after the dragon" fixed). Night = an **escalating siege** (more/hostile mobs per night survived, capped); **survive to dawn → reward** = scaling XP + a guaranteed rarity-climbing loot drop + **coins** (a new persistent currency seeding the future S4 shop). **Death = SOFT** (respawn, keep everything — your pick; locked by a test). **KNOBS to tune anytime (reversible constants):** siege ramp (maxMobs 16→40, hostileChance 0.7→0.95 over ~6 nights), dawn reward (50 XP + 10 coins per night survived + 1 loot drop, rarity climbing rare→epic→legendary). **ONE design Q:** night currently keeps the **dusk** mood and **obsidian (the dark dramatic mood) stays BOSS-only** (cleaner signature) — do you want deep/late nights to also tint toward obsidian? Default = no. 507 unit · 12/12 visual. **NEXT = M3c loot-juice** (rarity drop-beams + pickup pop — none exists today).

> **🗺️ CONTENT-DIVERSITY — you asked "when do we address mob variety/designs, landscape gen/designs, music?" (2026-06-03). Honest answer: none is a first-class milestone in the master plan — they're SEAMS (the same blind-spot that hit projectile-variety).** Current state: **mobs** = 6 box-template swaps (color + box-proportions + stats; render language done S1-B, but no distinct creature designs/behaviors); **landscape** = basic height-grid biome gen + S1 materials (no designed-biome variety); **music** = 3 procedural chord loops (day/night/boss), reactive — the plan literally says "audio folds into S1 polish" but S1 didn't deepen it. Why late = deliberate (pillar P1: signature DEPTH via the 4 Aspects before content BREADTH). **My proposed scheduling (proceeding unless you redirect):** (1) **music** — a light per-context motif pass folded into each S2-B Aspect (cheapest high-impact, rides work already happening); (2) **mob/bestiary designs** — a dedicated pass AFTER S2-B3 Soulbind (so creature designs serve the capture/transform mechanics); (3) **landscape/biome designs** — a "world design" milestone late-S2 (look/feel) with gen-systems hardening in S3. All three get concretely surfaced + measured by the **pre-S2-B content-variety sub-audit you already greenlit.** Want them formalized as named milestones in `SOTA-INITIATIVE.md` now, or keep folded + let the audit drive it? (Default: add to the plan + let the audit drive.)

> **💰 MONETIZATION — sell-power question RESOLVED (legal) + open (your S4 model choice). (2026-06-03, you asked + I verified multi-source.)** **Sell-power is NOT illegal / not a certain legal blocker** → per your rule, dropped the blanket no-P2W veto; it's no longer a pillar (monetization = an S4 decision, not a coherence invariant). **The certain legal lines that DO bind us** (FTC Genshin $20M = COPPA + deceiving kids about odds + *randomized lootboxes* to under-16): no randomized **gacha/lootboxes** (esp. to minors), **COPPA** (under-16 purchases need parental consent; no kid dark-patterns), odds-disclosure if random. Web-first = no app-store rules (keep ~97%); later iOS/Mac app adds Apple odds-disclosure-if-random + the 30% cut (Apple doesn't ban sell-power). **Commercial reality (your "more viral/sells more" hypothesis — half-right but mostly backwards for us):** P2W = more per-whale (0.19% players ≈ 48% rev) BUT hurts retention/word-of-mouth/virality; **cosmetics are the #1 IAP category (≈80% of revenue in Fortnite/Roblox/League)** + power the clip-worthy viral loop; Chinese→Western "detox" (gacha→cosmetics+fair-pass) RAISED retention while whales still spent. For a virality-led broad-audience game, fair/cosmetic-led beats P2W. Caveat: Crafty is PvE/co-op not competitive PvP → convenience/progression sales are more tolerable. **YOUR S4 DECISION (full evidence in `specs/crafty-coherence-pillars.md` S4 note):** cosmetic-led + transparent pass + optional convenience (my data-backed rec) — OR hybrid w/ some sold progression — OR sell-power (legal, your call). No randomized gacha either way. Not needed until S4; logged.

> **🎨 TASTE — the 4-Aspect talent-tree node taxonomy (M2c/A4 shipped on best judgment; reversible data table at `frontend/src/game/talentTree.js`).** The inert talent tree is now LIVE + structured into the 4 Aspects. These are FOUNDATIONAL stat nodes (the per-Aspect SIGNATURE abilities — beast-transform / gravity-grab / capture / reactive-terrain — come in S2-B). Each node = `+N` per rank to a core stat, which flows to combat (dmg/crit/mitigation) AND HP/mana pools. **Tune any name/number, or tell me to redesign:**
> &nbsp;&nbsp;• **Voidhand** (kinetic bruiser): Kinetic Force +3 STR/rk · Gravity Ward +6 armor/rk · Crushing Pull +2 STR/rk (needs Kinetic Force)
> &nbsp;&nbsp;• **Wildheart** (primal vitality/speed): Beast Vigor +3 STR/rk (HP) · Feral Swiftness +4 AGI/rk (crit) · Blood Frenzy +3 AGI/rk (needs Feral Swiftness)
> &nbsp;&nbsp;• **Soulbind** (warden/support): Soul Bond +3 INT/rk · Warden's Aegis +5 armor/rk · Spirit Link +2 INT/rk (needs Soul Bond)
> &nbsp;&nbsp;• **Elemancer** (elemental caster): Elemental Focus +4 INT/rk (mana) · Volatile Edge +3 AGI/rk (spell crit) · Cataclysm +3 INT/rk (needs Elemental Focus)
> &nbsp;&nbsp;Open Qs: are 3 nodes/tree enough for now, or want more depth pre-S2-B? · STR/AGI/INT/armor only, or add bespoke stats (lifesteal, cooldown, move-speed — would need new system wiring)?

> **✅ FIXED (2026-06-03, you reported it): boss fights now trigger the OBSIDIAN mood.** You defeated the Shadow Dragon + stayed in day-mode. Root cause: nothing in gameplay ever wrote `dangerLevel` (the obsidian-mood driver), so the boss-obsidian signature atmosphere never fired in real play (a known S1-audit A5 gap). Fixed: `useBossSystem` now bridges `bossActive → setDangerLevel(2)` (clears to 0 on defeat) → an active boss drives the obsidian atmosphere. Merged `a428df7`, capture-guarded, 425 unit · 12/12 visual.
> **✅ DECIDED (Kevin 2026-06-03): day↔night auto-cycle → WAIT for M3.** The cycle mechanism exists (`setGameTime` flips `isDay`) but nothing ticks `gameTime` (permanent day; manual Settings toggle only) — the day→build→night-SIEGE→dawn arc is the M3 "stakes loop". Not pulling it forward.
> **🌗 NOW BUILDING — S2-A-M3 (the stakes loop), decomposed M3a/M3b/M3c (2026-06-03). Feel/balance knobs below = reversible constants, proceeding on best judgment per "keep building + batch for me"; redirect any.**
> &nbsp;&nbsp;• **M3a — day/night CLOCK (building now):** wires a ticker so day↔night actually cycles (your "still day after the dragon" report) + fixes a latent flip bug (`setGameTime` only flipped on landing *exactly* on a 600-multiple → a resumed save at e.g. `gameTime=437` never flipped; now flips on boundary-CROSSING, robust). **KNOB: full cycle = 5 min (2.5 day / 2.5 night), pauses in menus / at click-to-play / on death / during visual-capture.** Alternatives: 8-min cozier / 3-min frantic — one constant (`GAME_UNITS_PER_SECOND`).
> &nbsp;&nbsp;• **M3b — night SIEGE + survive-to-dawn + reward (next, design for your input):** today `useSurvivalMode` is a stub (just a nightCount + toast; the "night danger" interval body is *empty*). PROPOSED default: each night ramps hostile spawn rate/count (escalating per night survived), ties into the existing `dangerLevel`→obsidian-mood (night = danger 1, deep-night/boss = 2); **survive to dawn → a reward** (proposed: bonus XP + a guaranteed loot drop scaling with night number). **Death stakes (your call):** proposed SOFT (respawn at base, keep progression — friendly for broad audience) vs HARD (drop some loot/penalty). I'll default SOFT unless you want stakes.
> &nbsp;&nbsp;• **M3c — loot juice (after M3b):** today drops have NO pickup feedback (grep: zero beam/flash VFX). PROPOSED: rarity-colored drop-beam (common→legendary tint, reusing the locked palette) + a pickup pop/sound. Pure feel layer.
> &nbsp;&nbsp;Open Qs for you: cycle length OK at 5 min? · death = soft or hard? · dawn reward shape (XP+loot vs currency vs cosmetic)?
> **✅ DECIDED (Kevin 2026-06-03): projectile-variety pass → AFTER S2-B (the Aspects).** Today the 4 spells share one projectile shape (colour + secondary-effect only). Scheduled as its own per-element spell-VFX-variety **signature pass with mockups for review** (distinct geometry/motion: fireball comet / iceball shatter-shard / lightning forked-chain / arcane piercing-orb), to run after the Aspects land.
> **🔍 PROCESS SELF-REFLECTION + ENHANCEMENT (Kevin asked "why weren't these caught?", 2026-06-03).** Honest root-cause: (a) the obsidian bug was NOT missed — the S1 audit *found* it (no gameplay `dangerLevel` writer); I *deferred* the cheap fix inside M3 → sequencing error, not detection. (b) projectile-sameness was a genuine plan gap (S1-D = spell *feel*, S2-B = ability *identity*; per-element *look* was unowned). Common root = an engineering/code-correctness lens under-weighting **player-experienced content quality** (the "sampler trap" at the asset level). **Enhanced the QA cadence (spec §7) with 4 standing additions:** (1) **content-variety / instance-sameness** check (are N instances distinct or colour-swaps?); (2) **signature-fires-in-prod** check (every mood/effect needs a *gameplay* trigger, not just a dev hook); (3) **finding-triage by cost×player-visibility** (cheap+visible → ship standalone NOW, never bundle into a deferred milestone); (4) **builder applies the player/artist lens** (play/look at the real flow before "done", not just tests-green). Captured as an EEE lesson + `feedback_player_experience_lens` memory. **PROPOSAL: a focused content-variety + signature-fires sub-audit as a pre-S2-B gate** (S2-B is signature work where instance-distinctness is the whole point) — would surface other likely-uniform content (mob distinctness, loot-beam variety, per-action sound variety) + any other dev-hook-only signals like `dangerLevel` was. Want it run before S2-B?

> **✅ S2-A-M2a COMPLETE + MERGED (2026-06-03) — progression-persistence core + save consolidation (the A3 "comprehensive save" + the slop teardown you green-lit).** RPG progression (level/XP/attributes/equipment/talents/spellLevels/**chests**/position) now **survives a reload** + **autosaves** (local-first, debounced on transitions + build/mine + tab-close). **Your "why are there both worldmanager and savegame" — answered + fixed:** there weren't two real systems; `useGameStore.saveGame`/`loadGame` were **dead axios calls to a backend that doesn't exist** (zero callers) — **deleted**. `WorldManager` (localStorage) was the only live path; now everything routes through ONE `buildSaveData` serializer (was the payload duplicated 4×). Also tore out: a 4× duplicated max-stat formula (→ one `progression.js`), a GameSystems **HP-ratchet bug** (+20 HP every equip toggle), and a baked `frost_shield` armor mutation (now derived). 406 unit · 12/12 visual · build clean; final adversarial review APPROVED.
> &nbsp;&nbsp;**DECISION I made (ratify or redirect): cloud save = deferred to S4.** Crafty is single-player + offline today; I made the save **local-first (localStorage)** and marked the WorldManager cloud-axios branches `// S4: cloud sync — backend not yet implemented`. Accounts/backend are an S4 concern per the master plan. OK?
> &nbsp;&nbsp;**✅ RESOLVED (2026-06-03, you asked me to "do the logged-for-later things"):** both carry-forwards are now FIXED + merged (`97f645a`). QuestSystem's divergent local quests/completedQuestIds/stats/unlockedAchievements now mirror into a serializable store `questState`, ride `buildSaveData`, restore via `loadWorldData` + a resync tick, and trigger autosave — so **quest progress + achievements survive a reload + autosave**, same as the rest of the slice. `test:unit` 413 · 12/12 visual. (Implementation: low-risk mirror+resync, gameplay logic untouched; the dead `achievements` store field is now superseded by `questState.unlockedAchievements` — left as harmless back-compat for a future slop pass.) **"Comprehensive save" is now honest to players.**

> **✅ S1-D SIGNATURES COMPLETE → ALL OF S1 COMPLETE (2026-06-02).** RESOLVED: mascot = **B "Crafty Hero"** (Kevin picked; polished + title-wired + stronger gem-glow); spell-VFX = premium-energy fireball (#1 polish done); cast-arc + atmosphere elevation done. 12/12 visual states. **1 OPEN TUNABLE for Kevin (low-priority, tweak anytime — documented knobs):** **magic-hour color band** — eyeball `frontend/tests/visual/baseline/explore-day.png` (+ explore-night/boss-obsidian); premium-not-candy is my default, dial via `src/render/mood.js MOOD_GRADE` (saturation/brightness/contrast per mood) + `src/render/LightMotes.jsx uScale` (mote presence).
> **✅ RESOLVED (2026-06-02, Kevin asked): studio-fixture mote isolation** — the always-on light motes were bleeding ~0.25% into the 3 sky-studio gate fixtures (character/boss-closeup, spell-cast). Fixed via a dedicated `captureStudio` store flag (declarative identity — the studio-card hooks SET it; `GameScene` gates `<LightMotes>` off when true; decoupled from `hudHidden`). Motes preserved in all in-world frames; the 3 studio frames re-baselined mote-free. New static gate `tests/gates/atmosphere-isolation-gates.test.js` (5 tests) + visual 12/12. ([[feedback_visual_regression_fixture_isolation]].)
> **✅ RESOLVED (2026-06-02, Kevin caught it): mob/character ink-outline regression** — outlines appeared at spawn then vanished mid-session. Root cause: `TIERS.low.charOutline` was false + the in-game `PerformanceMonitor.onDecline` ratchets the tier one-way to `low` under FPS pressure (mob-spawn → FPS drop → downgrade → outlines unmount permanently). Fix: `low.charOutline → true` (the cheap signature ink outline is now tier-independent; perf budget comes from the expensive toggles, not the outline). Red-first gate ("charOutline ON at every tier") + unit 332 + build clean (visual baselines force `high`, unaffected).
> **🔬 FLAGGED FOR S3 (perf-tier calibration — needs real-device profiling, the audit's #1 risk; NOT fixed blind):** (1) the `PerformanceMonitor` downgrade is a ONE-WAY ratchet (onDecline only, no onIncline) → tier never recovers even if FPS rebounds. (2) **Tier calibration (CORRECTED — my earlier ≤8 claim was STALE):** `high` IS reachable on modern 16GB+/8-core desktop Chrome (`navigator.deviceMemory` reports up to 16/32 per MDN, satisfying `>=12`); **Safari/Firefox lack the API → start at `low`**. Genuine residue: the one-way ratchet (no `onIncline` → a transient FPS dip never recovers) + the visual suite has **no forced-med/low baseline** (only forced-high). S3 (real-device profiling): add `onIncline` + recalibrate thresholds (treat `undefined` as mid, not 0) + add med/low baselines. (`outlineWorldEdge` in that list was a **fictional/never-built** flag.) Full ledger → `memory/REALITY-AUDIT-S1-2026-06-02.md`.
> **⏸️ `challenge-memory` audit — POSTPONED (Kevin 2026-06-02):** deferred past the S1 boundary; **surface it for Kevin's decision when S2 is done** (not before). Ritual in `feedback_memory_hygiene.md`; memory dir is git-backed → safe whenever run.

> **🔒 SECURITY (your call — queued, task #32): vitest 3.2.4 → 4.x bump (GHSA-5xrq-8626-4rwp, Critical).** Real risk to us ~nil: vitest is a **dev-only** devDependency (never shipped to players), we run `vitest run` not the vulnerable `--ui`/`--api` listening server, and `@vitest/ui` isn't installed — the exploit path isn't exercised. The only fix is a **breaking major bump** (3→4, no 3.x patch). **Recommended: defer past the M-build** (a breaking test-framework migration mid-TDD is the wrong moment). Two options: (a) **dismiss** the 2 Dependabot alerts as not-exploitable-in-our-usage; or (b) **migrate post-M1** on its own branch (bump + re-run unit/visual + fix any v4 breakages). I lean (b) soon.

> **📱 TOUCH/MOBILE INPUT — M0+M1+M2a SHIPPED, M2b/M3 next (loop iters 127-137, 2026-06-13).** 👁️ **FIRST TOUCH-UI EYEBALL FOR YOU:** `frontend/tests/visual/baseline/mobile.png` — the touch overlay over the game (left joystick ring + nub, bottom-right gold sword/cast/jump buttons, center crosshair, top-right pause). Honest call: it's a clean, functional, premium-enough FIRST touch UI, but the **joystick ring is a touch subtle** (M2b will crisp it) and several existing HUD elements (minimap, XP/level bar, keyboard cheatsheet, the left tool-column with inventory/craft/magic openers) are currently **HIDDEN on touch** (they collided with the thumb zones) — so on touch you can't open inventory/craft/magic yet (the M3 panel-TRAY restores access; M2b/M3 will reposition the minimap/XP-bar for touch rather than hide). Decision point if you want: is "hide-and-defer" OK for these, or prioritize the touch tray sooner? (Default: continue the ladder.) The §0 hard-frame gap: the game is currently UNPLAYABLE on its stated iPad/mobile target (verified ZERO touch handlers; Pointer-Lock-gated, which iOS Safari lacks). A grounded 3-lens design-gate workflow (`wf_1c9d17fc-f40` — live seam-map + SOTA-2026 touch-control research + adversarial pre-mortem) produced the committed spec `docs/superpowers/specs/2026-06-13-crafty-touch-input-design.md` (dual-zone floating-joystick + drag-look + crosshair + tiered actions; reuses the existing intent SoT / verb-router / active-gate seams with ZERO movement-consumer change; M0→M3 ladder, each device-free verifiable). **A real-bug NOTICE for you (verified, NOT touch-specific):** `gameStarted` is read 5× (MenuSystem :193/216/226, App :164/:171) but has NO setter anywhere in `src/` → it's always `undefined`, so those `gameStarted && requestPointerLock()` branches AND the App pause-on-unlock guard (App :171) are DEAD. Likely a latent desktop pause-menu bug; the touch focus model deliberately does NOT rely on it (uses isWorldBuilt + active + isAlive). Flagging for a separate desktop fix. **Self-gated touch-UX decisions-of-record (charter §5; reversal = spec/plan edits):** (1) touch pause = an explicit on-screen Pause button → `setActive(false)`, NOT the lock-transition effect (which auto-opens Settings); (2) `touchSensitivity ≈ 1.0–1.5×` the 0.002 rad/px PLC base + optional invert-Y, exposed in Settings (M2) — real value is your on-glass call; (3) zone split = left half move / right half look, buttons bottom-right, tap-vs-drag threshold on the right zone. **PARKED for you (real device only):** sustained FPS on a real iPad/phone under siege; drag-sensitivity & deadzone FEEL tuning; thumb reachability across sizes; real multi-touch hardware behavior; live iOS quirks. Everything else is machine-verified (unit + a new `mobile.png` visual fixture + static gates). M0 (pure `touchMath.js`) + M1 (the `TouchControls` overlay — capture-safe/desktop-inert, wires the joystick/look/action through the existing intent/verb/active seams) are SHIPPED; M1 was adversarially reviewed-clean (NO blocking/high; 3 MEDIUMs fixed) — 990 unit / 17 visual, the game is now WIRED to be touch-playable (no desktop change). M2 (the VISIBLE S1-C thumb-cluster surface — joystick nub / button cluster / crosshair — + the first overlay visual baseline `mobile.png` + a touch-sensitivity Setting) is next; that's the first touch item with an eyeball + the real-device feel/FPS pass (parked for you).
>
> **🎮 CAMERA-KICK juice (loop iter 139, 2026-06-13) — FEEL-CHECK for you (in-play, not an eyeball):** every melee swing now recoils the camera back+down and every spell cast pushes it up+forward — a brief tactile "weight" (the §6 per-verb-camera-kicks item). It's SUBTLE by design (a few cm, ~0.2s recovery) + capture-safe (no baseline change). **Tunables if it reads too strong / too weak / nauseating:** `frontend/src/game/cameraKick.js` → `KICK_PROFILES` (per-verb magnitudes) + `KICK_DECAY` (recovery speed). Slam (voidhand) + landing kicks SHIPPED @ iter 153 (every VOIDHAND slam now recoils hard-down+forward; every landing dips the camera — high-frequency tactile weight). The per-verb camera-kick set is now COMPLETE (melee/cast/slam/land). No decision needed — flag only if any kick's feel is off (tune in `KICK_PROFILES`).

> Accumulated while building out the master plan autonomously (per Kevin 2026-06-01: "keep building the entire plan, batch anything for me to review/decide for when you complete it, assume all approvals"). Each item = a rendered frame to eyeball or a decision to ratify. Nothing here blocks the build; I proceed on best judgment + log it here. **Review at the end (or any time).**

## 🖼️ Frames to eyeball (visual taste check)
- **S1-C-M1 showcase (DONE, on `main`):** `frontend/tests/visual/baseline/primitives-showcase-{en,zh}.png` — the bold-flat design system, matched to `final-A`. (You caught the first divergence; this is the fixed version.)
- **S1-C-M2a HUD (DONE):** `frontend/tests/visual/baseline/{explore-day,explore-night,boss-obsidian}.png` — the in-game HUD migrated to bold-flat (StatBars with icons, Slot hotbar, bold-flat spell chip + minimap/compass + XP/level; ❤/🍖 emoji + minecraft-bevel gone). My audit: hotbar crisp, StatBars clean. **Mid-migration mix is expected** — the top-left quest panel + top-right controls panel are still the old languages (quests=neon→M2c; controls=its own→folded into M2c). The HUD layout/positions are unchanged from before (M2a migrated chrome, not layout).

## 🤔 Decisions to ratify (proceeded on best judgment)
- **HUD layout polish (deferred):** M2a migrated the HUD *chrome* but kept the existing *layout* (bar positions, the quest panel + controls panel + spell chip all clustering top). A dedicated HUD *layout* pass (hierarchy, spacing, thumb-zone, decluttering) could be an S1-D or M2-polish item — flag if you want it scheduled.
- **zh-body font = real Alibaba PuHuiTi 3.0** (subset common-CJK, 2.9MB lazy) — sourced from a jsDelivr npm mirror (`alibabapuhuiti-3-55-regular`), verified genuine. OK as the shipping zh body?
- **game-icons.net = CC BY 3.0** → a credits screen is owed (scheduled for M3). OK to keep game-icons (vs Lucide-only)?
- **Monetization / S4** (cosmetics + transparent pass, NO gacha) — when I reach S4 I'll surface the concrete monetization plan here for your sign-off before any pricing/store wiring.

## 📋 Known tech-debt / residuals carried (non-blocking)
- explore-night ~0.06% residual (terrain chunk-stream meshing order) — under the 6% gate, separate subsystem.
- `showcase-scene.png` 753KB lives in `src/ui/` (DEV-only, tree-shaken from prod) — could be compressed; cosmetic.
- GameSystems/SimpleExperienceSystem VFX overlays still use raw Tailwind color classes (`text-red-500` etc.) on dramatic effects (not chrome) — a future raw-Tailwind→token pass if desired.

## ✅ Phases completed this run (all merged to `main`, gates green)
- S1-C-M1 (token foundation + primitives + i18n) + fidelity pass + all tech-debt + residuals.
- S1-C-M2a (HUD consolidation → bold-flat).
- _(appended as phases complete…)_

- **S1-C-M2b modals (DONE):** `frontend/tests/visual/baseline/inventory-open.png` — the migrated Inventory modal (glass→bold-flat: Panel shell, paper-doll + gear Slots, rarity-FILLED item grid w/ 2-tone icons, Combat-Stats panel, gold Equip). All 5 modals migrated. (CraftingTable/Magic/Building/Settings not separately captured — verified via build + the shared pattern.) Note: `AchievementsPanel` (QuestSystem) still glass → migrating in M2c.

- **S1-C-M2c neon→bold-flat (DONE):** `frontend/tests/visual/baseline/achievements-open.png` — migrated AchievementsPanel + QuestTracker; explore-day/night/boss-obsidian re-baselined (bold-flat quest panel/boss bar/notifications). The **single-UI-language hard gate is now GREEN**. Achievement/quest TEXT still has emoji (🔪/👣/⚔️…) — that's M3's data-decouple. **NEW: M2d** will retire the last 3 in-game glass usages (`SimplifiedNPCSystem.jsx` NPC trading modal + dialogue bubble).

- **S1-C-M2d NPC glass (DONE):** `SimplifiedNPCSystem.jsx` NPC trading modal + dialogue bubble + controls panel → bold-flat (the last in-game glass). Gate tightened to ban `backdrop-blur` in-game (only App.jsx pre-game splash + dev DebugOverlay excluded — both non-game-chrome). Not in a capture state (mounted via NPC-proximity) — no frame to eyeball; verified via build + the tightened gate. **S1-C UI consolidation COMPLETE.**

- **S1-C-M3 icons + emoji-decouple (DONE → S1-C COMPLETE):** **all emoji removed from `src/` (215→0)**; formalized the game-icon system (19 new filled game-icons.net glyphs, CC BY 3.0) + a centralized `src/data/items.js` registry; zero-emoji hard gate GREEN. **Frames to eyeball (re-baselined):** `frontend/tests/visual/baseline/{menu,explore-day,explore-night,boss-obsidian,inventory-open,achievements-open}.png`.
  - **`menu.png`** — the 🧙‍♂️ mascot → a filled **wizard-hat** icon; "Start Adventure" → sword icon + text. (Mascot is a *placeholder* — the real signature mascot is S1-D phase-2.)
  - **`achievements-open.png`** — unlocked = filled game-icons (footprints/sword/skull/star/pickaxe…), locked = lucide lock; quest tracker shows per-quest icons.
  - **`inventory-open.png`** — item tiles use filled 2-tone icons with rarity fills; **the deliberate rarity FIX is visible: Golden Crown now renders as a GOLD legendary tile** (was grey/common under the old emoji-prefix bug). My audit: icons render crisp, semantically correct, consistent bold-flat. No emoji anywhere.
  - I verified these 3 myself; the other 3 (explore/boss) changed only in quest-tracker/compass/boss-bar icons (sub-threshold). **One taste call for you:** the placeholder wizard-hat mascot on the title screen — fine until the S1-D real mascot, or want it sooner?

## 🤔 Decisions ratified in M3 (proceeded on best judgment)
- **game-icons.net KEPT + credited** — a `CreditsScreen.jsx` (reachable from Settings) attributes game-icons.net (CC BY 3.0) + the fonts. (Answers the M2 open question "OK to keep game-icons vs Lucide-only?" — kept, properly credited.)
- **Deliberate rarity fix shipped** — decoupling emoji from item names exposed + fixed a latent bug (emoji prefix broke exact-match rarity → Golden Crown/Star Fragment were COMMON, Mana Potion/Emerald/Ender Pearl mis-tiered). Now correct. Damage/Shield Scroll bumped common→rare (buff consumables) as a taste enhancement. Flag if you'd tier any item differently.
- **3D loot-drop sprite** — the floating loot drop previously painted the item's emoji on a canvas billboard; that emoji glyph was removed (kept the rarity-colored octahedron + light beam). A proper game-icon billboard on physical drops would be a small SVG→WebGL-texture task (candidate for S3 polish) — flag if wanted.
