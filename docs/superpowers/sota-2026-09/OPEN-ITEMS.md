# Crafty — every historical punted / open / owed / deferred item, verified against live code

**Produced 2026-09-22.** Verified at `HEAD = ceef70a4` (local, **2 commits ahead of `origin/main` = 46a36015**).
Working tree is dirty (see OI-29).

**Method, and its denominator.** Nine primary surfaces mined exhaustively (`memory/ACTIVE_PLAN.md`,
`memory/STATUS.md`, `memory/ROADMAP.md`, `memory/ARCHITECTURE.md`, `docs/superpowers/DECISIONS.md`,
`KEVIN-REVIEW-BATCH.md`, `LOOP-CHARTER.md`, `LOOP-KERNEL-PROMPT.md`, `SOTA-INITIATIVE.md`) plus fourteen audit
files, the two live plan docs (`plans/2026-08-11-crafty-phase-c-*`, `plans/2026-08-11-crafty-v1-gate-corpus.md`)
and `TERRAIN-GRASS-SOTA-PLAN.md`. **Every row below carries its own live check** — a grep, a file read, a
`node` run, `gh pr list`, `gh run list`, or a git-history query. No row is asserted OPEN on the strength of
the prose that mentions it. Where the doc and the code disagree, **the code wins and the doc is filed as
STALE** (see §G — 13 stale claims, and three are load-bearing).

**Counts.** 104 candidates enumerated — 97 carry an ID, 7 folded as duplicates. **73 STILL OPEN** (26 HIGH · 30 MED · 16 LOW, plus OI-76 filed under §D for clarity) · **19 ALREADY DONE** (the doc is stale, do not re-open) · **5 OBSOLETE or PARKED-BY-DESIGN**. **13 are Kevin decisions**; a further **7 are loop work that cannot close without his eye, ear or thumb.**

Legend — **who can decide:** `loop` = full loop authority under the 2026-08-11 grant · `Kevin` = irreversible,
outward-facing, taste, money, or a new dependency. **Blast radius** is *what it costs if it stays open*, not
effort.

---

## A. STILL OPEN — HIGH blast radius

| ID | Title | Source (file:line) | Cat | Status verdict + EVIDENCE | Blast | Decides |
|---|---|---|---|---|---|---|
| **OI-01** | **three r174 upgrade — take it for the GL fix, accepting a full reviewed re-baseline?** | `docs/superpowers/DECISIONS.md:20-73`; `memory/ACTIVE_PLAN.md` (08-13 night cursor) | decision | **STILL OPEN.** `frontend/package.json:24` is `"three": "^0.172.0"` and `require('three/package.json').version` → **0.172.0**. The experiment was fully reverted; nothing is committed but the decision entry. Measured with a presence control: r174 takes the production bundle's `glBlitFramebuffer` storm **6 → 0**. Cost: **19 of 31 frames over gate**, `explore-day` 99.70% local vs a 9.30% ceiling, visibly more saturated sky — a LOOK judgement. | HIGH | **Kevin** |
| **OI-02** | **Does r174 fix the SUN? — UNRESOLVED** | `DECISIONS.md:46-49` | decision | **STILL OPEN, and still unanswerable as posed.** `explore-day` shows no sun disc at that camera angle in either build, so the frame that would answer it has to be identified first. Needs a controlled **r174+6.39.1 vs r174+6.39.4** pair. Nothing in the repo has run it. | HIGH | loop (experiment) → **Kevin** (act) |
| **OI-03** | **`postprocessing` PINNED at 6.39.1 — upstream GodRays regression** | `ACTIVE_PLAN.md` (BLOCKED block); `DECISIONS.md:20-40` | debt | **STILL OPEN.** `package.json:19` `"postprocessing": "^6.39.1"`, installed **6.39.1**. Under 6.39.4 `GodRaysEffect` stops compositing the sun entirely (blue-channel ablation at y=4, x=95..165, both spell frames). 6.39.4 is still upstream `latest`, so there is no newer release to take. Cost of the pin: the glBlitFramebuffer warning class stays, and it **mutes the GL console channel** (OI-07). | HIGH | Kevin (tied to OI-01) |
| **OI-04** | **PR #18 is OPEN and would re-land the sun regression** | `gh pr list` — `dependabot/…/production-minor-patch-2f92656429`, opened **2026-09-01** | gate | **STILL OPEN — and NOT in any doc.** Dependabot re-proposed `postprocessing 6.39.1 → 6.39.4` and **CI went green on it** (run 33470990169, 16m26s, `success`). CI is structurally blind to this: the visual gate is in neither the pre-push hook nor `ci.yml`. A green tick here is a statement about unit tests and bundle size, not about whether the sun is in the sky. **Do not merge on green.** | HIGH | loop (close it) |
| **OI-05** | PR #17 open — dev-dependencies group, 4 updates | `gh pr list`, opened 2026-09-01, CI `success` | debt | **STILL OPEN.** Not in any doc. Precedent from PR #13: a "dev-dependency" bump silently split `@dimforge/rapier3d-compat` and made an integration test drive different WASM than production. Review the *entries*, not the section name. | HIGH | loop |
| **OI-06** | **`prod-smoke.mjs` cannot see the error class it exists to watch** | `frontend/scripts/ci/prod-smoke.mjs:78` | bug | **STILL OPEN.** Live line: `if (m.type() === 'error' && isFatalMessage(m.text())) errors.push(...)`. The GL storm arrives as `warning`, so the repo's own production gate is structurally blind to it. It *is* wired into CI (`.github/workflows/ci.yml:107`), which makes the blindness worse, not better. **Explicitly NOT gated on the r174 decision — widening it is cheap and independent.** | HIGH | loop |
| **OI-07** | **Production-bundle WebGL error storm (128× + 128× per frame, until Chrome mutes the context)** | `ACTIVE_PLAN.md` (OWED block, 2026-08-13) | bug | **STILL OPEN.** Root cause is now known (three.js depth-texture cloning, mrdoob/three.js#30585, fixed in r174) so it is blocked behind OI-01. 256 is a FLOOR, not a count — and once muted, a genuine GL error later in that same context is silenced too. That is the part that matters. Also measured and worth keeping: **production LCP 237 ms vs the dev server's 1559 ms** — any perf reasoning from dev numbers is 6.6× wrong. | HIGH | Kevin (via OI-01) |
| **OI-08** | `FATAL_GL_RE` does not match the storm, and capture runs the DEV server so it would never see it anyway | `frontend/scripts/visual/capture.mjs:212` | gate | **STILL OPEN.** Live regex: `/THREE\.WebGLProgram: Shader Error\|WebGL context lost\|CONTEXT_LOST_WEBGL\|Error linking program\|shader compilation/i` — none matches `GL_INVALID_OPERATION: glBlitFramebuffer`. The recorded instruction stands: **do not simply widen `FATAL_GL_RE`** — the fix is a probe that loads the PRODUCTION build and asserts on its console. | HIGH | loop |
| **OI-09** | **`explore-day` is NOT settled when it is shot — and it is refused by the ratchet** | `ACTIVE_PLAN.md` (⚠️ THE ONE UNIT IN FLIGHT); `frontend/tests/visual/.density-ledger.json` | bug | **STILL OPEN.** Live ledger: `_ungateable: {"explore-day":{"observed":0.3035,"wouldFreezeAt":0.547,"kept":0.093}}`. Two samples exist — 5.13% and 30.35% local — so this is a defect, not a tolerance. `waitForStableFrame` runs inside `shot()` and still does not catch a late chunk arrival on this pose. **Do NOT widen the ratchet to make this green.** | HIGH | loop |
| **OI-10** | **`needStable` parked at 2; the hypothesis that it should be 5 is unproven** | `frontend/scripts/visual/capture.mjs:236` (30-line parked block), `:264` | debt | **STILL OPEN.** Live call: `waitForStableFrame(page, { needStable: 2, interval: 200, max: 25, floor: 120 })`. Parked 2026-08-13 so PR #15 could be validated against ONE variable. `waitForStableTerrain` already demands **six** consecutive stable polls for exactly this reason. Measured cost of raising it: **~0** (57.2 s/frame vs 57.6 s/frame). Its one trial WEDGED. | HIGH | loop |
| **OI-11** | **A capture pair on a QUIET machine is OWED** | `ACTIVE_PLAN.md` (OWED, 2026-08-13) | gate | **STILL OPEN.** Nothing since. Last committed capture (`.capture-meta.json` in HEAD) finished **2026-08-12**; the machine was at load 21 on 14 cores when the needStable=5 trial ran. Preconditions recorded: expect frames MORE complete than the old baselines (that red is signal, not noise); `explore-day` stays `_ungateable` until a pair shows its variance has fallen; **do NOT re-baseline `ocean-coast` / `landmark` / `explore-day-med`** — the capture explicitly WARNs they never stabilize, and re-baselining one freezes noise. | HIGH | loop |
| **OI-12** | **Committed baselines are Chromium 147; the harness now runs 151** | `frontend/tests/visual/baseline/.capture-meta.json` (in HEAD) | debt | **STILL OPEN, and newly precise.** HEAD's capture-meta records `HeadlessChrome/**147**.0.0.0`; `puppeteer` is **25.6.0** (Chromium 151), bumped `2026-08-13` by `3c0b188`/`5848d64`. Last baseline commit is **`043dcdb0`, 2026-08-12** — i.e. *before* the bump. **Mixed provenance:** `.density-ledger.json` `_sources: ["rbA","rbB"]` IS from the Chromium-151 pair (frozen at `e077dc9`, 08-13), so the ledger and the PNGs it ratchets were shot on different renderers. Mitigating: the 151 pair measured 17/31 byte-identical and ZERO frames over the 6% gate, so the bump was a non-event — but the oracle's own provenance record is now wrong. | HIGH | loop |
| **OI-13** | **Density ledger: 23 of 31 frames are floor constants, not measurements; 1 rests on a single run** | `.density-ledger.json` `_unmeasured` / `_samples` | gate | **STILL OPEN — the seed's "23 of 31" is exactly right.** Live: `_unmeasured: 23`, and `_samples["title-mascot"]: 1` (every other frame is 2). For those 23 the ratchet compares against the constant `0.02`, so it cannot distinguish "reproduces almost exactly" from "regressed by less than the floor". The ledger's own note is the standing instruction: **do NOT raise `DENSITY_FLOOR` to shrink this number** — that widens every floored entry at once. | HIGH | loop |
| **OI-14** | **V1 — the gate corpus: 20 VACUOUS + 5 BRITTLE gates, seam-extraction Batches 0–9 unstarted** | `memory/STATUS.md:977`; `plans/2026-08-11-crafty-v1-gate-corpus.md` | debt | **STILL OPEN.** Classification IS done (115 files: MIXED 82 · VACUOUS 20 · STRUCTURAL-CORRECT 8 · BRITTLE 5); the **conversion** is not. Live: `node scripts/ci/gate-shape.mjs` → `388 assertions verified; 106 source-grep gates (ratchet holding)`, `.source-grep-ledger.json _count: 106`. The plan's ten batches (worker harness · jsdom render · SoundProvider · capture policy · combat attribution · worldgen seams · quality ladder · trade/inventory · compass/minimap · small lifts) are all unstarted. The unit is per-TEST, not per-FILE. | HIGH | loop |
| **OI-15** | **V2 — input-driven E2E harness ("Playable Truth")** | `memory/STATUS.md:1045` | debt | **STILL OPEN, and measured worse than the doc says.** Live: `grep -rlE "keyboard\.\|mouse\.\|\.click\(\|\.press\(" tests/e2e/` returns **exactly one** file (`panel-overflow.spec.js`) out of **20** specs. The gap IS closable — every verb reads `getInput().active`, not pointer-lock, so Playwright can flip it through `devtest/testBridge.js` and fire real keys. The input-abstraction layer was built for this and never used for testing. | HIGH | loop |
| **OI-16** | V3 — verb-truth / HUD-truth / click-truth on that harness | `memory/STATUS.md:1050` | debt | **STILL OPEN** (depends on OI-15). LMB=mine · LMB-on-mob=melee · RMB=cast/place · F=cast · T=melee · 1-4=select; bars render real state; Use→heal, talent unlock, craft, quest claim, chest open, respawn. | HIGH | loop |
| **OI-17** | V5 — seeded sim RNG + state-hash replay gate | `memory/STATUS.md:1056` | debt | **STILL OPEN, count re-measured.** Live: **73 `Math.random()` across 21 files** in `frontend/src` (the doc says 107, then 72/20 — all three are stale; re-measure, never quote). mulberry32 lives only in `captureMode`/`perfProbe`. A sim regression cannot be reproduced or bisected today. | HIGH | loop |
| **OI-18** | **The `beast-*` determinism root cause is still unfound (4th pass, 3 wrong conclusions)** | `memory/STATUS.md:897`, `:954` | bug | **STILL OPEN.** `spawnBeastTransform` is still the only showcase hook that depends on live physics state (`src/App.jsx:422`); `spawnCharacterCloseup` is the pattern to copy (FIXED constants, documented why). Success criterion is already written and checkable: **two consecutive `visual:capture` runs must produce byte-stable `beast-*` frames.** Next experiment recorded: run the FULL capture twice and diff `beast-*` against each other AND the stage before it. Note the 08-11/08-12 re-baselines (`8b1a45e`, `043dcdb`) replaced the empty-landscape beast baselines, so the *broken oracle* half is closed — the *race* is not. | HIGH | loop |
| **OI-19** | **D1 — the boss is the art emergency** | `memory/STATUS.md:1088` | feature | **STILL OPEN, root cause confirmed live.** `src/render/BossEntity.jsx:479-481`: `bodyColor = "#111029"` (correct obsidian) but `bodyEmissive = phase.color` at `emissiveIntensity` 0.8 → 2.2 → the indigo floods the ENTIRE torso and drowns the obsidian. It reads as a flat purple box — *cheaper-looking than the trash mobs*, and it is the payoff of the entire run. **Do NOT "fix" `bodyColor`;** move the phase colour off the torso onto DETAIL. | HIGH | loop (Kevin DE-GATED art 2026-07-13) |
| **OI-20** | **E1 — recurring apex threat; the project's own "single highest retention lever"** | `memory/STATUS.md:1115` | feature | **STILL OPEN.** `grep -rn "bossTier" src` → **zero hits**. The boss is a one-shot L5 event. | HIGH | loop |
| **OI-21** | **E2 — build-identity talents: no capstones, no mutually-exclusive picks, no respec** | `memory/STATUS.md:1117` | feature | **STILL OPEN.** `grep -rn "respec" src` → zero hits in talent code (only `prefers-reduced-motion` / `respective` false positives). ~20 flat +stat nodes drain by L18 → no reason for a 2nd run. | HIGH | loop |
| **OI-22** | **ROADMAP Phase 26 — physically-based sky (scattering · volumetric clouds · light shafts)** | `memory/ROADMAP.md` Phase 26 `[PLANNED]` | feature | **STILL OPEN.** Kevin's call 2026-08-13, deliberately deferred until the postprocessing work closes (i.e. behind OI-01/OI-03). **Read its warning block first:** the sun is a camera-locked billboard at a fixed 380 units existing as `GodRaysEffect`'s light source, reparented into the effect's `lightScene` every frame, bound by the vendor contract *"must not write depth and has to be flagged as transparent"*. Making it real scene geometry is NOT an upgrade. Volumetric clouds need a perf budget agreed **before** building — the cost lands on the constrained low/med tiers. | HIGH | Kevin (start) / loop (build) |
| **OI-23** | **S4 — multiplayer + monetization scope** | `memory/STATUS.md:1391` | decision | **STILL OPEN.** Servers, accounts, real money, legal. The loop will not start netcode or payments without Kevin's model. Hard line already recorded: no randomized gacha/lootboxes, COPPA, odds-disclosure-if-random. | HIGH | **Kevin** |
| **OI-24** | **#44 — the holistic playtest** | `memory/STATUS.md:1389` | gate | **STILL OPEN — the perennial one.** It gates every taste sign-off: spell look, movement feel, storm, **audio mix (his ear)**, real-device touch feel. Every other Kevin row below is downstream of it. | HIGH | **Kevin** |
| **OI-25** | `failOnFlakyTests` not flipped; the flip criterion is not met and cannot currently accrue | `DECISIONS.md:475-512` | gate | **STILL OPEN.** Live `playwright.config.js:16` is `retries: process.env.CI ? 2 : 0` with no `failOnFlakyTests`. Criterion: **10 consecutive completed CI runs on `main` reporting 0 flaky across all three shards.** `gh run list --workflow=CI` on `main`: the last four are `success` (2026-08-13 19:29 → 20:56), preceded by two `failure`. **No CI run on `main` since 2026-08-13** — the counter is at 4 and frozen. `scripts/ci/flaky-report.mjs` exists and runs `if: always()`, so the record accrues in the run log the moment pushes resume. | HIGH | loop |
| **OI-26** | **2 unpushed commits + a dirty working tree on `main`** | `git rev-list --left-right --count origin/main...HEAD` → `0 2`; `git status` | debt | **STILL OPEN (live state, in no doc).** Unpushed: `f5df6cc` (relocate `crafty-s0-reality-audit.js` off the unversioned `~/Code` launch root; its STALE note records a hardcoded DATE that OVERWRITES the S0 baseline — **do not re-run as-is**) and `ceef70a4`. Uncommitted: `.agent/AGENTS.md` (+23), `.claude/settings.json` (+76), `.gitignore`, **`.markdownlint.json` deleted**, `.state/compaction-events.jsonl`. Untracked: `.claude/helpers/`, `.claude/skills/`, `.githooks/post-commit`, `.githooks/pre-commit`, `.ignore`, `.mcp.json`, `.state/session-truth.jsonl`. Note `.githooks/pre-commit` being untracked means the commit-msg/trailer discipline is not reproducible from a fresh clone. | HIGH | loop |

---

## B. STILL OPEN — MEDIUM blast radius

| ID | Title | Source (file:line) | Cat | Status verdict + EVIDENCE | Blast | Decides |
|---|---|---|---|---|---|---|
| **OI-27** | Per-frame visual tolerances DEFERRED on insufficient n | `DECISIONS.md:433-473` | decision | **STILL OPEN, and deliberately so.** The audit bar is **n ≥ 10 separate-process captures, explicitly not a max-of-three**; n = 3. The calibration instrument is live and accruing: `diff.test.js` prints max local density for all 31 frames on every run, asserting nothing. Protocol when the data exists: tier by measured behaviour (byte-identical set → near-zero bound; trace-noise set → small; genuine 3D motion → its own). | MED | loop |
| **OI-28** | A determinism CONTROL inside the visual harness (capture twice, diff A-vs-B, fail above a floor) | `memory/STATUS.md:971` | gate | **STILL OPEN, partially superseded.** `assertIntraPageDeterminism` covers the intra-page half and the cross-process half was measured once at 0.0000%; the **Linux self-consistency CI job was explicitly declined** (`DECISIONS.md`, 2026-08-09 late). What is still absent is a standing A-vs-B run-to-run control in the harness itself — which is exactly the instrument OI-09 and OI-18 each needed and had to be hand-run. | MED | loop |
| **OI-29** | R2 — repo-wide sweep for closure-var-mutated-inside-a-setState-updater | `memory/STATUS.md:181` | bug | **STILL OPEN.** Never executed as a sweep. Live: 3 `set[A-Z]*((prev` sites in `src/**/*.jsx` remain to be read against the R1 shape. Fix only hits with a real multi-dispatch caller. | MED | loop |
| **OI-30** | R3 — `smoke.spec.js` canvas-detach race | `frontend/tests/e2e/smoke.spec.js:15` | bug | **STILL OPEN.** Live line is still `const canvas = page.locator('canvas').first();` while three `<Canvas>` mounts exist (`GameScene.jsx:125`, `render/TitleDiorama.jsx:60`, `render/mascots/MascotStudio.jsx:75`). Bind the game canvas explicitly. **Do not widen the 60 s timeout.** | MED | loop |
| **OI-31** | C1 — the four control-scheme Option-A enhancements, authorized and unbuilt | `memory/STATUS.md:1073` | feature | **STILL OPEN, all four verified unbuilt.** (1) verb-telegraph reticle — `input/verbRouter.js` already exports the pure `routeMouseVerb(button, ctx)`, so the reticle is "render what it already predicts"; (2) hold-Alt force-build — `grep -rn "altKey" src` → **zero hits**; (3) persistent control legend — today an 8-second auto-fade in `HUD.jsx`, *literally the undiscoverability bug the spec names*; (4) full key-rebinding. Kevin picked Option A on 2026-06-28 and these were AUTHORIZED loop work; the `[KEVIN-GATED]` tag on them was stale. | MED | loop |
| **OI-32** | D3 — the player/character reads *monstrous* | `memory/STATUS.md:1108` | feature | **STILL OPEN.** `character-closeup.png` is a green box-man with **red eyes** (this game's own hostile marker) and no arms. Judge by opening the PNG, not by reading the code. | MED | loop |
| **OI-33** | D4 — FPV viewmodel, verify against the live view | `memory/STATUS.md:1110` | gate | **OPEN as a VERIFICATION, not a build.** The gloved-hand art exists: `src/render/playerRender.jsx:30-32` defines the FPV glove tokens (`GLOVE_INK = '#2A2A33'`). What was never done is looking at it in the running game. `scripts/visual/hands-probe.mjs` exists. | MED | loop |
| **OI-34** | E3 remaining slice — the three AI arms are unchanged; a moss_brute still *pathfinds* like a zombie | `memory/STATUS.md:1135` | feature | **STILL OPEN.** `game/mobArchetypes.js` moved the *numbers* (aggro radius, leash, reach, cooldown, vertical reach) into per-type data — verified live at `src/workers/ai.worker.js:192`. The *movement* did not: `ai.worker.js:278` `else if (type === 'skeleton')` and `:299` `else if (type === 'spider')` are still the only two arms besides beeline+bonk. Distinct MOVEMENT (a brute that shoulders through, a hound that flanks) is the next slice. | MED | loop |
| **OI-35** | E4 — the dawn/dusk payoff spike half of the cinematic-beats lever | `memory/STATUS.md:1137` | feature | **HALF OPEN.** The boss-entrance beat SHIPPED 2026-08-05 (`game/bossEntrance.js`: 220 ms freeze + 650 ms bloom swell + 1.4 shake, capture-suppressed). The dawn/dusk spike did not — `grep -rn "dawnPayoff\|duskSpike\|dawnBeat" src` → zero hits. | MED | loop |
| **OI-36** | E-ter — no ROAR on the boss entrance; needs an audio-asset decision | `frontend/src/game/bossEntrance.js:14-16` | decision | **STILL OPEN, and the code says so in its own docblock.** The sound API exposes only `defeat`/`fanfare`/`victory`; nothing reads as a dragon. The beat deliberately ships SILENT rather than firing `playVictory` at an entrance because it was the closest thing to hand. Inventing an audio asset is outside a loop slice. | MED | **Kevin** (asset) |
| **OI-37** | E-ter — directional hurt flinch | `memory/STATUS.md:1260` | feature | **STILL OPEN.** `game/hurtFeel.js` grades the freeze on a fraction of max health, but `hurt` is undirected (`right = 0`) though the store already computes `lastHitDir.angle` and `trauma.js` already biases shake by direction. Deliberately not done blind — it is a feel change that wants a human eye on a real hit. | MED | loop → Kevin (feel) |
| **OI-38** | E-ter — the chest LMB data-loss bug | `DECISIONS.md` ("Older open items, dispositioned"); `memory/STATUS.md` B8→KEVIN block | bug | **STILL OPEN, confirmed live.** `src/input/verbRouter.js:36-42`: `button === 0` has **no `chestTargeted` branch** — it falls through to `'mine'`. Only `button === 2` (`:45`) returns `'interact'`. So LMB on a chest still mines it and loses its stored inventory. Already **re-assigned `[LOOP]`** on 2026-08-08 ("losing a chest's contents to a mis-click is data loss, not a balance question"); option (a) — LMB opens — is the only one that cannot destroy player property. Caveat carried from the audit: `verbRouter.test.js` §5-12 explicitly tests LMB→mine as existing cleanup, so that test moves with the fix. | MED | loop |
| **OI-39** | 500 ms **global** damage-lockout — a pack of N deals the damage of ONE | `frontend/src/store/useGameStore.jsx:825` | bug | **STILL OPEN, confirmed live:** `if (now - state.lastDamageTime < 500) return;`. Re-assigned `[LOOP]` 2026-08-08; belongs in a game-feel unit with a lived probe. | MED | loop |
| **OI-40** | Mob AI pathfinds in 2D | `memory/STATUS.md` B4 block; `DECISIONS.md` older-items | feature | **STILL OPEN.** Re-assigned `[LOOP]` 2026-08-08 with the standing caveat: *"3D pathfinding is a project, not a fix, and should be planned before it is started."* Note the related B4 defect **is** closed — `hasLineOfSight` is a real import in `ai.worker.js:18`/`:244` with its own pure tests, so mobs no longer hit you through 200 blocks of rock. | MED | loop |
| **OI-41** | R4b — glass places correctly but renders OPAQUE | `frontend/src/world/blockIds.js:44-46`; `src/world/mesher.js:79`,`:84` | debt | **STILL OPEN, confirmed live.** The greedy mesher's only non-solid block is water: `const aIsSolid = blockA > 0 && blockA !== 9`. True see-through glass needs a second transparent draw pass — a separate render slice. R4a (the id registry, ores 10-13, water 9, cobblestone 14, glass 15, refuse-don't-substitute) IS shipped. | MED | loop |
| **OI-42** | F1 — per-frame allocations, 3 of 4 sites still live | `memory/STATUS.md:1294` | debt | **PARTLY OPEN — one site is fixed, three are not.** FIXED: `Ocean.jsx:84` now calls `gerstnerNormalInto(_n, wx, wz, t)` (no fresh array per vertex per frame). STILL LIVE: `Components.jsx:1120` `new THREE.Vector3()` inside the movement branch; `render/MobModel.jsx:181` full-subtree `traverse()` per mob inside `useFrame`; `HUD.jsx:480` compass `container.innerHTML = markersHtml.join('')` inside an rAF loop (which also restarts the marker CSS animation). The CHANGELOG's "no per-frame allocs remain" is an overclaim — see STALE-10. | MED | loop |
| **OI-43** | F2 / terrain S2 — perf-harness honesty | `memory/STATUS.md:1298`; `TERRAIN-GRASS-SOTA-PLAN.md:53-58` | gate | **STILL OPEN, confirmed by the data file.** `memory/perf/m2-KZ-M3-Max-MacBook-1781111677559.json` (2026-06-10, the ONLY recorded measurement) reports `medianMs: 16.6999999…` — quantized to the 60 Hz vblank, so the M2 "budget" is effectively a boolean. The C−B scenario delta structurally cancels the ocean/weather/spell VFX it claims to measure. Success test already written: an A median that is **not** `16.7`. | MED | loop |
| **OI-44** | F3 / terrain S13 — biome ground-tint has zero readers | `memory/STATUS.md:1301`; `TERRAIN-GRASS-SOTA-PLAN.md:136-146` | feature | **STILL OPEN, confirmed live.** `src/world/biomeTable.js:38-49` declares a `tint` on all ten biomes; a repo-wide grep finds **no consumer**. Six biomes share `surfaceBlock: 1` (taiga/plains/forest/meadow/jungle/savanna) so six render pixel-identical at ground level. Kevin's own question: *"how do different biomes appear?"* Moves baselines on every ground-bearing frame; tint **strength** is an owner call. | MED | loop (build) / Kevin (strength) |
| **OI-45** | F4 — the i18n #73 unblock: items are still keyed by DISPLAY NAME | `memory/STATUS.md:1303` | debt | **STILL OPEN, confirmed live and explicitly re-asserted in code.** `src/data/lootTables.js:6-8`: *"Every `item` string MUST be a valid display name in `src/data/items.js`"*, and `useGameStore.jsx:640` keys `inventory.blocks` by whatever string it is handed. An `ITEMS` id registry + `NAME_TO_ID` + `getItemName(id)` now exist (`src/data/items.js:56,107`) and the crafting panel was converted (`CraftingTable.jsx:168` renders `getItemName(outKey(result))`) — so the refactor is *started*, not done. The id/display decouple is pure engineering and is the actual blocker on zh-CN content. | MED | loop (refactor) / **Kevin** (zh-CN go/no-go) |
| **OI-46** | Terrain **S10** — shadow frustum does not follow the player | `TERRAIN-GRASS-SOTA-PLAN.md:116` | feature | **STILL OPEN, confirmed live.** `GameScene.jsx:116-126` builds a static ±100 box memoized only on `q.shadowMapSize`, and `render/Atmosphere.jsx:244` hard-codes `position={[50, 100, 50]}`. Note Phase-C Batch 1 DID land (`castShadow` is now unconditional — shadows are in the oracle for the first time), which makes S10's payoff gateable rather than invisible. | MED | loop |
| **OI-47** | Terrain **S11** sky-coloured AO · **S12** macro-octave de-tile · **S14** unnormalized `Uint8` attributes · **S15** AO in the greedy merge key | `TERRAIN-GRASS-SOTA-PLAN.md:122`,`:129`,`:148`,`:155` | feature | **ALL FOUR STILL OPEN.** No implementation found for any. S15 is explicitly gated on the §5-Q2 trimesh-rebuild measurement clearing first, and needs `Uint32Array` (the `Uint16Array(4096)` mask silently truncates an 18-bit key). Q4 warns S11/S12 may be visible to the eye and invisible to the 6% gate. | MED | loop |
| **OI-48** | Terrain **S4** — AO-chosen quad diagonal, DEFERRED because three readings disagree | `memory/STATUS.md:424`; `TERRAIN-GRASS-SOTA-PLAN.md:71` | decision | **STILL DEFERRED, and honestly so.** `src/world/mesher.js` has no diagonal-flip logic. The plan says flip "when the current diagonal is the brighter one"; working the concrete case (one dark corner, three light: `a0+a2 = 3` vs `a1+a3 = 6`) says flip when it is the DARKER pair; the canonical voxel formulation reads a third way. Purely aesthetic — no behavioural test distinguishes correct from inverted, both windings stay CCW. Its recorded unblock condition (§B-race) is now satisfied, so it is *workable* — but the only verification is looking at a face with strong AO contrast. | MED | loop → Kevin (eye) |
| **OI-49** | Terrain §5 — six numbers **nobody has ever measured** | `TERRAIN-GRASS-SOTA-PLAN.md:223-`; commands given per item | gate | **ALL SIX STILL OPEN.** Q1 every frame-cost figure in all five terrain documents is structural, not measured (and the only recorded measurement pins all five scenarios at 16.6999 ms → blocked behind OI-43). Q2 Rapier trimesh BVH rebuild time per streamed chunk and per mined block — **this is S15's gate**. Q3 how many grass instances exist in a live session (81×50 is a ceiling; measured 32/50/0 on three chunks). Q4 whether S11/S12 move a baseline at all. Q5 whether MSAA is effective in this composer config. **Q6 — `Atmosphere.jsx` gated `castShadow` on capture, so every automated gate was blind to the entire shadow system; Phase-C Batch 1 closed that, and Q6 should be re-read as CLOSED-in-part.** | MED | loop |
| **OI-50** | Governance **A2** — generated counts still partial | `GOVERNANCE-REVIEW-2026-08-07.md:19` | debt | **STILL OPEN, confirmed live.** `node frontend/scripts/ci/measure.mjs` emits only `srcFiles`, `srcLoc`, `colocatedTestFiles` and `largeFiles` (banded ±10%). **Visual states, e2e specs, gate-file count and the queue split are still hand-typed** — and those are exactly the numbers that rotted twice already (this file's own history: "1182 unit / 20-state" corrected to "3189 / 31", itself a second-generation stale correction). | MED | loop |
| **OI-51** | Governance **A7** — the `[MECH:]` / `[ADVISORY]` tag pass stopped one-third done | `GOVERNANCE-REVIEW-2026-08-07.md:25` | debt | **STILL OPEN, counted live.** `grep -c "MECH:\|ADVISORY"`: `LOOP-CHARTER.md` **17** · `LOOP-KERNEL-PROMPT.md` **1** · `.agent/AGENTS.md` **0**. Until a rule says whether it has an enforcer, "no new floating TODO later" (`LOOP-CHARTER.md:251`, itself tagged `[ADVISORY — no mechanism]`) is indistinguishable from a gate. | MED | loop |
| **OI-52** | `heldf-probe.mjs` — premise six weeks stale, and it reports PASS on an absence with a dead positive control | `HOLISTIC-REVIEW-2026-07-21.md:18`; `frontend/scripts/visual/heldf-probe.mjs:4`,`:65`,`:72` | gate | **STILL OPEN — the ONE row the 215-finding review left behind.** Live header line 4 still reads *"F is now MELEE only; RMB owns cast"*, which Kevin's 2026-06-28 binding change reversed (F casts, T melees). Live line 72 still prints `PASS: held-F produced no cast; F is melee-only.` Root cause of the dead counter: the probe wraps `store.castSpell`, and `EnhancedMagicSystem.jsx:164` re-installs that function via `setState` whenever its effect re-runs, silently replacing the wrapper. The BINDING is correct; only the probe is wrong. Fix = re-premise it **and** assert its positive control before its negative one. | MED | loop |
| **OI-53** | G3 — session-close ritual at the context watermark | `memory/STATUS.md:1385` | debt | **STILL OPEN.** `grep -n "session-close\|session close"` over `.agent/AGENTS.md` and `LOOP-KERNEL-PROMPT.md` → **zero hits**. No mechanism refreshes the GitHub remote surfaces (README, repo description, CHANGELOG/STATUS) and pushes at session close. This is the proximate cause of OI-26 (two commits sitting unpushed) and OI-56 (three artifact pages drifting). | MED | loop |
| **OI-54** | X1/X2a/X2b — the touch rings have never been lived-verified OPEN | `memory/STATUS.md:1184`, `:1209`, `:1212`, `:1219` | gate | **STILL OPEN as a verification.** The geometry defect itself is FIXED — see STALE-04 — but every "NOT lived-verified — compositor fault" note still stands: the rings have only ever been SEEN closed. `touch-probe.mjs` exercises X1/X2a/X2b now, so this is one clean probe run plus Kevin's thumb. Carry forward the instrument lesson: `tapTestId` returns `dispatched=true` on non-zero size alone and **never checks that the point it taps is occupied by the element it named** — that is what produced two wrong registry lines. | MED | loop → Kevin (thumb) |
| **OI-55** | Touch HUD layout defects at 390×844, routed to Kevin | `memory/STATUS.md:1410-1412` | decision | **STILL OPEN.** Seen in the one live hour on 2026-08-05: the health/mana bars OVERLAP the hotbar's left slots, and the top-right controls collide with one clipping off the right edge. Also queued from B7: touch-target-size vs all-9-hotbar-slots-visible. The invariant to gate any fix with is pure and cheap: *no two simultaneously-visible touch targets overlap, and every one is fully inside a 390×844 viewport.* | MED | **Kevin** (ergonomics) |
| **OI-56** | Three artifact pages 10 commits behind | `node frontend/scripts/ci/artifact-currency.mjs` | debt | **STILL OPEN, measured live:** `era-review`, `loop-progress` and `sota-audit` are each **10 commits behind** (published `9b84efe`, HEAD `ceef70a4`). Under the 30-commit hard limit, so it blocks nothing today — but `artifact-currency` has blocked two pushes in one session before, because "refresh at session close" let them reach 31 behind. URLs are printed by the command. | MED | loop |

---

## C. STILL OPEN — LOW blast radius

| ID | Title | Source (file:line) | Cat | Status verdict + EVIDENCE | Blast | Decides |
|---|---|---|---|---|---|---|
| **OI-57** | Voidhand multi-phantom pool (cap-4) · Soulbind v2 faction protocol · Elemancer v2 terrain chemistry | `memory/STATUS.md:1280` | feature | **STILL OPEN.** Elemancer v2 is **parked behind the P4 no-mid-combat-re-mesh HARD VETO** — do not unpark it without a measured trimesh re-cook budget (which is OI-49 Q2). | LOW | loop |
| **OI-58** | World M4b biome palette · M4c topography · M5b seabed · M7 landmarks ("the deferred refinements") | `memory/STATUS.md:1283` | feature | **STILL OPEN.** M4b overlaps OI-44 (biome tint). | LOW | loop |
| **OI-59** | Item **sets / set-bonuses** (affixes exist, sets never built) | `memory/STATUS.md:1284` | feature | **STILL OPEN.** `grep -rn "setBonus\|itemSets\|SET_BONUS" src` → zero hits. | LOW | loop |
| **OI-60** | `aspect-underbanked` denied-reason | `memory/STATUS.md:1284` | feature | **STILL OPEN.** `grep -rn "underbanked" src` → zero hits. | LOW | loop |
| **OI-61** | WebGPU / TSL migration — specced, never done, and **technically refuted** | `memory/STATUS.md:1284`; `TERRAIN-GRASS-SOTA-PLAN.md:191` (KILLED table) | decision | **OPEN-BUT-REFUTED.** `grep -rn "WebGPU\|three/tsl" src` → zero hits. The terrain plan's KILLED table gives the reason: `onBeforeCompile` is **WebGLRenderer-only** and there are four live sites (`OptimizedGrassSystem.jsx:18`, `Ocean.jsx:87`, `characterStyle.js:48`, `Terrain.jsx:166`), plus a WebGL postprocessing chain — a pipeline rewrite for a non-bottleneck. Recommend closing it as WONTFIX rather than carrying it as a roadmap item with huge blast radius. | LOW | loop (to close) |
| **OI-62** | G1 residual — `SOTA-INITIATIVE.md` §3 is stamped "last: 2026-06-18" | `SOTA-INITIATIVE.md:69` | debt | **STILL OPEN.** The header of the LIVING master-plan status section is three months behind a repo that shipped through 2026-09. Note the file's *other* G1 defects are fixed: the `@react-three/test-renderer` trap carries an explicit **CORRECTED 2026-07-13** block (both here and at `LOOP-CHARTER.md:284`). ROADMAP already removed its hand-typed header stamp with the right reasoning — *"a date typed by hand at the top of a living file is a claim nothing checks"* — and §3 should do the same rather than be re-stamped. | LOW | loop |
| **OI-63** | G1 residual — `quality.js TIERS.outlineWorldEdge` has zero readers | `frontend/src/render/quality.js:20-22` | debt | **STILL OPEN, confirmed live.** Declared on all three tiers (`low:false`, `med:false`, `high:true`), read by nothing in `src/**/*.jsx`. Only other mention is a comment in `tests/gates/character-render-gates.test.js:66`. Classify-before-delete applies: trace the consumer graph, then wire or delete with the reason stated in the commit. | LOW | loop |
| **OI-64** | AGENTS.md MEASURED numbers have re-drifted since the 08-13 gardening | `.agent/AGENTS.md:149`,`:152` | debt | **STILL OPEN (small).** Doc says **72** raw `Math.random()` and **127** `isCaptureMode()` guards; live is **73 across 21 files** and **130 across 58 files**. This is precisely the class `measure.mjs` + OI-50 exist to end — the fix is to generate them, not to retype them. | LOW | loop |
| **OI-65** | Ocean in-cave suppression is not lived-confirmed | `memory/STATUS.md` B8 ocean block | gate | **STILL OPEN.** `oceanVisibleNear()` is pure-unit-covered for the all-land-above-sea case, and `ocean-probe.mjs` took four COAST shots (all clean) — but **no probe has ever taken a cave shot**, which is the case the gate exists for. | LOW | loop |
| **OI-66** | The deferred test-vacuity tail (~24 low-ROI items) from campaign v9 | `memory/STATUS.md:1434` | debt | **STILL OPEN.** Of 32 test-vacuity findings: mostly false positives, 3 genuinely-weak gates strengthened, ~24 deferred as low-ROI. Largely subsumed by OI-14 (the V1 corpus work classifies the same population properly). | LOW | loop |
| **OI-67** | Shareable-moment / clip / photo-mode tooling | `memory/STATUS.md:1394` | feature | **STILL OPEN.** Named by the project as a *commercial* blocker. | LOW | **Kevin** |
| **OI-68** | Final taste sign-offs — the v7 spell look, and the D art pass once it lands | `memory/STATUS.md:1395` | decision | **STILL OPEN.** Downstream of OI-24 and OI-19. | LOW | **Kevin** |
| **OI-69** | Ocean water aesthetic | `DECISIONS.md` ("Older open items, dispositioned") | decision | **STILL OPEN — and per the 2026-08-08 audit this is the ONE genuinely-Kevin row of the thirteen that queue carried.** Pure taste; the coast is lived-verified clean; **nothing is blocked on it.** | LOW | **Kevin** |
| **OI-70** | Terrain §2 — nine owner taste calls | `TERRAIN-GRASS-SOTA-PLAN.md:165-179` | decision | **STILL OPEN** (grass colour was decided; the other eight were not): grass **density** (stride 2→1, cap 150, hash-thinning to kill the bald +Z strip — note `grassTops` is structured-cloned, so 5× entries is 5× main-thread deserialize per chunk and `update_block` re-emits it on every mined block); blade **silhouette** (a tapered blade forks the bold-flat lock and needs `vertexColors: true`); **tint strength** (recommend 35% luminance-preserving, ladder at 25/35/50); **composer AA** (the scene currently pays 8× MSAA **and** a full SMAA pass — `multisampling` sits in the composer's `useMemo` deps, so wiring it to `qualityTier` rebuilds the whole composer on every `PerformanceMonitor` flip, exactly when FPS has already dipped); **shadow box size** after S10; **`castShadow` on blades** (recommend: keep off — sub-texel blade → acne not contact shadow, and no `customDepthMaterial` exists so the shadow would be unswayed under a swaying blade); **per-face grass texture** (two design locks, not one); **re-baseline batching** (two review batches, after S4 and after S13). | LOW | **Kevin** |
| **OI-71** | zh-CN full content translation (#73) | `DECISIONS.md` 06-15→06-29 migrated block | decision | **STILL OPEN, and correctly blocked.** The locale TOGGLE with English default is the decision of record; the content pass is Kevin's go/no-go and sits behind OI-45. Mechanically the surrounding work is healthy: `i18n-adoption` now reports **0 occurrences across 0 files** (the 109-string sweep is finished, ledger `_total: 0`) and `i18n-dead-keys` reports 0 dead. | LOW | **Kevin** |
| **OI-97** | The **accretion ratchet** is specified, has an owner, and was never built — and its named wiring target was archived | `docs/superpowers/specs/crafty-coherence-pillars.md:66`; `.agent/AGENTS.md` (Core Agent Skills) | debt | **STILL OPEN — half of it, and the half that was owned is the half that is gone.** The spec calls for a **deterministic script, advisory-flag only, never auto-delete**: net-LOC add/delete ratio, duplicate-block count (jscpd), dead-export/single-caller count (knip/ts-prune) and cyclomatic delta on the god-files, computed per phase-exit over the phase commit range. Live: `frontend/scripts/ci/` has `_density-ratchet.mjs` and `_gate-ratchet.mjs` and nothing else ratcheting; `grep -rn "jscpd\|ts-prune\|cyclomatic\|accretion"` over `package.json` + `scripts/ci/*.mjs` → **zero hits**. `knip` IS wired (CI), which is the better half already deterministic. The skill named as its wiring target (`ruthless-cleaner-kz`) was archived 2026-08-29, so the spec now points at nothing — decide whether the script IS the whole answer or whether a review layer sits on top of it. **The dependency half of that same note is CLOSED** — see STALE-13. | LOW | loop |

---

## D. ALREADY DONE — the doc is stale (do not re-open)

| ID | Title | Claimed open at | Evidence it is CLOSED |
|---|---|---|---|
| **OI-72** | "HOLISTIC-REVIEW: 30 open of 88" | `ACTIVE_PLAN.md` (THE CURSOR, 2026-08-12) | **DRAINED.** `HOLISTIC-REVIEW-2026-07-21.md:9` — *"All 215 confirmed findings are disposed of: 207 fixed, 8 dismissed with a runnable proof, 0 open."* Live marker count: **207 `▣✓`, 1 `▢`** — and that one `▢` is OI-52, found DURING the drain, not part of the 215. |
| **OI-73** | "PR #13 HELD, NOT ABANDONED" | `ACTIVE_PLAN.md` IN FLIGHT §2 | **MERGED** 2026-08-13 (`gh pr list` → `13 … MERGED`; commit `5848d64`), with the rapier dedup defect it exposed fixed at `9a64fc8` and Dependabot now ignoring `@dimforge/rapier3d-compat` at PATCH too. |
| **OI-74** | "PR #15 is open ON PURPOSE" | `ACTIVE_PLAN.md` OWED block | **MERGED, then correctly split.** `gh pr list` → `15 … MERGED`. History: `cd0c1bf` merged → `4a6bcdc` reverted (it removed the SUN and made hidden entities draw) → `70117975` landed the five unimplicated halves, verified visually inert. The sixth half is OI-03. |
| **OI-75** | "Filing the GodRays/sun bug upstream — NEEDS KEVIN" | `ACTIVE_PLAN.md` BLOCKED block | **FILED.** `DECISIONS.md:24-27` records that `vanruesc` replied to **pmndrs/postprocessing#750** (labelled `investigating`) within hours. What remains is the REPLY, which is OI-76 — a different act. |
| **OI-76** | Reply to #750 | `docs/superpowers/upstream-750-reply-draft.md`; `DECISIONS.md:71` | **STILL OPEN — listed here only because it is routinely confused with OI-75.** The draft exists, is complete (versions, ablation, pixel measurements, control stated) and is **NOT POSTED**; its own first line says so. Posting to a public upstream issue is outward-facing. → **KEVIN.** *(Counted as open in the totals.)* |
| **OI-77** | "docs REORG — not started; `docs/archive/` does not exist" | `memory/STATUS.md:1437` | **DONE.** `docs/archive/2026-Q2/plans/` exists with **65** archived docs; `docs/superpowers/plans/` is down to **39**. Wave-1 `56dd545` (62 zero-reference plans), Wave-2 `2d788f2` (3 of 4). The 4th — `world-purpose-sota` — **deliberately stays**, with the reason recorded: LOOP-CHARTER cites it twice as the live design-of-record, so archiving it would file a governing document under history. |
| **OI-78** | B-terrain "Next: S6, then S7-S15" | `memory/STATUS.md:420` | **STALE by four slices.** Verified by commit: S6 `869f71e` (delete the origin-stacked mote layer), S7 `34f11b0` (one wind driver, dead mob-bend fixed), S8 `947748f` (per-instance yaw/scale/jitter/hue), S9 `17964b8` + S9b `01e8156` (grass in the lighting equation; shade blades like the ground), grass colour B `83054c1`, re-baselined `6af95b7`. The live cursor is **S10**. |
| **OI-79** | X1-bis "the ring does not close" / "`ringLayout` collides" / "`touch-aspect-grab` is off-screen" | `memory/STATUS.md:1184-1219` | **FIXED, and the diagnosis in STATUS is superseded by the code.** `ringLayout` is gone; `src/input/aspectWheel.js:99-125` replaced it with a straight **FAN ROW** (`fanLayout`/`fanItemStyle`/`fanOpenerStyle`, `TOUCH_BTN 52`, `FAN_STEP 58`, `FAN_ANCHOR_RIGHT 26`, `ASPECT_ROW_BOTTOM 104`, `SPELL_ROW_BOTTOM 182`) whose docblock records why a quarter-arc also fails (four 52 px buttons need ~122 px of separation at r=78; the corner does not have 360° of room). `TouchControls.jsx:157-158` consumes `fanLayout`. What remains is OI-54 (looking at it). |
| **OI-80** | V6 residual — "the pre-push hook validates the WORKING TREE, not the pushed commits" | `memory/STATUS.md:1065` | **CLOSED.** `.githooks/pre-push:92-124` adds a **detached worktree at the pushed sha**, clears inherited `GIT_DIR`, symlinks `node_modules` when the pushed lockfile matches (else installs in-worktree), runs the gates there, and tears down on success/failure/interrupt. |
| **OI-81** | B4 — "mobs hit you through 200 blocks of rock" | `memory/STATUS.md:194` | **CLOSED.** `src/game/mobLineOfSight.js` is a real pure module with its own tests; `workers/ai.worker.js:18` imports `hasLineOfSight` and `:244` gates aggro on it. `verticalReach` is per-archetype (`:192`). |
| **OI-82** | B6c — 2 of 12 achievements dead on arrival (`updateLevel` had zero callers) | `memory/STATUS.md:194`,`:336` | **CLOSED 2026-08-05.** Published as `onLevelChanged`, called from the XP system's level effect, deliberately outside its increase-only branch so a save loaded at level 7 retroactively unlocks Rising Star. Gated at both ends, mutation-proven both ways. |
| **OI-83** | B5 — "VISUAL RE-BASELINE OWED, the HUD baselines show the old buried position" | `memory/STATUS.md:338` | **DISCHARGED 2026-08-13.** Three captures ran to completion, and the "harness times out above load ~10" mechanism is superseded — `cd75951` makes a wedged compositor ABORT by name instead of hanging silently for 31 minutes. |
| **OI-84** | The four `beast-*` baselines "contain no beast at all" | `memory/STATUS.md:1412` | **CLOSED as an oracle defect.** `git log -- tests/visual/baseline/beast-fire.png` → last written by `8b1a45e` (2026-08-11) and the set again by `043dcdb` (08-12). The *race* (OI-18) is a separate, still-open thing. |
| **OI-85** | "the sky-arch beacon re-baseline is OWED, blocked behind the settle fix" | `ACTIVE_PLAN.md` (ALSO OWED) | **DONE.** Commit `043dcdb0` (2026-08-12) — *"Re-baseline: the beacon is on screen, and the frames finally settle"* — touched the whole baseline set including `mobile.png` and `.capture-meta.json`. |
| **OI-86** | Batch 0.1 — "`diff.test.js` writes its contact sheet only when the GLOBAL gate reds" | `ACTIVE_PLAN.md` (AFTER THE 88); `plans/…phase-c…` | **DONE 2026-08-12.** `frontend/tests/visual/diff.test.js:112-121` reads the ledger once for both consumers; `:221` fires the artifact path on `ratio >= THRESHOLD || densityRed`; `:243-258` additionally writes a **3× zoomed crop** (baseline \| current \| mask) of the offending 128 px window. |
| **OI-87** | Camera-shake decays per frame, not per second | `memory/STATUS.md` B8→KEVIN | **FIXED.** `src/game/trauma.js:50-54` — `decayTrauma(trauma, dt, k, floor)` is exponential and frame-rate independent, replacing the linear `trauma - rate*dt` that lasted 0.52 s at 60 Hz and 0.26 s at 120 Hz. |
| **OI-88** | "a downgrade from high→low stops loading new chunks but frees nothing already loaded" (queued in passing 2026-08-02) | `DECISIONS.md:199-200` | **FIXED the same day.** `src/world/Terrain.jsx:722-755` adds a throttled RECLAIM pass engaging only past a 1.5× overshoot, capped at `RECLAIM_PER_TICK = 4`, trimming to `renderDistance + 1`; deliberately not implemented by shrinking `cullDist`, with the reason written inline. Covered by `tests/e2e/tier-downgrade-reclaim.spec.js`. |
| **OI-89** | Crafting panel says "5× Bow" and grants `Arrow` (reported three times, "still open" as of 2026-08-09) | `AUDIT-2026-08-09-full-source.md:229` | **FIXED.** `src/ui/panels/CraftingTable.jsx:168` now titles from `getItemName(outKey(result))` with the recipe name as a subtitle, and `:95` destructures `Object.entries(result.output)[0]`. The DRAINED header confirms all 108 findings closed. |
| **OI-90** | `showCredits` / `showChestInterface` / `activeChestCoords` / `setSelectedVillager` missing from the HUD slice | `AUDIT-2026-08-09-full-source.md:143-195` | **FIXED.** `src/store/hudState.js:50-73` now selects all of them, each with its setter, and carries inline comments naming the defect class. `:108-109` adds them to the paired-setter guard. |
| **OI-91** | The 109 hardcoded user-facing strings (i18n ratchet) | `DECISIONS.md:129-137` | **DRAINED.** `node frontend/scripts/ci/i18n-adoption.mjs` → *"0 occurrences across 0 files, none new"*; `tests/i18n/.i18n-adoption-ledger.json` is `{"_total": 0}`. |

---

## E. OBSOLETE / PARKED-BY-DESIGN — not work that is owed

| ID | Title | Source | Verdict |
|---|---|---|---|
| **OI-92** | The coherence **CUT-gate** | `memory/STATUS.md:1289` | **PARKED BY DESIGN, not owed.** Its own bound #1 makes it inadmissible until a blind calibration scores 100% on must-NOT-cut negatives. The governing consequence is the part to keep: *the pillars govern what to BUILD; they may NOT be used to authorize deletions.* |
| **OI-93** | Move the visual gate to CI / migrate capture to Playwright / adopt a different browser engine | `DECISIONS.md:395-410` | **SETTLED — do not re-propose.** 17 candidates, 16 ruled out (Kitesurf cannot render WebGL; Firefox hard-disables headless WebGL in source; Servo and Ladybird publish no determinism claim). Also settled: `pixelmatch`'s `windowSize` is documented on main and exists in NO published version. The one carve-out — a cheap Linux A-vs-B self-consistency job — was separately **declined** 2026-08-09. |
| **OI-94** | `--deterministic-mode` / `--font-render-hinting=none` as the determinism fix | `DECISIONS.md:349-370` | **OBSOLETE.** Verified by `strings -a` over the actual framework Puppeteer resolves, with a presence control: **0 exact-line hits** for either, while `use-angle`/`disable-gpu`/`headless` return 1/1/4. They ship only in `chrome-headless-shell`. Executing that step would have measured nothing and reported "no effect" for the wrong reason. |
| **OI-95** | The dpr pin "bought 2.7×" | `DECISIONS.md:372-378` | **OBSOLETE CLAIM (the change may stay).** `calculateDpr([1,2])` in installed R3F 9.5.0 is `Math.min(Math.max(dpr[0], window.devicePixelRatio), dpr[1])`, and `capture.mjs:197` sets the viewport with no `deviceScaleFactor`, which Puppeteer defaults to 1 — so `[1,2]` and `1` both resolve to 1. **Do not cite 2.7× as a magnitude anchor.** |
| **OI-96** | Phase C conversion Batches 2 / 4 / 5 / 6 | `ACTIVE_PLAN.md` (AFTER THE 88); `plans/2026-08-11-crafty-phase-c-suppression-conversion.md:38-45` | **DELIBERATELY STOPPED, and it is a decision rather than a punt.** They move ~0 pixels the 6% gate can resolve (HUD items are <1.5% global each; the whole spell-cast VFX ensemble is 0.64%). The governing finding from the first conversion: *a subsystem is worth converting not because it moves, but because its motion moves pixels the threshold can see.* Batch 1 (sun shadows) DID land and was the one worth having. Revisit only after per-frame/region-scoped tolerances (OI-27) exist. |

---

## F. DUPLICATE rows, folded

`E3 "STILL OPEN"` (STATUS:1135) and `D2 "the art has out-run the AI"` (STATUS:1103) → **OI-34**. ·
`F3` (STATUS:1301) and terrain `S13` (plan:136) → **OI-44**. ·
`F2` (STATUS:1298) and terrain `S2` (plan:53) → **OI-43**. ·
`i18n #73` (STATUS:1303 F4) and the 06-29 Kevin-gated zh-CN row → **OI-45** + **OI-71**. ·
`G1`'s test-renderer half (STATUS:1363) and `SOTA-INITIATIVE.md`'s corrected line → both closed; only the §3 stamp survives as **OI-62**. ·
`X2b "cannot be tapped while the ring is open"` (STATUS:1219) → same geometry root cause as X1-bis → **OI-79** (fixed) + **OI-54** (verify). ·
`ACTIVE_PLAN`'s "era-review 11 behind / loop-progress 6" and `artifact-currency`'s live 10/10/10 → **OI-56**.

---

## G. STALE DOC CLAIMS found while verifying — fix these or the next reader re-derives them

| # | File | The claim | The live fact |
|---|---|---|---|
| **STALE-01** | `memory/ACTIVE_PLAN.md` THE CURSOR | "the 88-finding holistic-review queue, 30 open of 88" | 0 open of 215. The file's own instruction — *"re-read the file for the live count"* — is the correct habit and the reason this was catchable. |
| **STALE-02** | `ACTIVE_PLAN.md` IN FLIGHT §2 | "PR #13 HELD, NOT ABANDONED" | Merged `5848d64`, 2026-08-13. |
| **STALE-03** | `ACTIVE_PLAN.md` OWED | "PR #15 is open ON PURPOSE" | Merged, reverted, then split — five halves landed at `70117975`. |
| **STALE-04** | `memory/STATUS.md:1184-1219` | X1-bis: `ringLayout(n,78)` collides, sector 0 lands on the spell toggle, grab is off-screen | `ringLayout` no longer exists; `fanLayout` shipped. The registry is describing a deleted function. |
| **STALE-05** | `memory/STATUS.md:420` | "Next: S6, then S7-S15" | S6/S7/S8/S9/S9b all shipped with named commits; the cursor is S10. |
| **STALE-06** | `memory/STATUS.md:1437` | "docs REORG not started; `docs/archive/` does not exist yet and plans still holds 100 files" | `docs/archive/2026-Q2/plans/` holds 65; `plans/` holds 39. |
| **STALE-07** | `memory/STATUS.md:1412` | "the four `beast-*` baselines are BROKEN — they contain no beast at all" | Replaced by `8b1a45e` / `043dcdb`. |
| **STALE-08** | `ACTIVE_PLAN.md` BLOCKED | "NEEDS KEVIN: filing this upstream" | Filed; the maintainer already replied on #750. Only the *reply* is unposted. |
| **STALE-09** | `.agent/AGENTS.md:149`,`:152` | 72 `Math.random()` · 127 `isCaptureMode()` | 73 across 21 files · 130 across 58 files. |
| **STALE-10** | `memory/CHANGELOG.md` (cited by STATUS G1) | "vein C DONE / no per-frame allocs remain" | Three sites still allocate or rebuild per frame (OI-42). |
| **STALE-11** | `SOTA-INITIATIVE.md:69` | "Status (LIVING — last: 2026-06-18)" | Three months behind. |
| **STALE-12** | `TERRAIN-GRASS-SOTA-PLAN.md:233` (Q6) | "every automated gate in this repo is blind to the entire shadow system" | `render/Atmosphere.jsx:242` now sets `castShadow` unconditionally — Phase-C Batch 1 put shadows in ten baselines. Q6 is closed in part, which *raises* S10's value. |
| **STALE-13** | `.agent/AGENTS.md` (Core Agent Skills → archived-skills note) | "Crafty has `ci.yml` but **no `dependabot.yml`** — so nothing replaced the skill here; the job is simply unowned" | `.github/dependabot.yml` **exists** (7,478 bytes), last touched by `9a64fc8` on 2026-08-13. It is a considered config, not a stub: monthly cadence, grouped dev/prod PRs, **no majors from any package**, and **no MINOR for the 0.x engine stack** (`c1d933e`, because for 0.x the MINOR position is the breaking one) — the very estate lesson the stale note tells the reader to go and learn. The dependency-upgrade job is OWNED; only the accretion-ratchet half (OI-97) is not. |


---

## H. Standing cautions carried forward (not items — read before working any of the above)

- **A pixel percentage is not a diagnosis.** It cannot distinguish "the renderer changed" from "the scene did". Open both frames first.
- **A mutation must reproduce the defect's SHAPE, not touch its line.** Five mutations stayed green in the 108-finding drain and every one was a defect in the test.
- **Never gate verification on a pipeline's exit status.** `npm run lint | tail -2 && …` shipped a commit with lint RED, because `tail` exits 0.
- **A name-level grep says "used"; only the consumer GRAPH says "read."**
- **Restore a mutation from a `cp` backup — never `git checkout <file>`**, which nukes untracked work (and this tree currently has eight untracked paths, OI-26).
- **A `Mutation-Proof:` trailer must start with exactly that token and a colon**, and a stamp must never be chained onto a commit that can be rejected.
- **Do not re-baseline `ocean-coast`, `landmark` or `explore-day-med`** — the capture explicitly WARNs they never stabilize, in both runs independently.
- **Verify every registry line against live code before working *or repeating* it.** Nineteen rows in this ledger were already closed; three of those had been re-listed as open for over a month.
