# S2-B1-M7 — THE LOOK, via the THIRD-PERSON BEACHHEAD (hybrid transform-cam)

> ✅ SHIPPED — part of WILDHEART, COMPLETE + MERGED to `main` (`458bbb5`, 2026-06-07). Historical plan-of-record (the milestone shipped); see CHANGELOG for the build record.


> **DIRECTIONAL DECISION (Kevin, 2026-06-07):** Crafty is heading toward **third-person**. Rationale: the entire S2 Aspect thesis (4 visible power-fantasies — beast / squad / orbiting-mass / elemental form) is structurally third-person; the S3 mobile/touch target forces a control rework where third-person + virtual-stick is native; it's an RPG (gear visibility); and every locked look-reference (Bayonetta/Hades/Genshin/LoL/Brawl Stars) is third-person. **The full pivot is deferred** to a deliberate milestone (likely fused with the S3 mobile-control rework) — decided AFTER Kevin sees the beachhead in-world. **M7 is the no-regret first step:** the hybrid transform-cam builds — once — a third-person follow-camera + a voxel beast avatar + the reveal spectacle, all reused whether or not the full pivot proceeds. Supersedes the spec §5 FPV-ambiguous framing of M7 (the look itself — ③·5 — is unchanged).

> Branch `s2b1-m7-thirdperson-beachhead` off `main@a1ae413`. Spec: the WILDHEART design-of-record §5 (the locked ③·5 look + the proven `SpellProjectileCore` recipe at `src/EnhancedMagicSystem.jsx:708-830`) + §3b (capture-determinism).

## The shape of it (Option C — hybrid)
On roar: a ~1.2s **third-person transform-cam** pulls back behind the player and plays the **3-beat morph** (anticipation charge → BURST flash that masks the collider swap → settle), showing the **full beast avatar** at the locked ③·5 glow — THE clip-worthy moment — then snaps back to first-person beast gameplay (claw-hands + ambient element glow; the avatar is hidden in FPV since the camera is inside it). The grayscale-silhouette sheet becomes real (the reveal frame is the per-beast review artifact).

## Reuse (cheap, on-brand — no AAA model)
- **Beast avatar = procedural box-construction + `MobToonMaterial`** (the exact mob render: `SimplifiedNPCSystem` MobMesh ~222 — a `<group>` of box body/head meshes + toon body + Fresnel rim), per-form proportions keyed off `BEAST_FORMS` (the M1 mass-shapes already encode the silhouettes: comet thin/short, bull fat/short, hawk thin/tall, golem tall/wide). PLUS the `SpellProjectileCore` 3-layer glow (silhouette mesh kept `toneMapped` sub-bloom + a tiny near-white `toneMapped={false}` HOT-CORE the global Bloom haloes + a soft outer element glow). The element color is the LAST identifier (silhouette first).
- **Camera = the R3F default** (Components `useThree().camera`), today lerped to head-height in FPV (Components:497-499). The transform-cam OVERRIDES that lerp during the window with a behind+above follow shot looking at the player, then returns.

## Phasing (build the riskiest-new-architecture first; each TDD-gated; ends at a Kevin in-world gate)
- **M7a — the transform-cam (camera state machine).** A PURE `src/game/transformCam.js`: given player position + facing yaw + a progress 0..1 over the window, return `{ position, lookAt }` for the behind+above follow shot (ease-in pull-back, ease-out return). The window is tied to the roar lifecycle: starts on `startCharge`, spans anticipation→commit→settle (~1.2s), ABORTS+returns on charge-cancel. Components overrides the FPV head-lerp during the window (transient ref-timer; Game-Loop-Isolation). Capture-guarded (never fires in the existing 16 capture states → visual stays 13/13). **Gate:** pure camera-state unit tests (pull-back monotonic, returns to ~FPV at progress 1, abort path); visual 13/13 unchanged.
- **M7b — the beast avatar (LEAD form = comet/fire).** The voxel beast shell (box-construction keyed off `BEAST_FORMS.fire` proportions) + `MobToonMaterial` + the `SpellProjectileCore` glow layers at ③·5, rendered when `beastFormActive` (seen in the transform-cam; hidden/НET in pure-FPV after the reveal). Layer-contract static gate (silhouette `toneMapped`; only the tiny core `toneMapped={false}`).
- **M7c — the 3-beat choreography + BURST mask.** Anticipation (inward-converging ferocity sparks, dark-contrast) → a <100ms opaque radial BURST that hides the collider/avatar swap frame → settle (rim eases to steady ambient beast-glow). A DEDICATED short-lived burst mesh (NOT the shared GPUSparkSystem pool — the 120/call cap + 1200 ring-buffer would evict; §5b). Capture-determinism: stable element-keyed seed `beast-transform-${form}`, frozen phase under `isCaptureMode()`.
- **M7d — capture + IN-WORLD JUDGMENT (the Kevin gate).** A `spawnBeastTransform` capture test-hook (in-world Caribbean ctx, `captureStudio:false`, stable seed, frozen flattering phase — §3b) → a per-beast capture state → render → I grayscale-silhouette-judge → **bring Kevin the in-world frame to nudge the aura-opacity dial** (~0.85 tight / ~0.55 wide). Re-baseline the M3/M4 provisional states. **NOT auto-merged — Kevin judges the look.**
- **M8 — replicate** to the other 3 beasts via `BEAST_FORMS` (per-form proportions/color; zero new swap code) + the grayscale 4-up content-variety sheet.

## Invariants
- **No-regret:** the camera + avatar + capture are all reused if/when the full third-person pivot proceeds.
- **FPV gameplay untouched:** the transform-cam is a transient OVERRIDE during the ~1.2s window only; combat aim/hit-reg (the FPV melee cone + spell-cast) are unchanged (the full-pivot aim rework is the deferred S3 milestone, NOT here).
- **Capture-determinism + Game-Loop-Isolation** per §3b (frozen phase, stable seed, transient reads, set() only on transitions).
- **VFX discipline:** apply the PROVEN `SpellProjectileCore` recipe (cite it); judge IN-WORLD (real Caribbean ctx, grayscale), not a sky-studio card; Kevin judges the dial before "done".
