# crafty-s0-reality-audit.js — relocated 2026-09-19 from ~/Code/.claude/workflows (unversioned launch root) into this repo. DO NOT RE-RUN AS-IS.
Row-level verified 2026-09-20: 13 confirmed, 8 corrected, 11 refuted (Opus, independent per row).
Every line below was re-checked against the live file and the live repo; refuted lines from the 2026-09-19 batched review were deleted. Fix before any re-use. (One of the 13 confirmations — L126 step 5, "verify the PNGs exist and are non-trivial" — confirmed a KEEP, i.e. no defect, so 20 defect lines follow.)

## Overwrite hazard — the blocking one
- **FILE-LEVEL.** Relocation is DONE (f5df6ccc, tracked in Crafty) — but it landed in Crafty's launch root as a live slash-registered workflow with DATE/ASSETS still hardcoded and no overwrite guard, so the "do not re-run" constraint is enforced by prose only; parameterise DATE/ASSETS/output via `args` (or move it out of `.claude/workflows/`) before it is runnable from a Crafty session.
- **L15.** `DATE` is hardcoded `2026-05-30`: a re-run OVERWRITES the committed S0 baseline `memory/REALITY-AUDIT-2026-05-30.md` (L258/L273/L276) and the five committed asset files. Parameterise DATE/ASSETS/output path via `args`.
- **L14.** `const ASSETS` is pinned to the same literal date, a directory that already holds the git-committed 2026-05-31 baseline capture (5 tracked files); the script writes those exact paths unconditionally (L115-L118 PNGs, L122 runtime-metrics.json), so any re-run silently overwrites the historical evidence — derive the path from `args.date` instead (Workflow scripts cannot call `new Date()`/`Date.now()`).
- **L108** `mkdir -p ${ASSETS}` is idempotent and needs no edit of its own — the overwrite happens at the write sites above, all fixed by parameterising DATE/ASSETS via `args`.
- **L3 (meta.description).** Bakes the dated output into the permission prompt, and "Read-only" over-claims: the run does `npm run build`, `mkdir -p`, writes `frontend/_s0_capture.mjs` and Writes the audit `.md`. Read-only is true of `src/` only — reword to "non-mutating on src/".

## CALIBRATION block — primes every finder (L100 live-capture, L171 all 12 finders, both refute lenses) with stale premises
- **L21.** Injects a stale "PROVEN FACT" — that `frontend/test_swarm.js` is the repo's only automated test and a blind rubber-stamp; the file was deleted 2026-06-16 (ffeb3fc0) and the repo now carries vitest, Playwright e2e, 270 test files incl. 181 `tests/gates` suites, 21 `scripts/ci` gates and 29 visual probes. A finder reading this discounts real gate coverage.
- **L19.** "largely built by Gemini 3.5 Flash … with a real slop/bug trail" describes the pre-2026-05-30 repo only; ~1,775 post-S0 commits (god-file dissolved, de-monolith A1, vitest+Playwright gate corpus, CI green 2026-08-13) mean a re-run primes against a codebase that no longer exists — delete it or re-scope it to the pre-S0 layer.
- **L22.** The recurrence counts (pointer-lock ~4x, see-through terrain ~4x, voxel winding ~3x) are frozen and undated at the 2026-05-30 cursor, so they now UNDER-state recurrence — pointer-lock was reworked twice more post-S0 (June 3 dual-authority collapse, June 11 lock/menu state machine) and winding again June 2 (ribbonIndices.js). Date or re-derive the counts; do not delete the bullet.
- **L28-32 (STRICT RULES).** Prose only — no `agent()` call sets `allowedTools` (zero hits in the file) — and the same block is injected at L100 into LIVE_PROMPT, which then orders `npm run build`, `mkdir -p`, writing `frontend/_s0_capture.mjs` and `node _s0_capture.mjs`, contradicting its own "READ-ONLY / Bash(grep, sed -n, wc, find, node --check)" limit.

## LIVE_PROMPT (capture stage)
- **L104.** Tells the capture agent "puppeteer 24.x is in devDependencies", but `frontend/package.json` pins `^25.6.0` (installed 25.6.0) since commit 3c0b1889 on 2026-08-12.
- **L112.** Specs a dead GL path and an undeclared option: `--use-gl=egl` already failed on this headless macOS host (REALITY-AUDIT-2026-05-30.md:59 fell back to `--use-angle=swiftshader`, which every shipped probe now pins), and `headless: 'new'` is not in puppeteer 25.6.0's `headless?: boolean | 'shell'` — use `headless: true` + `--use-angle=swiftshader`.
- **L109-124 (step 3).** Orders an untracked one-off `frontend/_s0_capture.mjs` that duplicates Crafty's git-tracked capture path — DELETE it and invoke the repo scripts instead (`scripts/visual/capture.mjs` + `_serve.mjs`, `scripts/perf/run-scenarios.mjs`, `drive-mobs.mjs`, `dayphase-probe.mjs`).
- **L127 (step 6).** Tells the agent to leave the generated `frontend/_s0_capture.mjs` in the working tree — untracked, never committed, covered by no `.gitignore` (`git check-ignore` exits 1) — so every run leaves `??` residue; the `src/` half of the line is already covered by the STRICT RULE at L29.
- **L116 (menu bypass).** `setGameMode('creative')` is a no-op — `gameMode` already defaults to `'creative'` (`frontend/src/store/useGameStore.jsx:579`) and the title screen gates on `gameStarted`/`isPointerLocked` (`frontend/src/ui/panelState.js` `shouldShowTitleMenu`), so `02-spawn-day` still shoots the menu and the FPS/heap samples are taken pre-game; use `markGameStarted()` (`useGameStore.jsx:790`).
- **L117 (night capture).** Hedges behind store introspection ("if a setter exists … else record that night could not be forced") when `setTimeOfDay` demonstrably exists (`useGameStore.jsx:771`) and `window.useGameStore` is exposed unconditionally in the production build (`src/index.jsx:62`) — call `setTimeOfDay(0)` directly (the recipe `scripts/visual/dayphase-probe.mjs` already encodes), so a missing night screenshot cannot pass as an honest negative.
- **L118-121 (combat / FPS / heap / draw calls).** Three of the four measurements already have first-class repo instruments the prompt ignores — mobs via `scripts/visual/drive-mobs.mjs`, and FPS *and* heap via `scripts/perf/run-scenarios.mjs` reading `window.__craftyPerfResult` from `src/devtest/PerfProbeRunner.jsx` (which emits heapStartMB/heapEndMB/heapGrowthMB and gates on M2_BUDGET with committed baselines in `memory/perf/`) — so ad-hoc re-derivation yields numbers non-comparable with the repo's own lineage. Draw calls is the exception: no repo instrument measures `renderer.info.render.calls`, and the `-1` sentinel is the correct convention. (`drive-mobs.mjs` is explicitly "NOT part of the visual gate", so no CI gate sits behind the mob path.)
- **L131-132.** The `agent()` call is API-correct, but its documented `null` return (user skip / terminal API failure) is never guarded — optional chaining only prevents a crash, the log prints `shots=0 errs=0` indistinguishably from a real clean capture, and the run proceeds to hand `RUNTIME_DIGEST={}` to all 12 finders and `null` to the synthesizer that must still write "## Live Runtime Truth". Add an explicit `if (!runtime)` branch that logs the skip and aborts or marks the runtime section uncaptured.

## Find / Refute plumbing
- **L35-56 FINDINGS_SCHEMA.** `dimension` (L38) is agent-echoed free text yet is the finding-ID prefix at L192 and the scorecard key at L194, while the real key exists only in the closure (`DIMENSIONS[].key`). Two finders echoing the same label produce identical ids, so `byId` (L232-235) cross-feeds their refutations — a `refuted` verdict belonging to the other finding flips a real one to `info` and drops it into the report's false-alarm section. Inject `d.key` from the closure instead of trusting the echo.
- **L87-96 VERDICT_SCHEMA.** `lens` is agent-supplied free text (no enum) and is never stamped from the closure (L225-226 spread `{ id: f.id, ...v }`), while L235-236 match it by exact string against `'code-trace'` / `'severity-calibration'` — the prompt only ever shows the headers "LENS = CODE-TRACE SKEPTIC"/"LENS = SEVERITY-CALIBRATION SKEPTIC" and the second call passes `'severity'`, so a `refuted` code-trace verdict can be silently lost and the finding stays `upheld` at its original severity.
- **L139-140 DIMENSIONS[voxel-mesher-texturing].** Still attributes the greedy mesher to `world/terrain.worker.js` and omits `world/mesher.js`, where it was extracted on 2026-08-05 (71c24cad); its claims also say "DataArrayTexture 9-layer" while `world/proceduralTextures.js:17` now builds 16 layers.

## Carried from the 2026-09-19 batched review — NOT re-verified row-by-row on 2026-09-20 (neither confirmed nor refuted; re-check before acting)
- DIMENSIONS L149-150 points `combat-magic-boss` at `AdvancedGameFeatures.jsx`, deleted 2026-06-13; L141-160 carry stale LOC figures for useGameStore / SimplifiedNPCSystem / SoundManager and a pre-S1 "does ANY touch handling exist?" premise.
- L159-160 + L174: the build/bundle focus files live under `frontend/`, not under `SRC`, yet the finder prompt asserts all focus files are under `${SRC}`.
- Refute fan-out L223-228: `.catch(() => null)` is redundant (parallel() already resolves a throwing thunk to null) and erases ERRORED refuters into "unrefuted"; L244-245 logs only refuted-vs-standing instead of the full denominator (upheld / downgraded / refuted / unrefuted-by-cap / errored).
- `CAP = 24` was BINDING on the only real run (7 high-severity findings passed through unrefuted).

## Dropped on 2026-09-20 as REFUTED — do not re-add without new evidence
`meta.phases` (L4-9) · `const ROOT` (L12) · `const SRC` (L13) · CALIBRATION bullet 2 (L20, author-agnostic and still true) · verdict taxonomy (L24-26) · RUNTIME_SCHEMA (L58-85) · LIVE_PROMPT preamble (L99-102) · step 1 build (L107) · store handle (L114) · fallback (L125) · DIMENSIONS[render-terrain] (L135-138).
