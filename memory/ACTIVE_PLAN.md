# Active Plan

> **📍 REPO LAYOUT (compaction-resilient — full map in `CLAUDE.md` at the repo root):** TWO-LEVEL repo. ROOT `/Users/kz/Code/Crafty/` holds `.git`, `docs/superpowers/`, `memory/`, `.superpowers/` (gitignored mockups/_icons.json). APP `/Users/kz/Code/Crafty/frontend/` holds `src/`, `tests/`, `package.json`, `node_modules` — **run npm/tests from `frontend/`; source is `frontend/src/`; docs+memory are at the ROOT, one level ABOVE `frontend/`**. Bash cwd drifts + resets on compaction → use ABSOLUTE paths; NEVER assert a file "gone/missing" from a relative `ls`/`find` (false-absence trap — bit twice 2026-06-01: `src` not-found, `.superpowers` "gone").

> **Method (session-archivist 4-piece):** this is the volatile current-task POINTER. It points at the detailed `superpowers` plan for the active work — it does NOT duplicate the TDD checklist. Ground truth = git `main` + `docs/superpowers/`.

## 🎮 MODE (2026-06-28): POST-AUDIT EXHAUSTIVE FIX/IMPLEMENT — drive the audit gap-list + campaign queue to done (Kevin: "fix/implement all, dont miss anything" + "keep going")

**LIVE CURSOR (2026-06-28):** The W1–W4 rebuild + Phase B are COMPLETE. A multi-agent status audit shipped (`docs/superpowers/AUDIT-2026-06-28-full-status.md`): completion ~70% · code-review MOSTLY (fresh all-file pass) · E2E/visual NO (now closing). **Shipped + pushed this session:** 3 confirmed-HIGH bugs (HUD health-bar `7b6e7b7`, melee id+position `c01e244`, block-debris rapier-2.2 `a36384e`); a Playwright gameplay-flow E2E foundation `9b94040` (5/5 green, `npm run test:e2e`); F=cast / T=melee binding `74fd858`; win-state persistence `114e5a6`; iceball freeze `91fbc38`; audit + control-scheme-design docs `811c776`. HEAD `91fbc38`, origin synced, 1744 unit / 5 e2e / 21 visual / build + eslint green.

**⚠️ THE AUDIT IS PARTLY STALE — VERIFY EACH ITEM vs LIVE code before fixing.** Task #1 ("boss reward=junk") was a verified FALSE ALARM (items registered, rarity resolves). Treat every audit finding as a T3 hypothesis.

**2026-06-28 CAMPAIGN QUEUE (the durable task list; harness TaskList mirrors it in-session):**
- ✅ #1 boss win-reward — NON-ISSUE (stale finding; no change).
- ✅ #2 win-state gameWon persistence (`114e5a6`).
- ✅ #3 iceball freeze/slow secondary (`91fbc38`).
- ✅ #4 TradingInterface stale-closure → subtractions now read fresh `prev` (block + crystal trades); trade-fresh-prev-gates + now end-to-end regression-guarded by tests/integration/trading-interface.test.jsx (`9a0b2f4`, real button-click → store-mutation across all 3 trade paths).
- ✅ #5 bossSystem: 3 notification timeouts now tracked (notifTimers ref + scheduleNotifClear) + cleared on unmount; boss-notif-timer-gates.
- ⏳ #6 HUD compass innerHTML — DEFERRED to a planned slice: it's a render-path hot-loop rewrite that must PRESERVE CSS anim (the boss marker uses animate-pulse/animate-ping; per-frame innerHTML restarts them — a real correctness bug, not just perf), and it's headless-blind (compass renders once in capture; needs a live camera). Per charter = plan-doc + hud-probe LIVE-LOOK, NOT a safe blind 3am rewrite of a core HUD. Pick up with a probe-verified tick or surface for Kevin's eye.
- ⏳ #7 per-frame allocation hoists (Components Player, GameScene WeatherSystem/occlusion, Ocean, MobModel traverse, OptimizedGrassSystem).
- ⏳ #8 affixes.js — wire into loot/equip OR remove dead (decision).
- ⏳ #9 control-scheme A enhancements — verb-telegraph reticle, hold-Alt force-build, persistent legend, key rebinding (FEEL → LIVE-LOOK + Kevin playtest).
- ⏳ #10 remaining MEDIUM/LOW sweep (small gated sub-batches) — IN PROGRESS: ✓ proc-music mute lock (arp ramp *PROC_MUSIC_GAIN; proc-music-mute-gates); ✓ HeartbeatAudio interval no longer re-arms every render (reads playHeartbeat via ref, dep [bucket] only; heartbeat-stable-interval-gates). VERIFIED-STALE (already fixed 06-20, skipped): SpellUpgradePanel lvl-clamp, TargetFrame HP-clamp, addAttributePoints NaN-guard. ✓ debris color: terrain-worker now builds the block_broken hex via linearRgbToHex (BLOCK_COLORS are LINEAR; was double-darkened) — game/colorHex.js pure+tested, visual gate 21/21 byte-stable. REMAINING (render-path → probe/capture tick, or surface): terrain-worker UV transpose, playerRender BufferAttribute churn. Pure-logic remaining: QuestSystem claimQuest stale-snapshot (low-impact, tangled — assess).
- 🟡 #11 SURFACE-only — IN PROGRESS: ✓ added a 2026-06-28 KEVIN-REVIEW-BATCH entry (campaign progress + F=cast/T=melee live-eye + affixes wire-vs-remove DECISION + compass/de-monolith FYI); ✓ corrected CLAUDE.md god-file count (3 still >900: Components 1345, SimplifiedNPCSystem 934 regrew, GameScene 932) + the S1 bloom-spec note (0.65 glowier INTENDED, not a violation). Remaining = await Kevin on affixes + the surfaced taste items. Original list:
- 🚩 #11 SURFACE-only (do NOT auto-change): de-monolith (Components 1341 + SimplifiedNPCSystem 933 — and CLAUDE.md OVER-CLAIMS "2 god-files/NPC de-god-filed" → correct the doc); full zh-CN i18n (#73, big id/display-decouple refactor); touch Aspect-verb wheel (Kevin DEFERRED to playtest); optimistic place/mine feedback. **CONFIRMED-INTENDED (do NOT change): bloom 0.65 glowier; grantXP full-heal on level-up** — reconcile the stale visual-spec note instead.

E2E COVERAGE EXPANSION (the audit's #1 gap — ongoing): +tests/e2e/day-night-cycle.spec.js (`d95e7e3` — vein-B gameplay-flow through the booted store: (1) setGameTime crossing a half-cycle boundary flips isDay both directions + does NOT flip within a bucket [the crossedHalfCycle fix]; (2) grantDawnReward(1) grants xp50/coins10/Health-Potion ONCE and a same-night re-fire returns null with no double-grant [lastRewardedNight guard]. Both flows run in a single page.evaluate so the real-time clock ticker can't interleave). +tests/e2e/beast-form.spec.js (`911a136` — vein-B: the WILDHEART beast-form transform state-machine through the booted store — enterBeastForm('fire') transforms + returns true [caller spends Ferocity on a REAL transform only], exitBeastForm clears it [no-permanent-beast invariant]; rejects a double-transform [existing form untouched], an unknown element, and transforming while dead).+tests/e2e/mana-economy.spec.js (`6c55895` — vein-B: the mana/cast resource layer through the booted store — useMana is the cast-affordability gate [afford→deduct+true; over-budget→reject+spend-nothing; exact-cost→drains to 0], restoreMana clamps to maxMana + is a no-op while dead. NOTE: no general 'cast' test-bridge hook exists [cast is input-driven]; the bridge registers only capture/showcase hooks, so the economy is tested via its store primitives, which ARE what the cast path calls). e2e now 7 spec files / 13 test cases (was 6/11). +save-load.spec.js (save→localStorage→reload round-trip restores progression/loot/coins/gameWon — live-verifies the "LOAD never probed" gap). +equipment-stats.spec.js (equip Iron Chestplate -> getEffectiveAttributes().armor +15 — proves equip flows to derived combat stats, not just the slot). e2e suite now 7 specs. +tests/integration/inventory-consume.test.jsx (jsdom render of the REAL Inventory panel → click Use on a Health Potion → +30 heal + one consumed; tests the handleConsume wiring the audit flagged untested; jsdom fireEvent ignores the group-hover CSS). +tests/integration/trading-interface.test.jsx (`9a0b2f4` — jsdom render of the REAL TradingInterface wrapped in SoundProvider → click the REAL "Trade" button for each of the 3 trade code-paths: block Stone→Crystal + crystal Crystals→Wand both end-to-end regression-guard the #4 fresh-`prev` stale-closure fix; coin Coins→Health-Potion exercises the spendCoins+addToInventory store path; + a disabled-insufficient-stock no-op assertion). +tests/integration/inventory-attributes.test.jsx (`5bc46ff` — jsdom render of the REAL Inventory panel → click the REAL "+" allocate buttons by aria-label: Strength/Agility/Intellect each bump the base attr +1 and spend one from the shared attributePoints pool via allocateAttribute; + a gating assertion that the buttons are NOT rendered at attributePoints=0). +tests/integration/spell-upgrade-talents.test.jsx (`abae6c6` — jsdom render of the REAL SpellUpgradePanel → click the REAL talent-node "Upgrade" buttons: a no-prereq node (voidhand_force/Kinetic Force) spends a point via spendTalentPoint, ranks up, and folds +3 STR into getEffectiveAttributes; a prereq node (voidhand_crush/Crushing Pull) becomes upgradeable only AFTER its parent unlocks [dynamic gate, +5 STR total]; and a zero-points no-op. NOTE during this: the spell-mastery upgradeSpell path is runtime-registered (store default null, set via setUpgradeSpell by the magic-system hook) so it needs the R3F hook mounted — the talent-tree path is the pure-store one and was chosen). +tests/integration/crafting-table.test.jsx (`d3df6b0` — jsdom render of the REAL CraftingTable → click the REAL 3×3 grid cells to lay down a Leather-Helmet pattern [edges filled so it survives normalizeGrid's bounding-box trim, unlike the null-bordered swords], then click the REAL result slot: materials consumed on placement + output added on craft; + a no-valid-pattern no-op + a click-filled-cell-returns-material path. Cells/craft are `<div onClick>` so selected structurally via `.bg-well`.children + the `.h-24` result-Slot parent). Unit 1769 (was 1766). VEIN-A DOM PANELS NOW COVERED: TradingInterface · Inventory consume + attribute-allocate · SpellUpgradePanel talent-tree · CraftingTable. Remaining: QuestLog is read-only (claim is via Q in the QuestTracker, not a panel button — covered by store/quest unit tests, not a jsdom target); Inventory equip/unequip-via-hover-footer (equip→derived-stat already e2e-covered). NEXT VEIN: (B) e2e gameplay-flow specs via the dev bridge (Aspect resource banks, day/night+siege, dodge i-frames, mana/cast economy) OR (C) capture-verified render-perf. DOM panels are jsdom-testable via @testing-library; canvas flows via the e2e bridge.

After the queue: continue SOTA-INITIATIVE master-plan work. Per-item discipline: verify-vs-live → TDD red → impl → chained gate (`cd frontend && npx vitest run && npm run build && npx eslint src`) → commit (no AI footer; no `git add -A`) → push `main` → update this cursor. S4 (multiplayer/monetization) stays Kevin-gated. — Superseded ↓.

## 🎮 MODE: COMPREHENSIVE SOTA REBUILD — executing the W1–W4 plans via subagent-driven-development (Kevin redirection 2026-06-17)

**WHY THE REBUILD:** Kevin reviewed the LIVE game on 2026-06-17 and found the prior "SOTA" work was largely **INVISIBLE** — green capture gates + "verify-done / code-exists" verdicts measured code-PRESENCE, not the lived daytime view (~309 commits since 06-13, but only ~21 capture frames changed — clustered in UI panels / menus / night — so the only always-on visible change was the grass). **Root lesson (load-bearing): a green pinned-capture gate is NECESSARY, NOT SUFFICIENT → EVERY visible slice must be LIVE-LOOK validated (drive the real game, screenshot, LOOK) + `verification-before-completion`.** See [[feedback_headless_gate_live_probe]] + `project_crafty.md` (2026-06-17 block).

**FULL SUPERPOWERS FLOW (done through plans; now EXECUTING):** audit → brainstorm → design-spec → plans → subagent-driven execution.
- **AUDIT of record:** `docs/superpowers/audits/2026-06-17-total-element-audit.json` + `…-digest.md` (11-agent; every element KEEP/IMPROVE/REBUILD/DELETE/DEAD_CODE/BUG + file:line) + `…-reality-diagnostic-8complaints.json`.
- **DESIGN SPEC (Kevin-APPROVED):** `docs/superpowers/specs/2026-06-17-crafty-sota-rebuild-design.md`.
- **PLANS (4, trust-first):** `docs/superpowers/plans/2026-06-17-crafty-W{1,2,3,4}-*.md` — W1 purge/fix (18 tasks), W2 look (18), W3 living-frontier (20 / 5 milestones), W4 polish (13); **503 bite-sized TDD steps total.**

**DECISIONS OF RECORD (Kevin 2026-06-17 — do NOT relitigate):** trust-first sequence **W1 purge/fix → W2 look → W3 content → W4 polish**; **DELETE** auth subsystem + legacy pet/tame system; title = **Cinematic 3D vista**; ocean = **stylized toon Caribbean** (a real animated water PLANE, not voxel tops); grade = **WARM MAGIC-HOUR** (glowier — **the restrained-NEUTRAL grade LOCK is REVERSED; glowier is now AUTHORIZED**); W3 = **Living-frontier MAX** (populated Hearth outpost + 3–5 NPCs + voxelKit buildings + ambient routines + travelers + narrative quest chain + HUD-completeness: nametags / target-frame / combat-log / cooldown-ability-bar / real-minimap + demote the always-on CONTROLS sheet); spells = **4 distinct silhouettes** + per-element trail/impact. MP out of scope.

**EXECUTION METHOD:** subagent-driven-development in **controlled BATCHES** via a workflow (sequential implement → spec-review → quality-review → commit per task), with a **controller CHECKPOINT between batches** = run the full gate (`npx vitest run` / `npm run build` / `npx eslint` from `frontend/`) + **LIVE-LOOK any visible change** + report + surface deferred taste forks to Kevin.

**LIVE CURSOR (2026-06-22 — PHASE B BUILD-OUT, Kevin "you pick, build all except B8"):** Building B1–B7 autonomously (B8 multiplayer/monetization parked), one gated slice per loop tick, sequenced by EV+verifiability. **DONE: B6 humble starting loadout** (`fc26af1` — strips the Diamond-Sword+full-armor+ore-hoard+5/5-potions starter so the loot/craft loop matters; building basics + tools + ONE Stone Sword + bootstrap ore + 2/1 potions; 6 new `tests/store/startingLoadout.test.js`; world-frame baselines re-baselined for the reduced DOM hotbar) **+ a capture determinism fix** (the 3 panel frames flaked day/night behind their translucent backdrop → capture.mjs hides the 3D canvas for those shots → deterministic, verified green ×2 — ends the loop's "21 green" luck-dependence). **DONE B2 spell-color unify** (`16a69f9` — all element-IDENTITY hues (SPELL_TYPES/SPELL_COLORS/WAND_CONFIGS/SPARK_PROFILE/mascot) derive from theme/tokens MAGIC; ENERGY_PROFILE pushed-hot projectile VFX preserved; sub-6% so no re-baseline; 12 tests/gates/spell-color-unify-gates). **DONE B7 wand/crystal economy** (`53c7478` — wand = capped spell mana-focus 6%/wand, 30% cap; closes the ore->crystals->wand dead sink; game/wandFocus.js pure + cast-chokepoint wire + trade-UI "-N% spell mana" label; 10 tests across wandFocus + wand-economy-gates). **DONE B4 quest rewards** (`728635c` — quests now grant coins (derived/explicit) + select-quest ITEM gear tied to B6: Hunter->Iron Sword, Miner->Iron Helmet, Builder->wood, Pilgrimage->Iron Chestplate, Champion->Diamond Sword; game/questRewards.js pure + claimQuest grants full bundle; 10 tests). **DONE B3 shrine model** (`860ddd5` — type-0 landmark redesigned generic-tower->SHRINE: altar plinth + 4 pillars + canopy lintel + warm crystal heart; world/shrineShape.js pure + 5 tests; structure-verified + proven Cube-path, but the captured landmark.png frames the type-1 ARCH so the shrine LOOK is NOT headless-capturable -> FLAGGED for Kevin's live eye). **DONE B1 jungle vine-canopy** (`e60b77a` — jungleShape: broad layered canopy radius-4 + emergent tuft + hanging vines, distinct from oak/pine/acacia/swamp; foliage.js pure + 5 tests; worker jungle branch; meadow flowers OUT OF SCOPE [new block id]; far biome -> no re-baseline; look flagged for Kevin). **DONE B5 mob art slice 1** (`5724619` — humanoid arms for zombie/skeleton/villager via pure mobFeatures; verify-before-assert found the boss [DRAGON_FEATURES] + featured mobs were already silhouetted, so arms were the clearest remaining box-stack tell; structure-locked + proven features.map path; no humanoid close-up capture so arm LOOK flagged for Kevin; further mob-art polish = taste/Kevin-directed). **🏁 ALL PHASE-B AUTONOMOUS BUILD-OUT COMPLETE (B1–B7 shipped this session): B6 fc26af1 · B2 16a69f9 · B7 53c7478 · B4 728635c · B3 860ddd5 · B1 e60b77a · B5 5724619.** Coherent loop: humble start -> earn crystals/gear -> wands cut mana / quests grant gear -> distinct biomes+shrine -> clearer creatures. **NOW: IDLE-HOLD for Kevin.** No clean autonomous build work left (B8 + held items R1/I1/I2/I3/F1-F5 are Kevin-gated; further mob-art is taste). Each tick: quick health check (git clean/in-sync/last-gate-green) + check KEVIN-REVIEW for a reply; if Kevin steered -> follow it (full discipline); else re-arm ~1800s + report ONE line; do NOT invent work / do NOT touch B8 or held items. **Flagged LIVE-LOOK eye-checks for Kevin (headless can't judge taste):** B3 shrine silhouette · per-biome flora incl. B1 jungle · B5 humanoid arms · B2 spell colors in play.** Per-slice discipline: milestone plan→TDD red→impl→chained gate (`cd frontend && npx vitest run >|/tmp/v.log && npm run build >|/tmp/b.log && npx eslint src >|/tmp/e.log && {commit}||FAIL`)&&visual; `git -C /Users/kz/Code/Crafty`; `>|` for /tmp; commit `-F /dev/stdin <<'MSG'`; LIVE-LOOK any visible change (verify-before-assert via image Read); locks hold; NO AI footer; leave `.state/` untracked; kill any zombie `vite --port 4178` before a visual run. Held Kevin-gated (do NOT auto-do): R1 debris (a/b/c), I1 serena MCP, I2 cloud reviewer, I3 dep-bump, F1–F5 taste, B8. **Superseded ↓.** **W3 (LIVING-FRONTIER) COMPLETE — on main `ca26383`, deployed.** All 5 milestones done: M-HUD✓ M-HUB✓ M-NPCS✓ M-AMBIENT✓ M-NARRATIVE✓ (.1 `97fd666` lore data · .2 `8733124` themed descriptions · .3 `ca26383` quest-log panel L). 1615 unit / 21 visual / build + eslint green.** M-AMBIENT.2: a capture-suppressed ambient tick in SimplifiedNPCSystem lerps each hub NPC along routinePosition (slow daytime patrol / night retreat-home) + re-raycasts ground Y so they stay flush; the NPC glyph (?/!/+) prepends the floating nametag as a talk-to-me cue. Verified by position reads (day-patrol shift, night→home anchors, y~52 flush). **Also fixed a capture NON-DETERMINISM** (`a7dc4b9`): the inventory/achievements/progression panel backgrounds rendered night-or-day at random because setTimeOfDay only FLIPS isDay on a half-cycle crossing — now capture.mjs pins isDay=true before those panels (a stash-test proved M-AMBIENT.2 innocent of the 33%/19%/9% phantom drift). 1607 unit / 21 visual / build + eslint green. **W4 (POLISH) IN PROGRESS** (plan docs/superpowers/plans/2026-06-17-crafty-W4-polish.md, 13 tasks): shipped **T12 boot-chrome** (favicon/PWA/OG/theme-color + de-Minecraft copy, `dd847b3`) + **T1 gameFeel pure-math** `2a3a0ee` + **T2 walk-velocity ramp** `f15d126` + **T3 coyote-time/jump-buffer** `6abc6cc` — the full movement game-feel wired into the Player loop (regression-safe superset; caught+corrected a bunny-hop-breaking bug in the plan T3; FEEL flagged for Kevin playtest in KEVIN-REVIEW). **W4 (POLISH) COMPLETE -> the WHOLE W1-W4 COMPREHENSIVE SOTA REBUILD is COMPLETE (2026-06-18, `8e5497e`), pending Kevin playtest.** W4 shipped: T1-T3 movement game-feel (accel ramp + coyote/jump-buffer) · T5-T8 weather (storm sky-darken + mount-density fix + biome gate) · T9a/T9b storm audio (pure synth + WeatherSystem wire via audioBridge/masterBus, capture-gated; AUDIO QUALITY -> Kevin ear) · T10 affix model · T11 touch nub-follow (imperative GLI-safe knob) · T12 boot-chrome. (T4 movement-probe skipped: optional/low-value, headless cannot drive WASD.) **1656 unit / 21 visual / build + eslint green; every visible slice LIVE-LOOK-verified** (holistic hud-probe: populated frontier + complete HUD reads SOTA). **NEXT = POST-REBUILD BUG/SLOP FIX SPRINT** (Kevin RE-OPENED the loop 2026-06-20: "keep fixing bugs/slop/inferior things autonomously"). A 10-agent reconcile of the 06-17 audit vs LIVE main found **92 already-fixed by W1-W4 + 28 autonomous-fixable + 19 Kevin-gated** (backlog file: /private/tmp/claude-501/-Users-kz-Code/a14114d4-93b7-49a2-b95b-5ec5f25e7bbc/tasks/w3yokptmv.output). SHIPPED (10 commits): consume-trap `17a06a6` · boss-shake `5482595` · mob-bar `2e6d347` · wind-bed `58f28ab` · dead-code-purge(Tooltip/Orbitron/particleCount) `58c3bd0` · touch-minimap `844cbdf` · over-exports(4) `e1f4578` · agents-doc `97b7991` · dead-synth-voices(magic/magicCharge) `a826330` · sound-wrappers(playMagic/Charge/Jump/Tone) `ee92c1e` (1663 unit / 21 visual / build+eslint green). REMAINING autonomous: stale comments, chain-lightning visible arc, impact-ring dedup. Kevin-gated -> KEVIN-REVIEW: wand/crystal economy, iceball-freeze, + 19 taste/scope items. CONTINUING the autonomous list (chain-lightning visible arc, impact-ring dedup, touch minimap, dead synth voices, over-exports, items.js Title-Case, stale docs) — each GROUNDED vs live + fully gated. Kevin-gated items (wand/crystal economy, starting-inventory trivializes loop, 5 parallel spell-color sources, biome-flora render-wiring, muted synthPad ~420 LOC, shrine model, quest rewards) -> KEVIN-REVIEW. **UNITY: decided NOT migrating** (`b2d90b2`; not open-sourced, proprietary C#/editor-bound, Crafty is R3F web). S4 = Kevin call; do NOT begin S4 autonomously. (verify-before-assert: much of the backlog may be already-built or Kevin-taste-gated, the strong W1-W3 pattern). STRONGLY recommend Kevin playtest the living frontier (populated outpost + ambient NPCs + completed HUD + quest log) before/alongside W4. KEVIN'S 4 LIVE-FEEDBACK BUGS all fixed+deployed (kill-FX warm-gold d9b7559 · F slash-arc b60ee80 · ESC single pause 4b9e0bb · travel+blight 8ca0d1e). CONTROLLER single-threaded; workflow CLOSED. Follow-ups for Kevin (KEVIN-REVIEW): ocean GPU-perf, biome-worker-wiring, title controls-hints, 5 dependabot vulns, + the panel-capture day/night determinism (now pinned). **TOOLING EVAL 2026-06-20 (Kevin directive) DONE:** 8-agent workflow evaluated codebase-memory-mcp + brooks-lint + SOTA alternatives -> BOTH SKIPPED as autonomous-loop fixtures (adversarial=skip/HIGH, deep-dive 3/5 each; memory-mcp trace_path broken on macOS-arm64 #480 + store/hook internals invisible #415 + silent-index-corruption #391/#333; brooks-lint negative-token-ROI + zero-R3F-awareness FP-flood + unsafe auto-fix). ACTED: wired **knip** `d5b85cb` (project's own .knip.json, never installed) — devDep + `npm run knip` report-only + made config test/scripts-aware; first run = 54 unused-exports + 1 unlisted-dep = NEW cleanup backlog (un-export-vs-delete + static-gate-string check per symbol -> careful gated batches). Kevin-gated -> KEVIN-REVIEW: install an MCP server? (serena LSP + ast-grep, local/MIT, hand-wired .mcp.json scoped to frontend/), cloud-LLM reviewer (privacy), npm-audit bump. chain-lightning arc = DONE (411a5c7+1633998). **REMAINING autonomous now: (a) work down the knip 54-export backlog in gated batches; (b) impact-ring dedup #7.** Then it's all Kevin-gated -> lengthen cadence + hold for playtest. **EXHAUSTIVE CODE REVIEW DONE 2026-06-20 (Kevin directive):** 39-agent workflow over every source file -> 157 raw/156 confirmed (report docs/superpowers/CODE-REVIEW-2026-06-20.md, db81098). serena installed SOTA-way + wired (next-session) + ast-grep installed; Dependabot ALL CLEARED (025ffae). **4/5 HIGH review-bugs FIXED+pushed:** boss fireball origin+leak 4b4ba75 · static-NPC cull+leap-NaN e5df50f · Terrain ChunkMesh hooks-order RENDER-CRASH 19475ba · zones frozen-slow-forever a28153d. **REMAINING (continuing backlog):** HIGH #3 worker update_block drops grassTops (DEFERRED — locked worker, check grass-revival-gates token surface first) + 36 mechanical autonomousFixQueue items (dispose/leak/dead-code/useShallow/NaN-guards/alloc-hoists) + knip 37-export backlog + impact-ring #7. 17 Kevin-decision items in the review doc. Work in small gated batches; ast-grep `sg` for codemods; LSP findReferences to prove dead before deleting. **✅ PHASE B M1 (biome-flora) — 4 SLICES DONE: taiga-pines/bare-mesa (8f4eec4) + density/jungle-height (4625123) + savanna-acacia (c3757a8) + swamp-trees (5a075b2), 1679 unit. Autonomous additive-flora captured (jungle marginal, meadow needs M4b block). Remaining Phase-B = Kevin taste/decision (spell-color re-baseline, shrine, quest-rewards, mob/boss-art, starting-inventory, economy, S4) -> KEVIN-REVIEW ask; ~1800s hold for Kevin's steer.** Superseded: **✅ PHASE B M1 (biome-flora wiring) DONE 2026-06-20 — taiga-pines/bare-mesa/gated-cacti (8f4eec4) + density/jungle-height (4625123), 1673 unit. Autonomous Phase-B runway now THIN -> remaining is Kevin-taste/eye/scope (flora slice-3 shapes, spell-color re-baseline, shrine/rewards/art/inventory/economy, S4); prioritization ask in KEVIN-REVIEW; cadence lengthened ~1800s, holding for Kevin's steer.** **✅ PHASE A COMPLETE 2026-06-20 (review backlog + knip-zero + kevinDecisions all done; impact-ring ddd62b5 last). NOW PHASE B feature build-out (biome-flora wiring, spell-color unify, shrine model, quest rewards, mob/boss art, starting-inventory, economy + SOTA-INITIATIVE; milestone discipline brainstorm->spec->plan->build; taste/art items surface to Kevin; S4 scope-confirm first).** **MANDATE EXPANDED 2026-06-20 (Kevin: "fix everything. and then build out everything too"):** loop charter updated (LOOP-CHARTER.md §0). Phase A FIX EVERYTHING: ALL 5 HIGH review-bugs DONE (boss 4b4ba75 · npc e5df50f · terrain-hooks 19475ba · zones a28153d · worker-grassTops 1a199bd); dispose-leak batch DONE (Ocean/combatVfx/ElementZoneRenderSystem 4d412df). REMAINING Phase A: ~33 mechanical autonomousFixQueue items + 17 kevinDecisions (now LOOP-DECIDED, best-judgment) + knip 37 + impact-ring. Phase B BUILD OUT EVERYTHING: deferred KEVIN-REVIEW features + master-plan, as milestones. S4 multiplayer/monetization = SURFACE scope-confirm first (only carve-out); all else full loop authority. FIX-SPRINT PROGRESS (gated+pushed): leaks 4d412df/a46d545 · dead-code 13a7d08/4fddace/4129e24 · guards 35dd84b. NEXT: GamePanels useShallow cluster, timer-leak cleanup, alloc hoists, misc guards, then kevinDecisions + knip 37 + impact-ring. UPDATE: useShallow cluster + guards + perf-hoists + triggerSpellCast/armor + worldSaves/index + chest-interval + WorldManager-success ALL DONE (46fff59..24d88dd). REMAINING Phase-A: #12 setTimeout-leaks (React18-benign) + #13 SoundManager arp WebAudio disposal + #34 SimpleExperienceSystem/CraftingTable timers; THEN kevinDecisions (de-aggro leash, save-validation, tree-thru-rock, BlockParticleSystem rapier-2.2 [live-verify], bossSystem idempotency, HUD a11y); THEN knip 37 + impact-ring; THEN Phase B.

**DEFERRED Kevin forks (surface at their workstream; the agents' safe defaults hold otherwise):** W2 ocean CPU-plane-vs-GPU + coastline distance + death-FX-capture-baseline; W3 hub footprint + cooldown store-mirror + H/L keybinds; W4 affix visibility. W1 defaults already set (M-key = wire the panel; upgrade cost = level-gate + leveled-mana-charge).

**GATES (pre-W1 baseline):** HEAD `2a55090`, origin synced, **1459 unit / 21-state visual / build + eslint clean.**

**HISTORY (superseded — see `CHANGELOG.md` + `SOTA-INITIATIVE.md` §3):** the four-Aspect spine + S1 art/UI + S3 de-monolith + the 2026-06-15 mega-directive Phase-2 (Ember-Frontier S5–S10) + ocean S1–S4 + touch + music + camera + the M1–M6 audit + #51 spell-upgrade revival + #52 modal a11y. The **Ember-Frontier game DIRECTION still stands**; the rebuild fixes LOOK + dead-code + content fidelity on top of it.
