# Active Plan

> **📍 REPO LAYOUT (compaction-resilient — full map in `CLAUDE.md` at the repo root):** TWO-LEVEL repo. ROOT `/Users/kz/Code/Crafty/` holds `.git`, `docs/superpowers/`, `memory/`, `.superpowers/` (gitignored mockups/_icons.json). APP `/Users/kz/Code/Crafty/frontend/` holds `src/`, `tests/`, `package.json`, `node_modules` — **run npm/tests from `frontend/`; source is `frontend/src/`; docs+memory are at the ROOT, one level ABOVE `frontend/`**. Bash cwd drifts + resets on compaction → use ABSOLUTE paths; NEVER assert a file "gone/missing" from a relative `ls`/`find` (false-absence trap — bit twice 2026-06-01: `src` not-found, `.superpowers` "gone").

> **Method (session-archivist 4-piece):** this is the volatile current-task POINTER. It points at the detailed `superpowers` plan for the active work — it does NOT duplicate the TDD checklist. Ground truth = git `main` + `docs/superpowers/`.

## 🎮 MODE: AUTONOMOUS on Kevin's explicit backlog (2026-06-14, camera + touch cold-start confirmed fixed). MINIMAL CODE. ElevenLabs key in keychain (no CLI; use API directly).

**🔴 P0 FIXED THIS SESSION (#2) — TOUCH COLD-START WAS DEAD (iPad/iPhone unplayable).** Root: the title menu hides ONLY when `active` is true, and every menu button (incl. "Start Adventure") opened that gate via `requestPointerLock()` — which DOES NOT EXIST on iOS Safari. So tapping Start Adventure on a real device did nothing; the player was stuck on the title screen forever (the exact untested-headless blind spot that hid the dead mouse-look — CLAUDE.md already flagged "touch/mobile is unbuilt, Pointer-Lock-gated"). The in-game touch SURFACE (M0-M3a joystick/look/verbs) was fine — only the menu→play ENTRY was never bridged on touch. **Fix:** a DRY touch-aware `enterPlay()` in MenuSystem (desktop = pointer-lock as before; touch = `setIsPointerLocked(true)` directly) wired to Start Adventure + all 8 panel-close relocks (collapsed 9 duplicated raw lock blocks → 1 helper). **PROVEN** by a NEW real-browser gate `scripts/visual/touch-probe.mjs` (iPhone-13 emulation + Pointer-Lock API DELETED to be iOS-faithful): Start Adventure → PLAYABLE, joystick Δpos=20, drag-look Δyaw=0.224, verb taps OK — all 6 checks green (were 3 FAIL pre-fix). Static regression gate `tests/gates/touch-entry-gate.test.js` (+4). Desktop mouse-look NOT regressed (`npm run test:look` yawΔ0.92). **⚠️ KEVIN: real-device check owed** (true iOS-Safari finger-feel; swiftshader ≈ GPU but not a real finger). Polish note: the QUESTS panel eats ~40% of a phone's width (M3b/M3c clutter).
**Kevin's backlog (continue the loop, enhance all):** **(1) MUSIC — OVERHAUL DONE** (ElevenLabs day/night/**boss** tracks + smooth crossfade replace the disliked procedural pad/arp via `ui/MusicPlayer.jsx`; `PROC_MUSIC_GAIN=0` mutes pad+arp, ambience kept; commits `25fc114`/`bdc0ba5`). EAR-CHECK owed (day→night→boss transitions, volume, fade). Optional later: menu/per-biome tracks. **(2) TERRAIN/WORLD — SLICE 1 SHIPPED (`de55046`):** flattened relief to gentle plains (40+n*18) + rarer highland mask (highland^2*120); Hearth/SEA_LEVEL held; re-baselined + validated with MY OWN EYES (`scripts/visual/pov-probe.mjs` ground-level before/after + every diorama frame). **SUN ORB SHRUNK** (radius 24→13, `0bfa03f`; POV-validated, off-frame in diorama so no re-baseline). NEXT candidates: MUSIC boss-track + smooth crossfade (his #1 want), TOUCH verify (device-emulation probe), optional further coast/ocean read. The POV probe is now the standing tool to eyeball world/feel myself (the diorama capture cam shoots from orbit, which hid the ground-level mountainousness). **(3) TOUCH (iPad/iPhone) — Kevin: "what happened to them? do those too"** → **COLD-START NOW FIXED (this session, P0 #2 above):** the in-game surface (M0-M3a joystick/look/verbs) was fine; the menu→play ENTRY was pointer-lock-gated → dead on iOS. Bridged via `enterPlay()`; verified end-to-end by the new `touch-probe.mjs` (emulated iPhone, all 6 checks green). **Standing tool: `node scripts/visual/touch-probe.mjs`** drives the REAL game on an iPhone-13 touch viewport (pointer-lock deleted = iOS-faithful) and asserts move/look/verbs + screenshots the touch HUD to `/tmp/crafty-touch/` — use it to self-validate touch the way the POV probe validates the world. **TOUCH POLISH (this session):** QUESTS-panel declutter DONE ✅ — QuestTracker collapses-by-default + narrower + keyboard-free "Claim" on touch (was eating ~60% of phone width + stealing the move-zone corner); `isTouchUIMode()`-gated, desktop unchanged; behavioral RTL gate + touch-probe screenshot confirm. **PROBE HARDENED ✅** — `touch-probe.mjs` was flaky (gave a false "cold-start dead" alarm): the entry tapped Start Adventure by stale coordinate (raced framer-motion's entrance) and drove movement before the player physics body existed (Rapier `setTranslation` race → Δpos=0). Fixed: element-`handle.tap()` (re-resolves live center) + entry retry; spawn-settle (`isSpawnChunkLoaded` + 3.5s) + move retry. Now DETERMINISTIC (4/4 fully-green runs, 0 `setTranslation` errors). The `setTranslation` error was a probe-only startup race (real players can't move before the world loads) → NOT a real bug, no code change. **SPELL-LABEL WRAP FIXED ✅** — "Spell: FIREBALL (15 MP)" broke onto two lines on a phone (anchored `left-1/2`, auto-width can only grow to the right edge); added `whitespace-nowrap` (universal, desktop no-op); proven by the probe at 390px (2 lines→1). NOTE: mobile.png UNCHANGED — the capture is 402px (12px wider) and the label sits right at the wrap boundary, so only ≤390px phones wrapped; the probe (not mobile.png) is the proof. **TOUCH XP READOUT DONE ✅** — touch players had ZERO level/XP visibility (desktop XP bar is `!isTouchUIMode()`, collides with the joystick); added a compact `SimpleExperienceBarTouch` (LV badge + thin XP fill) top-right `top-28` (below the spell-label band; moved from top-16 after the probe showed crowding); touch-only, static fill (capture-safe); RTL gate + probe + mobile.png re-baselined (LV1 visible). **REMAINING touch polish:** per-finger hit-area sizing; M3b/M3c ergonomics; real-device iOS feel (Kevin-owed). **TOUCH MINIMAP — JUDGED + SKIPPED** (the Minimap is a canvas mob-radar, desktop-only; but Crafty already has DamageDirection [incoming-hit arrows] + the boss compass marker covering threat-awareness, so a tiny phone radar is incremental value at real clutter cost → skip). **TOUCH IS NOW PROBE-EXHAUSTED — only Kevin real-device feel remains.**

> **(✅ SUPERSEDED — OCEAN MILESTONE COMPLETE [S1-S4 shipped 2026-06-14]; the live cursor is the 🔶 MEGA-DIRECTIVE banner below. Kept as history.)** **NEXT UNIT = OCEAN/COAST RENDER MILESTONE (Kevin's #1 unaddressed ask: "why is the ocean still looking unchanged… feels mountainous").** ASSESSED this iteration (throwaway surface/coast probe at the x≈-40 ocean): the water reads as flat, dark blue blocks from the surface — Kevin's complaint has merit. It is a SPECCED milestone (`docs/superpowers/specs/2026-06-13-crafty-world-design-hybrid.md` — oceans-as-a-place, beaches, coast landmarks) with real infra (`world/oceanProfile.js` SEA_LEVEL=28/BEACH_BAND_TOP=30/DEEP_FLOOR=6; water material `#3F76E4` + onBeforeCompile wave + night biolum). PLAN AUTHORED ✅ `docs/superpowers/plans/2026-06-14-crafty-ocean-coast.md` (reference-locked the SOTA stack via web search + assessed via the NEW reusable `scripts/visual/ocean-probe.mjs` HUD-hidden coast/surface/underwater views). **KEY FINDING:** the water shader (`world/Terrain.jsx` compileShader) HAS depth-tint + wave + night-biolum, BUT the depth-tint keys off `vWorldY` so it only shades SIDE/underwater faces — the TOP surface stays a flat `#3F76E4` and there's NO shore foam → why it reads flat from above. **SLICES:** S1 surface lift ✅ DONE (shader-only: shallow-teal lift + view-Fresnel sheen in `Terrain.jsx` compileShader; ocean-depth re-baselined to a vibrant teal→navy gradient; only that frame shifted), S2 shore foam — **S2a kernel ✅ DONE** (pure `shoreFoamFactor`/`isWaterTop` in `world/oceanProfile.js` + 4 red-first tests). **DESIGN FINDING:** the greedy mesher merges all water-top faces into ONE quad (uniform vertex color) → the plan's "per-vertex attr on a merged quad" can't vary per-column. **CHOSEN: Hyp A — unmerge water-top faces + bake per-block into spare `color.g`(foam)/`color.b`(depth); this same mesher change ALSO enables S3.** **S2b+S2c DONE ✅** — mesher unmerges water-top faces + bakes `color.g=shoreFoamFactor`; shader reads `vFoam` + POST-lighting foam mix → a crisp white foam line at every shore (probe-confirmed topdown+surface-skim). Wiring gate `ocean-foam-wiring.test.js` (+2). Visual 19/19 NO re-baseline (foam not in any capture camera's view — coverage gap noted for S4: add a coast capture state). **SHORE FOAM = the SOTA signature, SHIPPED.** **S3 DONE ✅** — `seabedDepthT` (pure, +1 test) + mesher down-scans the chunk column for the seabed and bakes `color.b`; `Terrain.jsx` `vDepthB` grades the TOP surface shallow-teal -> deep-navy (probe-confirmed: bright shallows -> darker offshore, foam intact). Visual 19/19 NO re-baseline (top-surface work isn't framed by any capture camera — same gap as foam). **S4 DONE ✅ → OCEAN MILESTONE COMPLETE.** Added an `ocean-coast` capture state (gate 19->20) that pixel-gates the foam+depth surface (passes the 6% gate reliably; md5 isn't byte-stable but that's sub-perceptual bloom — the 6% pixelmatch is the contract). Daytime-biolum already correct (nightFactor=0 at midday). Wave+horizon judged good-enough. The ocean is now a SOTA stylized sea (tropical teal + sun sheen + shore foam + depth-from-above) vs the original flat blue.

## 🔶 MEGA-DIRECTIVE (Kevin 2026-06-15) — EXECUTING, loop OFF hold

**Kevin's 2026-06-15 steer (operator-confirmed, `.state/session-truth.jsonl`):** music OK so far; now (1) thorough SFX review+enhancement; (2) world/terrain/biome/ocean STILL reads mountainous (highland^2*120 swell — confirmed via diorama+POV probe with my own eyes) + POINTLESS (pretty green mound in empty sky, no destinations/journey, dim flat lighting) -> deep SOTA-2026 world+visual overhaul AND give the game a POINT; (3) full codebase debt audit (treat ALL pre-masterplan code suspicious until reviewed for SOTA-contribution); (4) visual-stunning across all aspects = overarching bar; (5) agentic SOTA game E2E testing research (ask before installs). Vercel auto-deploys from GitHub main (`frontend/vercel.json` present; Kevin verifies dashboard). **PHASE 1 COMPLETE 2026-06-15:** all 3 workflows synthesized -> `docs/superpowers/research/2026-06-15-crafty-codebase-reality-audit.md` + `...-agentic-e2e-testing.md` + `docs/superpowers/plans/2026-06-15-crafty-world-purpose-sota.md`. Shipped already: DebugOverlay DEV-gate + debug console.log drop (audit rank-2, `dc6f3cc`). RECOMMENDED DIRECTION: "Ember Frontier + grafted Blight-Heart climax" (Kevin to confirm). KEY: capture mode was hiding cast shadows + landmark emissive beacons, so my visual baselines were the FLATTEST version (not real play). ToneMapping already NEUTRAL + MOOD_GRADE already ships (don't redo). **PHASE 2 (build, plan-driven slices) — SHIPPED ✅ (details in CHANGELOG, newest first):** surface-texture S2 richer tiles + bold-flat invariant gate (`90c3edf`) · surface-texture S1 de-tile (`72bfce1`) · S1 VERTEX AO steps 1-4 (`0f93208`) · S2 aerial-perspective haze · S5 shared-heightAt drift-fix (`61b6438`) · S4 tame-mountains (`192039e`; peaks +36->+12.8, highland 15%->4.4%, pinned by worldShape.test.js) · DebugOverlay DEV-gate (`dc6f3cc`) · E2E tools (`0f8cad9`). **FINDINGS (don't redo):** the bold-flat NEUTRAL-tonemap pipeline is ROBUST to gentle GLOBAL knobs (aerial/fill/bloom/exposure ALL came sub-6%) -> use STRUCTURAL levers (AO + de-tile + richer tiles all landed); BLOOM S1-B-LOCKED >=0.85 + EXPOSURE compressed by the NEUTRAL tonemap -> both Kevin-taste, DEFAULT KEEP; capture-mode HID shadows+beacons so baselines are the flattest version — but that is CORRECT-for-regression (see S3 reframe below), real play is viewable via pov-probe; AO_MIN=0.55 strength = Kevin batch-taste (item #39); lesson: verify structural-shading wiring with a HARD-threshold diagnostic + a logic harness, NOT the gate (gentle effects sub-6%). **DECISIONS RESOLVED (Kevin):** DIRECTION = Ember Frontier + Blight-Heart climax · E2E installs done · re-baseline = BATCH. **LOOP-MACHINERY:** kernel v3 + 12 LOOP-CHARTER.md edits applied; LOOP-KERNEL-PROMPT.md synced. **SURFACE-TEXTURE FIDELITY milestone — PLAN AUTHORED ✅** (`docs/superpowers/plans/2026-06-15-crafty-surface-texture-fidelity.md`). DESIGN-FORK RESOLVED (Goal-Frame catch): the kernel's "128px + PBR normal maps" CROSSES the LOCKED bold-flat/anti-AI-slop aesthetic (textures = deliberate 32px NearestFilter pixel-art, no mipmaps) -> REFRAMED to ON-BRAND levers (Slice 1 DE-TILE: world-pos-hashed +/-0.08 value jitter = the #1 stylized de-monotony lever; Slice 2 richer baked patterns + a bold-flat-invariant regression gate test; Slice 3 optional flat accents). PBR/normal-maps = KEVIN-TASTE fork, **DEFAULT REJECTED** (don't auto-cross the lock; surfaced item #39). **Slice 1 (de-tile) ✅ DONE**: `world/detile.js` `tileValueOffset` (+4 tests, 1201) mirrored in Terrain.jsx (vWorldPos varying + +/-0.08 per-cell value jitter, non-water, capture-deterministic); LOOK (hearth/biome-snow/pov) = organic per-block variation NOT speckly, bold-flat preserved; sub-6% like AO; 15 baselines re-blessed, gate 20/20; the ocean-depth-tint static gate was kept VERBATIM (added vWorldPos alongside vWorldY, didn't weaken it — a refactor of a gated line broke it; restore-verbatim is the fix). **Slice 2 ✅ DONE** (bold-flat invariant gate `proceduralTextures.test.js` +4 -> 1205; enriched stone/sand/snow tiles judged via an atlas-PNG dump — snow sparkle now in-tile so Slice 3 folds in; gate 20/20, 15 baselines re-blessed). **SURFACE-TEXTURE FIDELITY MILESTONE COMPLETE** (AO + de-tile + richer tiles = the structural "stunning land" levers all landed; PBR/normal-maps stay the Kevin fork item #39). **S3 (un-gate shadows/beacons in CAPTURE) — DEPRECATED (Goal-Frame catch 2026-06-15):** the gate's clean deterministic frames are CORRECT BY DESIGN for regression, and `pov-probe` already renders REAL play (non-capture: `<Canvas shadows>` on + beacons un-gated) — so un-gating capture would break the gate's purpose to solve a non-problem. Residual real value (real-play shadows read WEAK + landmark cyan beacons FAINT in pov-1, LOOK-verified) folds into the gameplay/wayfinding work, NOT a capture hack. **PIVOT (visuals solid -> the POINT half):** AO + de-tile + textures + ocean + tame-terrain all shipped = the world LOOKS good; Kevin's bigger complaint was POINTLESS/aimless. **S7 DISTANCE-TIER ✅ COMPLETE** (`world/zoneTier.js` kernel + siege RISK [`siegeParams(n,zoneTier)`] + loot REWARD [`game/lootTier.js`]; tier-0 byte-identical; S7.4 biome-roster DEFERRED=invasive worker; difficulty-curve playtest #41). **S8 SHRINE WAYFINDING ✅ COMPLETE** (`world/shrines.js nearestLandmark` finder + HUD cyan SHRINE marker [capture-suppressed + gate-locked] + Pilgrimage `reach_shrine` quest [replaced the DEAD Travel-500 explorer] + taller silhouettes/brighter beacons -> the deterministic landmarks are now navigable destinations = the answer to "wanders aimlessly"; #42 + redeploy reminder; S8c-bis reward-chest-at-shrine DEFERRED=cross-hook bridge). **S9 BLIGHT-HEART CLIMAX in progress** (plan `docs/superpowers/plans/2026-06-15-crafty-gameplay-point-s9-climax.md`). **S9a `blightHeartSite` kernel ✅ DONE** (`world/blightHeart.js` fixed far lair, radius 1280 = deep tier 4, `ceil(BLIGHT_RADIUS/SQRT2)`; +2 tests -> 1237; unused so gate 20/20). **S9b Part 1 (Blight Heart compass marker) ✅ DONE** (HUD.jsx obsidian-violet "BLIGHT HEART (Nm)" from `blightHeartSite()` + `bearingToMarker`, capture-suppressed + LOCKED by `blight-marker-gates.test.js`; +3 tests -> 1240; captured+gate 20/20 no leak; compass now spans HOME+SHRINE+BLIGHT-HEART). **S9b Part 2 (boss relocated to the lair) ✅ DONE** (`world/bossSystem.js`: ~1.5s arrival poll spawns the dragon at `blightHeartSite()` when the lvl>=5 player reaches ~24 blocks; the `playerPos+25` ambush removed; once-guard + level-gate + DEV `forceBossSpawn` fixture kept; locked by `boss-lair-gates.test.js`; +3 tests -> 1243; capture-safe by construction so gate 20/20. Lair distance 1280 + ambush->journey FEEL = KEVIN playtest #43). **S9c (WIN-STATE) ✅ DONE** (`VictoryOverlay` in GameSystems.jsx mirrors DeathScreen, triumphant amber "The Blight Heart is shattered -- the frontier is yours" + "Keep exploring" dismiss = post-climax endless handoff; mounted in HUD gated on `bossSystem.bossDefeated && !victoryDismissed` -- used bossDefeated directly, no redundant store flag; capture-safe (bossDefeated false in capture), captured+gate 20/20, 1243). **S9 BLIGHT-HEART CLIMAX MILESTONE COMPLETE** (S9a kernel + S9b marker + S9b relocation + S9c win). **THE "GIVE IT A POINT" ARC IS WHOLE: S7 stakes -> S8 destinations -> S9 climax+victory** = the complete answer to "wanders aimlessly"; surfaced KEVIN-REVIEW #44 (loop + redeploy + playtest victory beat). **S10a (onboarding goal toast) ✅ DONE** (App.jsx: a 2nd one-time capture-safe toast names the goal -- "journey to the frontier shrines, then shatter the Blight Heart -- follow the compass"; references the S8/S9 compass markers; build clean, 1243, gate 20/20 by construction). S10b menu-tagline goal-copy = KEVIN-TASTE (surface; changes menu.png); S10c starter-quest = optional. **THE FULL S7-S10 PURPOSE ARC IS COMPLETE + LEGIBLE** (stakes -> destinations -> climax+win -> stated-goal onboarding) = the complete answer to "wanders aimlessly". **SFX OVERHAUL in progress** (Kevin directive (1), plan `docs/superpowers/plans/2026-06-15-crafty-sfx-overhaul.md`; verified GAP: every voice connects straight to ctx.destination = no master bus/limiter so the summed mix can clip). **Slice 1 `createMasterBus` kernel ✅ DONE** (`audio/masterBus.js` gain->DynamicsCompressor-limiter->destination, pure, +3 fake-ctx tests -> 1246; unused so gate 20/20). **Slice 2 (route the WebAudio mix through the bus) ✅ DONE** [SCOPE verified live: the AUDIBLE content the limiter governs = SFX + ambient wind-bed (the real clip risk); procedural pad/arp route too but are MUTED `PROC_MUSIC_GAIN=0`; the ElevenLabs MUSIC plays via a SEPARATE HTML-`<audio>` path `ui/MusicPlayer.jsx` NOT in this WebAudio graph = own volume, no clip risk -> unify would need a `MediaElementSource` bridge = a Slice-4 note]: `SoundManager.jsx` lazy `getMasterBus()` cached on `masterBusRef` keyed by ctx (rebuilds on swap, returns the input node) + all 5 connect-sites [pad/waterBass/arp/playSound/playTone] now `getMasterBus() || audioContext.current.destination` -> the summed WebAudio mix limits before the speakers, no clipping hole, one headroom point, `||destination` = never-silence fallback; locked by `master-bus-gates.test.js` +5 -> 1251 [imports createMasterBus, lazy getMasterBus, caches+rebuilds on ctx change, ZERO direct-to-destination, fallback form]; build+eslint clean; captured + gate 20/20 no leak; EAR-CHECK [fuller no-clip mix when many sounds fire] -> KEVIN-REVIEW. **Slice 3 (autoplay-resume robustness) ✅ DONE — REAL GAP found+fixed**: the ONLY ctx-resume sites were inside `startSynthPad`/`startArpeggiator`, both early-return on `!musicEnabled`, and App.jsx only ran `playBackgroundMusic()` when `isPointerLocked && musicEnabled` -> a player with MUSIC OFF never resumed the suspended ctx so ALL SFX were silently dropped (SFX wrongly depended on music). FIX: idempotent `resumeAudio()` on SoundManager (resumes only if suspended; capture-safe) exposed on the SoundContext + App.jsx `if (isPointerLocked) { resumeAudio(); if (musicEnabled) playBackgroundMusic(); }` = SFX-resume decoupled from music. Locked by `audio-resume-gates.test.js` +4 -> 1255; build+eslint clean; captured + gate 20/20 (resume never fires in capture). EAR-CHECK: SFX audible with music OFF (were silent) -> KEVIN-REVIEW. **Slice 4 (per-SFX review + missing-SFX) ✅ DONE — GAP FOUND+FILLED**: the S9c VictoryOverlay (the climax win screen) fired NO audio = the single biggest beat was silent. Added a dedicated `victory` voice (`makeVictorySound`: ascending C-major run -> held triad ~1.3s, grander than the reward fanfare), registered (37->38), characterization-tested, wired on VictoryOverlay mount via the `window.*` bridge (playVictory verb -> App window-bridge -> mount useEffect; capture-safe). Shrine-reach checked = already covered by the quest-complete fanfare (not a gap). Locked by `victory-audio-gate.test.js` +4 -> 1259; build+eslint clean; captured+gate 20/20. EAR-CHECK: slay the Blight Heart -> triumphant sting (KEVIN #45). **🔊 SFX OVERHAUL COMPLETE (Kevin directive 1): S1 bus+limiter kernel -> S2 whole-mix routed -> S3 resume-robustness -> S4 victory sting; remaining = EAR-GATED per-voice balance (KEVIN #45 checklist).** **POLISH/BACKLOG PHASE. S6 ORES-BY-DEPTH milestone — PLAN AUTHORED ✅** (`docs/superpowers/plans/2026-06-15-crafty-s6-ores-by-depth.md`). GAP VERIFIED LIVE (Goal-Frame): ores are defined in `world/Blocks.js` + crafted (`data/recipes.js`) + mob-dropped (`data/lootTables.js`) but `terrain.worker.js` places ZERO ore (deep solid = uniform Stone code 3) + the mine-drop `BLOCK_ID_MAP` (Terrain.jsx, `{1:grass..8:cactus}`) has no ore codes -> mining yields only stone. Block-code pipeline mapped: worker numeric code -> mesh `vBlockType` -> shader atlas layer == code (atlas `numLayers=10`, codes 0-9) -> `block_broken` drop map. FIX rides existing infra in 3 slices: **S1** bake 4 ore tiles into the atlas (numLayers 10->14; shader samples `round(vBlockType)` so 10-13 render free) · **S2** deterministic depth-banded ore-gen in the worker deep-stone branch (`world/oreGen.js` pure `oreCodeFor`, capture-safe, NO re-mesh) · **S3** extend BLOCK_ID_MAP (10:Coal,11:Iron Nugget,12:Gold,13:Diamond — verify names resolve). Bold-flat/NO-PBR/NO-RE-MESH/capture-determinism locks called out; rarity-curve = playtest tunable. **S6 Slice 1 (atlas ore tiles) ✅ DONE** (`proceduralTextures.js` numLayers 10->14; coal10/iron11/gold12/diamond13 = stone base + clustered ore-color nuggets, deterministic getNoise, bold-flat no-PBR; test 10->14 + ore-distinct-from-stone invariant, locks kept; unit 1260, build clean, captured+gate 20/20 since layers 0-9 byte-identical + no ore voxels yet). **S6 Slice 2 (deterministic depth-banded ore-gen) ✅ DONE**: pure `world/oreGen.js` `oreCodeFor(worldX,worldY,worldZ,surfaceY)` -> 3|10|11|12|13 (ONE position hash, no RNG/seed-dep; disjoint depth-gated bands coal shallow-common -> diamond deepest-rarest; tunable thresholds) + `oreGen.test.js` (+5: determinism, valid-codes, shallow-never-diamond, deepest-can-diamond, rarity coal>iron>gold>diamond + ores <8%); wired into terrain.worker.js deep-solid branch (cave/surface/secondary/water untouched, NO re-mesh — gen-time only). unit 1260->1265, build clean (the `./oreGen.js` module-worker import resolves), captured + gate 20/20 (ores deep, surface frames unchanged). Block code is now ONE integer end-to-end: worker-gen == atlas layer (S1) == drop-map key (S3 next). **S6 Slice 3 (mined ore drops its item) ✅ DONE**: BLOCK_ID_MAP (Terrain.jsx) extended 10:'coal' 11:'iron' 12:'gold' 13:'diamond' — VERIFIED lowercase block keys (addToInventory uses a raw key; recipes.js matches lowercase incl. a Gold Helmet recipe; inventory renders via BLOCK_TYPES[key]); the kernel's display-name candidates ('Iron Nugget' etc.) would have BROKEN recipe-matching — verify-first caught it. Gate `ore-drop-gates.test.js` +2 (ore codes + no-regression); unit 1267, build clean, captured+gate 20/20 (logic-only). **⛏️ S6 ORES-BY-DEPTH MILESTONE COMPLETE** — gen (S2 worker depth-banded placement) -> render (S1 atlas tiles 10-13) -> mine/drop/craft (S3 BLOCK_ID_MAP -> lowercase key -> recipes), ONE block-code integer end-to-end; digging deep now pays off (coal shallow/common -> diamond deepest/rarest). Rarity/depth curve = playtest tunable (`oreGen.js`) -> KEVIN-REVIEW #46. **S8c-bis CHEST-AT-SHRINE milestone — PLAN AUTHORED ✅** (`docs/superpowers/plans/2026-06-15-crafty-s8c-bis-chest-at-shrine.md`). GROUNDED (Goal-Frame): the original cross-hook defer reason DISSOLVED — the chest system is `useTreasureChests()` in QuestSystem.jsx which ALREADY owns setChests/openChest/checkChestProximity AND already imports nearestLandmark + zoneTier; putting the shrine-chest spawn inside that hook needs NO bridge. 3D chests render from `treasureChests.chests` (HUD) + open via the existing proximity->openChest->CHEST_LOOT path, so a `{...chest,shrine:true}` entry renders+opens for free; the reach_shrine poll already shows the once-per-shrine ref-Set pattern to mirror. Plan: S1 = deterministic capture-guarded once-per-shrine chest spawn at the shrine base (inside useTreasureChests, 12-block trigger, exempt from the 5-cap); S2 = tier-scaled reward (far shrines pay more via zoneTier). **S8c-bis Slice 1 (reward chest spawns at each shrine) ✅ DONE**: `shrineChestsSpawned` ref Set + capture-guarded ~3s poll in `useTreasureChests` -> spawns `{...chest, shrine:true}` at the shrine base when player within 12 blocks of nearestLandmark, once per chunk, exempt from the 5-cap (mirrors reachedShrines); NO cross-hook bridge (spawn in the setChests-owning hook -> eslint clean); renders/opens via the existing path. Gate `shrine-chest-gates.test.js` +3 -> 1270; build+eslint clean; captured+gate 20/20 (guarded, no baseline leak). **S8c-bis Slice 2 (tier-scaled reward) ✅ DONE**: openChest (chest-find moved above the loot roll) gives a `chest.shrine` chest `(1 + zoneTier)` extra rolls biased to the rarer half of CHEST_LOOT (zoneTier caps at MAX_TIER -> far shrines pay more, S7 lootTier philosophy); non-shrine unchanged. Gate +1 (openChest refs chest.shrine + zoneTier) -> 1271; build+eslint clean; captured+gate 20/20. **🗝️ S8c-bis CHEST-AT-SHRINE MILESTONE COMPLETE** — S8 destinations now have a tangible frontier-scaled payoff (see shrine -> journey -> guaranteed reward chest, richer farther out + Pilgrimage XP), NO cross-hook bridge. Reward magnitude + 12-block radius = playtest tunables -> KEVIN-REVIEW #47. **S4b de-island — GROUNDED -> NON-BUILD ✅ (Goal-Frame catch)**: the world is INFINITE PROCEDURAL CONTINENTS (`continent = noise2D(worldX*0.002, worldZ*0.002)` in heightAt.js, NO radial falloff) — there is no finite island. The "seemingly on an island" feel = `OCEAN_CONTINENT_THRESHOLD=-0.15` (oceanProfile.js); lowering it shrinks oceans/enlarges land but the code comment flags it as a "how far should sea be" TASTE call that relocates the ocean capture cameras + trades against the Kevin-requested ocean/coast work + re-baselines. Per ASK-GATES (world-scale + ratified hybrid) -> NOT an autonomous re-shape; SURFACED as KEVIN-REVIEW #48 (the one-line threshold tradeoff). Did NOT touch worldgen. **KEVIN-REVIEW hygiene ✅ DONE** (added a dated "📌 CURRENT OPEN DECISIONS — 2026-06-15" snapshot at the top of KEVIN-REVIEW-BATCH.md: the live asks = #44 single holistic redeploy/playtest [subsumes #37-#40] · playtest curves #41/#43/#46/#47/#48 · ear-taste #45/#42/S10b/#4 · standing locks; append-only, historical log intact. Verified LOOP-KERNEL-PROMPT.md CONTINUE already encodes idle-watch — no edit needed). **🔶 KEVIN MEGA-DIRECTIVE #2 (2026-06-16) — TOTAL SOTA AUDIT + ENHANCE EVERY ELEMENT (blank-slate authority).** Kevin: "map/review/TEST every single line of code; make sure everything actually works AND that the SOTA plan actually enhanced EVERY element/aspect/feature (down to the tiniest control button / UI / effect) — a lot still looks/feels PRE-master-plan; treat it like a TOTAL FRESH GAME; you're allowed to re-design/enhance from a blank slate; keep autonomously working it out." This is the dominant mode now — supersedes idle-watch. **First spawn-direction fix SHIPPED 2026-06-16:** persistent `ObjectiveTracker` HUD cue (always-on, facing-independent arrow + objective text + distance; `bearingDeg` in compass.js + `shrineReached` store flag; capture-safe; +6 -> 1277; gate 20/20). That was a microcosm of Kevin's complaint: a SOTA system shipped but its value never SURFACED to a real returning player. **Phase A AUDIT COMPLETE ✅** (16-agent workflow `wd28y0to9`, 2.6M tokens -> `docs/superpowers/2026-06-16-SOTA-TOTAL-AUDIT.md`). VERDICT: a deep well-tested ENGINE wearing a half-finished COSTUME (~55-60% SOTA, ~40-45% pre-masterplan) along 4 themes: (1) works-but-flat/static/floaty (flat hitstop, random shake, no coyote/buffer, ZERO attack telegraphs, instant deaths, DEAD wind-grass, inert landmarks); (2) built-but-unwired/muted (playCraft silent, arp muted, boss banks no Aspect meter, ore-debris white, no mute UI); (3) off-brand first/last impressions (AuthModal/splashes/Death/Victory raw Tailwind); (4) below-2026 a11y/tunability (no reduced-motion/intensity toggles, touch has NO dodge, iPad on flattest tier). Almost all AUTONOMOUS within the locks. **NEXT = BUILD the backlog (Phase C), Phase-B-verifying each premise vs LIVE code first (audits are T3 single-source).** **M1 GAME-FEEL CORE ✅ COMPLETE 2026-06-16** (`1a242b1` bug cluster + `dffb49f` trauma pure core + wired: shakeOffset trauma^2 shake + weight-tiered HITSTOP + juiceIntensity dial; 1290 tests / 20/20). **M2 DIRECTIONAL IMPACT #9 ✅ COMPLETE 2026-06-16** (Slice A flinch-lean `0e12ad2` + B spark-cone `d89f2cc` + C camera-shake-dir `2f0bcef`; `entity.hitDirection` [was zero-consumer] now drives flinch lean [`flinchTilt` facing-local] + spark cone [`biasAlong`] + camera lurch [`cameraShakeDir` preserved through decay]; 1299 tests / 20/20 / build+eslint clean; pov-probe live no-crash; FEEL magnitudes → KEVIN #50). **M2 TELEGRAPHS #4 IN PROGRESS** — Slice 1 timing core ✅ DONE (`7a74a14`: pure `game/attackTelegraph.js` dodgeable windup->strike machine [WINDUP_MS=380] + AI-worker windup gate [3 attack sites stage pendingAttack, gated; inline mirror, classic worker can't import] + windupUntil round-trip SNS↔worker; 1310 tests / 20/20; invisible timing, worker frozen in capture -> no drift). Plan: `docs/superpowers/plans/2026-06-16-crafty-m2-telegraphs.md`. Slice 2 render ✅ + Slice 3 boss lava telegraph ✅ DONE (`fa60ee6`/`d82ed8c`). **#4 ATTACK TELEGRAPHS COMPLETE** — mob dodgeable windup (pose+glow) + boss lava ring spawns as a harmless forming warning [750ms] before arming (BossEntity has its OWN attack loop, not the ai.worker; lava was instant-at-feet=undodgeable). 1321 / 20/20. **M2 Theme 3 #7 S1 DEATH WEIGHT ✅ DONE** (`f82f2a5` mob dissolve: game/deathFx.js dissolvePose [320ms shrink+spin], health<=0 finisher guarded+fires-once, ecs.remove DEFERRED to a dying-sweep, render filter keeps corpse rendering, MobModel dissolve via windupRamp; `5f91d47` boss-kill slow-mo [HITSTOP.boss] + bloom flash. 1332 / 20/20). #7 S2 ✅ DONE (`2a6b999`: Death/Victory overlays rebuilt on Panel/Button + tokens + run summary [level/nights]; LOOK-verified via scripts/visual/death-probe.mjs). **🏆 M2 COMBAT-READABILITY COMPLETE** (#9 directional impact + #4 telegraphs + #7 death/victory beats). 1336 / 20/20. **M3 #3/#6 IN PROGRESS** — plan: docs/superpowers/plans/2026-06-16-crafty-m3-settings-a11y.md. S1 feedback-intensity slider ✅ DONE (`c5d1b7d`: SettingsPanel [ui/GamePanels.jsx] now has a Feedback Intensity slider bound to the M1 juiceIntensity dial; LOOK-verified via scripts/visual/settings-probe.mjs). 1339 / 20/20. S2 reduced-motion ✅ DONE (`ab8d96c`: game/a11y.js motionIntensity + App matchMedia listener -> setJuiceIntensity(0), capture-gated + explicit Settings 'Reduced Motion' toggle; LOOK-verified). 1346 / 20/20. S3a SFX-volume slider ✅ DONE (`b1bd1cf`: game/audioSettings.js audioGain + store sfxVolume + SoundManager applies it to the live master-bus gain + getMasterBus seeds it; SettingsPanel 'Sound Effects' slider; LOOK-verified). 1352 / 20/20. #3 SETTINGS/A11Y ✅ COMPLETE — S3a SFX-volume + S3b music-volume + master-mute (`b1bd1cf`/`e368af3`: store sfxVolume/musicVolume/masterMuted; SoundManager applies audioGain(sfxVolume,masterMuted) to the live SFX bus; MusicPlayer target = masterMuted?0:VOL*musicVolume; SettingsPanel Sound-Effects/Music sliders + Mute-All toggle; LOOK-verified). 1355 / 20/20. #6 touch dodge ✅ DONE (`4eb001c`: Wind dodge button in the touch action cluster -> the existing setIntent('dodge') edge-trigger; mobile.png re-baselined; LOOK-verified). **🏆 M3 SETTINGS/A11Y COMPLETE** (#3 feedback-intensity + reduced-motion + audio[SFX/music/master-mute] + #6 touch dodge). 1358 / 20/20. Deferred (future, not Kevin-gated): controls-remap, colorblind/text-scale, settings persistence, input-buffering/coyote/jump-buffer. **M4 LIVING WORLD in progress.** #5 WIND-GRASS REVIVED ✅ (S1a `bd9ab34` worker grass-tops + S1b `059777c` mount OptimizedGrassSystem per chunk; blade 0.4x0.7 readable tuft; ground-probe LOOK-confirmed; gate 20/20 sub-6%). 1369 / 20/20. #8 landmarks ✅ MOSTLY DONE (`ca5d0c7`: Blight-Heart obsidian monolith + violet beacon at the lair, capture-safe + pure blightHeartChunk; Verify-Before-Assert found the shrines were already non-flat via S8d). 1374 / 20/20. Deferred: interactable shrines + obelisk-grow-on-approach -> KEVIN #50. **NEXT = M4 S3 -- far-LOD (#18).** GROUND Terrain.jsx chunk streamer (render distance / cull radius / any existing LOD) + the fog/horizon (mood.js fogDensity, the aerial-perspective haze from the surface-texture work). The audit: distant terrain pops / hard horizon edge. Add a cheap far-distance read (fog-blend or a low-detail far silhouette) so the horizon reads as DEPTH not a cliff -- gentle GLOBAL knobs are sub-6% (re-baseline or stays green). Capture-safe or deliberate re-baseline. TDD any pure LOD/fog math. Source-gate. LOOK via pov/grass-probe. THEN S4 biomes #12 -> mark M4 COMPLETE. Audit theme-1 'static/dead world': #5 wind grass (the GPU wind-grass shader is DEAD CODE -- ZERO ground vegetation; GROUND world/Terrain.jsx + any grass/foliage/instanced-vegetation system + the dead shader) + #8 real landmarks (inert geometry today) + #18 far-LOD + #12 biomes. Lowest-risk slice first (likely wind-grass revival or a small landmark pass); each VISIBLE -> capture+gate (DELIBERATE re-baseline per intended look change, LOOK-review each) + pov-probe LOOK. Locks hold (bold-flat). FEEL → KEVIN #50. Plans: docs/superpowers/plans/2026-06-16-crafty-m2-combat-readability.md (Theme 3) + ...-m2-telegraphs.md (done). FEEL → KEVIN #50. FEEL/timing → KEVIN #50. Milestone order: ~~M1 game-feel core~~ (#1 `game/trauma.js` trauma-shake + weight-tiered hitstop + `juiceIntensity` dial) + the verified quick-win BUG cluster (ore-debris codes 10-13 in BLOCK_COLORS; wire playCraft; spatial pitch-jitter; repoint `npm test`). -> M2 telegraphs(#4)+directional-impact(#9)+death/victory beats(#7). -> M3 Settings/a11y(#3)+input-buffer/coyote/touch-dodge(#6). -> M4 living world: wind-grass(#5)+real-landmarks(#8)+far-LOD(#18)+biomes(#12). -> M5 depth: inventory-correctness(#15)+element-zone-VFX/beast-verbs(#11)+mining-feel(#13)+squad-depth(#19). -> M6 polish: lighting-bounce(#14)+HUD-aliveness(#10)+adaptive-audio(#16)+pre-game-brand(#17)+repo-health(#20). EACH milestone: plan-doc (writing-plans) FIRST, TDD red-first, gates (unit holds-or-grows · build clean · visual 20/20 or deliberate re-baseline) + LIVE-PROBE LOOK on visuals (green gate != surfaces-to-player). Spawn deep workflows/agents + multi-search freely (Kevin authorized). Kevin-gated (surface, don't decide): render glowier-vs-restrained lock · FEEL timing/balance playtest · title-screen look(#17) · arp keep-vs-delete(#16) · real-device perf(#20) · de-island(#48) · standing #41-#49 + #3/#4/debt/bloom/AO/PBR · S10b tagline · i18n #73 BLOCKED. THEN S2 (tier reward) -> mark S8c-bis COMPLETE. **THEN backlog:** S4b de-island (delicate worldgen — plan+Goal-Frame first) · KEVIN-REVIEW-BATCH contract-drift cleanup · S10b menu-tagline (Kevin-taste). If ALL remaining is Kevin-gated (taste/playtest/device), SAY SO + lengthen cadence to ~30min. i18n #73 BLOCKED. Re-read world-purpose-sota + codebase-reality-audit for the next SOTA lever. THEN S6 S2 (worker ore-gen) -> S6 S3 (drop map) -> mark S6 COMPLETE. **THEN remaining backlog:** S8c-bis chest-at-shrine (cross-hook bridge) · S4b de-island · S10b menu-tagline (Kevin-taste) · KEVIN-REVIEW-BATCH contract-drift cleanup; OR Kevin-playtest tuning once #44/#45 land. If all remaining is Kevin-gated, say so + lengthen cadence. i18n #73 BLOCKED. Re-read world-purpose-sota + codebase-reality-audit for the next SOTA lever. Kevin decisions (defaults, surfaced): shrine-theming #4 generic+1-accent · grade-flip #3 deferred · sequencing #5 purpose-first · shrine-look #42 · difficulty-curve #41 · AO_MIN #39 · bloom-lock keep · texture-PBR keep-bold-flat #39. **KERNEL SYNCED to v3** — `LOOP-KERNEL-PROMPT.md` (the durable copy) now matches the live prompt (+ STABILIZE-first echo, ANTI-REDO trimmed to a pointer, probe-path fix); charter §1.6 + CLAUDE.md compaction-list now name it (fixes the drift Kevin caught — the on-disk kernel was stale v2). DO NOT blind-tweak terrain constants — consts live in `heightAt.js`.

## 🟢 (superseded by the mega-directive above) ALL SELF-VALIDATABLE FRONTS DONE — only Kevin checks remain (steady-state hold, ~45min cadence)
**GATES (HEAD `eb924db`): 1182 unit · 20-state visual (incl. `ocean-coast`) · build clean.**
> **RESUME ANCHOR (post-compaction, read this):** HEAD = `eb924db`, all 5 fronts SHIPPED + pushed to main (camera fix · music · terrain+sun · touch · ocean S1-S4). This-session NEW tests: touch-entry-gate, quest-tracker-touch, touch-xp-readout, ocean-foam-kernel, ocean-foam-wiring, seabedDepthT (tests/world/oceanProfile.test.js). NEW standing self-validation probes: `frontend/scripts/visual/{look-e2e,touch-probe,ocean-probe,pov-probe}.mjs`. **POST-COMPACTION STANCE:** HOLD steady-state — do NOT manufacture churn; resume the tight build cadence ONLY on Kevin steer (a new front, or the KEVIN-REVIEW #37/#38 checks come back: redeploy crafty-sand.vercel.app · real-device iOS touch feel · music ear-check · ocean taste), or a genuinely high-value/regressed self-validatable unit (confirm live via a probe first).
Camera ✅, Music ✅, Terrain+Sun ✅, Touch ✅ (probe-exhausted), Ocean ✅ (S1-S4). **What remains needs Kevin (batched in KEVIN-REVIEW #37 + new #38):** (1) REDEPLOY crafty-sand.vercel.app (camera + touch + ocean all on main, not yet deployed); (2) real-device iOS touch feel; (3) music ear-check (day/night/boss + volume); (4) ocean taste sign-off (foam strength / teal hue / depth). Optional-later self-validatable (low-ROI): sun-arc (complex), menu/per-biome music (ear-check anyway). **LOOP STANCE:** lengthen to ~25min; each tick re-orient + either pick a clean optional-later unit OR idle-tick if only Kevin-gated work remains. Don't manufacture low-value churn. Each: red-first where logic; re-capture + `git diff HEAD` + HD-eyeball EACH changed water frame + ocean-probe re-run; re-baseline deliberately. Self-executable; Kevin taste async (#37). **REMAINING touch polish (next candidates):** M3b/M3c ergonomics; per-finger-size hit-area tuning; check other HUD elements for mobile fit (XP/health/spell label top cluster); real-device iOS-Safari feel (Kevin-owed). **Next unit: pick — more touch polish, or rotate to another front; verify-before-assert, ship, continue.** Write MINIMAL code.

**🔴 P0 FIXED THIS SESSION — DESKTOP MOUSE-LOOK WAS DEAD (Kevin caught it live: camera stuck).** Root: drei `<PointerLockControls>` (element-match-fragile + drei/three version-drift + UNTESTABLE in the capture harness → a dead camera shipped with zero gate). Replaced with our own `src/input/pointerLook.js` (lenient pointer-lock gate, reuses the tested `applyLook`). **PROVEN** via a new real-browser gate `npm run test:look` (yawΔ0.92 rad on real mouse move). Commit `85ff7b5`. **⚠️ KEVIN: REDEPLOY crafty-sand.vercel.app to get the fix** (it's in `main`, not yet on the deploy). Also live-verify JUMP (code intact, but the headless probe was too flaky to confirm; movement + spell-select confirmed working).

**LESSON (the real one):** the headless gates (visual = static pinned-camera frames; unit = pure logic) NEVER exercise live input/camera/feel — so "passes the gates" ≠ "plays right." I overclaimed "mature/SOTA" while the camera was dead. The loop is good at logic/render + regression, BLIND to live play. High-value work from here needs Kevin in the loop (playtest). `npm run test:look` now covers the camera class; more live-input gates are option (C).

> **(historical) iter 190:** HEALTH POTION ALCHEMY RECIPE — craft healing from loot (content/depth); all green. Verify-before-assert confirmed iter-189 cooking PAYS OFF (Cooked = feed40+heal10 vs Raw feed15) → sound. Added a Health Potion recipe (Spider Eye + Emerald + Spider Eye → heal-30 potion) — a proactive sustain-agency loop (the chest/coin/dawn/food sources are passive/timed/purchased). Pure data in `recipes.js` + recipe-gate assertion; pattern unique (no-dup check). **unit 1153→1154 (+1) · build clean · visual 19/19** (crafting panel not a capture state → byte-identical). Commit `f76790a`. **HONEST STATE: per-iteration value is now clearly diminishing.** The game is mature + feature-complete on validatable axes; recent units (cooking, potion, siege-ladder, etc.) are incremental polish. The genuine HIGH-value work is the 4 MILESTONE levers, all combat-FEEL/playtest-gated → **awaiting Kevin's steer (KEVIN-REVIEW #35)**. **RESUME options:** (a) keep shipping small validatable content/polish (recipes, content-data, capture-safe visuals) at the current cadence; (b) a doc/hygiene pass; (c) **lengthen the wakeup cadence** (the loop's been firing ~90s for 10+ iters; stretching to a few min is honest given diminishing returns + the Kevin-gate). De-monolith + i18n PARKED. **ALWAYS `cd /Users/kz/Code/Crafty/frontend` first.**

> **(historical) iter 189 close-out:** COOKING RECIPES — raw→cooked over coal + `RECIPES` extracted to pure `src/data/recipes.js`. Commit `ce08cf5`. Verified the gap (mobs drop Raw Porkchop/Beef; inventory consumes Cooked Porkchop/Beef as better food; NO recipe bridged raw→cooked → food loop incomplete). Added "cook over coal" recipes (Raw + coal → Cooked; output→addToInventory→inventory.blocks = usable). Extracted `RECIPES` to a pure `src/data/recipes.js` (loot-tables precedent: testable + a small CraftingTable de-monolith; byte-exact) + a gate (every recipe valid + cooking present + NO duplicate patterns = a free integrity check). **unit 1150→1153 (+3) · build clean · visual 19/19** (crafting panel is a C-toggle modal, not a capture state → byte-identical). Commit `ce08cf5`. **RESUME = PICK a VALIDATABLE feature** (milestone levers stay Kevin-gated, KEVIN-REVIEW #35). Candidates getting thin/incremental: a new mob (re-baseline bestiary), #2 day-dial (re-baseline heavy), more recipes/content-data, more audio (ear-check). **Per-iteration value is diminishing — the high-value work now genuinely needs Kevin's steer on the milestones; keep shipping clean validatable polish/content meanwhile.** De-monolith (Player/GameScene) + i18n PARKED. **ALWAYS `cd /Users/kz/Code/Crafty/frontend` first.**

> **(historical) iter 188 close-out:** DOC-CURRENCY SWEEP — §3 banner refreshed (iters 181-187 arc + scale 1150/53); headers clean. Commit `d4ce9ea`. Refreshed the `SOTA-INITIATIVE.md` §3 LIVING banner with the iters 181-187 arc (holistic Workflow → 22-item backlog → harvested quick-wins: dusk-warning/place-puff/reward-audio+bug/coin-sink/home-marker/endless-quests/siege-ladder; + the Kevin-gated milestone note) + live scale (1150 tests / 156 test files / 53 gate files). Verified plan/spec headers clean (no stale draft/plan/awaiting). KEVIN-REVIEW (#35) + backlog currency confirmed. **No code → no test/build/visual run (nothing renders).** Chose hygiene because doc-drift is the charter's twice-caught failure + the docs now carry Kevin-facing decisions that must stay trustworthy. **RESUME = PICK a VALIDATABLE feature (priority-5)** — the milestone levers stay Kevin-gated (KEVIN-REVIEW #35; don't build combat-feel blind). Candidates: a CONTENT unit (new mob — validatable via the mob-bestiary capture frame + needs MOB_TYPES+mobFeatures+lootTable[gate-required]+bestiary re-baseline; OR a new crafting recipe — 3×3 pattern in `ui/panels/CraftingTable.jsx`, validatable via build/gate), #10 recallable onboarding (verify the controls panel first), #2 day-phase dial (re-baseline heavy). De-monolith + i18n PARKED. **ALWAYS `cd /Users/kz/Code/Crafty/frontend` first.**

> **(historical) iter 187 close-out:** NUMBERED SIEGE LADDER — `siegeWarning(nightCount)` numbered+tiered nightfall message (UX; #8). Commit `10087e4`. Also flagged the 4 milestone levers as combat-feel/playtest-gated (KEVIN-REVIEW #35). Verified #8 real (nightfall banner had no night number; siege ramps via siegeParams invisibly). Added pure `siegeWarning(nightCount)` in dayNight.js (numbered + tiered: "Night N — <first siege…relentless>. Hold until dawn!") + wired survivalSystem. **unit 1146→1150 (+4) · build clean · visual 19/19** (nightfall frozen in capture → byte-identical). Commit `10087e4`. **KEY STRATEGIC NOTE (recorded this iter):** the quick-win backlog is HARVESTED. The remaining high-value levers are the 4 MILESTONES (recurring boss spine, build-identity talents, **enemy attack telegraphs**, cinematic-beat) — but their CRUX is FEEL/combat-balance, which the headless loop CANNOT validate (AI frozen in capture; no mob-attack capture state; no in-world eyeball). These are **Kevin-playtest-gated** — logged to KEVIN-REVIEW #35 (the telegraph design is the top combat-depth lever). **RESUME = PICK a VALIDATABLE unit** (pure logic / data / capture-safe visual / audio-with-ear-check): remaining small backlog (#10 recallable onboarding [verify controls panel], #2 day-dial [re-baseline]), a CONTENT unit (new mob — validatable via the mob-bestiary capture frame), or a doc-currency sweep (due ~now, ~7 iters since iter-180). Don't build combat-feel milestones blind. De-monolith + i18n PARKED. **ALWAYS `cd /Users/kz/Code/Crafty/frontend` first.**

> **(historical) iter 186 close-out:** ENDLESS BOUNTY QUESTS (progression; #9) — `makeRepeatableQuest` fallback in claimQuest. Commit `def2636`. Fresh axis (progression). Verified #9 real (`claimQuest` refilled only from the finite QUEST_LIST → tracker went empty late-game). Added pure `makeRepeatableQuest(seq)` (a scaling `bounty_<seq>` kill-quest: 15+5n mobs, 150+50n XP) + a `claimQuest` fallback (authored quests first, then endless bounties keyed off the existing-bounty count for unique ids). **unit 1142→1146 (+4) · build clean · visual 19/19** (claim is input-only → byte-identical). Commit `def2636`. **RESUME = PICK, verify each backlog gap live first.** Remaining quick-ish: #8 night-count in nightfall toast (clean, siege), #10 recallable onboarding (verify the controls panel first), #2 day-phase dial (re-baseline heavy). **The quick-win backlog is now largely harvested** — consider the 4 MILESTONE levers (each needs a plan doc): recurring apex/boss spine (top retention), build-identity talents, enemy telegraphs+archetypes, cinematic-beat pass; OR a content unit (new mob/biome) OR a polish/tuning pass. De-monolith + i18n PARKED. **ALWAYS `cd /Users/kz/Code/Crafty/frontend` first.**

> **(historical) iter 185 close-out:** HOME MARKER on the compass (navigation UX; #17) — pure `game/compass.js` bearingToMarker + gold HOME marker, capture-suppressed. Commit `0b4a223`. Varied axis off the siege theme. Verified #17 real (Compass showed boss+chest markers, never home → a built base was unfindable). Extracted a pure `game/compass.js` `bearingToMarker()` (dx/dz→atan2→heading-diff→pct; node-tested) + a gold "HOME (Nm)" Compass marker pointing at world origin (the Hearth plinth per `world/homeAnchor.js`), hidden when standing on it, **capture-suppressed** like boss/chest → no re-baseline. **unit 1136→1142 (+6) · build clean · visual 19/19** (byte-identical). Commit `0b4a223`. In-play eyeball owed (not a capture frame; mirrors the boss-marker style). **RESUME = PICK, verify each backlog gap live first.** Remaining plausibly-real: #8 night-count in nightfall toast (clean, siege), #2 day-phase dial (re-baseline heavy), #10 recallable onboarding (verify the controls panel first). Given backlog unreliability + maturity, a POLISH/TUNING pass or a CONTENT unit (new recipe/mob — fresh axis) are also strong. The 4 milestone levers (apex/boss spine, build-identity talents, enemy telegraphs, cinematic-beat pass) each need a plan doc. De-monolith + i18n PARKED. **ALWAYS `cd /Users/kz/Code/Crafty/frontend` first.**

> **(historical) iter 184 close-out:** COIN SINK — `spendCoins` + coin→potion trades at the merchant (economy; #5). Commit `2d34e88`. Verified #5 is REAL (unlike the prior 4 over-optimistic items): coins were earned/shown/persisted but had NO spend path (`spendCoins` didn't exist; the merchant bartered items, never coins). Added `spendCoins(n)` (deduct-iff-affordable, returns bool, clamps negatives) + two coin→potion trades in TradingInterface (12c→Health, 10c→Mana via `executeCoinTrade`→`addToInventory`, which routes to the flat `inventory.blocks` bucket GamePanels consumes potions from → genuinely usable) + a Coins display in the merchant. Closes the dawn-reward→spend loop. **unit 1131→1136 (+5) · build clean · visual 19/19** (merchant is a villager-proximity modal, not a capture state → byte-identical). Commit `2d34e88`. **Eyeball note:** the merchant panel isn't a capture frame (can't gate-verify visually) but the coin rows + coins cell mirror the existing trade-row/resource-cell patterns exactly (low taste risk). **RESUME = PICK, verify each backlog gap live first.** Remaining plausibly-real: #8 night-count in nightfall toast (clean), #2 day-phase dial (re-baseline heavy), #10 recallable onboarding, #17 hearth-on-compass. Given the backlog's unreliability + game maturity, a POLISH/TUNING pass (eyeball-and-refine existing systems) is also high-value. De-monolith + i18n PARKED. **ALWAYS `cd /Users/kz/Code/Crafty/frontend` first.**

> **(historical) iter 183 close-out:** REWARD-BEAT AUDIO — fixed the DEAD level-up sound (unassigned `window.playLevelUpSound`) + added achievement/quest fanfare. Commit `25a3e8a`. Heavy verify-before-assert pass: the backlog's top items are SYSTEMATICALLY over-optimistic (4 corrected this iter — the holistic reviewers missed wired systems): **#3 spell-upgrades = REDUNDANT** (spell damage already scales via the intellect attribute through `solveSpellDamage`; `SPELL_UPGRADES` is an unused parallel system → re-filed: delete-or-repurpose, NOT a feature gap); **#1 break-debris** already exists; **#4 incoming-hit flash+screenshake** already fire from `damagePlayer`. The genuinely-real find: **a BUG — `window.playLevelUpSound` is referenced (SimpleExperienceSystem) but never assigned → level-up was SILENT** despite the voice+verb existing. Fixed it + added a `makeFanfare` voice (brass arpeggio, distinct from dawn-chime) wired to level-up + achievement + quest-complete via App's window-bridge (the codebase's existing `window.play*` pattern; null in capture). **unit 1127→1131 (+4) · build clean · visual 19/19** (audio-only → byte-identical). Commit `25a3e8a`. Ear-check owed (KEVIN-REVIEW). **RESUME = PICK, but the backlog is UNRELIABLE on premises — VERIFY each "gap" is genuinely missing FIRST (4/top-items were already-done/redundant).** Remaining plausibly-real: #5 coin sink (verify no spend exists; needs UI), #8 night-count in nightfall toast (clean, siege-themed), #1 day-phase dial (re-baseline heavy), #10 recallable onboarding. Consider: the game is MATURE enough that POLISH/TUNING of existing systems (eyeball-and-refine via probes) may beat new features. De-monolith + i18n PARKED. **ALWAYS `cd /Users/kz/Code/Crafty/frontend` first.**

> **(historical) iter 182 close-out:** BLOCK-PLACE PUFF — colored dust on placing (game-feel; #1b). Commit `c851ac2`. Picked from the backlog. Verify-before-assert against the agent's T3 claims corrected TWO: the #1 "mining has no juice" is FALSE for breaking (worker `block_broken`→`BlockParticleSystem` already shatters colored debris) — only PLACE was silent; and #3 (spell-upgrades) is MEDIUM not a quick-win (re-filed: `SpellUpgradePanel` is the TALENT panel, `upgradeSpell` has ZERO callers → doubly-dead, needs combat-wire + a real upgrade UI + restore-on-load). Built the clean fresh-axis win: `place()` fires a gentle GPU-spark dust puff at the placed block's `BLOCK_TYPES` color (break shatters / place poofs — distinct per-verb feel). **unit 1124→1127 (+3) · build clean · visual 19/19** (place() input-only → byte-identical). Commit `c851ac2`. **RESUME = PICK from the backlog (`docs/superpowers/specs/2026-06-14-crafty-next-levers-backlog.md`; verify each seam live — the agent's tractability estimates run optimistic).** Strong next options: **#3 spell-upgrades** (highest value but MEDIUM → write a brief plan: combat-wire [balance-safe, L1 matches base] + a "Spell Mastery" section in the progression panel + restore-on-load), #5 coin sink, #7 reward fanfare (verify the sound-trigger path — `window.playLevelUpSound` set-site is unconfirmed), the day-phase dial (#2). Vary axis (siege theme is saturated). De-monolith + i18n PARKED. **ALWAYS `cd /Users/kz/Code/Crafty/frontend` first.**

> **(historical) iter 181 close-out:** HOLISTIC STEP-BACK (design-exploration Workflow → 22-item ranked backlog) + DUSK PRE-WARNING shipped. Commit `067ccc8`/`4678a22`. Ran the flagged holistic step-back as a code-grounded multi-agent Workflow (`crafty-next-lever-review`: 4 player-lens reviewers over `frontend/src` → ranked synthesis) → committed `docs/superpowers/specs/2026-06-14-crafty-next-levers-backlog.md` (22 ranked gaps + 4 milestone levers — the loop's near-term queue; NO more "what's the next gap" deliberation). **Verify-before-assert corrected the T3 findings:** #1 "mining has no juice" is FALSE for breaking (worker→`BlockParticleSystem` already spawns colored debris + auto-collect) — only PLACE lacks a puff; #2 day-clock is REAL → split into the clean capture-free **dusk pre-warning** (built) + a heavier day-phase DIAL (fast-follow, needs placement + re-baseline). **Built the dusk pre-warning** (`game/dayPhase.js` pure + `ui/DuskWarning.jsx` 1s-poll watcher, fires once on the dusk rising edge, capture-safe) — completes the siege legibility arc: PREPARE → horn+night → survive → dawn. **unit 1116→1124 (+8) · build clean · visual 19/19** (null watcher + capture-guarded → byte-identical). Commit `067ccc8`. **RESUME = PICK from the backlog (verify each seam live first).** Top verified quick-wins: **#3 wire the spell-upgrade DEAD CODE** (`spellUpgrades.js`/`getSpellStats` built + store-exposed but `EnhancedMagicSystem` reads static `spell.damage` — ~90% built, huge payoff), #5 coin sink, #7 reward fanfare, the day-phase dial, #1b place-puff. Bigger levers (plan-doc each): recurring apex/boss spine, build-identity talents, enemy telegraphs+archetypes, cinematic-beat pass. De-monolith + i18n PARKED. **ALWAYS `cd /Users/kz/Code/Crafty/frontend` first.**

> **(historical) iter 180 close-out:** SURVIVAL-PROGRESSION QUESTS — survive-nights + new-mob hunts. New `survive_nights` quest type (Nightwatch/Siege Veteran) via an `onNightSurvived` dawn hook (once per survived night) + Brute Breaker/Ember Hunter targeted hunts. unit 1116, visual 19/19. Commit `d4452cb`. Verify-before-assert REJECTED the visual siege/dawn-skybox candidate (the mood system — explore/dusk/obsidian palettes + lighting + magic-hour grade in `render/mood.js` — already shifts the sky dramatically at every transition; rebuilding it = high-risk/low-ROI). Pivoted to the flagged satellite-coverage gap but elevated it: a genuinely-NEW quest dimension that ties the siege loop together (onboarding promise → siege audio → survive-nights GOAL). Added quest type `survive_nights` (Nightwatch/3, Siege Veteran/7) driven by a new `onNightSurvived` hook fired from useSurvivalMode's dawn branch (gated on `grantDawnReward` returning → exactly once per survived night) + 2 targeted new-mob hunts (Brute Breaker/moss_brute, Ember Hunter/emberhusk; common new mobs stay covered by generic 'kill' quests by design). Exported QUEST_LIST. **unit 1111→1116 (+5 gate) · build clean · visual 19/19** (capture-safety invariant: all new quests tier≥2 → initial active set unchanged → HUD QuestTracker frames stable). Commit `d4452cb`. **RESUME = PICK per §2** — game is MATURE; rotate axes. The siege loop is now coherent (promise→audio→goal). Candidates for a FRESH axis: a game-FEEL interleave (verify a genuine gap first — feel is mature), a VISUAL element NOT already mature (e.g. the 3D loot-drop billboard — physical drops are a colored octahedron+beam with no item-icon; constantly-seen, ties to iter-178 loot; needs SVG→canvas-texture, render-bearing→re-baseline loot-showcase), or a bigger content unit (2nd biome/boss → plan doc). Consider a holistic step-back (Workflow design-exploration) to find the next BIG lever — gaps are getting incremental. De-monolith + i18n PARKED. **ALWAYS `cd /Users/kz/Code/Crafty/frontend` first.**

> **(historical) iter 179 close-out:** SIEGE-HORN + DAWN-CHIME — day/night transition audio (audio interleave). The siege/dawn had a banner + gradual music shift but no one-shot sting; added `makeSiegeHorn`/`makeDawnChime` voices + verbs + `ui/DayNightAudio.jsx` (isDay-edge watcher) + a gate. unit 1111, visual 19/19. Commit `256a86e`. Ear-check owed (KEVIN-REVIEW #33). Verify-before-assert: the onboarding toast (iter 177) promises "survive the night siege", but the siege onset had only a `SurvivalWarning` text banner + a gradual DAY/NIGHT music shift — **no one-shot sting** to punctuate the moment (it began silently). Audio = the charter's most-neglected first-class axis. Added 2 synth voices (`makeSiegeHorn` ominous war-horn at nightfall; `makeDawnChime` bright ascending bell at daybreak; both with the unclipped headroom guard) + `playSiegeHorn`/`playDawnChime` verbs + `ui/DayNightAudio.jsx` (a tiny HUD-mounted store-watcher firing on the `isDay` edge, mirrors HeartbeatAudio/UISounds; renders null; capture-frozen clock → never fires in capture) + a wiring gate. **unit 1107→1111 (+4 gate) · build clean · visual 19/19** (null component + edge-only fire → byte-identical). Commit `256a86e`. EAR-CHECK owed to Kevin (timbre/volume — can't hear headless; KEVIN-REVIEW). **RESUME = PICK per §2** — keep rotating axes; the game is MATURE. Open candidates: the flagged satellite-coverage follow-up (quest `kill_type` for the 4 new hostiles — content-data); a visual interleave (siege/dawn SKYBOX moment — pairs with this audio; reference-lock first, render-bearing → re-baseline); or a bigger content unit (2nd biome/boss, milestone-scale → plan doc). De-monolith + i18n PARKED. **ALWAYS `cd /Users/kz/Code/Crafty/frontend` first.**

> **(historical) iter 178 close-out:** LOOT TABLES FOR ALL HOSTILE MOBS + LOOT-COVERAGE GATE (content-coherence). Verify-before-assert found a real content gap: the QuestSystem kill path does `LOOT_TABLES[mobType] || []`, but `LOOT_TABLES` only covered the original 5 (pig/cow/zombie/skeleton/spider) — the 4 variety/content hostiles (skitterling/duskhound/moss_brute/emberhusk) dropped NOTHING, silently breaking the kill→loot→reward loop (worst: moss_brute, a 220-HP rare tank rewarding zero items). Extracted `LOOT_TABLES`+`CHEST_LOOT` → pure `src/data/lootTables.js` (byte-exact; QuestSystem re-exports), added 4 thematic tables using only registry items (`src/data/items.js`), and added a TDD guard (`tests/gates/loot-coverage-gates.test.js`) so every HOSTILE mob must drop a real registry item — lootless-hostile drift is now impossible. **unit 1107 (1098+9) · build clean · visual 19/19** (loot data consumed only on mob-kill, never in capture → frames byte-identical). Commit `e4222d3`. **RESUME = PICK per §2** — game is MATURE across all axes; keep rotating SOTA-experience (content-data depth / more mobs / a bigger 2nd boss-or-biome / polish). **New content-coherence heuristic surfaced this iter: when a data-driven registry (mobs/items/spells) gains entries, SWEEP the satellite tables keyed off it (loot, quests-by-type, i18n strings, icons) for coverage gaps** — candidate next-sweep: quest `kill_type` only references zombie/spider/skeleton (the 4 new hostiles have no targeted quest). De-monolith + i18n PARKED. **ALWAYS `cd /Users/kz/Code/Crafty/frontend` first.**

> **(historical) iter 177 close-out:** FIRST-SESSION ONBOARDING NUDGE (UX legibility). Verify-before-assert ruled out weather (rain/snow/fireflies already exist) + menu (premium) → the gap was in-world onboarding (title teaches KEYS not the GOAL loop). One-time first-spawn toast ("Build by day, survive the night siege — defeat foes to unlock powerful Aspect abilities") via the existing addNotification pipe, localStorage-flagged + capture-suppressed. Probe-confirmed (non-capture DOM + flag). **unit 1098 · build clean · visual 19/19.** Commit `9893a79`.

> **(historical) iter 176 close-out:** PREMIUM LEVEL-UP FLOURISH + XP CHIP (reward-feel coherence). The XP-bar was bold-flat but the LEVEL-UP effect + +XP chip were still AI-generic (raw yellow) — upgraded both to S1-C bold-flat (gold display-face sticker banner + ink frame + gold radial burst for LEVEL UP; gold chip for +XP). Balances the recent danger-side feel with a premium reward moment. Verified tokens (--ui-accent/-raise/-deep/-ink etc.). Transient → capture-null → no fixture; eyeballed via a temp grant-XP probe (reads premium). **unit 1098 · build clean · lint clean · visual 19/19.** Commit `9642e68`. **RESUME = PICK per §2** — rotate experience axes (content/audio/feel/visual/UX); the de-monolith is PARKED (documented iter 175). Candidates: more content (recipes / biome props / another mob behavior), UX legibility, or a fresh visual/audio interleave. **ALWAYS `cd /Users/kz/Code/Crafty/frontend` first.**

> **(historical) iter 175 close-out:** NEW MOB emberhusk (content variety) + de-monolith formally PARKED. A charred night-siege hostile: warm ember-rust color (palette variety vs the roster greens/greys/purples) + a jagged dark head-crest silhouette (mobFeatures) + mid-tier biped stats. Fully DATA-DRIVEN (spawner reads `Object.keys(MOB_TYPES)` + weights + passive-filter; generic AI) → the registry entry alone makes it spawn; no spawn/AI wiring. Added to the mob-bestiary fixture (6 subjects, re-framed) + FEATURED_TYPES. Eyeballed (reads distinct; moved crest back→head after the front-on eyeball). **unit 1098 · build clean · lint clean · visual 19/19 (mob-bestiary re-baselined).** Commit `136bfbd`. **DE-MONOLITH PARKED (documented decision):** the last 2 god-files (Components/Player ~1272, GameScene ~916) — the cheap pure-kernel cuts are EXHAUSTED (spawn/locomotion/dodge/cameraKick out); the remainder is imperative loop ORCHESTRATION + SM-wiring that's risky to extract (the 3 iter-159/161 crashes were extraction-caused), and the size is NOT actively blocking (recent milestones added new modules, never touched the Player loop). Per the charter's "judge as best", the big SM-wiring cut is LOW-ROI/HIGH-RISK for a mature working game → PARKED until it actively blocks a milestone OR a dedicated Workflow design-gate is warranted; stop re-litigating it each iter. **RESUME = PICK per §2** — SOTA experience interleaves (content variety [more mobs/recipes/biome props]; audio/feel; visual polish) remain the highest-EV play-value axis; rotate axes for variety. **ALWAYS `cd /Users/kz/Code/Crafty/frontend` first.**

> **(historical) iter 174 close-out:** LOW-HEALTH HEARTBEAT AUDIO — danger feedback SEEN + HEARD; i18n-blocked finding recorded. Completed the iter-173 danger cue: a soft low lub-dub heartbeat that repeats while HP is critical (<35%) + quickens toward 0 HP. `audio/synthVoices.makeHeartbeat` (VOICES 33→34) + pure `game/lowHealth.heartbeatPeriod(intensity)` (3 tests) + `ui/HeartbeatAudio.jsx` (HUD watcher, quantized-bucket interval, silent at safe HP) + `playHeartbeat` verb. **unit 1095→1098 · build clean · lint clean · visual 19/19 (audio-null capture).** Commit `a47c0ce`. EAR-CHECK owed to Kevin (timbre/volume/cadence — can't hear in headless). **FINDING (i18n #73 BLOCKED — record):** game-content i18n is entangled — item NAMES are used as IDENTITY/keys (`NAME_TO_ID`, `EQUIPMENT_STATS[name]`, `name.includes('Potion')`, food-name `.includes()`); routing names→t() would break logic. i18n content needs an **id/display-name DECOUPLE refactor FIRST** (invasive: inventory stores by name, many sites key off name) — a big risky structural prereq, low immediate value (zh is the secondary toggle; en default unaffected). DEFER i18n until that decouple is scoped. **RESUME = PICK per §2** — content variety (new mob TYPE leveraging the mobFeatures system; or recipes/biome props), another game-feel/audio interleave, OR the deferred Player SM-wiring de-monolith Workflow design-gate. **ALWAYS `cd /Users/kz/Code/Crafty/frontend` first.**

> **(historical) iter 173 close-out:** LOW-HEALTH DANGER VIGNETTE — survival-legibility game-feel (the visual half). Verify-before-assert confirmed the gap (no sustained critical-HP cue — only per-hit flash/direction; the Vignette postprocess is static; i18n content is unrouted [a bigger owed milestone]). Added a red edge vignette that fades in + PULSES as HP goes critical (<35% max), intensifying/quickening toward 0 HP. `game/lowHealth.js` pure `lowHealthIntensity(health,maxHealth)` (6 tests) + `ui/LowHealthVignette.jsx` (HUD-mounted, zIndex below DamageDirection; null at safe HP → capture byte-identical, no fixture, like camera-kick/damage-direction; framer-motion pulse in-play, fixed-opacity capture path). Eyeballed via temp probe (12% HP → clear red danger frame, center playable). **unit 1089→1095 · build clean · lint clean · visual 19/19 (null at full HP).** Commit `a371e6e`. **RESUME = PICK per §2** — more game-feel/content (e.g., the owed i18n #73 zh-CN content pass is a real milestone; or a low-HP heartbeat AUDIO follow-up; or content variety) OR the deferred Player SM-wiring de-monolith Workflow design-gate. **ALWAYS `cd /Users/kz/Code/Crafty/frontend` first.**

> **(historical) iter 172 close-out:** BOSS SILHOUETTE features — Shadow Dragon reads as a dragon (horns/tail/ridge). Fast-follow to the mob-distinctness milestone: added bold-flat `DRAGON_FEATURES` (swept-back horns + tapering tail + back-ridge spikes) to BossEntity (torso material, flash/phase together, boss outline, static→capture-frozen). Also de-staled spawnBossCloseup (clears ECS mobs via `ecs.remove` — the old "zombie can't be cleared from a hook" note was wrong; the boss is a bossSystem entity not an ECS mob). Horns read on the front-on boss-closeup card; tail/ridge eyeballed via a 3/4 reveal during build (kept the fixture front-on+level since a 3/4 down-tilt catches distant terrain) + render in-world. **unit 1089 · build clean · lint clean · visual 19/19 (boss-closeup re-baselined: horns).** Commit `170ae61`. **RESUME = SWITCH AXIS for variety (4 visual iters run: aura/night-sky/mobs/boss).** Candidates: a GAME-FEEL or CONTENT or AUDIO interleave (verify-before-assert each — audio/game-feel are mature, so look for a genuine gap), or the deferred Player SM-wiring de-monolith Workflow design-gate, or i18n #73 (the owed zh-CN content pass). Remaining boss/mob fast-follows (pig/zombie features; boss 3/4 showcase fixture) are LOW priority. **ALWAYS `cd /Users/kz/Code/Crafty/frontend` first.**

> **(historical) iter 171 close-out:** MOB-DISTINCTNESS milestone COMPLETE (T1 specs + T2 fixture + T3 render). T3 wired MobModel to render `mobFeatures(type,dims)` boxes (charOutline-gated, toon material + outline + tone shade). All 5 featured mobs now read by SILHOUETTE: skeleton ribs, duskhound ears+tail, moss_brute shoulder-slabs+moss-crown, skitterling antennae, cow horns. One taste-tune pass (skitterling antennae taller, cow horns bigger) after the in-world eyeball. Unfeatured types unchanged → character-closeup byte-identical; only mob-bestiary re-baselined (deliberate). **unit 1089 · build clean · lint clean · visual 19/19.** Commit `51ddcfe`; spec bannered ✅ SHIPPED. The generic-voxel mob look (master-plan-forbidden) is fixed. **RESUME = PICK per §2** — another SOTA interleave (content/UX/visual — audio+game-feel mature; the day-sky clouds carry coherence risk; OTHER mobs/bosses could get the same treatment as a fast-follow) OR the Player SM-wiring de-monolith design-gate (Workflow). T4 (mob color-accents / zombie head-tilt) is optional, deferred (silhouette read landed without it). **ALWAYS `cd /Users/kz/Code/Crafty/frontend` first.**

> **(historical) iter 170 close-out:** MOB-DISTINCTNESS T2 — mob-bestiary capture fixture (gate 18→19). Added a `mobBestiary` DEV hook (App.jsx, mirrors mobShowcase) spawning the 5 FEATURED types in a studio row at x=280 (skitterling/duskhound/skeleton/cow/moss_brute, front-on, self-clearing, off-frame from all other fixture cameras) + a capture.mjs state + diff.test STATES entry. Baselined the FEATURELESS row (the clean before-reference; confirmed it shows the generic-voxel gap — duskhound/skeleton/cow/moss_brute read as colored boxes). Deterministic (byte-stable across 2 runs). **lint clean · build clean · unit 1089 · visual 19/19.** Commit `a28a499`. **RESUME = T3 (the payoff):** wire MobModel to render `mobFeatures(type, dims)` boxes (gated by `q.charOutline` tier, same toon material + outline + `tone` shade hint) → re-capture → HD-eyeball the mob-bestiary frame (each species' silhouette unmistakable?) → per-type TUNE → re-baseline `mob-bestiary` + `character-closeup` (the zombie head-tilt tweak) → KEVIN-REVIEW before/after. T3 is render-bearing (re-baseline + taste-tune; watch the STUCK rule on per-type taste). Then T4 (optional color accents). **dims source:** MobModel's `bodyW/bodyH/bodyD/headW/headH/headD` (resolved from MOB_TYPES bodySize/headSize). **ALWAYS `cd /Users/kz/Code/Crafty/frontend` first.**

> **(historical) iter 169 close-out:** MOB-DISTINCTNESS design-gate + T1 pure feature specs (game/mobFeatures.js). Verify-before-assert found audio/music + UX are already mature (synthPad+LFO+threat-scaled arpeggiator; UX-M1 honest-controls shipped) → the real high-value gap is the **generic-voxel mob look** (9 types differ only by box-dimension/color/leg-mode — the master-plan-forbidden look, on the constant-combat subjects). Design-gated the §6 "mob bestiary distinctness" milestone: per-type SILHOUETTE features in the SAME bold-flat box vocabulary (brute shoulder-slabs+moss-crown, hound ears+tail, skitterling antennae, skeleton ribs, cow horns) — read-by-outline not color. Spec: `docs/superpowers/specs/2026-06-14-crafty-mob-distinctness-design.md` (goal-frame + reference-lock + per-type map + capture/re-baseline plan + T1..T4). **T1 SHIPPED (capture-safe):** `game/mobFeatures.js` pure `mobFeatures(type,dims)` → body-local accessory box specs (scale per body dims) + 6 invariant tests; NOT wired to MobModel yet → zero render change. **unit 1083→1089 · build clean · lint clean · visual 18/18 (T1 non-rendering).** Commit `a3e4ad1`. **RESUME = T2** (the `mob-bestiary` capture fixture — a `mobBestiary` test hook spawning one of each distinct type in a studio row + a capture.mjs state ordered BEFORE boss/spell/beast per the iter-164 leak lesson; baseline it) **then T3** (wire MobModel to render `mobFeatures()` boxes gated by `q.charOutline` + re-baseline `mob-bestiary`+`character-closeup` + in-world eyeball + per-type tune). **ALWAYS `cd /Users/kz/Code/Crafty/frontend` first.**

> **(historical) iter 168 close-out:** PLAYER DE-MONOLITH — WASD moveVector → game/locomotion.js; judged the Player pure-kernel grind diminishing-returns. The camera-relative WASD movement (planar basis + combine-by-intent + normalize*speed) was inline in the Player loop + untested → `game/locomotion.moveVector` (pure, byte-equivalent: 0.001 fallback, the `>0` move-gate, diagonal normalized to speed). 7 characterization tests. Components.jsx 1288→1272. Capture-bypassed → 18 baselines byte-identical. **unit 1076→1083 · build clean · lint clean · visual 18/18 (re-captured).** Commit `890d0a3`. **DECISION (judged): the Player pure-kernel grind is reaching diminishing returns** — dodge (167) + WASD (168) shaved Components 1299→1272 (−27); the remaining bulk is the imperative loop ORCHESTRATION (attack/cast/beast-SM/voidhand/soulbind wiring), not pure math → small entangled kernels with low ROI. The meaningful remaining cut is the big Player-loop SM-wiring split, which needs a dedicated **Workflow-assisted design-gate** (a focused iteration), NOT more marginal kernel-picking. **RESUME = either (a) a SOTA experience interleave (content variety / UX legibility — fresh axes; or the day-sky cloud pass with reference-lock first), OR (b) the Player SM-wiring design-gate (Workflow).** Lean toward (a) for player value; do (b) when ready to dedicate an iteration. **Self-notes:** use `->` not `→` in .js comments (zero-emoji gate); no backticks in `git -m "..."` (shell expands them). **ALWAYS `cd /Users/kz/Code/Crafty/frontend` first.**

> **(historical) iter 167 close-out:** PLAYER DE-MONOLITH — dodge math extracted to game/dodge.js. The dodge mechanic's pure math was inline in the ~1210-line Player loop + UNTESTED → extracted to `game/dodge.js` (no THREE/store/refs): `planarBasis` (camera XZ → forward+side unit vectors + the (0,0,-1) fallback), `dodgeDirection` (combine by move intents, forward when no input), `dodgeSpeed` (lerp(28,10,progress) i-frame burst), `isDodgeInvincible` (active && within i-frame window). Byte-equivalent (exact 0.001 thresholds). 17 characterization tests. Components.jsx 1299→1288. Dodge path is capture-bypassed → 18 baselines byte-identical. **unit 1059→1076 · build clean · lint clean · visual 18/18 (re-captured, no crash).** Commit `54cfd1e`. **RESUME = continue the Player de-monolith** (more pure kernels: the WASD planar-move combine duplicates `planarBasis` — a DRY+extract candidate; the camera-follow lerp/shake/bob/fov math; the ledge-vault/parkour math) — characterization-first, low-risk; OR another SOTA interleave (content/UX). The big risky cut (Player-loop SM-wiring as components) still needs a dedicated Workflow-assisted design-gate. **ALWAYS `cd /Users/kz/Code/Crafty/frontend` first.**

> **(historical) iter 166 close-out:** DOC-CURRENCY SWEEP — docs verified clean (75 plans + specs), §3 master-plan banner refreshed. Assessed all 75 plan docs (ALL properly bannered ✅ SHIPPED/PARKED/SUPERSEDED — prior diligence held; my first grep falsely flagged them by matching "Plan" in titles) + all specs (clean — coherence-pillars already has current ✅ ACTIVE markers above its historical "at authoring" note). Only real drift = the SOTA-INITIATIVE.md §3 LIVING status banner (stale ~13 iters) → refreshed to cover the iters 158-165 arc (the 3-crash broken-main arc + the silent-crash-hole closure + the ESLint crash-class gate @162 + loot aura @163 + cube-leak fix @164 + twilight night sky @165) + the test count (1049→1059) + date. Docs-only (no code → gates untouched). **RESUME = the DE-MONOLITH design-gate is the explicit next build unit (§2 #4, deferred ~14 iters):** Components.jsx is one ~1210-line `Player` component (the risky Player-loop SM-wiring); look for the next SAFE pure-kernel extraction (the spawnPlacement/locomotion precedent @125-126 — characterization-first, TDD) OR author a spec for the risky cut with an adversarial Workflow. The eslint gate now de-risks the orphan-symbol class. ALT: another SOTA interleave (content/UX, or a day-sky cloud pass — reference-lock first). **ALWAYS `cd /Users/kz/Code/Crafty/frontend` first.** Build clean, unit 1059, visual 18/18, lint clean.

> **(historical) iter 165 close-out:** TWILIGHT NIGHT SKY SHIPPED — moon disc + stars on the dusk mood. The Atmosphere skydome was gradient + sun-glow only; the everyday-night (dusk) sky now has a clean cool MOON disc (bold-flat defined disc + soft halo, upper-right in-FOV opposite the sun) + a sparse twilight STARFIELD. Gated by a pure `starIntensity(mood)` (`render/nightSky.js`, TDD red-first) — a triangle peaking at dusk (mood 1), 0 at day (0) and obsidian (2) → NEVER touches the day or boss sky (those stay <6%). Deterministic (hash from vDir, mood snapped in capture). Reference-locked to the existing additive sun-disc technique. Iterated the moon to a DEFINED disc (a pow-glow washed out in the bright sunset) + repositioned in-FOV (first placement was behind the −Z camera) — STUCK-rule technique-switch. **unit 1053→1059 (6 tests) · build clean · lint clean · visual 18/18 (explore-night + explore-night-low re-baselined, HD-eyeballed: clear moon + stars over the dusk landscape).** Commit `8bbb40a`. **RESUME = PICK per §2** — another SOTA interleave (a DAY-sky cloud pass is the natural next visual, but it's higher-risk/multi-baseline → reference-lock/spec FIRST; or content variety / UX legibility) or design-gate a risky de-monolith (Components 1297 / GameScene 914). Finding logged: the game's "night" is a bright SUNSET/dusk (not a dark night), which limits star/moon contrast — a darker-night mood is a future design decision. **ALWAYS `cd /Users/kz/Code/Crafty/frontend` first.**

> **(historical) iter 164 close-out:** LOOT-SHOWCASE CUBE-LEAK FIXED + clean aura re-baseline; gate fully trustworthy. Root-caused the deterministic grey-cube at the loot-showcase bottom-edge: the force-spawned boss-closeup dragon (box torso + inverted-hull outline → grey under studio light) PERSISTED into loot-showcase because a studio-card hook can't despawn the boss (its `bossActive` is component-local `useState` in bossSystem, only mirrored to the store). An isolation probe proved loot-showcase is clean with no boss/spell before it. **Fix: reordered capture.mjs so loot-showcase runs right after boss-obsidian — BEFORE the entity-spawning closeup/boss/spell/beast fixtures** (the 4 drops at x=80 sit ≥38 units off-axis from every later camera + 90 above the beast frames → no forward leak; full diff confirms all 17 others byte-identical). This unblocked a CLEAN loot-showcase re-baseline (iter-163 aura, no cube): `978a5a2`. **unit 1053 · build clean · lint clean · visual 18/18 (loot-showcase ~0%, re-baselined + HD-eyeballed).** The loot aura is now LOCKED in the baseline. **RESUME = PICK per §2** — a SOTA experience interleave (visual/content/UX — audio + game-feel are mature) or design-gate a risky de-monolith (Components 1297 / GameScene 914). Lower-priority residuals: the non-fatal React "unique key" warning at loot-showcase; the broader capture-harness state-accumulation pattern (studio-card fixtures don't despawn prior entities — handled for loot by ordering; a general reset-between-cards pass is a future quality unit). **ALWAYS `cd /Users/kz/Code/Crafty/frontend` first.**

> **(historical) iter 163 close-out:** LOOT RARITY AURA SHIPPED — first SOTA experience interleave after the crash-fix/infra streak. Each loot drop now carries a soft bloom-softened glow-shell around its gem, tier-scaled (radius+opacity climb common→legendary, opacity capped 0.38 = glow not ball), `toneMapped:false` so the global Bloom halos it (the spellVfx outer-glow technique — coherent, texture-free). The beam stays the across-map rarity tell; the aura is the up-close any-angle "it glows" tell. Verify-before-assert killed two false candidates (hitstop + biomeAmbience both already exist). Redesigned from the parked ground-disc (which a near-horizontal fixture view shows edge-on) → a sphere shell (any-angle in-game + judged front-on). **unit 1050→1053 · build clean · lint clean · visual 18/18 (aura <6% → ships in CODE, no re-baseline).** Commit `9e18242`. **OPEN follow-up (deferred, non-blocking): a DETERMINISTIC grey outlined cube leaks at the loot-showcase studio bottom-edge** (NOT aura-related — separate location; appeared between the 04:37 baseline and now, likely a beast-loop state-leak rendering post the iter-160 playerRender fix; dev-fixture-only, game unaffected). It blocks a CLEAN loot-showcase re-baseline (don't bless an unexplained element) — investigate + suppress in the lootShowcase hook, THEN re-baseline a clean aura card. **RESUME = PICK per §2** (the leak-fix is a small quality unit; or another SOTA interleave [visual/content/UX]; or design-gate a risky de-monolith [Components 1297 / GameScene 914]). **ALWAYS `cd /Users/kz/Code/Crafty/frontend` first.**

> **(historical) iter 162 close-out:** BUG-CLASS PERMANENTLY GUARDED — ESLint crash-class gate shipped; all systems green. The 3× broken-main bug-class (used-but-undefined symbol orphaned by a byte-exact extraction) now has a STATIC tripwire: `eslint.config.js` (targeted flat config, only `no-undef` + `react/jsx-no-undef`, permissive globals) + `tests/gates/eslint-crash-class.test.js` (runs the ESLint API across src/ inside `npx vitest run`) + `npm run lint`. Validated against a probe (catches both plain-JS refs AND undefined JSX components — would have caught all 3 crashes). The runtime guard (loud capture, iter 161) + this static guard (commit-time) together fully close the class. **RESUME = PICK the next unit per charter §2** (broken-main backlog cleared + now guarded; cheap de-monolith wins exhausted [Components 1297 + GameScene 914 remain, both risky] → a SOTA interleave [visual polish / content variety / touch M3] or design-gate a risky de-monolith). The parked loot-glow (`plans/2026-06-13-crafty-loot-glow-PARKED.md`) can re-baseline against the trustworthy current/. Minor quality-debt: the non-fatal React "unique key" console warning at the loot-showcase capture stage (nested re-render artifact; non-gating). **ALWAYS `cd /Users/kz/Code/Crafty/frontend` first — cwd drifts.** Build clean, unit 1050, visual 18/18, lint clean.

> **(historical) iter 161 close-out:** ALL THREE latent crashes fixed — visual gate 18/18 GREEN; game fully playable. (1) lookSensitivity wrong-scope (`c5c51ef`, @159), (2) MagicWand un-imported (`852d592`, @160), (3) `_trailDir` orphaned scratch-vectors in spell-projectile render (`f7b1399`, @161) — all three ReferenceError-class, all the SAME bug-class (a symbol orphaned by a byte-exact extraction), all latent on main + hidden by the diff-on-stale-current verification hole. **#3 was broken-main: casting ANY spell crashed the game** (EnhancedSpellProjectile rendered live per projectile; `_trailDir/_trailMid/_trailQuat/_trailUp` were left in EMS by the iter-152 spellVfx extraction → moved into spellVfx.jsx). The earlier "spell-cast/loot-showcase show a purple blocky construct" was a misdiagnosis: it was the FROZEN last good frame (the boss-closeup dragon — `#c084fc` purple eyes + obsidian-emissive body) after `_trailDir` threw every frame and halted the R3F loop; all 3D fixtures from spell-cast onward inherited it (md5-identical). **The silent-crash hole is now PERMANENTLY closed:** capture.mjs listens for pageerror/console.error tagged by stage + FAILS the run (non-zero exit) on any non-boot render crash (`8c8cccc`). **RESUME — RECOMMENDED NEXT UNIT: an ESLint `no-undef` static gate (kills the recurring bug-class at commit-time).** All THREE crashes (lookSensitivity / MagicWand / _trailDir) were used-but-undefined identifiers that the rollup build can't catch but ESLint `no-undef` WOULD catch statically — a single gate that ends the entire class forever (the runtime loud-capture only covers fixtured render paths; an un-fixtured live path could still harbor a 4th). I swept the `_`-prefixed sub-class clean (only false positives: `target="_blank"`, an `_icons.json` comment, `_pos` unused-param), but CamelCase orphans like MagicWand are NOT covered by a grep — wire eslint (check if a config exists first) + a lint step in the gate. **Else PICK per §2** (broken-main backlog cleared; cheap de-monolith wins exhausted → a SOTA interleave [visual polish / content / touch M3] or design-gate a risky de-monolith). The parked loot-glow (`plans/2026-06-13-crafty-loot-glow-PARKED.md`) can now re-baseline against a trustworthy current/. **Minor follow-up (non-fatal, quality-debt): a React "unique key" console warning fires at the loot-showcase stage — not in pickupVfx/SimplifiedNPCSystem top-level maps (all keyed); a nested re-render artifact; non-gating.** **ALWAYS `cd /Users/kz/Code/Crafty/frontend` first — cwd drifts.** Build clean, unit 1049, visual 18/18.

> **🔁 LOOP ITER 177 (2026-06-14, ultracode): 🧭 FIRST-SESSION ONBOARDING NUDGE (UX legibility).** Verify-before-assert ruled out several candidates (weather rain/snow/fireflies ALREADY exist in GameScene; the title screen is premium; §3 current). The real gap: the title teaches KEYS but a new player gets no in-world GOAL orientation (broad-audience P5). Added a one-time first-spawn toast (the day-build/night-survive loop + Aspect progression) via the existing addNotification pipe, localStorage-flagged + capture-suppressed. Probe-confirmed firing (non-capture DOM check + flag set, after a wait-for-spawn to dodge a build-timing flake; simplified 2 tips → 1 clean line). **unit 1098 · build clean · lint clean · visual 19/19.** Commit `9893a79`. NEXT = rotate axes (content-data depth / more mobs / bigger content); de-monolith+i18n parked. The game is mature → consider a holistic step-back when gaps get hard to find.
>
> **🔁 LOOP ITER 176 (2026-06-14, ultracode): 🏅 PREMIUM LEVEL-UP FLOURISH + XP CHIP (reward-feel + coherence).** Found a taste/coherence gap: the XP-bar was bold-flat but SimpleLevelUpEffect + SimpleXPGainVisual were still AI-generic (raw bg-yellow-400 + drop-shadow text). Upgraded both to S1-C bold-flat (gold display-face LEVEL UP sticker banner + ink frame + a soft gold radial burst; gold +XP chip) — a premium REWARD moment balancing the recent danger-side feel (vignette/heartbeat). Verified the --ui-* tokens. Transient (capture-null) → no fixture; eyeballed via a temp grant-XP probe (premium gold banner). **unit 1098 · build clean · lint clean · visual 19/19.** Commit `9642e68`. NEXT = rotate experience axes (content/UX/visual/audio); de-monolith stays PARKED.
>
> **🔁 LOOP ITER 175 (2026-06-14, ultracode): 👹 NEW MOB emberhusk (content variety) + DE-MONOLITH formally PARKED.** Added a charred night-siege hostile: ember-rust color (palette variety) + a jagged dark head-crest (mobFeatures) + mid biped stats; fully data-driven (spawner = Object.keys(MOB_TYPES) + weights + passive-filter; generic AI → registry entry alone spawns it). Added to mob-bestiary (6 subjects, re-framed) + FEATURED_TYPES. Eyeballed → moved the crest back→head (front-on read). **unit 1098 · build clean · lint clean · visual 19/19.** Commit `136bfbd`. PARKED the Player/GameScene de-monolith (documented rationale: cheap kernels exhausted; remaining = risky imperative-orchestration cuts; not actively blocking; 3 recent crashes were extraction-caused → low-ROI/high-risk for a mature game; revisit only if it blocks a milestone or warrants a Workflow design-gate). NEXT = experience interleaves (content/audio/feel/visual), rotating axes.
>
> **🔁 LOOP ITER 174 (2026-06-14, ultracode): 🫀 LOW-HEALTH HEARTBEAT AUDIO — completes the danger feedback (seen+heard).** Verify-before-assert killed i18n as this iter's pick: game-content i18n is BLOCKED by name-as-identity entanglement (item names are keys: NAME_TO_ID, EQUIPMENT_STATS[name], name.includes('Potion'), food .includes() — needs an id/display decouple refactor FIRST; deferred + documented). Pivoted to the audio half of the iter-173 vignette: `makeHeartbeat` lub-dub voice (VOICES 33→34) + pure `heartbeatPeriod(intensity)` (1.3s→0.55s, 3 tests) + `ui/HeartbeatAudio` HUD watcher (quantized-bucket interval, Game-Loop-safe, silent at safe HP) + `playHeartbeat` verb. Capture-safe (audio null + full-HP in capture). **unit 1095→1098 · build clean · lint clean · visual 19/19.** Commit `a47c0ce`. Ear-check owed (can't hear in headless). NEXT = content variety (new mob type / recipes / biome props) or another interleave or the Player de-monolith Workflow design-gate.
>
> **🔁 LOOP ITER 173 (2026-06-14, ultracode): ❤️‍🩹 LOW-HEALTH DANGER VIGNETTE — survival-legibility game-feel (axis switch after 4 visual iters).** Verify-before-assert across candidate gaps: no sustained critical-HP cue (only per-hit flash + direction; the postprocess Vignette is static; i18n game-content unrouted = a bigger owed milestone). Built a red edge vignette that fades in + PULSES as HP goes critical (<35% of max), intensifying + quickening toward 0 HP. `game/lowHealth.js` pure `lowHealthIntensity` (6 tests: boundary/ramp/monotonic/guard-safe) + `ui/LowHealthVignette.jsx` (HUD-mounted; null at safe HP → all capture frames byte-identical, no fixture needed [camera-kick/damage-direction precedent]; framer-motion pulse in play + fixed-opacity capture path). Eyeballed via a throwaway low-HP probe (12% HP → clear red danger frame, center stays playable). **unit 1089→1095 · build clean · lint clean · visual 19/19.** Commit `a371e6e`. NEXT = PICK per §2 (content/game-feel — i18n #73 is the big owed one; or a low-HP heartbeat audio follow-up; or the Player SM-wiring de-monolith Workflow design-gate).
>
> **🔁 LOOP ITER 172 (2026-06-14, ultracode): 🐉 BOSS SILHOUETTE features — the Shadow Dragon reads as a dragon (mob-distinctness fast-follow).** Added `DRAGON_FEATURES` to BossEntity (swept-back horns + tapering tail + back-ridge spikes, bold-flat static boxes, torso material so they flash/phase with the body + boss outline). Front-on boss-closeup shows the horns (the gate cue); tail/ridge validated via a 3/4 reveal during build (the fixture stays front-on+level — a 3/4 down-tilt caught distant terrain, cluttering the sky card; STUCK-rule: reverted after 3 framing tries). De-staled spawnBossCloseup (clears ECS mobs via `ecs.remove`; old "can't clear from hook" note was wrong). **unit 1089 · build clean · lint clean · visual 19/19 (boss-closeup re-baselined, horns; diff = upper-center horn region only).** Commit `170ae61`. NEXT = SWITCH AXIS (4 visual iters run) → game-feel/content/audio interleave (verify gaps first) or Player SM-wiring de-monolith Workflow design-gate or i18n #73.
>
> **🔁 LOOP ITER 171 (2026-06-14, ultracode): 🎨 MOB-DISTINCTNESS T3 — silhouette features RENDERED → milestone COMPLETE.** Wired MobModel to render the `mobFeatures()` boxes (charOutline-gated; toon material + inverted-hull outline; `tone` shades them bone/dark). All 5 featured mobs now read by SILHOUETTE not color: skeleton ribs, duskhound ears+tail, moss_brute shoulder-slabs+moss-crown, skitterling antennae, cow horns. In-world eyeball (HD crops + amplified diff) → one taste-tune pass (skitterling antennae taller, cow horns bigger/angled — the 2 weakest reads). Unfeatured types return [] → zero render change → character-closeup byte-identical; only mob-bestiary re-baselined (features were <6% but locked deliberately per §4). Capture-deterministic (static feature geometry). **unit 1089 · build clean · lint clean · visual 19/19.** Commit `51ddcfe`; spec ✅ SHIPPED. The master-plan-forbidden generic-voxel mob look is fixed. NEXT = PICK per §2 (interleave: same treatment for OTHER mobs/bosses as a fast-follow / content / UX; or the Player SM-wiring de-monolith Workflow design-gate).
>
> **🔁 LOOP ITER 170 (2026-06-14, ultracode): 🎨 MOB-DISTINCTNESS T2 — the mob-bestiary capture fixture (eyeball surface).** Added a `mobBestiary` DEV hook (App.jsx, mirroring mobShowcase) — the 5 FEATURED types in a studio row at x=280 (skitterling/duskhound/skeleton/cow/moss_brute), front-on, self-clearing, off-frame from all other cameras → order-independent + leak-safe. Wired a capture.mjs state (early cluster) + diff.test STATES (gate 18→19). Baselined the FEATURELESS row — the clean before-reference (confirmed the generic-voxel gap: duskhound/skeleton/cow/moss_brute = colored boxes). Deterministic (byte-stable across 2 capture runs). **lint clean · build clean · unit 1089 (no new unit; T2 is a fixture) · visual 19/19 (new fixture + 18 originals byte-identical).** Commit `a28a499`. NEXT = T3: wire MobModel → render `mobFeatures()` boxes (charOutline-gated, toon material + outline + tone) → re-capture → HD-eyeball + per-type tune → re-baseline mob-bestiary + character-closeup → KEVIN-REVIEW before/after.
>
> **🔁 LOOP ITER 169 (2026-06-14, ultracode): 🎨 MOB-DISTINCTNESS milestone — design-gate + T1 (the highest-value remaining visual).** Verify-before-assert ruled out the easy interleaves (audio/music already rich: synthPad+LFO+threat-arpeggiator; UX-M1 done; day-sky clouds = coherence+taste+multi-baseline risk) → the real gap is the generic-voxel MOB look (9 types = box-body+head+legs, distinct only by dim/color/leg-mode — forbidden by master-plan §1, on the constant-combat subjects). Design-gated per-type SILHOUETTE features (bold-flat box vocabulary; brute shoulders+crown, hound ears+tail, skitterling antennae, skeleton ribs, cow horns) → committed spec `specs/2026-06-14-crafty-mob-distinctness-design.md` (goal-frame L1-L4 + reference-lock + per-type map + capture/re-baseline plan + T1..T4). T1 SHIPPED capture-safe: `game/mobFeatures.js` pure `mobFeatures(type,dims)`→body-local accessory box specs (dim-scaled) + `FEATURED_TYPES` + 6 invariant tests; UNWIRED (no render change). **unit 1083→1089 · build clean · lint clean · visual 18/18.** Commit `a3e4ad1`. NEXT = T2 (`mob-bestiary` capture fixture, ordered before boss/spell/beast) → T3 (wire MobModel + re-baseline + in-world eyeball + per-type tune).
>
> **🔁 LOOP ITER 168 (2026-06-14, ultracode): 🧱 PLAYER DE-MONOLITH — WASD moveVector → game/locomotion.js (§2 #4).** Continued the Player pure-kernel extraction. The camera-relative WASD move math (basis dup of dodge.planarBasis + combine + normalize*speed) → `game/locomotion.moveVector` (pure, byte-equivalent: 0.001 fallback, `>0` move-gate, diagonal normalized to speed; kept self-contained to avoid a backwards locomotion→dodge import — 6 trivial basis lines noted to unify). 7 characterization tests (cardinals/diagonal/opposing-cancel/speed-scale/degenerate). Components 1288→1272. Capture-bypassed → 18 baselines byte-identical. **unit 1076→1083 · build clean · lint clean · visual 18/18.** Commit `890d0a3` (zero-emoji gate caught a U+2192 arrow in my comment — fixed; self-note logged). **JUDGED: Player pure-kernel grind = diminishing returns** (dodge+WASD = −27 LOC; the rest is imperative loop ORCHESTRATION, not pure math). NEXT = lean to a SOTA interleave (content/UX/day-sky-clouds) for player value; the big SM-wiring cut needs a dedicated Workflow design-gate.
>
> **🔁 LOOP ITER 167 (2026-06-14, ultracode): 🧱 PLAYER DE-MONOLITH — dodge math → game/dodge.js (§2 #4, the deferred structural spine).** Resumed the de-monolith. Components.jsx is one ~1210-line `Player` component (no clean leaf cuts left — only pure-kernel extractions from the loop, the spawnPlacement/locomotion precedent). Extracted the dodge mechanic's pure math (was inline + untested): `planarBasis`/`dodgeDirection`/`dodgeSpeed`/`isDodgeInvincible` → `game/dodge.js` (no THREE/store/refs; byte-equivalent, exact 0.001 degeneracy thresholds). 4 surgical Player-loop edits (import + dir + speed + iframe wirings). 17 characterization tests (cardinals/diagonals/no-input→forward/opposing-cancel/speed-clamp/iframe-boundary). Components 1299→1288. Capture-bypassed (camera early-return precedes the dodge path) → 18 baselines byte-identical. **unit 1059→1076 · build clean · lint clean (the crash-class gate validated the risky-file edit) · visual 18/18 (re-captured).** Commit `54cfd1e`. NEXT = more Player pure-kernels (WASD planar-move dups planarBasis → DRY; camera-follow lerp/shake/fov; ledge-vault) or an interleave; the big SM-wiring cut still needs a Workflow design-gate.
>
> **🔁 LOOP ITER 166 (2026-06-14, ultracode): 🧾 DOC-CURRENCY SWEEP (charter §6, 15 iters overdue) — docs verified clean; §3 master-plan banner refreshed.** Full assessment: all 75 `docs/superpowers/plans/*.md` are properly bannered (✅ SHIPPED/PARKED/SUPERSEDED — prior per-iteration diligence held; an initial grep falsely flagged them by matching the word "Plan" in titles, corrected by checking for the real markers) + all specs clean (coherence-pillars already carries current ✅ ACTIVE markers above its historical "at authoring" note). Only genuine drift = SOTA-INITIATIVE.md §3 (the LIVING status banner, stale ~13 iters) → refreshed: added the iters 158-165 arc (3-crash broken-main fixes [lookSensitivity/MagicWand/_trailDir, all extraction-orphan class] + the silent-crash-hole closure + the ESLint crash-class gate @162 + loot aura @163 + cube-leak fix @164 + twilight night sky @165), bumped test count 1049→1059, date → 2026-06-14, interleave ledger updated. Docs-only (no code → no gate impact; build/unit/visual unchanged at 1059/18). Commit pending. NEXT = the DE-MONOLITH design-gate (§2 #4) — a safe pure-kernel extraction from the Player loop (characterization-first) or a Workflow-assisted spec for the risky cut; ALT another interleave.
>
> **🔁 LOOP ITER 165 (2026-06-14, ultracode): 🌙 TWILIGHT NIGHT SKY SHIPPED — moon disc + stars on the dusk mood (SOTA visual interleave).** Verify-before-assert found the gap: the Atmosphere skydome shader was gradient + sun-glow only (no stars/moon). Added a clean cool MOON disc + sparse twilight STARFIELD to the dusk sky, gated by a pure `starIntensity(mood)` (TDD red-first, `render/nightSky.js`) — a triangle peaking at dusk (mood 1), 0 at day(0)/obsidian(2) so the day + boss skies stay byte-within-gate. Deterministic hash-from-vDir + mood snapped in capture → capture-stable. STUCK-rule discipline on the moon: a pow-glow washed into the bright sunset (attempt 1 was also behind the −Z camera → off-frame) → SWITCHED to a DEFINED smoothstep disc repositioned upper-right in-FOV opposite the sun → reads as a clear moon. **unit 1053→1059 · build clean · lint clean · visual 18/18.** explore-night + explore-night-low re-baselined (deliberate, HD-eyeballed — clear moon + stars over the sunset landscape; all 17 other gated frames stay <6%: uStar=0 at day/obsidian + sub-6% shader-recompile FP drift elsewhere). Commit `8bbb40a`. Finding: the game's "night" is a bright SUNSET (not dark) → limits celestial contrast; a darker-night mood is a future design call. NEXT = another interleave (a day-sky cloud pass — reference-lock first; or content/UX) or a de-monolith design-gate.
>
> **🔁 LOOP ITER 164 (2026-06-14, ultracode): 🧹 LOOT-SHOWCASE CUBE-LEAK FIXED + clean aura re-baseline — the visual gate is fully trustworthy again.** Picked the deferred iter-163 follow-up. Root-caused via an isolation probe (throwaway `probe-loot.mjs`, deleted): the deterministic grey-cube at the loot-showcase bottom-edge was the force-spawned boss-closeup DRAGON (box torso + inverted-hull outline → reads grey under bright studio light) PERSISTING into loot-showcase — a studio-card hook can't despawn the boss (`bossActive` is component-local `useState` in bossSystem, store-mirrored only). Probe bisection: loot is clean in isolation + with the beast loop; the cube needs a prior boss. Fix WITHOUT despawning (impossible from a hook): **reordered capture.mjs so loot-showcase runs right after boss-obsidian, BEFORE the closeup/boss/spell/beast fixtures.** The 4 drops at x=80 sit ≥38 units off-axis from every later fixture camera (boss x=40, spell x=120, beasts x=0) + 90 units above the in-world beast frames → no forward leak (full diff: all 17 other frames byte-identical). Re-baselined a CLEAN loot-showcase (aura halos, no cube — HD-eyeballed). **unit 1053 · build clean · lint clean · visual 18/18.** Commit `978a5a2`. STUCK-rule note: 3 probe reproductions failed to show the cube outside the real full sequence (probe boss didn't spawn — timing), so I switched from root-cause-via-probe to the ORDER fix (proven by the clean isolation result). NEXT = a SOTA interleave (visual/content/UX) or a risky de-monolith design-gate.
>
> **🔁 LOOP ITER 163 (2026-06-14, ultracode): ✨ LOOT RARITY AURA SHIPPED — the first SOTA experience interleave after 5 fix/infra iters.** Picked a visual-polish interleave (the least-recently-SHIPPED axis). Verify-before-assert killed TWO candidates before building (hitstop ALREADY exists S1-D-M1; biomeAmbience ALREADY complete+wired) — audio + game-feel are mature. Settled on the parked loot-glow BUT REDESIGNED it: the parked ground-disc is judged edge-on by the near-horizontal loot-showcase camera (thin ellipse, barely changes the baseline); a camera-facing-equivalent sphere glow-SHELL is strictly better (any-angle in-game for floating loot + judged front-on). Reference-locked to the established spellVfx outer-glow-shell + Bloom technique (coherent, texture-free): `lootJuice.rarityBeam` now returns `auraRadius`/`auraOpacity` (tier-scaled, opacity capped 0.38 = glow not ball), `pickupVfx.LootDropRender` nests a soft additive `toneMapped:false` sphere under the gem. TDD red-first (3 aura tests). **unit 1050→1053 · build clean · lint clean · visual 18/18.** HD-eyeballed loot-showcase before/after: 4 drops with tier-scaled halos, legendary biggest/brightest, soft. Aura is <6% so it SHIPS IN CODE (renders for every in-game drop) — NO re-baseline. Commit `9e18242`; parked plan marked SUPERSEDED. **Caught (deferred, non-blocking): a DETERMINISTIC grey outlined cube leaks at the loot-showcase bottom-edge** (run1==run2 byte-identical; NOT aura-related; explore-day/ocean/character-closeup are byte-identical so it's loot-showcase-specific, likely a beast-loop state-leak post the iter-160 playerRender fix; dev-fixture-only). Blocks a clean re-baseline → tracked. NEXT = the leak-fix (small) or another interleave / risky de-monolith.
>
> **🔁 LOOP ITER 162 (2026-06-14, ultracode): 🛡️ ESLint CRASH-CLASS GATE SHIPPED — the 3× broken-main bug-class is now caught STATICALLY at commit-time.** Picked the quality-infra unit I recommended last iter (justified by 3 broken-main crashes in 7 iters of the identical class: a symbol used-but-undefined, orphaned by a byte-exact god-file extraction; rollup/vite can't catch free-var refs to module-locals nor undefined JSX components). Goal-Frame check: L2 risk = a noisy gate (false positives) is useless → solved by the `globals` package's curated browser+worker+node lists (and the insight that orphaned app symbols are never in ANY globals list, so permissive globals don't weaken detection); L3 metric = MUST catch all 3 known patterns → validated against an injected probe. **Verify-before-assert caught a real gap:** plain `no-undef` does NOT flag undefined JSX components (`<MagicWand/>` class) — that needs `eslint-plugin-react`'s `react/jsx-no-undef`. Installed eslint+globals+eslint-plugin-react (devDeps); wrote a TARGETED flat `eslint.config.js` (ONLY those 2 rules as errors, scoped src/, NOT a style linter); wired it into the gate via `tests/gates/eslint-crash-class.test.js` (ESLint API across src/ inside `npx vitest run` — the test ratchet enforces it) + a `npm run lint` script. **Probe proof:** clean src→exit 0 (zero false positives); inject `_orphanedScratchVar` + `<OrphanedComponent/>`→flags BOTH (no-undef + react/jsx-no-undef) — i.e. it would have caught all 3 crashes. **Verify: unit 1049→1050 · build clean · visual 18/18 (diff-alone, justified — pure infra, no src render code touched) · lint clean.** Commit `b8e26b9`. NEXT = PICK per §2 (broken-main backlog cleared + guarded → a SOTA interleave or design-gate a risky de-monolith).
>
> **🔁 LOOP ITER 161 (2026-06-14, ultracode): 🔴→🟢 THIRD latent crash fixed (broken-main) + the silent-crash hole CLOSED FOR GOOD → visual gate 18/18.** Resumed to investigate the 3 red capture states (spell-cast/loot-showcase/title-mascot showing a "purple blocky construct"). Forensics: the 3 current/ frames + boss-closeup were md5-IDENTICAL across the entire beast loop → the WebGL render loop had FROZEN at the boss-closeup→spell-cast boundary (the "purple construct" was just the frozen boss-closeup dragon — `#c084fc` purple eyes + obsidian-emissive body + thin wings; NOT a staging bug, NOT the beast form). Past the STUCK rule on visual forensics (6+ hypotheses), I SWITCHED APPROACH → added pageerror/console.error capture to capture.mjs (the harness had silently swallowed all 3 crashes). It immediately printed the truth: **`ReferenceError: _trailDir is not defined` @spell-cast**, every frame. Same bug-class as iter-160's MagicWand: the iter-152 spellVfx extraction moved `EnhancedSpellProjectile` (rendered LIVE per active projectile at EMS:436) but left its module-local scratch vectors `_trailDir/_trailMid/_trailQuat/_trailUp` behind in EnhancedMagicSystem → undefined in spellVfx → threw → froze the loop. **Broken-main: casting any spell crashed the game, latent since iter 152.** FIX (`f7b1399`): moved the 4 scratch consts into spellVfx.jsx (EMS no longer used them); dep-completeness grep confirmed ZERO other orphaned module-locals. HARDENING (`8c8cccc`): capture.mjs now FAILS LOUD (non-zero exit) on any non-boot render crash + logs React dev warnings as non-fatal — the diff-on-stale-current hole that hid 3 crashes for ~6 iters is permanently closed. **Verify: build clean · unit 1049/1049 · visual gate 18/18 (all frozen frames restored) · spell-cast frame eyeballed (telegraph + stretch-trail fireball + spark spray correct).** NEXT = PICK per §2 (broken-main backlog cleared; interleave / risky de-monolith).
>
> **🔁 LOOP ITER 160 (2026-06-13, ultracode): 🟢 game UN-broken — fixed the SECOND latent crash + diagnosed the full cascade.** Ran a clean capture (menu graceful-degradation from @159 let it complete) → ALL 3D states were the ErrorBoundary (93% diff). First crash (lookSensitivity) was fixed @159; this run surfaced the SECOND: `MagicWand is not defined` — playerRender`s StableMagicHands renders `<MagicWand>` (:394) but never imported it (the iter-124 extraction missed it; MagicWand moved to render/spellVfx @152). Added the import (`852d592`). **Both fixes → 15/18 visual states now match baseline (game playable again).** Remaining 3 are capture-harness artifacts (NOT live regressions): title-mascot stale crash-screen; spell-cast/loot-showcase show the player-at-origin instead of their studio-card (camera-staging not applying — exitBeastForm reset had no effect, so not the beast). Reverted that ineffective reset + the parked loot-glow stays parked. **The verification hole (diff-on-stale-current) hid BOTH crashes for ~6 iters — charter §1.5 fixed @159.** NEXT = clean-load capture to investigate the studio-card staging + clear the stale title-mascot.
>
> **🔁 LOOP ITER 159 (2026-06-13, ultracode): 🔴→🟢 CRITICAL — fixed a broken-main GameScene crash hidden for 5 iters + closed the verification hole that hid it.** Picked visual-polish (loot-drop ground-glow) again; re-applied it + finally got a capture to COMPLETE by making the menu`s flaky 45s mascot-wait NON-FATAL (graceful degradation: skip menu.png + continue). The completed capture surfaced an ErrorBoundary: `ReferenceError: lookSensitivity is not defined (GameScene)` — my iter-154 selector was declared in SpatialAudioController`s scope but used in GameScene`s PLC `pointerSpeed`. **Latent on main since ccd4bbb (@154) — the game was UNPLAYABLE** — UNCAUGHT because the "visual 18/18" @154-158 diffed STALE current/ frames (no re-capture since @150) so GameScene was never re-rendered. FIXED the scope (`c5c51ef`); kept the capture graceful-degradation (`302be99`); UPDATED LOOP-CHARTER §1.5 (render changes MUST re-capture, not diff-alone). Reverted the loot-glow again (the loot-showcase capture also showed a beast-arcane state-leak under the partial run → cannot cleanly re-baseline; STILL PARKED). Build clean, unit 1049, crash fix baseline-safe by construction (PLC pointerSpeed default 1 = drei default = pre-154; non-rendering). NEXT = a CLEAN capture+diff to re-establish a trustworthy current/ + confirm 18/18.
>
> **🔁 LOOP ITER 158 (2026-06-13, ultracode): ⏸ LOOT-DROP GROUND-GLOW — built, then REVERTED + PARKED (capture-flake blocker).** Picked the deferred VISUAL-POLISH axis: a premium rarity-coloured ground-glow halo under loot drops (tier-scaled, additive, static). Wrote it all (pure `rarityBeam` glowRadius/glowOpacity + a `LootDropRender` ground disc + a lootJuice test) — **unit-green 1050 + build clean.** BUT the `loot-showcase` re-baseline + HD eyeball need a capture, and `npm run visual:capture` aborted with `TimeoutError 45000ms` on **3 consecutive runs** (an early settle-wait flaking under machine load — NOT my code; it captured fine @146-150). Per §4 (never ship an unseen visual) + the STUCK rule (stop at 2 same-approach fails), I REVERTED all 4 files (main clean, unit back to 1049) + parked the design with exact re-apply notes (`plans/2026-06-13-crafty-loot-glow-PARKED.md`). **Lesson: the capture harness is load-flaky + has no single-state mode → every visual re-baseline is hostage to the full 18+-state run.** NEXT = build a capture `--only=<state>` flag (capture-free quality-infra) → it unblocks the loot-glow + all future visual work.
>
> **🔁 LOOP ITER 157 (2026-06-13, ultracode): 📖 ASPECT-UNLOCK TEACHING TOAST SHIPPED — just-in-time UX.** Unlocking a signature Aspect verb taught the player nothing at the moment (the cheatsheet is an easy-to-miss corner reference). Now the FIRST unlock of an Aspect verb-talent pops a 5s toast — e.g. "WILDHEART unlocked — press R to roar" — the key + verb just-in-time. Pure `aspectUnlockHint(talentId)` in `game/aspectHints.js` sourced from KEY_MAP (the binding SoT → the taught key cannot drift from the live handler; null for non-verb talents; 2 tests) + `store.spendTalentPoint` sets `aspectHint` on `currentVal===0` + a HUD-mounted `<AspectHintToast>` (Toast primitive, auto-clears 5s) + an aspect-hint-gate. **Capture-safe** (aspectHint null in capture → toast renders nothing → 18 baselines byte-identical). **1045→1049 unit · build clean · visual 18/18.** Commit `939585e`. NEXT = PICK per §2 (anti-tunneling — VISUAL POLISH is the deferred axis; or content / touch M3 / i18n).
>
> **🔁 LOOP ITER 156 (2026-06-13, ultracode): ✨ MOB-DEATH SPARK FINISHER SHIPPED — kills now have a visual payoff.** A fresh-axis content+game-feel unit (finally off the audio/feel/settings streak): the mobKill path fed quests + Ferocity but mobs just VANISHED on death. Now a kill fires a bigger, mob-coloured spark burst (distinct from the per-hit sparkFor). Pure `deathBurst(mobType)` in `game/mobHitFx.js` → `{color: mob body colour, count: 40+xp clamped 50-110}` (tougher mobs burst harder; 4 tests) + the damageMob death path calls `store.triggerGPUSparks(pos, dColor, dCount, "death")` before `emitMobKill` — **reuses the proven, baseline-validated 1200-spark GPU pool** (look pre-validated; only colour+count differ, like the camera-kick reuse) + an element-impact-gates signature-fires assertion. **Capture-safe** (spawns/kills suppressed under capture → 18 baselines byte-identical). trap-1 note: the gate regex `[^)]*` broke on the inner `Vector3(...)` paren → asserted the unique `dColor, dCount` args instead. **1040→1045 unit · build clean · visual 18/18.** Commit `4c711de`. NEXT = PICK per §2 (content / visual / UX / touch M3; a doc-sweep is ~due).
>
> **🔁 LOOP ITER 155 (2026-06-13, ultracode): 🔊 UI PANEL FOLEY SHIPPED — the silent menu chrome now clicks.** A capture-safe audio interleave (decisive pick after several iters of PICK-deliberation — chose the clean bounded capture-safe win over chasing a flaky-capture unit). Two subtle low-gain voices (`makeUIOpen` rising 480→740Hz / `makeUIClose` falling 620→400Hz, peak ~0.2, 0.13s) → VOICES (contract 31→33) + `playUIOpen`/`playUIClose` verbs + a `<UISounds>` watcher (HUD-mounted) firing on the false↔true edge of the core panel layer (inventory/craft/build/magic) — a single reactive edge-watcher (Game-Loop-safe), renders nothing. **Capture-safe** (audio null under capture + no render → 18 baselines byte-identical) + a `ui-sounds-gate`. **1037→1040 unit · build clean · visual 18/18.** Commit `fba5f15`. **NOTE: a fresh-axis SIGNIFICANT unit (content/UX/visual) is now overdue — next iter must commit to one (use the early-capture trick for the eyeball).**
>
> **🔁 LOOP ITER 154 (2026-06-13, ultracode): ⚙️ Look Sensitivity setting SHIPPED (touch M2b, mouse+touch).** Picked touch M2b (finish-in-flight) but BROADENED the touch-only sensitivity into a unified setting BOTH platforms use (desktop-verifiable). Verify-before-assert: confirmed drei PLC spreads the three-stdlib `pointerSpeed` field → usable as a prop. Store `lookSensitivity` (default 1, clamped 0.3-2.5) feeds desktop `<PointerLockControls pointerSpeed={lookSensitivity}>` (GameScene) + touch `onMove` `applyLook` sensitivity (was hardcoded 1) + a "Look Sensitivity" SettingsPanel slider (Build-Size idiom) + a `look-sensitivity-gate` (3 consumers + setter wired). **Capture-safe** (default 1 == drei`s pointerSpeed default → 18/18 byte-identical). **1033→1037 unit · build clean · visual 18/18.** Commit `ccd4bbb`. NEXT = PICK per §2 (anti-tunneling — a FRESH-axis significant unit is due: content variety / UX onboarding / visual polish).
>
> **🔁 LOOP ITER 153 (2026-06-13, ultracode): 🎮 GAME-FEEL — slam + landing camera-kicks wired (the deferred @139 tail).** The per-verb camera-kick set is now COMPLETE (melee/cast/slam/land). Every VOIDHAND slam recoils the camera hard-down+forward (look-dir-relative via `localToWorldKick`, mirroring melee/cast, @Components:653); every landing dips it (`KICK_PROFILES.land`, pure vertical, at the landing edge :1168 where the landing thud already fires) — high-frequency tactile weight. **Capture-immune** (the kick applies only via `stepKick` in the follow-cam, below the Player loop`s isCaptureMode early-return → the 18 baselines never see it) + a `camera-kick-gate` (all 4 profiles dispatched — signature-fires insurance). **Picked over touch M2b** because the kick is desktop-VERIFIABLE + capture-immune (no flaky harness), whereas the cheap de-monolith wins are exhausted. **1031→1033 unit · build clean · visual 18/18.** Commit `592c1c3`. **Finding (for future M2b): gate-PLC-on-touch is coupled to the relock-site refactor** (PLC always-mounted by design for respawn lock-stability). NEXT = PICK per §2 — a player-facing SIGNIFICANT unit is due (touch M2b touch-sensitivity / M3b / content variety / UX).
>
> **🔁 LOOP ITER 152 (2026-06-13, ultracode): 🧱 S3 DE-MONOLITH — EMS de-god-filed (spellVfx extraction).** Per §2 #4 (structural spine, after the @149-150 interleave). Byte-exact slice of the 5 spell-VFX render sub-components (EnhancedSpellProjectile / SpellProjectileCore / SpellImpactPop / CastTelegraph / MagicWand, `HEAD:473-904`, 432 lines) → `render/spellVfx.jsx` (**byte-equality PROVEN**). They reference each other → moved as a group (refs stay intra-module); the main system imports back the 3 it renders, SpellProjectileCore stays internal, EMS re-exports MagicWand (Components:56 → playerRender uses it). Pruned ENERGY_PROFILE/_defaultEnergy/WAND_CONFIGS (render-only; SPARK_PROFILE stays). **Dep-completeness: ZERO module-local free-vars** (the SPELL_UPGRADES landmine from this very file was already defused by S3-M2). **trap-1 caught + fixed same-commit:** `spell-vfx-gates` pinned the VFX code by reading EMS → repointed it to read BOTH files (the system now spans 2). **EMS 904→474 LOC (<900) · 1031 unit (gate 22/22) · build clean · visual 18/18 byte-identical (spell-cast VFX).** Commit `b64c663`. **Only Components 1297 + GameScene 914 remain >900 (both risky) — the cheap de-monolith wins are exhausted.** NEXT = PIVOT (touch M2b / interleave / design-gate a risky de-monolith).
>
> **🔁 LOOP ITER 151 (2026-06-13, ultracode): 🧹 DOC-SWEEP (charter §6, ~5-iter hygiene) — cleared the doc-drift before it could be caught.** Bannered **5 stale completed plans** ✅ SHIPPED (s2b2-m2pre-perf #68, s2b3-m6-fuse [SOULBIND], respawn-pointerlock-cleanup, s3-m6-npc-steppers @140-142, touch-m2-visible @134-137) + corrected **3 spec "Status (at authoring): awaiting Kevin HARD-GATE" headers** (s2-game-design, s2b1-wildheart, coherence-pillars) — all SHIPPED under the loop SELF-GATE (charter §5 replaced the Kevin gate); prepended a current-status marker, preserved the historical note. Docs-only (no code → 1031 unit / build / 18/18 unchanged). NEXT = a build unit per §2 (EMS de-monolith / touch M2b / a SOTA interleave).
>
> **🔁 LOOP ITER 150 (2026-06-13, ultracode): 💥 DAMAGE-DIRECTION T2 (HUD cue) SHIPPED → the interleave is COMPLETE.** `ui/DamageDirection.jsx`: a red glow at the screen perimeter pointing toward the most recent hit (`store.lastHitDir`), centre placed from the angle (0=top/+pi-2=right/-pi-2=left/+-pi=bottom — aspect-ratio robust, no rotation); framer-motion fade ~0.8s in play (re-keyed per hit, matching DamageOverlay), and under capture a FIXED-opacity div gated on a `hitDir` capture-opt (the 18 baselines pass no opt → null, like showTouch). Mounted in the HUD beside DamageOverlay. HD self-eyeballed via a TEMP early-capture fixture (right-rear hit → lower-right glow, +26.5 red-dominance vs the opposite corner; bumped alpha 0.55→0.66 for punch) — the temp capture state was reverted (the cue is capture-null in normal play, so no permanent fixture needed, like the camera-kick). **Capture flake lesson: the title-mascot is the LAST capture state + flakes the 45s wait, so a temp eyeball frame placed AFTER it never runs — moved it EARLY (after mobile) to dodge the flake.** **1031 unit · build clean · visual 18/18 byte-identical (no re-baseline).** Commit `83e6968`. Projectile/boss source-pos threading is a noted follow-up (the projectile stepper returns a hit COUNT). NEXT = PICK per §2 (anti-tunneling — de-monolith EMS / touch M2b / another interleave; a doc-sweep is ~due).
>
> **🔁 LOOP ITER 149 (2026-06-13, ultracode): 💥 DAMAGE-DIRECTION CUES — T1 (pure math + plumbing) SHIPPED.** A combat-legibility interleave (charter §5/§6 "damage direction cues") per the interleave-floor (audio @145 → touch → de-monolith @148 → an experience interleave was due) — a fresh combat-feel axis. When a hit lands you should instinctively know WHICH WAY it came from (beyond the existing non-directional damageFlash + screenShake). T1 (capture-SAFE): pure `game/damageDirection.js` `hitDirection` (3 tests — 4 cardinals + a yaw-rotation case [caught + fixed my own wrong test expectation: +yaw is a LEFT turn in three.js, so a world-front hit reads screen-RIGHT — the MATH was right] + null-safety) + a NON-breaking optional `sourcePos` 3rd param on `damagePlayer` (records `lastHitDir={angle,t}` from the live `gameCamera` yaw + `playerPosition`) + threaded the mob-melee call-site (`:278`, `attack.position`). Capture-safe by construction (no HUD + capture never damages → `lastHitDir` null → 18 baselines untouched). **1028→1031 unit · build clean · visual 18/18.** Commit `1abebbc`. NEXT = T2 (the HUD `<DamageDirection>` cue + a capture fixture for the eyeball + thread the projectile/boss call-sites).
>
> **🔁 LOOP ITER 148 (2026-06-13, ultracode): 🧱 S3 DE-MONOLITH — GamePanels de-god-filed (CraftingTable extraction).** Capture-safe axis-change to the charter-#4 structural spine after 2 touch iters (a welcome change after iter-147 fought the flaky capture harness). Byte-exact slice of `CraftingTable` (`HEAD:473-816`, 344 lines) → `ui/panels/CraftingTable.jsx` (**byte-equality PROVEN** — moved slice == HEAD identical) + the shared `ItemIcon` → `ui/panels/itemUi.jsx` (both Inventory + CraftingTable import it; avoids a GamePanels↔CraftingTable circular import). GamePanels re-exports CraftingTable so MenuSystem is unchanged; `RECIPES` is internal to CraftingTable (moved with it); pruned 2 dead imports (getItemIcon, useCallback). **GamePanels 1094→739 LOC — UNDER the 900 god-file threshold.** Behavior-lock: build clean (import-resolution + the re-export), 1028 unit, visual **18/18 byte-identical** (inventory-open exercises ItemIcon via the itemUi import). Commit `456065f`. Remaining god-files: Components 1297, GameScene 914, EMS 904. NEXT = PICK per §2 (anti-tunneling — de-monolith continuation / a SOTA interleave / touch M2b).
>
> **🔁 LOOP ITER 147 (2026-06-13, ultracode): 📱 TOUCH M3a-T2 SHIPPED — the panel-access tray is VISIBLE + usable; touch panels are reachable at last.** Built the `<TouchTray>` surface: a left-edge grid icon → a center-anchored vertical column of 4 lucide openers (Inventory/Craft/Build/Magic) calling `togglePanel(p, getState())` + `setActive(false)` so the panel is natively interactive. Focus handling: the touch move/look listeners now gate on `getInput().active` (no camera-drag / preventDefault fighting an open panel) + tap-to-play is suppressed while any panel is open. **Two real bugs found + fixed via the HD self-eyeball loop:** (1) a flaky slam-sound clip >1.0 (~4%) → headroom-normalize guard (deterministic green, 0/30); (2) the touch glyphs rendered DARK (since iter-137 M2a!) — inherited `currentColor` does not cascade to the fixed overlay AND `color: var(...)` renders inconsistently in the headless capture → switched to a LITERAL `#C9A86A` (tray gold-px 1→4093; the M2a sword/zap/jump cluster glyphs are now gold too). Also resolved a quest-tracker collision (center-anchored the tray). mobile.png re-baselined (premium, collision-free; KEVIN-REVIEW #21). **1028 unit · build clean · visual 18/18.** Commits `b21318d` (slam) · `c82f4b5` (T2). NEXT = PICK per charter §2 (anti-tunneling — axis-change: S3 de-monolith / interleave; or touch M2b/M3b).
>
> **🔁 LOOP ITER 146 (2026-06-13, ultracode): 📱 TOUCH M3a (panel-access tray) STARTED — T1 (pure registry) SHIPPED.** Axis-return to touch (the least-recently-touched major workstream, ~8 iters; charter §2 #2 finish-in-flight) after the audio interleave. The post-M2a gap: E/C/B/M open inventory/craft/build/magic on desktop (InputManager:117-120 → store setters) but touch has no keyboard + M2a hid the openers → panels unreachable on touch. Scoped M3 (the big verb-sprawl+analog bundle) → M3a (tray) per writing-plans, ladder-structured like the proven M0→M1→M2a (pure-first). T1 (capture-SAFE): `ui/touchTray.js` — `TRAY_PANELS` (4 panels → the verified store actions setShowInventory/Crafting/BuildingTools/Magic, value-or-fn updaters @useGameStore:542) + `togglePanel` (fn-updater flip, null-safe) + a `touch-tray-gate` (every opener maps to a real store action). **1024→1028 unit · build clean · visual 18/18 byte-identical (T1 renders nothing).** Commit `4c5f3f0`. NEXT = M3a-T2 (the visible `<TouchTray>` surface + mobile.png re-baseline — needs the capture env).
>
> **🔁 LOOP ITER 145 (2026-06-13, ultracode): 🔊 MOB AGGRO-SNARL audio interleave SHIPPED — enemies now SOUND like a threat.** Axis-change per the §5 interleave-floor (5 straight de-monolith iters @140-144 → an interleave was overdue; audio last touched @122). **VERIFY-BEFORE-ASSERT paid off:** my first premise (aspect motifs + the roar are dead) was FALSIFIED by live code — all 4 motifs + the WILDHEART roar are fully wired (motifWildheart `Components:589`, motifVoidhand `:659`, motifSoulbind `:755`, motifElemancer `ElementZoneSystem:55`; the #74 motif-v2 is DONE). The REAL gap (player-experience lens): mobs were silent until hit/killed — no threat cue. Built the §6 "player-vs-enemy audio split": a pure `makeAggroGrowl` voice (a short 0.34s gnarly bark — higher/shorter/nastier than the heroic 0.7s roar; lunging pitch + a dissonant-fifth menace) registered in VOICES (contract 30→31) + the false→true `isAggro` edge wired in the worker-update loop (`SimplifiedNPCSystem:296`, spatial, global-cooldown 2.5s so a turning siege snarls ONCE) + a `mob-aggro-audio-gate` (signature-fires-in-prod insurance). **Capture-safe by construction** (mobs suppressed under isCaptureMode → the edge never fires in capture; audio never renders). **1022→1024 unit · build clean · visual 18/18 byte-identical (no re-baseline).** Commit `c725e73`. NEXT = PICK per charter §2 (touch M3 finish-in-flight, or back to S3 de-monolith — Components/GamePanels/GameScene still >900).
>
> **🔁 LOOP ITER 143-144 (2026-06-13, ultracode): 🧱 S3-M6 — MobModel render extraction SHIPPED → the NPC god-file is DISSOLVED.** iter 143 design-gated the cut (found CLEAN, not the gate-heavy nightmare the spec warned of: GATE-FREE [no gate asserts MobModel's render], trap-3-safe [MobModel has no brace-less isCaptureMode-return → the worker-tick anchor untouched], baseline-safe; plan `plans/2026-06-13-crafty-s3-m6-mobmodel.md`). iter 144 BUILT it: a byte-exact 309-line slice of `MobModel`+`HealthBar` (`HEAD:85-393`) → `render/MobModel.jsx` — **byte-equality PROVEN** (diff of the moved slice vs HEAD == identical, zero mangling); moved the one module-local `OUTLINE_RIM_STRENGTH` (its sole user) into the new header; pruned 5 now-dead NPC imports (drei Html/Outlines, primitives Panel/Icon, MobToonMaterial, characterStyle flashable/OUTLINE/RIM, quality TIERS); NPCSystem imports MobModel back. **NPC 1115→802 LOC — UNDER the 900 god-file threshold → the de-monolith's stated NPC goal MET (the last god-file is gone).** Behavior-lock: build clean (the import-resolution gate), 1022 unit, character-render trap-3 gate 5/5 (worker-tick anchor intact), visual **18/18 BYTE-IDENTICAL** (spawnCharacterCloseup renders a mob via MobModel → proves the render path). Adversarial verification = the byte-diff itself (stronger than an LLM review for a byte-exact move). Commit `6369a19`. NEXT = PICK per charter §2 (anti-tunneling — change axis: touch M3 / a SOTA interleave; SpawnerSystem + AIWorkerSystem are optional remaining S3-M6 cuts).
>
> **🔁 LOOP ITER 140 (2026-06-13, ultracode): 🧱 S3-M6 (NPC de-monolith) STARTED — the XP-orb stepper cut.** Axis-change within charter #4 (after ~13 touch iters + 1 interleave): the de-monolith spine is capture-safe (unit-verifiable, sidesteps the flaky capture harness). Per "lowest-blast-radius-first" picked a SAFE pure-stepper cut over the risky Components part-3 / the gate-heavy MobModel. Extracted `game/xpOrbStepper.js` (`stepXPOrb` — byte-equivalent to the inline XP-orb physics: gravity/bounce, magnetic pull `max(4,80/(dist+0.2))`, collect <1.2, the `playerY-0.5` core-pull) characterization-first (5 tests — the orb physics's FIRST coverage) + wired `XPOrbSystem` to it (component keeps the ECS loop + grantXP/spawnXPText/sound/remove on `collected`). Gate-free (the orb RENDER was already in `render/pickupVfx` from S3-M3; the physics isn't gated). **1006→1011 unit · build clean · visual 18/18 · NPC 1217→1182 LOC (−35).** Commits `85bedbd`/`344b2a8`. (cwd-drift bit again — battery must run from `frontend/`.) **ITER 141: loot stepper ✅ — unified BOTH pickups behind `_stepMagnet` (range/base/floor params: orb 12/80/4, loot 7/40/3) + wired LootSystem byte-exact; NPC 1182→1140; 1015 unit / 18/18; commit `7d0bb64`. ITER 142: damageMob pure pulls → `game/mobHitFx.js` (`sparkFor` + `hitKnockback`, 7 tests) + trap-1 gate-repoint (element-impact-gates → mobHitFx); NPC 1140→1115; 1022 unit; commit `e786bf6`. NEXT = MobModel+HealthBar render (DESIGN-GATE the trap-1 gate-repoints + trap-3 anchor first — the high-impact ~270-LOC cut) or touch M3.**
>
> **🔁 LOOP ITER 138 (2026-06-13, ultracode): 🎮 CAMERA-KICK juice interleave PICKED + T1 (pure module) SHIPPED.** Axis-change from touch (anti-tunneling: 3 touch milestones with 0 interleaves → the §5 interleave-floor was due). Surveyed candidate interleaves + ruled out the constrained ones (visuals mature; audio ear-only; full-zh-CN i18n too big — strings.js has 52 UI-chrome keys w/ full en/zh parity but game CONTENT isn't routed through t(); damage-direction needs threading source-pos through ~6 call sites). PICKED **per-verb camera kick** (§6-named game-feel): capture-IMMUNE (the kick lives below the Player loop's `isCaptureMode` early-return → invisible in baselines → no capture-regen, sidesteps the flaky harness), unit-verifiable, bounded, fresh-axis. Seam: the follow-cam already adds `shakeX/Y/Z` (random damage-jitter from `cameraShakeIntensity`) to the camera target at Components:1210 → the kick folds in there. **T1 = pure `game/cameraKick.js`** (`makeKick`/`addKick`/`stepKick` + `KICK_PROFILES` melee/cast/slam/land + `KICK_DECAY=14` ~0.2s recovery) — a per-verb decaying positional impulse, 5 unit tests (decays-to-zero, accumulates, no-NaN). **1002 unit (+5) · build · visual 18/18.** Commit `7cc330e`. **T2 ✅ (iter 139): `localToWorldKick` (pure, tested) + wired into the follow-cam (folds into `shakeX/Y/Z`) + the melee/cast triggers — 1006 unit / build / visual 18/18; commit `9311c68`. Interleave DONE** (slam/land kicks = optional follow-up). NEXT = touch M3/M2b (finish-in-flight) or de-monolith.
>
> **🔁 LOOP ITER 134-137 (2026-06-13, ultracode): 📱 TOUCH M2a SHIPPED — the touch UI is now VISIBLE + premium (first touch baseline blessed).** M2 plan (iter 134; `showTouch` capture-opt-in + a shared `<TouchControlsSurface>` + 3-way guard) → M2a build (135) → polish-attempt (136) → premium + baseline (137). Lessons: (1) the FIRST HD self-eyeball worked as the taste gate — caught the frame wasn't premium → held the baseline rather than bless slop. (2) **Full-res crops** (`PIL im.crop`) were essential — the downscaled full-page preview hid the issues. (3) The real problem was MOBILE-HUD-LAYOUT, not styling: touch controls COLLIDED with the existing minimap/XP-bar/level-bar/tool-column, and the **2-tone game-icons don't tint** (`color` ignored — baked fills) → switched to **lucide** chrome icons (tint via currentColor). (4) Landed via: lucide gold glyphs + **near-black button fill** (pops off navy HUD) + **hide minimap+XP-bar+cheatsheet+tool-column on touch** (`isTouchUIMode`) + darkened joystick ring + a corner scrim. (5) Capture-env flaked hard (mascot wait 15s→45s + retries). **Result: clean S1-C touch UI — gold sword/zap/jump/pause dark buttons + joystick ring + crosshair; colliding HUD hidden. 997 unit / build / visual 18/18 (mobile.png blessed; 17 desktop frames byte-identical — touch gates isTouchUIMode-off on desktop).** Commits `63709e5`/`5aa7107`/`d9ee9f9`/`a176eba`/`0e65f66`. M2b/M3 queued. NEXT UNIT = PICK per charter §2 (anti-tunneling — change axis from touch).
>
> **🔁 LOOP ITER 130-133 (2026-06-13, ultracode): 📱 TOUCH M1 (producer-wiring) SHIPPED + adversarially reviewed-clean — the game is now wired to be playable by touch.** Built across 5 TDD cuts: T1 `input/touchDevice.js` (`isTouchDevice` 3-tier any-pointer:coarse, hybrid-iPad safe) · T2 `input/touchHandlers.js` (DI glue: `makeTouchRouter`→`joystickToMove`→`setIntent` for move, `applyLook`→`camera.rotation` for look; null-camera-safe; unit-proven) · T3 extracted `performVerb(button)` from Components `handleMouseDown` byte-exact (3 surgical edits) + registered to store · T4 `ui/TouchControls.jsx` (1st line `if(isCaptureMode()||!isTouchDevice())return null` — the [BLOCKING] capture + [HIGH] desktop-inert guards; transparent zones bind raw touch w/ touch-action:none + passive:false; refs-not-setState; Tap-to-Play/Pause→`setActive`; `store.performVerb(0)`; `data-touch-btn` guard so taps don't drag-look) + App mount · T5 `Terrain` highlight gate→`getInput().active` + App pause `!isTouchDevice()` guard + wiring static gates. **Adversarial-review Workflow `wf_74357759-ced` (3 lenses, 275k tokens): NO blocking/high.** 3 MEDIUMs fixed same-iter: (a) the Terrain gate-flip lost capture-safety [the capture `start` hook sets active=true] → re-added `|| isCaptureMode()` (a real latent baseline-trap I introduced); (b) `active` could stick true on unmount [no touch surface to clear it] → cleanup now `setActive(false)`; (c) unconditional `preventDefault` on touchstart could suppress iOS button onPointerUp → made conditional on a real move/look touch. + 4 gate-hardening assertions. The reviewer's "6 failing tests" claim was NOT reproduced (my runs 990/990 — discarded per delegated-research-reconciliation). **947→990 unit across M0+M1 · build clean · visual 17/17 byte-identical (desktop-inert + capture-null proof).** Commits `24dab0d`/`6a58f92`/`db09dab`/`39490ea`/`67a1e4e`. 2 LOWs → M2 (visible pause overlay; gate PLC on !isTouchDevice). NEXT UNIT = M2 (the visible thumb-cluster surface + the `mobile.png` fixture).
>
> **🔁 LOOP ITER 129 (2026-06-13, ultracode): 📱 TOUCH M1 (producer-wiring) PLAN authored.** `plans/2026-06-13-crafty-touch-m1-producer-wiring.md` — 5 TDD tasks, placeholder-free, grounded in live-seam reads: T1 `input/touchDevice.js` (`isTouchDevice` 3-tier any-pointer:coarse) · T2 `input/touchHandlers.js` (DI glue: `makeTouchRouter`→`joystickToMove`/`setIntent` + `applyLook`→`camera.rotation`, unit-testable w/ fake camera + spy) · T3 extract `performVerb(button)` from Components' `handleMouseDown` (:420-481, all closures in-scope) + register to store (byte-exact desktop) · T4 `ui/TouchControls.jsx` (1st line `if(isCaptureMode()||!isTouchDevice())return null` — the [BLOCKING] capture + [HIGH] desktop-inert guards; transparent zones bind raw touch w/ touch-action:none/passive:false; refs-not-setState; Tap-to-Play/Pause→`setActive` focus model) + mount in App · T5 flip `Terrain.jsx:269` highlight gate to `getInput().active` + `!isTouchDevice()` guard on the App pause-on-unlock effect + 4 static gates + an adversarial-review Workflow (charter §4 — touches the player controller + the single-active-authority invariant). **Verify-before-assert caught a real plan bug: `isWorldBuilt` is App-LOCAL useState (App.jsx:105), NOT a store key → corrected to a prop (a `useGameStore(s=>s.isWorldBuilt)` selector would've been always-undefined).** `useActiveInput`/`isCaptureMode`/`isAlive` export names all verified. **M1 verified WITHOUT a device: static gates + unit tests + the 17 baselines staying byte-identical = the desktop-inert/capture-null proof (NO re-baseline — the visible surface + `mobile.png` fixture is M2).** NEXT UNIT = BUILD M1 T1 (`isTouchDevice`) → T2 → ... → T5.
>
> **🔁 LOOP ITER 128 (2026-06-13, ultracode): 📱 TOUCH M0 SHIPPED — the pure `touchMath.js` math lib (the touch milestone's first cut).** Built TDD red-first (RED confirmed — module-missing — before impl): `joystickToMove(vecX,vecY,deadzone)` → the four boolean move intents via deadzone + 8-way dominant-octant, with a full -180..180 sweep test PROVING the no-opposing-pair invariant (never moveF&moveB / moveL&moveR — the noisy-thumb guard); `applyLook(yaw,pitch,dx,dy,sens)` → clamped `{yaw,pitch}` reusing three PointerLockControls' VERBATIM constants (`LOOK_BASE_SENSITIVITY=0.002`, `MAX_PITCH=π/2−0.05` — same as the Components:1218 defensive clamp), pitch saturates / yaw wraps; `makeTouchRouter()` → per-`Touch.identifier` zone routing (left=move from-origin / right=look incremental-delta) with the interleaved-3-finger test proving a look-drag id NEVER mutates the move vector (the [HIGH] multi-touch-identity trap, killed in pure math). PURE (no React/Three/DOM — same contract as inputState.js) + a purity static gate. **947 → 970 unit (+23: 21 module + 2 gate) · build clean · visual 17/17 unchanged (M0 renders nothing).** One catch the gate earned: the zero-emoji HARD gate (`tests/gates/static-gates.test.js`, range `←-⇿`) caught U+2192 `→` arrows in my doc-comments (it scans co-located `src/**/*.test.js` too) → ASCII-cleaned to `->`. Commits: `2304fd1` (module) · `bb183cb` (gate + fix). **The 6 traps live in M1 (integration), not M0 (pure math) — M0 is the safe opening cut, charter characterization-first.** NEXT UNIT = the M1 (producer-wiring) plan doc → build the touch overlay (capture-safe, desktop-inert).
>
> **🔁 LOOP ITER 127 (2026-06-13, ultracode): 📱 PICKED TOUCH/MOBILE INPUT — the §0 hard-frame gap — + launched the design-gate workflow.** Surveyed 3 visual-interleave candidates first and found them DONE/mature (per-element projectile geometry ALREADY shape-branches at `EnhancedMagicSystem.jsx:576 switch(profile.shape)`; the sky/Atmosphere is already a 3-stop gradient + sun-glow + height-fog + mood-lerp system) → visuals are MATURE, so I did NOT force a marginal interleave. **Pivoted to the highest-value confirmed gap: touch/mobile input.** VERIFIED against live code (not just the doc claim): ZERO touch handlers (`touchstart/move/end`=0), the only mobile signal is `quality.js:42 coarsePointer` (render-tier, not input), and `input/inputState.js:15` EXPLICITLY says the intent SoT was designed so "later a virtual-joystick / touch layer writes the SAME intents... sets active from its own focus model." Per CLAUDE.md the "biggest gap (touch/mobile unbuilt — Pointer-Lock-gated)"; per charter §0 the "web + iPad + **mobile** envelope" is a HARD frame *never overridden by taste*. The safe S3 de-monolith cuts are exhausted + the rest are risky/invisible; touch/mobile is high-value, fresh-axis (platform), clean-seam, autonomous-buildable + unit-verifiable (real-device FPS/feel is the only Kevin-parked slice). **Seam mapped:** intent SoT is boolean (`INTENT_KEYS`); producers = WASD→setIntent (Components :342-412) + `routeMouseVerb(button,ctx)` (:464, reusable by a touch button); mouselook = drei `<PointerLockControls>` (GameScene :833, desktop-only → touch-drag must replicate YXZ yaw/pitch + the :1216 pitch-clamp); active-gate = `setActive(!!document.pointerLockElement)` (:486, built for a touch focus model); player loop self-nulls under `isCaptureMode()` (:511 → a touch overlay MUST too, or it pollutes all 17 baselines incl. the menu). **Ran a grounded 3-lens design-gate Workflow `wf_1c9d17fc-f40`** (287k tokens; A: exhaustive live seam-map · B: SOTA-2026 touch-control research w/ live citations · C: adversarial pre-mortem + milestone-split + device-free test strategy) → **synthesized + COMMITTED the design-of-record spec** `specs/2026-06-13-crafty-touch-input-design.md` (dual-zone floating-joystick + drag-look + crosshair + tiered actions; reuses intent-SoT/verb-router/active-gate with ZERO movement-consumer change; 6-trap catalog incl. [BLOCKING] capture-pollution + [HIGH] desktop-regression/focus-lifecycle/multi-touch; iOS gotcha checklist; M0→M3 ladder). Re-verified the 3 load-bearing delegated claims vs source (the `gameStarted` latent bug = always-undefined, `Terrain.jsx:269` highlight gate, the `input-abstraction-gates` ≤1 pointerLockElement invariant). **Authored the M0 plan** `plans/2026-06-13-crafty-touch-m0-touchmath.md` (pure `touchMath.js`, TDD). KEVIN-REVIEW-BATCH updated (the verified `gameStarted` bug NOTICE + 3 self-gated touch-UX decisions). **NEXT UNIT = BUILD touch M0** (the pure math lib — lowest-risk first cut, charter characterization-first). MILESTONE (multi-iteration): spec → per-milestone plans, lowest-risk-first.
>
> **🔁 LOOP ITER 126 (2026-06-13, ultracode): 🔧 S3-M5 part 2 (Components pure kernels) SHIPPED — the player loop's first characterization.** `game/spawnPlacement.js` (resolveSpawnGround/isVoidFall/spawnTargetY — the spawn-probe decision) + `game/locomotion.js` (moveSpeed/jumpVelocity/applyGravity — move/jump/gravity math, -50 terminal clamp), extracted PURE from the Player loop; the imperative setTranslation/velocityY assignments STAY. **Characterization-FIRST: 11 tests pin the exact literals (the §3 ledger's named-missing spawn + locomotion characterization, now covered).** A review-catch: added a `probeAvailable` param to resolveSpawnGround to preserve the getMobGroundLevel-missing edge byte-exact (immediate fallback, no retry). Components 1300 → 1286. 947 unit (+11) · build · visual 17/17 (capture completes — player still spawns/moves). Commits: T1 (spawn) · T2 (locomotion). **The SAFE Components cuts are now exhausted (render/UI leaves + pure kernels); part 3 = the DANGEROUS Player-loop SM-wiring (trap-4 — extract as functions not siblings).** NEXT UNIT = PICK (interleave is ~due, or design-gate the risky part 3).
>
> **🔁 LOOP ITER 125 (2026-06-13, ultracode): the S3-M5 part-2 (pure kernels) plan doc SHIPPED.** Surveyed the Player loop + pinned two pure-kernel I/O contracts: `spawnPlacement` (the spawn-ground decision — `resolveSpawnGround` mirroring the increment-then-`<120`-check + the y=60 fallback + the `physicsY<=15` reject + the `<10` void-guard + the +1.2 eye-offset) and `locomotion` (move/jump/gravity math + the -50 terminal clamp). Authored the plan CHARACTERIZATION-FIRST (these ARE unit-testable, unlike part-1's render leaves — the charter ledger named both as un-characterized): the kernel test pins the exact literals, then Player calls the pure fn (the imperative setTranslation/velocityY assignments STAY). Verification note: spawn is off the pinned capture camera, so the kernel tests + battery + capture-completes are the lock (not a pixel-diff). NEXT UNIT = BUILD part-2 T1 (spawnPlacement + its characterization test).
>
> **🔁 LOOP ITER 124 (2026-06-13, ultracode): 🔧 S3-M5 part 1 (Components render/UI leaves) SHIPPED — the god-file shrinks −512 LOC.** Byte-exact line-range slices (the proven S3 method): HUD chrome (MinecraftHotbar + GameUI) → `ui/GameHud.jsx` (HUD.jsx repointed); the FPV render cluster (ProceduralWeapon + RibbonTrail + StableMagicHands + spell hand FX) → `render/playerRender.jsx` (consolidated the plan's 2 render cuts — StableMagicHands orchestrates the rest intra-module; Player imports it, `export` added). Dep-analysis confirmed ZERO missed module-local consts (the SPELL_UPGRADES guard — the capitalized-bareword scan caught only imported consts). 10 dead imports pruned. **Components 1812 → 1300 LOC**; ZERO gate churn (as the spec predicted for leaves). One build-catch: StableMagicHands needed `export` (was intra-file `const`) — rollup caught it, build is the import-resolution gate. The off-frame FPV hands aren't pixel-tested, so the capture COMPLETING (exit 0, no mount-crash) is the guard. 936 unit · build · visual 17/17. Commits: T1 (HUD) · T2 (renders). NEXT UNIT = the S3-M5 part-2 (pure kernels: spawnPlacement + locomotion, characterization-first) plan doc.
>
> **🔁 LOOP ITER 123 (2026-06-13, ultracode): PICKED S3-M5 (Components de-monolith) + the part-1 plan doc SHIPPED.** With the world-design + audio push complete (#5), the charter's #4 STRUCTURAL spine (S3 de-monolith) is due — a healthy work-type pivot. Components.jsx is the biggest god-file (1812 LOC); the deferred new-blocks (M4b/M5b) are parked-not-in-flight so #4 outranks. The S3 spec already design-gated S3-M5 = "Components: leaves + kernels" → authored the part-1 plan (the SAFE render/UI leaves, ZERO gate churn, Player's loop LAST): HUD chrome → `ui/GameHud.jsx`, weapon renders → `render/proceduralWeapon.jsx`, hands → `render/playerHands.jsx` via the proven byte-exact anchored-slice + per-move dep-completeness grep (the SPELL_UPGRADES lesson). Also ran the ~5-iter doc-sweep: recent docs current; the stale-header matches are OLD completed-milestone plans (historical, not active drift — deferred to a hygiene pass). NEXT UNIT = BUILD S3-M5 part-1 T1 (the HUD chrome extraction).
>
> **🔁 LOOP ITER 122 (2026-06-13, ultracode): 🔊 BIOME-AMBIENT AUDIO interleave SHIPPED — each biome now SOUNDS distinct.** `audio/biomeAmbience.js` (pure biome→{cutoff,gain} map: snow airy / desert dry / stone hollow / plains soft / underwater muffled; 4 unit tests) + a looping wind-bed voice in SoundManager (a `makeNoise` source → bandpass → gain, started with the synthPad; the EXISTING 8s pad step-interval reads `playerPosition` [store, written by Components] → `climate.surfaceBlockAt` → `biomeAmbience` → ramps the bed's filter/gain; faded+stopped+disconnected in stopSynthPad — no leaked looping source). Game-Loop-safe (reuses the transient getState interval); rides `world/climate.js`. 933 unit (+7: 4 map + 3 gate) · build · visual **17/17 no change** (audio-only). Commits: T1 (map) · T2 (wind bed+gate). Audio EAR-verified by Kevin. **The world-pass audio capstone: see distinct biomes → hear distinct footsteps → hear distinct ambience.** NEXT UNIT = PICK (recommend the new-blocks batch M4b+M5b, then S3-M5 de-monolith).
>
> **🔁 LOOP ITER 121 (2026-06-13, ultracode): the BIOME-AMBIENT AUDIO interleave plan doc SHIPPED (+ PICK made).** With the M1-M6 world ladder complete, the interleave was due (audio = most-neglected axis, 2 milestones since @116). PICKED biome-ambient audio — the thematic capstone (see distinct biomes → hear distinct footsteps → now distinct ambience), and `world/climate.js` already unlocks it. Design-gated: the store ALREADY exposes `playerPosition` (Components:124, every 200ms — NO new wiring needed for SoundManager to know the biome); `makeNoise` (synthVoices) is the wind-bed source; the existing pad `setInterval` (:160) is the per-biome update hook (Game-Loop-safe). Authored the plan: a pure `audio/biomeAmbience.js` (biome→{cutoff,gain} map, unit-testable) + a looping wind-bed voice in SoundManager (climate-keyed, ramped, torn down in stop). Contained ADD, not a new subsystem. Audio ear-verified by Kevin. NEXT UNIT = BUILD T1 (the biomeAmbience map).
>
> **🔁 LOOP ITER 120 (2026-06-13, ultracode): 🏆 WORLD-M6 (signature landmarks) SHIPPED — the M1-M6 LADDER IS COMPLETE.** `world/landmarks.js` (greenfield pure: deterministic imul-hash placement ~1.4%, 2 types, NO Math.random) + `<LandmarksRender chunks={chunks}/>` — two tall voxelKit types (Spire glowing-tower + Sky-arch) driven by the streamer's LOADED `chunks` set (in-range-cull + tier-cap FREE), LAND-only via `climate.surfaceBlockAt`, sized `top=max(baseY+46,92)` to clear fog Y56, castShadow-off on tall parts, Emissive top capture-null. Added a `landmark` fixture (probed Sky-arch [40,-88]) — HD-eyeballed: a distinctive monument silhouette clearing the forest, glow null-in-capture. 929 unit (+6) · build · visual 16→**17/17** (16 prior byte-identical — the far camera-jump + restore + re-settle clean). Commits T1·T2·T3. **🏆 The world-design pass (the gap Kevin twice asked about) is substantially BUILT: M1 Hearth · M2 divable oceans · M3 biome-table · M4a biome-foliage · M5a ocean-depth · M6 landmarks + the locomotion-audio interleave.** NEXT UNIT = PICK (recommend a biome-ambient audio interleave — climate.js already unlocks it).
>
> **🔁 LOOP ITER 119 (2026-06-13, ultracode): the WORLD-M6 (signature landmarks) plan doc SHIPPED.** Design-gated the seams (read the chunk-streamer): the camera-keyed `chunks` state [Terrain.jsx:439/:563-609] is render-radius-bounded + tier-capped → a `<LandmarksRender chunks={chunks}/>` driven by it gets in-range-cull + the count-cap FOR FREE (no separate distance gate); `climate.surfaceBlockAt` (reused M5a) gives the ground height + a land/ocean gate; placement by a fresh main-thread imul-hash (deterministic, NO Math.random). Authored the M6 plan: a greenfield pure `world/landmarks.js` (sparse ~1.4% placement, 2 types) + `<LandmarksRender/>` (voxelKit Spire glowing-tower + Sky-arch, tall `top=max(baseY+46,92)` to clear fog Y56, castShadow-off on tall parts, Emissive capture-null) + a `landmark` fixture. **This is the FINAL spec-ladder milestone — after it M1-M6 is COMPLETE.** NEXT UNIT = BUILD M6 T1 (the placement module).
>
> **🔁 LOOP ITER 118 (2026-06-13, ultracode): 🌊 WORLD-M5a (ocean water depth-tint) SHIPPED — divable basins READ deep.** A `vWorldY` varying in the shared `compileShader` + a water-gated fragment mix toward deep-navy by `clamp((SEA_LEVEL-vWorldY)/22,0,1)*0.82` (SEA_LEVEL compile-time-injected from oceanProfile — DRY, simpler than the spec's uniform). Static → capture-stable; water-gated (opaque unaffected); survives the obsidian-mood desat. **The M1 coverage lesson AGAIN: the tint shades by world-Y so it's INVISIBLE top-down (surface=depth 0) → gate stayed 15/15 = zero coverage → probed the nearest deep basin ([-40,0], seabed y7/depth 21) + added an `ocean-depth` underwater fixture (camera-override) showing the navy-deepening water column over the sandy seabed (HD-eyeballed — a real "diving feels deep" payoff). explore-day deliberately re-baselined (6.07% — the diorama's ~25° angle catches water side-faces darkening; HD-confirmed correct).** 923 unit (+3 gate) · build · visual 15→**16/16**. Commits: T1 (shader) · T2 (fixture+re-baseline). M5b layered seabed (new blocks) → folds with M4b. NEXT UNIT = the world-M6 (signature landmarks — the FINAL spec-ladder milestone) plan doc.
>
> **🔁 LOOP ITER 117 (2026-06-13, ultracode): the WORLD-M5a (ocean water depth-tint) plan doc SHIPPED.** Design-gated the water-shader seam (read Terrain.jsx:44-156): ONE shared `compileShader` serves BOTH opaque + water materials → the depth-tint MUST stay water-gated (like the biolum :137); the fragment had only VIEW-space Y → a NEW world-Y varying (`vWorldY`) is genuinely needed; SEA_LEVEL as a compile-time-injected const (from oceanProfile, DRY) is simpler than the spec's uniform/useFrame-pump. SPLIT M5 → M5a depth-tint (shader-only, NO new blocks — this iteration's plan) + M5b layered seabed (sand→gravel→stone, needs NEW blocks → folds with M4b, deferred). Authored the M5a plan (shader edit + static structure gate + a deliberate re-baseline; an `ocean-depth` fixture if the gradient isn't visible top-down — the M1 coverage lesson). NEXT UNIT = BUILD M5a T1 (the shader depth-tint).
>
> **🔁 LOOP ITER 116 (2026-06-13, ultracode): 🔊 LOCOMOTION-AUDIO interleave SHIPPED — walking is no longer silent.** `world/climate.js` (greenfield pure main-thread biome/surface sampler — replicates the worker climate noise [seed 12345], reuses pickBiome+oceanProfile; 5 characterization tests agree with the worker: origin grass, [0,-40] snow) + Game-Loop-safe wiring in the Components movement useFrame: surface-keyed FOOTSTEPS (stride-throttled, grass/sand/snow/stone rate variation — each biome SOUNDS distinct, riding M4a), a landing thud (prevGroundedRef edge), a jump cue — all via the store's `playSpatialSound` (the :384 precedent; transient getState() + refs, no React state). 916 unit (+9: 5 climate + 4 gate) · build · visual **15/15 no diff** (audio-only). Commits: T1 (climate.js) · T2 (wiring+gate). Audio is EAR-verified by Kevin (KRB). `world/climate.js` ALSO unlocks biome-ambient music later. NEXT UNIT = the world-M5 (ocean seabed + depth-tint) plan doc.
>
> **🔁 LOOP ITER 115 (2026-06-13, ultracode): the LOCOMOTION-AUDIO interleave plan doc SHIPPED (+ PICK made).** The experience interleave was DUE (4 world milestones since UX@98; audio most-neglected). Scouted the audio/feel state: **hitstop + screenShake ALREADY exist (don't rebuild); `playFootstep(pos,type)` EXISTS but is NEVER called → walking is SILENT.** PICKED a locomotion-audio interleave (surface-keyed footsteps + landing + jump) — highest feel-per-effort + RIDES M4a (each biome SOUNDS distinct). Authored the plan: a greenfield pure `world/climate.js` (main-thread biome/surface sampler — replicates the worker noise, reuses pickBiome+oceanProfile; ALSO unlocks biome-ambient music later) + the Components useFrame wiring (Game-Loop-safe, throttled, via the store's playSpatialSound like the :384 precedent). Audio = ear-verified by Kevin (headless can't hear). NEXT UNIT = BUILD T1 (climate.js + characterization test).
>
> **🔁 LOOP ITER 114 (2026-06-13, ultracode): 🌲 WORLD-M4a (biome foliage distinctness) SHIPPED — snow pines.** `world/foliage.js` `pineShape` (greenfield pure, tapered conical evergreen, 4 unit tests) + a `surfaceBlock===5` snow-pine branch in the worker foliage pass — the snow biome was BARREN, now grows pines (trunk wood 6 + needle-cone leaves 7; reuse existing blocks, NO new ids; deterministic `vegRandom` salt 4; chunk-border-clamped like the tree). **The M1 coverage lesson applied AGAIN: the snow pines were off the diorama frame (gate 14/14 unchanged = zero coverage) → node-probed the nearest solid snowfield ([0,-40], ~95% snow) + added a `biome-snow` camera-override fixture (default pose restored → the 14 downstream frames byte-identical); HD-eyeballed (evergreens on a snowy highland).** 911 unit (+10) · build · visual 14→**15/15**. Commits: T1 (pineShape+branch) · T2 (gate) · T3 (fixture+baseline). NEXT = the interleave is DUE (recommend biome-ambient audio) OR world-M5 (ocean visual).
>
> **🔁 LOOP ITER 113 (2026-06-13, ultracode): the WORLD-M4 SPLIT + the M4a (biome foliage) plan doc SHIPPED.** Grounded the M4 seams (read `proceduralTextures.js` fully — the DataArrayTexture is `numLayers=10`, each layer index=blockType, authored as ~10-line `setPixel` blocks; read the height/biome order + the foliage pass). **Decided the M4 SPLIT (charter too-big→split): M4a foliage (lowest-risk, no new blocks/topography → no biome-border cliffs) → M4b palette (the numLayers++/32×32-layer DataArrayTexture work) → M4c topography (risky — discrete per-biome heightScale = ugly cliffs at biome borders; needs a continuous/blended curve, design-gate).** Authored the M4a plan: a greenfield pure `world/foliage.js` `pineShape` (tapered conical evergreen, unit-testable) + a snow-pine branch (snow biome was BARREN) + a `biome-snow` capture fixture (the M1 off-frame-coverage lesson). Also TRIMMED the bloated Current-Task pointer (the M2/M3 SHIPPED detail now lives only in banners + plan docs). NEXT UNIT = BUILD M4a T1 (the pineShape module + worker branch).
>
> **🔁 LOOP ITER 112 (2026-06-13, ultracode): 🗺️ WORLD-M3 (biome-table refactor) SHIPPED — byte-identical.** `world/biomeTable.js` (greenfield: `BIOMES` map + `pickBiome`) replaced the inline 3-branch (terrain.worker.js:396-404) with `let {surfaceBlock,secondaryBlock} = pickBiome(temperature,moisture,continent)`; the beach override stays untouched after it (the `let` preserves its reassign). **Byte-identical confirmed TWO ways: an oracle-grid characterization (pickBiome ≡ the old branch over a fine (t,m) grid) AND the visual gate holding 14/14 with ZERO diff (the render-level proof — the chunk arrays are unchanged).** 4 characterization + 3 static-gate tests. 901 unit (+7) · build · visual 14/14 (no re-baseline — look-neutral by design). Commits: T1 (biomeTable+wiring) · T2 (gate). The clean M4 seam: pickBiome is where biome columns (heightScale/foliage/seabed) get added. NEXT UNIT = the M4 (biome-distinctness — first GATED look-changing milestone; the numLayers++/32×32-layer block-id reality) plan doc.
>
> **🔁 LOOP ITER 111 (2026-06-13, ultracode): the WORLD-M3 (biome-table refactor) plan doc SHIPPED.** Authored `docs/superpowers/plans/2026-06-13-crafty-world-m3-biome-table.md` (writing-plans, 4 tasks, placeholder-free). The byte-identical refactor: a greenfield `world/biomeTable.js` (BIOMES map + `pickBiome`) replaces the inline 3-branch (terrain.worker.js:396-404); beach override :406-409 stays untouched (the `let` binding preserves its reassign; pickBiome runs BEFORE it — the M2 order lesson). **The byte-identical PROOF = pickBiome reproduces the old branch over a fine (temperature,moisture) oracle grid (genChunk is otherwise untouched), AND the visual gate staying 14/14 with NO diff is the render-level proof — a diff = STOP+reconcile, NOT a re-baseline (this is the one milestone that proves NOTHING changed).** NEXT UNIT = BUILD T1 (the module + worker wiring).
>
> **🔁 LOOP ITER 110 (2026-06-13, ultracode): 🌊 WORLD-M2 (ocean depth + coastline consts) SHIPPED.** `world/oceanProfile.js` (greenfield pure module: SEA_LEVEL=28 / BEACH_BAND_TOP=30 — two distinct consts / DEEP_FLOOR=6 + `oceanSurfaceY`) wired into the worker at all 4 sites (ocean branch / beach / water fill / foliage gate); water `castShadow` OFF. **Verify-the-real-field paid off again: a node probe of the actual noise field (not just the unit test) caught an n-overshoot to ~[-0.1,1.1] pushing the deepest seabed to depth 23 past the claimed 18-22 → clamped n → STRICTLY 18-22.** Probe also confirmed oceans = 37% of the world, depth p50=20 — deep divable basins are real + substantial. 7 behavioral + 4 static-gate tests. 897 unit (+11) · build · visual **14/14 (no re-baseline** — the deep seabed is sub-surface; the water-shadow removal stayed sub-6%). Commits: c1eeb2e(T1) · 380d9c5(T2) · d970d6a(clamp). NEXT UNIT = the M3 (biome-table refactor, byte-identical) plan doc → build.
>
> **🔁 LOOP ITER 109 (2026-06-13, ultracode): the WORLD-M2 (ocean depth + coastline consts) plan doc SHIPPED.** Authored `docs/superpowers/plans/2026-06-13-crafty-world-m2-ocean-consts.md` (writing-plans, 4 TDD tasks, placeholder-free). Plan-time live-seam verify re-confirmed the worker lines (all +1 vs spec from the M1 import) AND resolved two M1-lesson design questions before trusting the spec: (1) **the CA cave pass carves y<20** — a deep seabed (y6-10) sits in that zone, but a FLAT seabed block keeps ~18 solid neighbors (survives); only slopes nibble, and the CURRENT shallow seabed (12-24) already overlaps the CA zone → deepening adds no new bug class (flag for M5, measure-first). (2) **the deep seabed is SUB-SURFACE** (water surface stays y28) → deepening isn't skyline-visible; the gate-visible delta is the water-castShadow removal → unit tests (depth 18-22 invariant) are M2's primary verification, no new fixture until M5's visible seabed work. Design: a small greenfield `world/oceanProfile.js` (consts + `oceanSurfaceY`) — a TARGETED extraction of just the ocean formula, NOT the biome branch (that's M3's byte-identical refactor). NEXT UNIT = BUILD T1 (the pure module + worker wiring).
>
> **🔁 LOOP ITER 108 (2026-06-13, ultracode): 🏆 WORLD-M1 (HOME ANCHOR) SHIPPED — plinth + lodge + a review-caught pit-bug fix.** Built T1 (`world/homeAnchor.js` `stampHomeAnchor` — flattens the 4 origin chunks to a crafted pad, deterministic/no-RNG, AFTER foliage BEFORE player-mod-replay, 6 characterization tests) + T2 (`<HomeAnchorRender/>` voxelKit lodge+brazier, brazier capture-null, 6 static gates). **Visual verification then caught TWO defects the batteries couldn't: (1) the spec's "~Y30 origin" was WRONG (real ≈Y50, computed against the worldgen formula) → `HEARTH_Y=32` carved a sunken PIT; fixed → 56 (a raised crafted pad, eyeballed in-world: grey stone plinth + red-roofed lodge + brazier, glow null-in-capture). (2) the 13 fixtures are sky-lane/HUD — NONE frames origin ground, so the Hearth had ZERO gate coverage (iter-101 vacuous-gate, new form); added a `hearth` capture fixture (camera-override pose, default restored after → the 13 downstream frames byte-identical).** 886 unit (+12) · build · visual 13→**14/14**. Commits: T1 · T2 · 82d3523 (fix+fixture). NEXT UNIT = the M2 (ocean consts) plan doc → build.
>
> **🔁 LOOP ITER 107 (2026-06-13, ultracode): the WORLD-M1 (Home Anchor) plan doc SHIPPED.** Authored `docs/superpowers/plans/2026-06-13-crafty-world-m1-home-anchor.md` (writing-plans discipline, 4 TDD tasks, placeholder-free). Plan-time live-seam verification re-confirmed every line# (worker gen order, BLOCK_COLORS ids 1-9, TreasureChestsRender :316-359/:779, voxelKit API) AND resolved the one correctness risk: the spawn probe is height-agnostic (`groundY+1.2`), so a HEARTH_Y=32 plinth is found+stood-on with NO spawn change — confirming the spec's "drop the spawn-fix claim" correction. Design call: `stampHomeAnchor` is GREENFIELD → authored as a PURE importable module (`world/homeAnchor.js`) from birth (fully TDD-able, zero extraction risk on the 753-LOC worker). Pier/hamlet deferred-not-floated (no water near origin pre-M2). NEXT UNIT = BUILD T1 (the pure module + worker wiring — lowest-risk).
>
> **🔁 LOOP ITER 106 (2026-06-13, ultracode): WORLD-DESIGN design-gated + spec committed.** A 3-phase grounded workflow (survey the LIVE engine → design → adversarial critique; 6 agents/600k tokens) → I synthesized the committed "Anchored Infinite" HYBRID spec (4 pieces, each hooking a cited terrain.worker.js/Terrain.jsx seam — all gen-time/static-R3F → NO-RE-MESH + capture-deterministic, a layer ON the engine not a rewrite). The critique caught real design errors I folded in (the block-id/texture-layer reality; M1 plinth-not-spawn-fix; SEA_LEVEL≠BEACH two-consts; depth-tint vWorldY; landmark cap; voxelKit-not-PBR). 6-milestone ladder, lowest-risk-first (M1 = the Home Anchor). NEXT UNIT = the M1 (Home Anchor) plan doc → build.
>
> **🔁 LOOP ITER 105 (2026-06-13, ultracode): 🏆 S3-M4 COMPLETE — AGF DELETED + a review-caught BLOCKING crash fixed.** Built T4 (BossEntity + destroyVoxelsInRadius → render/, 3 char-render repoints, gated boss baselines held byte-exact) + T5 (deleted AdvancedGameFeatures.jsx, 1397→0; allegiance file-list follows the moved worldBlocks code). The boss-finale adversarial review then caught THE BIG ONE: the app had been CRASHING at mount since iter 101 (orphaned SPELL_UPGRADES — a module-local const that didn't move with its hook) — which SILENTLY VACUOUS-ED the visual gate (capture timed out → diff compared stale current/) across parts 2-5; I'd misread the iter-102 capture timeout as "flaky." FIXED (table restored to world/spellUpgrades.js) + the boss gate de-shadowed; now LIVE-verified (capture completes, 13/13 fresh). 874 unit/105 · build · visual 13/13 (LIVE). NEXT UNIT = the WORLD-DESIGN pass (Kevin's HYBRID) — a §4 design-gate workflow first.
>
> **🔁 LOOP ITER 104 (2026-06-13, ultracode): boss finale T1-T3 SHIPPED (config + system + healthbar).** BOSS_CONFIG → game/bossConfig.js (dd4091e) · useBossSystem + dangerLevel bridge → world/bossSystem.js (e0a1e46, danger-bridge ×2 repoint) · BossHealthBar → ui/ (9a7872c). All byte-exact, 874/105/build/13. **Build-time catch: a non-exported `destroyVoxelsInRadius` helper (boss terrain-destruction, postMessage) sits between BossHealthBar and BossEntity — the part-4 plan missed it; it MUST move WITH BossEntity in T4 (verified not on the elemancer-noremesh forbidden-list).** AGF down to 527 LOC (helper + BossEntity). NEXT UNIT = T4 (BossEntity + the helper → render/BossEntity.jsx, the 3 character-render repoints, the gated boss-baseline verify) + T5 (delete AGF + review + close S3-M4).
>
> **🔁 LOOP ITER 103 (2026-06-13, ultracode): the S3-M4 part 4 (BOSS finale) plan doc SHIPPED.** Deep boss recon: the split (game/bossConfig.js + world/bossSystem.js + ui/BossHealthBar.jsx + render/BossEntity.jsx), the 5 gate-assertion repoints (character-render ×3 → BossEntity.jsx incl. the trap-3 indexOf; danger-bridge ×2 → bossSystem.js), the BOSS_CONFIG-staging order, the gated boss-obsidian/boss-closeup baseline-verify (measure, don't trust), and the milestone close (DELETE AGF after 0 importers + drop it from the allegiance worldBlocks file-list + an adversarial review). NEXT UNIT = BUILD part 4 T1→T5 — the risky finale; after it AGF dissolves entirely (1397→0).
>
> **🔁 LOOP ITER 102 (2026-06-13, ultracode): S3-M4 PART 3 (the pet domain) SHIPPED — plan + build same iteration.** usePetSystem → world/petSystem.js, PetIndicator → ui/PetIndicator.jsx, PetEntities → render/PetEntities.jsx (306f4e2), byte-exact + capture-clean, no dead imports, no gate repoint (allegiance unaffected — its AGF ref is the worldBlocks check). AGF 994→689 LOC (boss-only; 51% of the original 1397 cleared across parts 1-3). 874 unit/105 · build · visual 13/13. NEXT UNIT = the S3-M4 part 4 (boss) plan doc — the reviewed finale (trap-3 indexOf + the gated boss baselines + danger-bridge-gate).
>
> **🔁 LOOP ITER 101 (2026-06-13, ultracode): S3-M4 PART 2 (survival + the upgrades hook) SHIPPED — plan + build same iteration.** Authored the part-2 plan (recon: pets/survival have NO capture fixtures → capture-clean; boss IS in gated baselines → deferred to part 4) then built it: T1 survival → world/survivalSystem.js + ui/SurvivalWarning.jsx (f2bdc10; Toast pruned; siege-gate trap-1 repoint) + T2 useSpellUpgrades → world/spellUpgrades.js (1e43bf2). Both byte-exact + capture-clean. AGF 1120→994 LOC. 874 unit/105 · build · visual 13/13. NEXT UNIT = the S3-M4 part 3 (pets) plan doc.
>
> **🔁 LOOP ITER 100 (2026-06-13, ultracode): S3-M4 PART 1 (the AGF safe strips) SHIPPED.** Built from the iter-99 plan: T1 the 2 talent/chest panels → ui/ (a9ceed8) + T2 the 3 accrual hooks → world/accrualHooks.js (def1e37), both byte-exact (anchored slice), each with its trap-1 gate repoint, 9 dead imports pruned (each grep-verified zero-use), the boss indexOf gate (trap 3) verified untouched. AGF 1397→1120 LOC; capture-clean (no re-baseline). 874 unit/105 · build · visual 13/13. The proven safe-strip pattern made a full review unnecessary for this surface; part 2's stateful cores WILL get one. NEXT UNIT = the S3-M4 part 2 plan doc.
>
> **🔁 LOOP ITER 99 (2026-06-13, ultracode): the S3-M4 (AGF dissolve part 1) plan doc SHIPPED + the world-design gap captured for Kevin.** Kevin asked when the master plan addresses the landscape/oceans → I found a real gap (the "world-design/landmarks pass" was scheduled late-S2 but slipped; no world/terrain/ocean spec exists) + captured a decision-ready KRB entry (#11, fork A/B/C, hybrid-recommended) — he re-fired the STANDARD loop (no redirect), so I continued standard priority. Authored the S3-M4 plan: AGF mapped (5 export-groups / 4 domains); part 1 scoped to the SAFE strips (2 panels → ui/, accrual hooks → world/) with both trap-1 gate repoints pre-specified + the boss indexOf gate verified untouched. NEXT UNIT = BUILD S3-M4 part 1 T1→T3.
>
> **🔁 LOOP ITER 98 (2026-06-13, ultracode): UX-legibility T4+T5 SHIPPED + reviewed — the interleave is COMPLETE.** T4: the controls panel renders from the keyMap SoT (grouped keycap-badge, teaches R/V/X/Z + honest M), curated tight via panelHide. The re-baseline was the process-discipline highlight — a blind `visual:baseline` mixed the change with sub-6% capture noise on unrelated states, so I reverted, gate-isolated the truly-affected states, and HD-eyeballed before baselining. A 3-lens adversarial review (wf_9124f8a4) then caught a re-baseline SCOPE bug: the unconditional-HUD panel ALSO shifts boss/inventory/achievements (passing only at a fragile sub-6% margin, their baselines still depicting the old M-lie) — FIXED (8 HUD states re-baselined, HD-eyeballed in-context). 1 LOW (aspect-underbanked) deferred with rationale. T5 close-out: plan ✅, spec SHIPPED, CHANGELOG, SOTA ledger UX@98. 874 unit/105 · build · visual 13/13. NEXT UNIT = S3-M4 (AGF dissolve part 1).
>
> **🔁 LOOP ITER 97 (2026-06-13, ultracode): UX-legibility T1+T2+T3 SHIPPED (the correctness trifecta).** Built from the iter-96 plan: keyMap SoT + the anti-drift gate (the lie is now un-reintroducible) + the KeyM fix (a real advertised-but-dead control, now live) + denied-action feedback (no-mana cast + the 4 locked Aspect verbs now teach the unlock path, capture-safe + throttled). One honest design refinement recorded: the verb-denial hooks the KEYDOWN talent-check (clean, no SM surgery) not the gate-definition sites. 874 unit/105 files (+8) · build · visual 13/13 (toasts self-null in capture → zero baseline drift). NEXT UNIT = T4 (the panel redesign + the deliberate HD-eyeballed explore re-baseline) + T5 (close-out + adversarial review).
>
<!-- prior current-task (S3-M3, shipped) preserved in the iter-95 banner below -->
### (archived) S3-M3 ✅ SHIPPED + adversarially reviewed CLEAN (loop iter 95, ultracode)
All 3 tasks landed: T1 ui/ panels (9519745) · T2 MOB_TYPES → `game/mobTypes.js` + mobVariety textual→live (a74836e) · T3 the 5 leaf VFX renderers → `render/combatVfx` + `render/pickupVfx`, byte-exact anchored slice, the trap-1 loot-juice repoint + the new vfx-extraction capture-freeze gate (fb49d19). SimplifiedNPCSystem −10,984 chars. A 4-lens adversarial-review Workflow returned CLEAN — zero blocking/high/medium. T4 doc-currency: plan ✅, spec row ✅, the missing S3 CHANGELOG entry written, SOTA §3 refreshed (3 milestones stale). 866 unit/103 files · 26 gate files · build · visual 13/13. All 3 tasks landed: T1 ui/ panels (9519745) · T2 MOB_TYPES → `game/mobTypes.js` + mobVariety textual→live (a74836e) · T3 the 5 leaf VFX renderers → `render/combatVfx` + `render/pickupVfx`, byte-exact anchored slice, the trap-1 loot-juice repoint + the new vfx-extraction capture-freeze gate (fb49d19). SimplifiedNPCSystem −10,984 chars. A **4-lens adversarial-review Workflow** (byte-equality / imports / gate-integrity / runtime-wiring) returned CLEAN — zero blocking/high/medium; the only LOW items (stale ACTIVE_PLAN + 2 whitespace gaps) were fixed in this close-out. T4 doc-currency DONE: plan ✅ SHIPPED-bannered; spec S3-M3 row ✅; the **missing S3 CHANGELOG entry** written (M1/M2/M3 were all un-logged); **SOTA §3 refreshed** (it was 3 milestones stale — the doc-drift the charter warns about, caught in this ~5-iter sweep). 866 unit/103 files · 26 gate files · build · visual 13/13. **RESUME = the EXPERIENCE INTERLEAVE (DUE — ledger: content @83, audio @93; the every-2-3-milestone cadence puts it at THIS M3/M4 boundary).** Candidates (pick one via the §4 design self-gate, reference-lock first, then a plan doc): (a) **a night-siege juice pass** (siegeParams already ramps hostileChance with nightCount — add the FELT escalation: a dusk-warning audio sting + a per-wave screen/camera cue + intensifying ambient), (b) **a per-element projectile-geometry variety pass** (fireball/iceball/lightning/arcane each get a distinct mesh — the `game/spellVisualProfiles.js` module from S3-M2 is the clean seam; visual SOTA), (c) **a UX-legibility sweep** (#71). THEN S3-M4 (the AdvancedGameFeatures dissolve part 1 — 6 gate files incl. 2 position-sensitive indexOf gates; the NPC-lens unit map governs).

> **🔁 LOOP ITER 96 (2026-06-13, ultracode): the experience interleave DESIGN-GATED via a workflow.** Ran a 3-candidate grounded design-gate (each seam-mapped vs LIVE code, then adversarially ranked + grounded): UX-legibility WON (8.7) over night-siege juice (7.6) + projectile geometry (6.4) — it's the only untouched experience axis and fixes 3 VBA-verified wounds (the M-lie real bug, the undiscoverable R/V/X/Z Aspects, silent denied actions). Committed the spec (the §4 hard-gate) + the TDD plan. The headline self-gate call: always-visible tight controls redesign (NOT collapse-by-default) → a deliberate explore re-baseline, because a legibility pass must keep controls SEEN. NEXT UNIT = BUILD the UX-legibility plan T1→T5.
>
> **🔁 LOOP ITER 95 (2026-06-13, ultracode): S3-M3 FINISHED end-to-end + adversarially reviewed CLEAN.** Picked "finish the in-flight milestone" (charter #2): T2 (MOB_TYPES → game/mobTypes.js, the variety characterization went live) + T3 (the 5 leaf VFX renderers → render/ via a byte-exact anchored slice — the safe method since NONE of them appear in the visual baselines, so a transcription error would be gate-invisible) + the trap-1 loot-juice repoint + a new capture-freeze gate. A 4-lens adversarial-review Workflow (wf_436d9076-303; 4 agents / ~360k tokens / 7 min) verified the whole delta CLEAN: byte-equality confirmed (diffed pre-move slices), imports/circular-deps clean, gates faithful, wiring intact — zero blocking/high/medium; the 2 LOW cosmetic items (whitespace gaps) fixed in close-out. T4 doc-currency: plan ✅, spec row ✅, the missing S3 CHANGELOG entry written, SOTA §3 refreshed (it was 3 milestones stale — caught in this ~5-iter sweep). 866 unit/103 files · 26 gates · build · visual 13/13. NEXT UNIT = the experience interleave (content @83, audio @93 — due), then S3-M4.
>
> **🔁 LOOP ITER 94 (2026-06-13): loop RESUMED after Kevin's M3-Max restart — the S3-M3 plan doc + T1 SHIPPED.** Plan-time verification pinned the whole milestone placeholder-free: `getItemRarity` is NOT NPC-internal (it's a `data/items.js` re-export → render/ files import it directly, NO circular import), the trap-1 fire is exactly loot-juice line 18 (`rarityBeam(rarity)`, LootDropRender-only — the LootSystem branch uses `rarityBeam(getItemRarity(...))` so line 16's import survives), and the trap-3 capture anchors (:824 worker / :1248 LootSystem) belong to systems that STAY (M6). T1 (the zero-gate UI strips) executed same iteration; the post-restart toolchain is healthy (battery ran clean). 861 unit/102 files · build · visual 13/13. NEXT UNIT = T2 (MOB_TYPES + the live characterization) then T3 (the VFX renderers + the trap-1 repoint + the new gate).
>
> **🔁 LOOP ITER 93 (2026-06-11): the motif interleave SHIPPED + the loop paused clean on Kevin's restart request.** The stingers: melodic, in-identity, signature-moment-only, rare by design (10s guards / natural price-rarity / once-per-day). The scoreboard at the pause: 93 iterations · 861 unit (115 files) · 23 gates · 13/13 · the four-Aspect spine + Kevin's fix arc + the Aspect-UX pass + 2.5 de-monolith milestones + 3 interleaves all shipped; main == origin/main, tree clean.

> **🔁 LOOP ITER 92 (2026-06-11): S3-M2 CLOSED.** The de-monolith scoreboard so far: SoundManager 1036→~560 · EMS 1103→902 · the damage path + DSP + chain targeting all characterized for the first time. The milestone's best moment: the TDD red exposed my WRONG mental model of the live falloff (characterization pinned reality, the docstring states the quirk precisely). NEXT UNIT = the music-motif interleave (design note → build).

> **🔁 LOOP ITER 91 (2026-06-11): M2 T1+T2 SHIPPED.** The data pulls landed with one honest catch: the spell-vfx gate FAILED on the moved literals — exactly the trap-1 class the S3 spec predicted; my plan-time verify had sampled only the call-pins. The gate did its job; the repoint preserves its full intent. Unit 851→**855** (112 files). NEXT UNIT = T3 (the chain-lightning pure pull).

> **🔁 LOOP ITER 90 (2026-06-11): the M2 plan + T0 SHIPPED.** Unit 844→**851**. NEXT UNIT = T1+T2 (the data moves — one iteration), then T3.

> **🔁 LOOP ITER 89 (2026-06-11): S3-M1 SHIPPED — the de-monolith pattern is proven.** The first god-file cut: verbatim factory moves behind ONE registry, the characterization that only extraction made possible, the gate following same-commit. Two build-time corrections recorded honestly (the 26-vs-24 miscount; the fake-ctx float truncation). Unit 837→**844** (109 files) · build · visual 13/13. NEXT UNIT = the S3-M2 plan doc.

> **🔁 LOOP ITER 88 (2026-06-11): the S3-M1 plan doc SHIPPED.** The plan-time verification confirmed the cleanest possible first cut: every DSP closure parameterizes on exactly one thing (the ctx), and the one by-path gate's repoint shape is pre-written. NEXT UNIT = T1 (the extraction + the characterization, one commit).

> **🔁 LOOP ITER 87 (2026-06-11): the S3 design-of-record SHIPPED.** The ranking's logic recorded: SoundManager first because it's the SAFEST cut (zero R3F/visual exposure, one gate) with the LARGEST characterization delta (the DSP closures go 0%→covered, and only extraction makes them testable); the NPC file last because it's the most by-path-pinned in the repo (8 gates + a live import + the textual characterization) with the worker seam. The trap catalog is the milestone checklist. NEXT UNIT = the S3-M1 plan doc.

> **🔁 LOOP ITER 86 (2026-06-11): the S3 scoping workflow LAUNCHED.** The lens briefs encode the stream rules: extraction-only, the proven game/*.js pure-pull pattern (15+ Aspect-era extractions), characterization-before-de-monolith (charter §2.6), and the literal-token gate trap (gates reference files BY PATH — moves must update the lists same-commit). Main green at af4d53f (837 unit · build · 13/13). NEXT UNIT = the S3 spec synthesis off the findings.

> **🔁 LOOP ITER 85 (2026-06-11): challenge-memory EXECUTED (after 3 postponements).** Every stale surface refreshed: the charter no longer points at a completed spine; ROADMAP/ARCHITECTURE carry the Aspect era; the auto-memory is current (the noclobber trap bit once — caught because the battery habit checks). The direction decision RECORDED: S3 de-monolith (no external gate, taxes everything) > B4-v1.5 (flavor, no urgency) > S4 (Kevin-gated). NEXT UNIT = the S3 scoping workflow.

> **🔁 LOOP ITER 84 (2026-06-11): the interleave closed with the judge.** The card verified the skitterling (live spider legs — the legMode wiring proven visually) + the brute's mass; the iteration's FINDING: MobModel has exactly TWO leg modes (spider/default-quad) — 'quad' labels are descriptive, and the duskhound's front-on read is the card's framing hiding the long Z-body (recorded as an instrument limitation). 837 unit · build · 13/13. NEXT UNIT = challenge-memory (then the direction decision).

> **🔁 LOOP ITER 83 (2026-06-11): the variety-pass build SHIPPED.** Three new mobs stretch the procedural axes with zero renderer work (the entity-first dims + legMode seams from B3 paid off — the registry just FEEDS them); spawning is finally weighted; the FUSE roster grows to four hybrids. Unit 831→**837** (106 files) · build · visual 13/13. NEXT UNIT = the look judge + KRB, then challenge-memory.

> **🔁 LOOP ITER 82 (2026-06-11): M7 + THE SPINE CLOSED.** The budget table (zones = priced area-denial + CC — burning ~13 dps in-zone, conductive ~20 bounded by ttl+price, frozen 0.4× the tactical star; never out-DPSing the player; the four dampers hold); the reagent fold to v1.5 exercised the pre-agreed clause WITH its reasoning (a new block TYPE = atlas+worker work — the risk-class this Aspect existed to avoid); the 🏆 banners across spec/SOTA/CHANGELOG/KRB (the playtest protocol filed). NEXT UNIT = the interleave (lean content-variety: new mob types).

> **🔁 LOOP ITER 81 (2026-06-11): M6 CLOSED with the look judged.** The card verified the four element colors + radii AND caught the additive wash-out over bright backdrops; the one permitted iteration landed the INK SKIRT (the toon answer: additive over ink pops) — a real look improvement that grounds every zone decal on any surface. The judge driver (the proven capture.mjs recipe verbatim — the first attempt diverged on 3 details and timed out; the recipe is LOAD-BEARING) now lives at scripts/visual/drive-elemancer.mjs. **B4 stands M1-M6 of 7 — one milestone from the full spine.** NEXT UNIT = the M7 plan doc.

> **🔁 LOOP ITER 80 (2026-06-11): M6 T2+T3 SHIPPED.** The four voices (noise-whoosh ignite / crystalline freeze / sawtooth zap / the fifth-apart rune hum — the catalyst bends, never bites) wired at the bridge's spawn moment (guarded; refresh replays accepted; annihilation = silent steam); the Resonance bar lands as the 4th meter twin with the review-resolved StatBar delta. Unit 831 (103 files) · build · visual 13/13. NEXT UNIT = T4 (the judge card — the look milestone's verdict moment).

> **🔁 LOOP ITER 79 (2026-06-11): the Aspect-UX unit SHIPPED — Kevin's arc closed.** The diagnosis: the talent panel presented the Aspects as stat columns under stale pre-Aspect copy; nothing stated the four-powers/four-keys/four-meters model. The fix lives where the player already looks: a guide card atop each column (key chip + meter source + the 3-4 step loop, every mechanic verified against the shipped modules). The first-unlock toast CUT with a recorded reduction (unlocks happen directly under the card). Unit 827→**831** (102 files) · build · visual 13/13. NEXT UNIT = M6 T2 (the voices).

> **🔁 LOOP ITER 78 (2026-06-11): C5+C6 SHIPPED — the Kevin fix arc COMPLETE.** ESC now means pause via the unlock TRANSITION (the keydown is browser-consumed while locked — the old branch was unreachable; this WAS the "ESC menu" confusion); the title menu can never occlude the DeathScreen (gated + tested); intents reset on death. The iteration's own catch: a missing useRef import in App — builds clean, kills the page at mount; the visual gate's zero-captures timeout flagged it (the 2nd builds-clean-crashes-at-mount catch this arc — the gate is the real hero of this bug class). Unit 826→**827** · build · visual 13/13. NEXT UNIT = the Aspect-UX design note + build.

> **🔁 LOOP ITER 77 (2026-06-11): C3+C4 SHIPPED.** A process note worth keeping: the single-authority gate (input-abstraction-gates) caught the literal token 'pointerlockchange' in my own C3 COMMENT — the strict-literal gate family keeps proving itself (reworded, green). Battery: 826 unit (100 files) · build · visual 13/13 at both commits. NEXT UNIT = C5+C6 (the panel-flag store migration is the prerequisite for a correct unlock-transition handler — do it first).

> **🔁 LOOP ITER 76 (2026-06-11): the fix plan + C1+C2 SHIPPED.** The marquee diagnosis (workflow wf_1a0b03d0-b93, 11 root causes, all code-traced): the respawn freeze was a CAMERA-keyed chunk-streamer deadlock — the body teleported home, the camera stayed at the corpse, origin chunks never loaded, the ground probe nulled forever, and the frame loop starved before movement while the window-level fire path kept routing (the gate asymmetry Kevin felt). Battery green at both commits: 826 unit (100 files) · build · visual 13/13. NEXT UNIT = C3+C4 (one iteration), then C5+C6.

> **🔁 LOOP ITER 75 (2026-06-11): M6-T1 landed review-hardened + the loop pivoted to Kevin's live feedback.** The 2-lens plan review (NEEDS_FIXES ×2) caught 4 would-have-shipped bugs BEFORE build: the mount-site trap (rings riding the player), the r172 instanceColor null-deref (would have killed all 13 baselines), the rotated-parent world-swizzle, and the dawn char-diff race — all baked into T1's first commit. Unit 825→**826** (100 files) · build · visual 13/13. NEXT UNIT = the Kevin-fix plan off the diagnostic findings.

> **🔁 LOOP ITER 74 (2026-06-10, resumed after an ECONNRESET that ate only the iter-73 report): the M6 plan doc SHIPPED + the review workflow LAUNCHED.** The load-bearing architectural call recorded: GATE THE LOGIC, NOT THE RENDER — the capture-gated bridge keeps liveZones empty in every normal capture (13 baselines safe with an un-gated renderer), which is exactly what lets the showcase inject zones and draw them in capture; the pulse freezes via now=0. NEXT UNIT = findings → T1.

> **🔁 LOOP ITER 73 (2026-06-10): M5 CLOSED (iters 71-73, plan→build).** T3 (the impact half) + T4 (the tell + the resonance token chain). **A build lesson worth keeping: the first T4 wiring landed the imbueArmed selector and its consumer in DIFFERENT components — an undefined identifier that BUILDS CLEAN (esbuild doesn't catch runtime references) and crashes at render; the VISUAL GATE caught it (the capture timeout at primitives-showcase was the symptom), fixed same-iteration.** Unit 825 (99 files) · build · visual 13/13. **B4 stands M1-M5 of 7.** NEXT UNIT = the M6 plan doc (THE LOOK — the reference-lock discipline applies per the charter: pick the zone visual language vs the toon+rim style BEFORE building).

> **🔁 LOOP ITER 72 (2026-06-10): M5 T1+T2 SHIPPED.** The verb's input half is live end-to-end-minus-impact: Z arms (bank ∧ talent), the cast consumes + hands the kind through the cast-arm slot. Unit 822→**825** (99 files) · build · visual 13/13. NEXT UNIT = T3+T4 (the impact half + the tell — one iteration likely).

> **🔁 LOOP ITER 71 (2026-06-10): the M5 plan doc SHIPPED.** The impact seam verified at plan time (createSpellImpact fires at the groundLevel terrain check AND mob hits — BOTH spawn the imbued zone; age-out fizzles). A spec clarification recorded: the design's surfaceHint is NOT needed for zone spawning (the KIND comes from the SPELL, not the surface) — it becomes load-bearing at M6+ for fire-spread along worldBlocks wood; M6 picks it up. NEXT UNIT = M5 T1+T2 (the pure pieces + the Components wiring).

> **🔁 LOOP ITER 70 (2026-06-10, post-compaction): M4 SHIPPED — all 3 tasks one iteration.** Unit 814→**822** (99 files) · build · visual 13/13. **B4 stands M1-M4 of 7: the chemistry CORE is fully live; what remains is the VERB (M5), the LOOK (M6), the close (M7).** NEXT UNIT = the M5 plan doc (the impact-point seam is the one open question — verify it before writing).

> **🔁 LOOP ITER 63 (2026-06-10): B4-M1 SHIPPED.** The design workflow's incidental find fixed (the comma-key reader at the spawn-ground fast path — silent always-miss, raycast-covered) + gated against recurrence; the 'hazard' source enum documented + construction-pinned (a 'player'-filtered economy handler banks zero from a 'hazard' emit — the test fails loudly if a future subscriber forgets the filter). Unit 784→786 (93 files) · build · visual 13/13. NEXT UNIT = the M2 plan doc (note: the imbue SM is simpler than voidhand's — a LATCH not a channel; resonance accrual hooks the mine/place executors where the M1 key-fix already touched).

> **🔁 LOOP ITER 62b (2026-06-10): the ELEMANCER design-of-record SHIPPED.** Synthesis resolutions (recorded): the adversarial lens's IMBUED-CAST ZONES = the buildable core + the experience lens's two honest showpieces (fire face-spread along YOUR built wood [pristine-voxel blindness makes player wood the honest fuel — and the day-build payoff IS the fantasy] + frost plates on real y≤28 water [the trimesh already collides water — mostly visual + one pooled cuboid prop]) + CONDUCT wet-chains + ARCANE = the catalyst rune (the 4th element's identity: bends, doesn't react) + the NOVEL economy (Resonance banks from BUILD verbs — the untaken slot) + KeyZ imbue (completing R/V/X/Z) + white-gold #F5D76E. Workflow incidental finds queued: the comma-key bug (M1), the dead mobSlowEffects writes (M4 replaces+deletes), the explosion N-re-mesh anti-pattern (the v2 batch message retrofits it). 7 milestones; the reagent blocks are the pre-agreed first cut. KRB §8 batch filed.

> **🔁 LOOP ITER 61 (2026-06-10): the feel pass SHIPPED in one iteration** (design note + build + battery). The ally swing pulse (0.2s squash-stretch off the existing lastAllyAttack stamp — zero new fields, capture-safe by the exact-pose branch) + the bind ceremony (an expanding jade halo via a snareChannel one-shot, fired on bind AND the hybrid's birth). KRB cues filed. Unit 783→**784** (93 files) · build · visual 13/13. The interleave ledger: audio @34, feel @61 — the cadence holds.

> **🔁 LOOP ITER 60 (2026-06-10): THE ASPECT CLOSED.** T2 the balance table (verdict: as-specced — squad ≈10-17 effective DPS vs night-1's ~20-mob/0.75-hostile wave; the four dampers: 18m leash · zero banking · zero tanking · the dawn-bled Soul price) + T3 the 🏆 banner across spec/SOTA/CHANGELOG/KRB (incl. the showcase console shortcut for Kevin). **THE SPINE: WILDHEART ✅ · VOIDHAND ✅ · SOULBIND ✅ — three of four Aspects live.** Process note: a wrong-root vitest invocation flashed 56 false failures — re-ran from frontend/ before any reaction (783 green); the battery habit held.

> **🔁 LOOP ITER 59 (2026-06-10): T1 LANDED — the bug family closed + the instrument works.** The second half: applyFusion's hybrid lacked `rotation` (undefined → NaN Euler → invisible) + the renderer-read fields; the iter-58 "dark marrowspinner" was a STRAY SPIDER from a silently-missed capture (gone with the contract fix — strays now 0). The card: all five render, silhouettes distinct (skirmisher/tall/spider-mid/massive-quad/light-spider), the jade family coherent. MINOR look note for KRB: the marrowspinner's head renders dark vs its pale body (reads as 'bone spider' — acceptable; flag for Kevin's taste). 783 unit (92 files) · build · visual 13/13. NEXT UNIT = T2+T3 (both doc-weighted — one iteration).

> **🔁 LOOP ITER 55 (2026-06-10): the M7 plan doc SHIPPED — with the instrument REDEMPTION.** App.jsx already carries a PROVEN sky-studio fixture family (character/boss/spell/loot showcase hooks, x-lanes 0/40/80/120, y=146, capture-first — and the 13 BASELINES prove subjects render in capture this way, so iter-54's "capture suppresses entities" was wrong in some hand-rolled detail). T1 twins lootShowcase verbatim at lane x=160. Flagged at plan time: the TETHER hides under isCaptureMode → the thread may need the non-capture variant of the fixture (both outcomes pre-decided in the plan). NEXT UNIT = M7 T1.

> **🔁 LOOP ITER 54 (2026-06-10): M6 CLOSED with the asterisk + the instrument verdict.** The studio (start→enterCapture, correctly ordered) renders the vista but SUPPRESSES live ecs entities by design — the 4th and final instrument refuted; there is NO existing fixture for deterministic creature look-judging (the forensic series: C-probe phantom/orbit · FPV-controller heading re-assert · spawn-faces-a-cliff · capture-suppression). The RIGHT tool (?showcase=soulbind route) is M7 task 1. M6's record: function PROVEN (census +25 meshes; live fusion), Kevin's 2-min eyeball asked in KRB with cues. Main green: 781 unit (92 files) · build · 13/13. NEXT UNIT = the M7 plan doc.

> **🔁 LOOP ITER 53 (2026-06-10): the suspected ally-render defect is REFUTED with hard evidence** (the census above; M5 vindicated; no code changed — main green at 781 · build · 13/13). Root cause of the scare: a camera-control artifact unique to the plain game (the FPV controller re-asserts heading per frame). The framing-budget rule applied: ONE more studio attempt next iteration, then M6 closes on the honest record either way.

> **🔁 LOOP ITER 52 (2026-06-10): T5 forensics — two solves + one suspected defect.** SOLVED: the dark-quad mystery (= the block-targeting outline, benign) · the judge ENV (plain dev + test bridge + noon). CONFIRMED WORKING LIVE: fusion (applyFusion returned the dreadweaver; SquadAISystem walks it). **SUSPECTED DEFECT: allies/hybrids don't render in prod** — the M5 claim was never visually verified (the iteration's own honesty lesson: suite+build+13/13 ≠ SEEN). No code changed this iteration; main stays green (781 · build · 13/13 as of 45fb207). NEXT UNIT = the render-defect diag + fix (function before look).

> **🔁 LOOP ITER 51 (2026-06-10): M6 T3+T4 SHIPPED — FUSE PLAYS.** The X-key arbiter (snare target WINS; else the fuse channel arms only-when-completable) · applyFusion at the pair midpoint + the down-pitched bind chime · the tether doubles as the fusion thread BETWEEN the creatures · the Soul bar (KineticBar twin, jade, unlock-gated, 4-surface token chain). 781 unit (92 files) · build · visual 13/13. NEXT UNIT = T5 the look judge (fresh budget; injection-based framing is now LEGAL for allies/hybrids — they're worker-invisible by construction).

> **🔁 LOOP ITER 50 (2026-06-10): M6 T1+T2 SHIPPED.** The roster (Dreadweaver/Bonehide Bulwark/Marrowspinner — order-free lookup, midpoint fusion, first-id reuse) + the FUSE reducer (arms only when fusion CAN complete; breaks free; the boundary epsilon inherited). **Verify-at-build catch: MobModel read dims from MOB_TYPES[type]||pig — a hybrid would have rendered AS A PIG**; entity-first fallbacks landed for bodySize/headSize/legMode (baselines untouched — no pre-M6 entity carries them). 781 unit (92 files) · build · visual 13/13. NEXT UNIT = T3+T4 (one iteration), then T5 (the consolidated look judge with fresh budget).

> **🔁 LOOP ITER 49 (2026-06-10): the M6 plan doc SHIPPED.** Design decisions recorded: **the FUSE channel only STARTS when fusion CAN complete** (pair-near + bank + roster entry pre-vetted → the spec's refuse-toast became a start-gate — no consumed-nothing fail state exists); ONE key (X) with a deterministic arbiter (snare target wins); the hybrid spawn reuses the first consumed ally's id (unique by construction, no allocator); the fuse visual reuses the snare tether transient BETWEEN the two allies; the fuse SFX = the bind chime at rate 0.7 (deliberate reuse, M7 may upgrade). NEXT UNIT = M6 T1+T2 (both pure TDD — one iteration), then T3+T4, then T5 (the look judge gets its own iteration with fresh budget).

> **🔁 LOOP ITER 48 (2026-06-10): M5 SHIPPED (plan→build in 2 iterations).** The pure brain (8/8: ring hold-band, player-anchored engage, cooldowns, leash, atomic player-death disengage, dead-target guard) + the 15Hz bridge (ground-snap, damageMob 'ally', victims aggro'd) + the ally render beside the mobs map (the vanish-on-bind gap CLOSED) + the noremesh gate extended. KRB cue corrected honestly. 774 unit (90 files) · build · visual 13/13. **SOULBIND stands M1-M5 of 7.** NEXT UNIT = the M6 plan doc (FUSE is the Aspect's distinctive hook + M6 carries the consolidated look-judge debt).

> **🔁 LOOP ITER 47 (2026-06-10): the M5 plan doc SHIPPED — with an honesty finding.** **A bound ally currently has NO renderer** (conversion exits mobsQuery, whose NPCSystem map was the only render path — the creature VANISHES on bind); M5-T2's render block closes a real gap, and the M4 KRB cue ("stands guard") gets corrected at close-out. The plan is code-complete: the pure brain (FOLLOW ring 3-5m hold-band, ENGAGE player-anchored 18m, ATTACK 2.2m/1.5s-cooldown via lastAllyAttack, LEASH 40m teleport-to-ring, player-dead = atomic empty orders) + the 15Hz bridge (the :820-824 accumulator clone; ground-snap; damageMob 'ally'; victim.isAggro=true) + the ally render = an allies.map beside the mobs.map INSIDE SimplifiedNPCSystem (MobModel stays internal — zero export churn; the jade entity-color from M4 already differentiates; the rim deferred to M6's judge). NEXT UNIT = M5 T1+T2.

> **🔁 LOOP ITER 46 (2026-06-10): M4 CLOSED with the asterisk.** The 6th framing attempt eliminated the held-phantom hypothesis (the dark quad persists with the phantom cleared — it's a pre-existing C-scene element); the STUCK rule parked the aesthetic judge with a full writeup (`.superpowers/s2b3-soulbind-m4-refs/HOW.md`: 3 ranked untested hypotheses, the NDC-projection check first) and FOLDED it into M6's look-judge (where tether + ally tint + hybrids get judged together anyway). Spec M4 row ✅* · plan bannered · KRB playtest cue (honest: 'stands guard until M5'). Process note: zsh noclobber bit AGAIN (use >| — the recorded macOS lesson). NEXT UNIT = the M5 plan doc (the squad-AI brain is pure-module TDD gold: the §4 edge table [player-death disengage, leash, cooldowns] becomes the test list).

> **🔁 LOOP ITER 45 (2026-06-10): M4 T3 SHIPPED with verification honesty.** SnareTetherSystem (transient-driven open cylinder, tightens w/ progress, capture-nulled, noremesh-gated) + the jade weakened tell (health-bar fill at <=30% hostiles). **The MECHANISM is numerically verified live** (scene-traverse diag: visible=true, opacity 0.66 ↔ progress 0.75, scaleY=7.5=length) — the envelope method again — **but the AESTHETIC judge frame is OWED** (4 framing attempts: C's phantom blocks the lens; B stops at the menu; the STUCK rule stopped the spiral). 765 unit (88 files) · build · visual 13/13 — main is GREEN, the debt is a LOOK-judgment, not function.

> **🔁 LOOP ITER 44 (2026-06-10): M4 T1+T2 SHIPPED — SNARE IS PLAYABLE.** KeyX → snare intent (R/V/X row, 13 inputState tests) · the SM block sibling-to-voidhand (per-frame cone targeting, cap 2+pack, bind = debit+captureMob+jade+chime) · snareChannel transient (GLI-clean) · the bind chime (G4→C5 resolve, gate-locked into aspect-sfx NAMES) · the targetId-preservation contract pinned. NEXT UNIT = T3 (the tether + tell — the look, judged in-world; remember the noremesh GATED-list extension for the new system file).

> **🔁 LOOP ITER 43 (2026-06-10): the M4 plan doc SHIPPED.** 4 tasks, the T2 SM block code-complete (per-frame nearest-snareable from the aim cone [12m, π/8, unflattened — the aimedMobDist precedent], squad cap 2+pack, canSnare = bank ∧ talent ∧ cap, bind = debit + captureMob + 45% jade lerp + the chime); snareChannel = the hurlChannel-pattern transient (zero setState/frame); the tether = always-mounted + transient-driven + capture-nulled (the impact-flash recipe); the tell = the mob health bar tints jade at ≤30% (honest "weakened" read, capture-static). **The flagged SM question resolved from the M2 code: 'bind' PRESERVES sm.targetId (only break/cancel null it) → `ssm.targetId` at the apply-site; pin with a soulbind.test.js addition at build.** NEXT UNIT = M4 T1+T2 (the playable core), then T3 (the look, judged) + T4.

> **🔁 LOOP ITER 42 (2026-06-10): M3 SHIPPED (plan→build in 2 iterations).** alliesQuery + convertMobToAlly (4 invariant tests vs the REAL miniplex world incl. the stale-apply-map simulation) + GameMethods.captureMob + the only-door gate (nothing outside allegiance.js may removeComponent isMob). The five hostile surfaces exclude allies by query-construction — pinned so future caching can't silently re-open the stomp. 759 unit (85 files) · build · visual 13/13. NEXT UNIT = the M4 plan doc (the FIRST M4 step must read the Components voidhand SM block + inputState INTENT_KEYS fresh for exact line shapes; M4 is the LOOK+FEEL milestone of the Aspect — ribbon + tell judged in-world per the charter).

> **🔁 LOOP ITER 41 (2026-06-10): the M3 plan doc SHIPPED — with a milestone-shrinking finding.** Fresh read of :750-785: **the apply loop rebuilds its entityMap FRESH from mobsQuery.entities per TICK_RESULT** → removing `isMob` closes the stomp window at BOTH ends in one synchronous miniplex component-swap; the design's feared "serializer + apply loop in the SAME commit" two-ended trap is satisfied BY CONSTRUCTION (and the cull/spawn-cap/minimap/melee-cone all read mobsQuery live too — five surfaces exit atomically). The spec's "static gate" upgraded to a RUNTIME invariant test (simulate the apply map post-conversion — stronger than a grep). Villager blocklisted at the converter (the deepest layer). NEXT UNIT = M3 T1+T2 (small milestone — both fit one iteration; tests vs the real ecs world, plan has full code).

> **🔁 LOOP ITER 40 (2026-06-10): M2 T3+T4+T5 SHIPPED → M2 COMPLETE (iters 38-40).** soulBanked fully wired (store triplet + both load sites + progression slice + dawn bleed + M1-filtered accrual + autosave diff) `ca3cb59` · soulbind_snare/pack nodes `40a7cfb` · spec row + plan bannered. **Test-change-with-justification recorded: the kill-attribution gate's exact-count (2 filters) failed on in-contract growth (the 3rd correctly-filtered meter) → upgraded to the per-subscriber invariant (filtered === subscribers, >=3) — STRONGER + self-extending.** 753 unit (83 files) · build · visual 13/13. NEXT UNIT = the M3 plan doc (the two-ended stomp trap is the critical step — serializer + apply loop excluded in ONE commit; read SimplifiedNPCSystem :650-900 fresh at plan time for exact current lines).

> **🔁 LOOP ITER 39 (2026-06-10): M2 T1+T2 SHIPPED.** soul.js (kinetic twin: 100/35/50, the 8/16/60 gradient, canSnare/canFuse) + soulbind.js (the CHANNEL-shaped SM: aim-validity per frame, breaks FREE, binds arm 1.5s cooldown). 747 unit (81 files) · build clean. **Two recorded process notes: (1) the grep-exit-code chain trap bit AGAIN (commits landed before the failure check — iter 31's lesson not yet habituated) → battery habit changed: explicit failure check BEFORE any commit in the chain; (2) the red was a genuine float-boundary bug** ((10+1.1)-10 < 1.1) — fixed in the SM with a 1e-9 boundary-INCLUSIVE epsilon (implementation fix, the plan's test held). NEXT UNIT = M2 T3+T4 (wiring + talents), then T5 close-out.

> **🔁 LOOP ITER 38 (2026-06-10): the B3-M2 plan doc SHIPPED.** 5 tasks, all code-complete in the plan: T1 soul.js (kinetic copy-rename, +FUSE_COST/canFuse, tests pin 100/35/50 + the 8/16/60 gradient); T2 soulbind.js (CHANNEL-shaped voidhand twin — full implementation in the plan: validity loss = FREE break [no cooldown], completion = bind + 1.5s cooldown, targetId locked at startChannel); T3 store+saveSchema+dawn+useSoulAccrual (locate-then-twin every kineticBanked site — both load sites, autosave diff, the dawn block); T4 soulbind_snare (prereq soulbind_bond — verified to exist at talentTree.js:35) + soulbind_pack (+1 cap, prereq snare). NEXT UNIT = M2 T1+T2 (both pure-module TDD — fit one iteration), then T3+T4 the next.

> **🔁 LOOP ITER 37 (2026-06-10): B3-M1 SHIPPED (T1 `d352a00` + T2 `14aeaf6`).** The bus carries source (additive, default 'player'); ferocity/kinetic/quest credit filter player-only; damageMob's hitstop/shake/XP-orbs are player-gated + the death emit threads source; wiring pinned by `tests/gates/kill-attribution-gates.test.js`. Unit 730→**736** (79 files) · build · visual 13/13. The AFK-farm + kill-your-own-ally exploits are CLOSED before any ally exists. **NEXT UNIT = the B3-M2 plan doc** (consts above are the design §2 numbers — Kevin-tunable; the SM mirrors voidhand.js [charging/heldUntil → channeling/targetId]; the meter mirrors kinetic.js exactly incl. save clamp-on-load + autosave diff + dawn bleed sites).

> **🔁 LOOP ITER 36 (2026-06-10): the B3-M1 plan doc SHIPPED.** Compact 3-task plan: T1 additive `emitMobKill(type,pos,source='player')` (TDD, back-compat default); T2 ferocity/kinetic/quest filters (`source==='player'`) + `damageMob(...,source='player')` gating hitstop/shake/XP-orbs + threading source into the emit + a kill-attribution static gate; T3 close-out. **Design-doc precision fix recorded in the plan's self-review: ally deaths DO emit (with source='ally', so future squad-UI subscribers can react) — the exploit truth is "bank nothing", not "never emit".** NEXT UNIT = M1 T1+T2 (small milestone — both fit one iteration, per-task commits).

> **🔁 LOOP ITER 35 (2026-06-10): the SOULBIND design-of-record SHIPPED.** 4-lens workflow findings (verified, file:line'd): targeting is player-only hardcoded · the 15Hz bridge stomps ANY mobsQuery entity AND the query drives 5 surfaces (serializer/cull/spawn-cap/minimap/melee-cone) → **allies leave the query by archetype (isAlly), exiting all 5 by construction** (two-ended trap: serializer + apply loop in the SAME commit) · the kill bus is attribution-blind → ferocity/kinetic/quests/XP all farmable by ally kills → **M1 = attribution FIRST** · damageMob bleeds hitstop+camera-shake → the source param is load-bearing · pets are cosmetic React-state (KeyT double-bound) → v1 sidesteps, Kevin decides the merge. **Loop-resolved divergences: v1 = "player stays the tank"** (the worker faction protocol = the named v2 seam — it was every lens's schedule risk); KeyX snare + auto-assist (no command UI v1); fusion stays v1 (3 hybrids: Dreadweaver/Bonehide Bulwark/Marrowspinner) as the pre-agreed first cut; jade #3DFFB0; cap 2+1-talent; SNARE 35 / FUSE 50. 7 milestones, exploit-closers first. KRB §8 batch filed. **NEXT UNIT = the B3-M1 plan doc.**

> **🔁 LOOP ITER 34 (2026-06-10): the AUDIO UNIT SHIPPED (charter §2.5 debt PAID).** 5 all-synth builders in the SoundManager idiom: **the roar finally SOUNDS** (owed since B1) + grab (whole-tone motif stinger) / hurl / slam / anvilHit; spatial call sites at beast-enter, the voidhand apply branches, and the anvil impact; wiring pinned by `tests/gates/aspect-sfx-gates.test.js` (headless can't hear → registry/call-site shape = the testable truth); KRB ear-check cue. Unit 727→**730** (78 files) · build · visual 13/13. **NEXT UNIT = the SOULBIND design workflow kickoff** (read SOTA-INITIATIVE §4's B3 line + the S2 spec §4 B3 section first; the design workflow should mirror the WILDHEART 11-agent shape scaled sensibly: seam-mappers vs LIVE code [mobKillBus, capture mechanics vs the beast-capture #64 known flake, mob AI/types, talent tree slot] + adversarial; output = the B3 design-of-record + KRB decision batch).

> **🔁 LOOP ITER 33 (2026-06-10): the audio design pass SHIPPED.** Grounded in the LIVE SoundManager (811 LOC, ALL WebAudio-synth, zero samples: named-buffer registry + mood pad + arpeggiator). **#74 POLICY DECIDED (loop-owned): ALL-SYNTH IN-ENGINE** — procedural timbre IS the sound identity; ElevenLabs/external rejected for v1 (asset pipeline + coherence + spend=Kevin); reversal = S4 side-by-side. **Motif mechanism: Aspect-keyed STINGERS** (wildheart = feral pentatonic low; voidhand = whole-tone shimmer on grab), not a new music system. The §4 table carries exact builders + call sites (roar = OWED since B1 → the beastTransform commit site; grab/hurl/slam → the Components apply branches; anvilHit → HurlSystem, spatial). Verification honesty: headless can't hear → registry static gate + call-site spies + Kevin's ear (KRB).

> **🔁 LOOP ITER 32 (2026-06-10): THE ASPECT CLOSE-OUT SHIPPED.** Spec header carries the 🏆 Aspect-complete banner (full kit summary + parked-to-Kevin list) · SOTA §3 = B2 COMPLETE → audio unit → B3 · CHANGELOG M8+Aspect entry (32 iterations end-to-end, not one re-baseline) · KRB already current (combined playtest cue + iPad protocol + decisions-of-record). **NEXT UNIT = the audio design pass** (read SoundManager + the existing spell/hit SFX paths first; the motif question: WebAudio-synth motifs in-engine vs external assets — Kevin's old note 'ElevenLabs decide-later' is in KRB HISTORICAL; the loop owns the call now per the charter transfer list; AUDIO IS THE MOST-NEGLECTED AXIS per the charter §4 — the bar is significant).

> **🔁 LOOP ITER 31 (2026-06-10): M8 SHIPPED (the question resolved by ENUMERATION).** The worker's REAL type space = 9 (grass/dirt/stone/sand/snow/wood/leaves/trunk/water — enumerated from generation, NOT the collapsed 7-numeric blockIdMap guess) → "8+" satisfied with NO new seam: PHANTOM_BLOCK_COLORS covers all 9 distinct (test-locked; honest reach note: v1 grab resolves the placeable {1,2,3,4,6,7} subset). Feel-distinctness: structural (ballistic+directional vs AoE+radial+1.3×+wide-flash) + the new SLAM camera kick (heavy-verb signature; hurl keeps hitstop). **Process slip recorded: e9aeb4c briefly landed red (the noremesh gate caught the literal worker token in MY DOCSTRING — grep-exit-code chain trap); fixed forward same iteration in 61b8e2f.** 727 unit · build · visual 13/13.

> **🔁 LOOP ITER 30 (2026-06-10): M7-T4 CLOSE-OUT SHIPPED → M7 COMPLETE (iters 26-30).** Design doc ✅ BUILT · spec M7 status + the M8 type-space question recorded · SOTA banner (scale verified live: 22.6k LOC / 128 files / 726 unit / 21 gates) · CHANGELOG (M5/M6/M7 combined entry) · KRB AFTER-frames pointer. **VOIDHAND stands M1-M7 + #72 COMPLETE — one milestone left (M8), then the Aspect close (banner the Aspect, FPV-feel & iPad runs stay parked to Kevin) and the spine moves to SOULBIND (B3) — whose design workflow should ALSO weigh the charter §2.5 interleave rule: 5 milestones since the last SOTA-experience unit; strong candidates = the per-Aspect music motif (#74, loop-owned) or the roar SFX backfill BEFORE B3 starts.**

> **🔁 LOOP ITER 29 (2026-06-10): M7-T3 SHIPPED** — the gold WALL HIT! label via a new `isAnvil` damage-number variant (the proven isXP/spawnXPText pattern: gold gradient #ffe9a3→#ffb300, 3.0×1.5 scale, registered `GameMethods.spawnAnvilText`, fired from the HurlSystem anvil branch next to the bigger flash). 726 unit · build · visual 13/13. **NEXT UNIT = M7-T4 close-out** (the M7 design doc → ✅ BUILT; AFTER frames are already in `.superpowers/s2b2-voidhand-m7-refs/` [after-t1-locked-*, flash-slam-v2-*] — reference them in KRB; spec §12 M7 row ✅; SOTA banner; CHANGELOG). Then **M8 = content enumeration** (spec row: 8+ block types distinct-tinted + HURL vs SLAM feel-distinct — note PHANTOM_BLOCK_COLORS has 6 numeric entries covering the worker's collapsed type space; the enumeration check is whether the TYPE SPACE itself needs widening, an honest spec-vs-reality question for the M8 pass).

> **🔁 LOOP ITER 28 (2026-06-10): M7-T2 SHIPPED + judged.** White-hot additive impact core (always-mounted, transient-driven — zero setState/light-count; capture-inert): slam ~3m-AoE-scale, hurl smaller, anvil 2.2× base. One tune pass after the first in-world judge read undersized (0.12s/1.6→0.18s/3.0). **Judge evidence: numeric envelope (~170ms, op .82→.04, r 1.7→2.6m expanding) + bloom-glint frames** (CDP screenshot latency > flash duration — the envelope method is the judge for sub-200ms VFX, recorded). 726 unit · build · visual 13/13. **NEXT UNIT = M7-T3** (gold WALL HIT! label: damage numbers are CombatSystem-closure-internal {id,damage,type,position} → cheapest seam = a registered `GameMethods.anvilHit(pos)` that CombatSystem consumes into a gold label entry, or extend the damage-number object with `label` via the damageMob path — pick at build; the DamageNumber renderer + its color-by-type live near SimplifiedNPCSystem ~1390).

> **🔁 LOOP ITER 27 (2026-06-10): M7-T1 SHIPPED + judged.** Emissive lift (block tint @0.22) — the grabbed-block identity now reads at night (BEFORE `held-1.png` black face → AFTER `after-t1-locked-1.png` clear green grass inside the violet rim; no bloom blob, rim stays the only glow). **Method upgrade recorded in HOW.md: camera-LOCK via `cam.lookAt(phantomWorldPos)` beats bursting** (deterministic centered frames). 726 unit · build · visual 13/13. **NEXT UNIT = M7-T2** (HurlSystem: a transient one-shot impact flash — small additive sphere/sprite at r.hit.pos, ~120ms decay, white-hot core per the proven SpellProjectileCore recipe; never fires in capture by construction [no clicks]; keep GLI — transient mesh + setState only on flash start/end or a pooled always-mounted flash with scale/opacity driven in useFrame).

> **🔁 LOOP ITER 26 (2026-06-10): M7 reference pass SHIPPED (charter §4 self-gate, judged IN-WORLD).** BEFORE frames captured live (`.superpowers/s2b2-voidhand-m7-refs/held-*.png` via `?perf=C` — discovery: in LIVE mode the SM max-hold drops a store-forced hold on frame 1, so probe mode is the review-frame method; documented in HOW.md). **Verdict: the M1 architecture is 80% of the lock** — night phantom = ink-silhouette + crisp violet rim + violet light pool (KEPT; on-discipline, no blob). Gap: block-face identity dies at night → **DELTA 1 emissive face-lift (~0.22)**; **DELTA 2 impact kit** (white-hot core flash ~120ms + the gold WALL HIT! label via the damage-number channel); element held-tint = NO (spec rec confirmed); grab-tint stays worldBlocks-only v1. **Expected re-baselines: NONE** (phantom self-nulls in capture; the spec's "re-baseline provisional frames" was drift — no voidhand frames were ever gate-tracked, corrected in the design doc §3). KRB before/after entry filed. **NEXT UNIT = M7-T1.**

> **🔁 LOOP ITER 25 (2026-06-10): the M5/M6 spec-vs-reality pass SHIPPED.** M5 locked by `tests/gates/element-impact-gates.test.js` (3 gates: channel carries NO element [the no-desync mechanism] · HurlSystem reads activeSpell at both impact sites · all 4 SPELL_TO_ELEMENT keys have distinct damage-layer cases). M6 reduced honestly: no pool ⇒ no eviction; the real invariants were already covered (death-drop, re-press-drop, single-flight); pool UX deferred to if/when multi-phantom ships. Spec §12 statuses updated IN the header. Unit 723→**726** (77 files). **NEXT UNIT = M7 THE LOOK — design/reference pass first** (spec §5 has the ref-lock decision recs: rim-glow + exact-color-with-faint-violet-tint, element impact-only v1; the M1 placeholder + M4 deferred WALL-HIT flash + the grab-tint partial all converge here; re-baselining the 13 frames is EXPECTED — follow charter §4: render → self-eyeball HD → rationale + KRB entry).

> **🔁 LOOP ITER 24 (2026-06-10): M4 Task 5 CLOSE-OUT SHIPPED → M4 COMPLETE (iters 19-24).** Doc-currency: spec M4 status ✅ (+ the M5/M6 spec-vs-reality flags recorded IN the spec header) · plan ✅ SHIPPED (deviations: save-slice test fix, accrual-smoke-by-precedent) · SOTA banner (scale re-measured live: 22.5k LOC / 128 files / 723 unit / 20 gates) · CHANGELOG milestone entry · KRB anvil playtest cue + M4 decisions-of-record. **VOIDHAND now stands M1-M4 + #72 complete; remaining: M5-verify, M6-invariants, M7 THE LOOK (reference-lock + re-baseline of the provisional frames + the WALL-HIT gold label + pristine-terrain phantom tint decision), M8 content enumeration.** NEXT UNIT = the M5/M6 verify pass (start by reading spec §12 M5/M6 rows + the existing element/spark tests).

> **🔁 LOOP ITER 23 (2026-06-10): M4 Task 4 SHIPPED** — KineticBar (FerocityBar mirror: self-null-at-zero + the voidhand_grasp UNLOCK gate → **visual 13/13 verified, zero diffs, no re-baseline**); token SoT chain extended end-to-end (tokens.js `kinetic:'#B36BFF'` the phantom-rim identity → cssVars → tailwind → StatBar maps); 'GRAB!' label at ≥ GRAB_COST; `force` icon verified in the baked set. Pushed. **NEXT UNIT = M4 Task 5 close-out** (doc-currency + KRB: unlock-gated-bar decision, WALL-HIT label→M7, walls-PRISTINE per Decision #3) → then the spec seq says parallel(M5 element-charge transparency · M6 pool-eviction safety) — M5 is largely DONE-BY-CONSTRUCTION (element read AT IMPACT since M3; verify + test rather than build) so check reality vs the spec's M5 row first.

> **🔁 LOOP ITER 22 (2026-06-10): M4 Task 3 SHIPPED (TDD)** — `resolveAnvil` pure decision (4 stub-ray tests: next-to-wall 3× / open-air 1× / beyond-range 1× / tuning) + `GameMethods.castWorldRay(origin,dir,maxToi)` registered in Terrain (player-filtered, `timeOfImpact`) + HurlSystem hurl-impact = `HURL_DAMAGE × anvil`. Day-building is now a combat weapon (spec §3e closure). Unit 719→**723**. Pushed. **NEXT UNIT = M4 Task 4** (HUD.jsx: kinetic StatBar under ferocity's, gated on `voidhand_grasp` unlocked [loop decision: capture saves lack it → baselines hold]; check StatBar kind→color map for a 'kinetic' entry, voidhand violet #B36BFF family; battery incl. visual 13/13 + accrual smoke).

> **🔁 LOOP ITER 21 (2026-06-10): M4 Task 2 SHIPPED** — `voidhand_grasp` effect-less unlock node (fold-tolerance locked by mirrored regression tests) · SM ctx `canGrab` = bank ≥ GRAB_COST && talent rank > 0 (the M1-OPEN gate is closed) · the 'grab' apply debits GRAB_COST. Unit 717→**719**. Pushed. **NEXT UNIT = M4 Task 3** (plan steps: failing resolveAnvil tests in hurl.test.js → ANVIL_RANGE/ANVIL_MULT + resolveAnvil in hurl.js → castWorldRay registered in Terrain's #72 executor effect [timeOfImpact!] → HurlSystem hurl-impact uses HURL_DAMAGE×anvil).

> **🔁 LOOP ITER 20 (2026-06-10): M4 Task 1 SHIPPED (TDD)** — kineticBanked store twin (set/accrue clamped) + save `progression` slice (saveSchema) + load clamp + load-set inclusion + App autosave diff + `useKineticAccrual` (kill-bus, day-only, capture-gated, mounted) + dawn bleed next to ferocity's. Unit 714→**717**. Test-fix recorded honestly: the save assertion initially targeted game_state, corrected to the progression slice (ferocity's slice = the contract). Pushed. **NEXT UNIT = M4 Task 2** (talentTree.js voidhand_grasp effect-less node after voidhand_crush · Components SM ctx `canGrab: kCanGrab(stv.kineticBanked) && (stv.unlockedTalents?.['voidhand_grasp'] > 0)` · `stv.accrueKinetic(-GRAB_COST)` first line of the 'grab' apply branch · fold-tolerance check).

> **🔁 LOOP ITER 19 (2026-06-10): M4 plan doc SHIPPED** (writing-plans; seams verified at HEAD: ferocity chain store:565-568/load~768/save~821/autosave-diff/useFerocityAccrual:20/dawn-bleed-in-useSurvivalMode/StatBar:47 + talent read pattern :557 + kinetic.js already complete from M1). 5 tasks: T1 store twin+persistence+accrual+dawn-bleed (TDD) · T2 voidhand_grasp node + canGrab composition + GRAB_COST spend · T3 anvil 3× (pure resolveAnvil over an injected ray fn, TDD + GameMethods.castWorldRay registered in Terrain w/ timeOfImpact) · T4 HUD bar **UNLOCK-GATED (loop decision: renders only with voidhand_grasp → visual baselines UNCHANGED, no re-baseline; gold WALL-HIT label → M7)** · T5 close-out. **NEXT UNIT = M4 Task 1.**

> **🔁 LOOP ITER 18 (2026-06-10): M3 Task 5 CLOSE-OUT SHIPPED → M3 COMPLETE (iters 13-18 end-to-end).** Scenario E → REAL hurls (PerfProbeSystem = probe-to-gameplay adapter, Rapier stand-in deleted); **re-gate PASS: C−B 0.00/−0.10ms · E−B 0.00/0.00ms** (`memory/S2B2-M2-PERF.md` §3b addendum); zero-emoji gate caught a `→` in a comment (fixed); doc-currency: spec M3 status ✅ + plan ✅ SHIPPED + SOTA banner (scale re-measured: 22.4k LOC / 128 files / 714 unit / 76 files) + CHANGELOG + **KRB FPV-feel cue** (Kevin's human gate, non-blocking; flags the slam-at-orbit-point taste decision). Evidence: 714 unit · build · visual 13/13. **NEXT UNIT = the M4 plan doc** (inputs: spec §3e anvil one-shot-guard + §3f kinetic economy [Decision #2: mirror Ferocity — full dawn-bleed, no death penalty] + canGrab composition in the SM ctx [currently hardcoded true] + GRAB_COST spend at the apply-site + autosave-diff for kineticBanked + a provisional StatBar like ferocity's).

> **🔁 LOOP ITER 17 (2026-06-10): M3 Task 4 SHIPPED — HURL/SLAM are PLAYABLE.** SM wiring per spec §3d (held clicks → verb edges → SM; melee/spell suppressed while held; slam at phantomWorldPos; grab tint via castBuildRay+worldBlocks). **Smoke-found REAL defect fixed TDD: frame-spike tunneling** (observed dt=0.50s jumped the flight 11m past a 1.4m radius) → `stepHurlChunked` substepped integrator (≤50ms substeps, 0.25s cap, +3 tests). **Verified vs a REAL spawned mob: arc-compensated hurl at a spider @30.3m → 60→30 HP; slam → ×1.3 dmg.** Smoke-harness lesson recorded: hand-injected ECS mobs get positions stomped by the AI bridge → always smoke vs REAL mobs. Evidence: 714 unit · build · 0 __hurlDebug in prod · visual 13/13. **NEXT UNIT = M3 Task 5 close-out** (PerfProbeSystem E → real hurlChannel; run perf B/C/E re-gate; S2B2-M2-PERF addendum; spec/plan/SOTA/CHANGELOG; KRB FPV-feel cue).

> **🔁 LOOP ITER 16 (2026-06-10): M3 Task 3 SHIPPED** — `world/HurlSystem.jsx` (single-flight channel consumer; impact = damageMob + entity.knockback, element AT impact; transient mesh, setState only on membership transitions; capture-inert) + PhantomBlockSystem exports transient `phantomWorldPos` (slam aim) + mounted next to PhantomBlockSystem + gate entry red→green. 711 unit · build · visual 13/13. Pushed. **NEXT UNIT = M3 Task 4** (plan step-by-step in the doc: PHANTOM_BLOCK_COLORS in voidhand.js → voidhandVerbRef + dispatch held-branch → SM ctx feed vAtk/vCast → apply-site 'grab' tint/'hurl' launch/'slam' at phantomWorldPos → SM edge test → full battery → headless smoke).

> **🔁 LOOP ITER 15 (2026-06-10): M3 Task 2 SHIPPED** — `game/hurlChannel.js` (single-slot transient request channel, perfProbe pattern) + the no-re-mesh GATED list grew to hurl.js + hurlChannel.js (red-first per-file; HurlSystem.jsx joins the gate IN Task 3 when its file is created — keeps main green every iteration). Unit 706→**710** (76 files). Pushed.

> **🔁 LOOP ITER 14 (2026-06-10): M3 Task 1 SHIPPED (TDD red→green)** — `src/game/hurl.js`: makeHurl (22 m/s + lift arc), stepHurl (in-place ballistic step, nearest-mob proximity hit ≤1.4m, hit event = {id, pos, dir-normalized} = the M4 anvil seam, TTL 1.5s), resolveSlam (3m radial horizontal knocks, under-center fallback), tuning table exported. Unit 699→**706** (75 files). Pushed.

> **🔁 LOOP ITER 13 (2026-06-10): M3 plan doc SHIPPED** (writing-plans; inputs = spec §3c/§3d/§3e/§4 + the #72 HELD contract + M1 deferreds + the M2 re-gate). 5 tasks: T1 `game/hurl.js` pure ballistic stepper + slam resolver (TDD, 7 tests — mobs are COLLIDER-LESS → proximity math, no physics body); T2 `game/hurlChannel.js` transient request channel + gate extension red-first (GATED += hurl/channel/HurlSystem); T3 `world/HurlSystem.jsx` (single-flight transient mesh; impact = damageMob(id, dmg, **activeSpell read AT IMPACT**) + entity.knockback nudge; hit event carries pos+dir = the M4 anvil seam) + `phantomWorldPos` transient export; T4 Components SM wiring (held attack/cast → verb-edge ref → SM 'hurl'/'slam'; suppression of melee/spell while held; grab tint from castBuildRay+worldBlocks); T5 close-out (scenario E → REAL hurls = the honest M2 re-gate; run B/C/E; KRB FPV-feel cue = Kevin's human gate, non-blocking). **Loop decisions recorded in the plan header** (pure-math arc not Rapier body; SLAM at the orbit point = timing-skill; stun-proxy = knockback; partial grab-tint honesty). **NEXT UNIT = M3 Task 1.**

> **🔁 LOOP ITER 12 (2026-06-10): #72 CLOSE-OUT SHIPPED → #72 COMPLETE (loop iters 8-12 end-to-end: design workflow → spec → plan → TDD build → gates → doc-currency).** Spec → ✅ BUILT (records the NaN mine/place discovery honestly) · plan → ✅ SHIPPED (deviations-of-record: T2+T3 fused to keep main green; the stronger executor smoke) · SOTA §3 banner refreshed (scale re-measured live: 22.1k LOC / 123 files / 699 unit / 20 gate files) · CHANGELOG milestone entry · KRB cue (click-feel + "mining works again" heads-up + the lane-model reversal path). **NEXT UNIT = the VOIDHAND M3 plan doc** (the Aspect spine resumes; M3 = HURL + SLAM + impact + element-charge + the FPV-feel check; remember: the voidhand SM apply-site in Components currently feeds `attack:false, cast:false` — M3 feeds the REAL intent edges and re-skins them while HELD per spec §3d; the router needs ZERO changes).

> **🔁 LOOP ITER 11 (2026-06-10): #72 Tasks 2+3 SHIPPED (`99e0c35`) + a MAJOR smoke-found engine bug fixed.** Gate-first seam surgery done: Terrain mousedown DELETED → `GameMethods.castBuildRay` + `terrainVerbs.{mine,place,open}` executors; Components routes every click (ctx = live melee cone + π/8×24m aim cone + the single 8m ray). **DISCOVERY: rapier exposes `timeOfImpact` — the legacy `hit.toi` was undefined → instant mine/place at HEAD was SILENTLY BROKEN (NaN coords, junk worldBlocks keys, SFX-no-edit). Fixed + holistic-swept 2 more dead-field sites (GameScene:181 occlusion march, Terrain:290 target outline; :478 already defensive). Executor smoke now mines a REAL voxel (toi 2.20m, worldBlocks 0→1).** Evidence: 699 unit (74 files) · build · visual 13/13 · inactive-gate + executor smokes green. **NEXT UNIT = plan Task 4 close-out** (doc-currency; KRB playtest cue: click-feel + "mining works again" note), then back to the Aspect spine = **VOIDHAND M3 plan doc** (HURL/SLAM + impact + FPV-feel; M2 re-gate item rides along).

> **🔁 LOOP ITER 10 (2026-06-10): #72 Task 1 SHIPPED (TDD red→green)** — `src/input/verbRouter.js` pure ladder + the spec §5 14-row table as 15 colocated tests (incl. tie→combat, through-mob guard, HELD short-circuits). Unit 681→**696** (73 files). Pushed. **NEXT UNIT = plan Task 2**: write `tests/gates/verb-router-gates.test.js` FIRST (red on today's double-listener) → Terrain.jsx surgery (handleClick bodies → `GameMethods.castBuildRay` + `GameMethods.terrainVerbs.{mine,place,open}` as VERBATIM moves inside the same useEffect closure; mousedown registration + raw pointerLockElement gate DELETED; registry cleanup in teardown; `import { GameMethods } from '../GameMethods'`). Then T3 Components wiring, T4 close-out.

> **🔁 LOOP ITER 9 (2026-06-10): #72 plan doc SHIPPED** (writing-plans; input = the design spec §5/§6). 4 tasks: T1 `src/input/verbRouter.js` pure ladder + the 14-row table as colocated tests (incl. tie-break→combat, `<=` on mob-vs-terrain; `interact` needs strict `<`); T2 NEW `tests/gates/verb-router-gates.test.js` FIRST (red on today's double-listener: Terrain-no-mousedown + Components-routes + router-purity) → Terrain surgery (handleClick bodies → `GameMethods.castBuildRay` + `terrainVerbs.{mine,place,open}` VERBATIM moves; listener + raw pointerLock gate DELETED); T3 Components handleMouseDown = ctx build (live melee cone + narrow AIM_CONE π/8×24m over collider-less mobs + the single 8m ray via registry) → route → dispatch; T4 doc-currency + KRB playtest cue. Expected counts: ~699 unit (+15 router +3 gate) · visual 13/13 (active-gated; capture never pointer-locks).

> **🔁 LOOP ITER 8 (2026-06-10): #72 DESIGN-OF-RECORD SHIPPED (charter §5 self-gate).** 3-lens design Workflow (genre · code-reality · adversarial) + orchestrator synthesis. **DECISION: target-priority router** — pure `src/input/verbRouter.js` ladder (b0: held→attack / meleeCone→attack / aimedMob<terrain→attack-whiff [the THROUGH-MOB GUARD] / terrain<8m→mine / whiff; b2: held→cast / chest-occlusion→interact / aimedMob<terrain→cast / terrain→place / cast), ONE listener (Components active-gated), Terrain listener DELETED → executors via registry, day/night NEVER routes, M3 HELD = ladder rule 1 (router unchanged at M3). **Lens contradiction resolved by MY grep: mobs are collider-less (zero RigidBody in SimplifiedNPCSystem) → ray can't classify mobs → pure cone/aim math required; the adversarial lens's lane model rested partly on the refuted claim.** Lane/hand-slot model = recorded reversal path (revisit w/ #71 hotbar honesty). §5 = the 14-row edge-case table (becomes the unit-test list). **NEXT UNIT = the #72 plan doc** (`docs/superpowers/plans/2026-06-10-crafty-72-verb-router.md`; inputs = the design spec §5/§6), then build (router TDD → seam surgery → gates).

> **🔁 LOOP ITER 7 (2026-06-10): M2 Task 7 CLOSE-OUT SHIPPED → M2 COMPLETE.** Doc-currency executed: plan doc bannered ✅ SHIPPED · spec status header updated (M2 DONE, Decision #5 resolved, M3 re-gate item, NEXT = #72→M3) · SOTA-INITIATIVE §3 banner refreshed (scale re-measured: ~21.9k LOC / 121 files / 681 unit / 72 files) · CHANGELOG milestone entry · KRB LIVE #2 = the READY one-tap iPad protocol (bundles #63) + M2 decisions-of-record. **M2 evidence chain:** plan `docs/superpowers/plans/2026-06-10-crafty-s2b2-m2-fps-gate.md` → verdict `memory/S2B2-M2-PERF.md` → JSONs `memory/perf/`. **NEXT UNIT = #72 (mouse verb-mode seam):** STATE-REVIEW finding — mouse place/break verbs double-fire eroding the player's own wall (undermines BASE-AS-ANVIL); spec'd as REQUIRED before M3 wires HURL/SLAM onto attack/cast. Read STATE-REVIEW §#9/#72 + the spec §3d/§4 first; milestone-scale → plan doc first per the standing rule.

> **🔁 LOOP ITER 6 (2026-06-10): M2 Task 6 SHIPPED — DESKTOP GATE = PASS ✅** — full A-E run on ANGLE Metal (M3 Max): all scenarios vsync-locked ~60fps; **C−B = 0.00ms median / +0.10ms p95 vs the 1.5/3.0 budget**; **D = 29 grab/drop toggles → 1 long frame (< control's 2) — the light-pool fix EMPIRICALLY closed the #68 grab-edge re-link carry-forward**; E (3 dynamic bodies) unresolvable at desktop scale → **M3 re-gate item recorded** (re-run E−B + C−B after real HURL/SLAM). Verdict doc `memory/S2B2-M2-PERF.md` + evidence JSON committed. Desktop = necessary-not-sufficient; iPad confirmation = Kevin one-tap protocol (§4 of the doc, bundles #63), does NOT block M3. **NEXT UNIT = plan Task 7 close-out**, THEN M2 is DONE → next per spec seq = **#72 mouse verb-mode seam (the plan-marked pre-M3 blocker)** → M3 (HURL/SLAM + FPV playtest).

> **🔁 LOOP ITER 5 (2026-06-10): M2 Task 5 SHIPPED (perf:m2 runner)** — `frontend/scripts/perf/run-scenarios.mjs` + npm `perf:m2`. STUCK-RULE layer-switch applied: 2 headed-Chrome attempts died to macOS occlusion suspension (agent context can't keep a visible window unthrottled → CDP timeouts) → switched to **headless 'new' + `--enable-gpu --use-angle=metal` = REAL GPU verified ("ANGLE Metal Renderer: Apple M3 Max")**, renderer string stamped into every report JSON. Smoke A: 59.7 fps · median 16.70ms; evidence JSON committed at `memory/perf/`. Runner prints the C−B verdict via the tested `withinBudget`/`M2_BUDGET` import. **NEXT UNIT = plan Task 6**: `npm run perf:m2` full A-E (~8 min), author `memory/S2B2-M2-PERF.md` (mirror S2B1 doc: methodology, numbers table, C−B verdict vs 1.5ms/3ms budget, D edge reading, E reading + M3 re-gate note, desktop-proxy honesty, levers), commit evidence. Then T7 close-out (KRB device protocol bundling #63, spec/banner/CHANGELOG).

> **🔁 LOOP ITER 4 (2026-06-10): M2 Task 4 SHIPPED (probe runtime)** — `?perf=A..E` dev-only probe: perfProbe.js (mode/phase/hurl-channel/seeded-RNG) + PerfProbeRunner.jsx (DOM overlay, terrain-wait → t0 writes → 60s rAF sampling → `window.__craftyPerfResult`) + PerfProbeSystem.jsx (scenario-E 3-body dynamic hurl pool) + GameScene pinning (dpr=1.5, monitors off under probe) + App mount; 3 probe files joined the voidhand no-re-mesh gate (red-first). **Smoke caught a REAL defect:** Components' voidhand SM max-hold-auto-dropped the probe's phantom on frame 1 (heldUntil=0) → C measured an unheld siege; fixed via `if (!isPerfProbe())` SM-skip (runner's store-held authoritative; prod constant-false). perf-config-gates monitor-guard literal updated to the STRENGTHENED `{!isCaptureMode && !isPerfProbe() && (` (intent-preserving, justification in commit body). Evidence: **681 unit · build clean · tree-shake 0 probe strings in build/ · visual 13/13 · smoke C = 3581 frames/60s held=true**. **NEXT UNIT = plan Task 5** (runner script + npm `perf:m2`, smoke `--scenarios=A`), then T6 RUN all five + record verdict in memory/S2B2-M2-PERF.md, then T7 close-out.

> **🔁 LOOP ITER 3 (2026-06-10): M2 Task 3 SHIPPED** — PhantomBlockSystem light-POOL: pointLight ALWAYS mounted, `intensity={active ? 1.4 : 0}` (grab/drop never changes light COUNT → no shader program re-link at the grab edge; the #68 carry-forward). Mesh stays held-conditional (capture-absent). Evidence: 678 unit · build clean · **visual 13/13 no re-baseline**. Pushed. **NEXT UNIT = plan Task 4** (extend voidhand-noremesh GATED list FIRST → red ENOENT → create perfProbe.js / PerfProbeRunner.jsx / PerfProbeSystem.jsx → wire GameScene dpr/monitors + App mount → smoke `?perf=C` + `?perf=E`).

> **🔁 LOOP ITER 2 (2026-06-10): M2 Tasks 1+2 SHIPPED (TDD red→green)** — `src/devtest/frameStats.js` (quantile/frameStats/compareScenarios/withinBudget + `M2_BUDGET` {1.5ms median, 3ms p95}) + `src/devtest/perfScenarios.js` (the A-E table + scenarioEvents schedule). Commits `adacdba` + `9e0cf63`, pushed. Unit 667→**678** (72 files). **NEXT UNIT = plan Task 3** (light-POOL: PhantomBlockSystem pointLight always-mounted intensity-gated; verify visual 13/13 no re-baseline) → T4 probe runtime → T5 runner → T6 RUN+record → T7 close-out. **Hygiene queue:** GitHub Dependabot 1 CRITICAL on main (`security/dependabot/29`) — investigate in a hygiene iteration soon.

> **🔁 LOOP ITER 1 (2026-06-10): VOIDHAND M2 plan doc SHIPPED** — `docs/superpowers/plans/2026-06-10-crafty-s2b2-m2-fps-gate.md` (writing-plans discipline; inputs = STATE-REVIEW §4 scenarios A-E + the #68 carry-forwards + the WILDHEART S2B1-M2-PERF pattern). Key design decisions (loop-decided per charter §5, reversal-path = the plan doc): dev-only `?perf=A..E` probe mode (physics + spawns LIVE — NOT capture mode), pinned DPR 1.5 + tier high + PerformanceMonitor/AdaptiveDpr off; **budget PINNED: C−B ≤1.5ms median / ≤3ms p95** (STATE-REVIEW rec adopted); **light-POOL restructure** of PhantomBlockSystem (always-mounted intensity-gated pointLight kills the grab-edge program re-link by construction); **scenario-E dynamic hurl stand-in** (3-body Rapier pool, 22 m/s, 3s cadence — the physics presence M1's render-only phantom lacks); desktop headed-puppeteer gate = autonomous evidence; **one-tap iPad URL protocol parked to Kevin (bundles #63), does NOT block M3** (WILDHEART precedent). **NEXT UNIT = plan Task 1+2** (frameStats + perfScenarios, TDD) → T3 light-pool → T4 probe runtime → T5 runner → T6 RUN+record verdict → T7 close-out/doc-currency.

> **✅ #68 PERF REMEDIATION SHIPPED (plan-doc'd, TDD, adversarially reviewed, all on `main`):** T1 EnemyProjectileSystem GLI fix (pure `game/enemyProjectiles.js` stepper + transient meshes — the BLOCKING breach) · T2 `getMobGroundLevel` Ray+filter hoist (the hottest physics call, per-call alloc gone) · T3 **15Hz AI bridge** (accumulated-delta parity + damped MobModel sync; knockback snap-sync keeps hit-feel instant) · T4 weather probe stride (bounded above-band clip ≤16 frames) · T5 spell-light castShadow removed + a repo-walking **dynamic-light gate** (pointLights never cast) · T6 React.memo'd per-entity renderers · T7 Components.jsx joined the voidhand no-re-mesh GATED list. **4-lens review (8 agents) caught 1 real parity break I missed — React.memo froze the cover-seeking aura (JSX read of a worker-mutated field) — fixed via transient `.visible` toggle.** 667 unit (70 files) · build · visual 13/13, no re-baseline. **M2-input carry-forwards:** fixed light-POOL (stop light-count program re-links) folded into M2 design; gate-regex hardening + `.castShadow=true` imperative scan → #69; `setCheckCollision` hoist = optional nibble. **LOOP-CHARTER live** (`docs/superpowers/LOOP-CHARTER.md` + CLAUDE.md compaction/loop sections): Kevin's autonomous-build authority grant (broad audience kids→adults; zh-CN = toggle, EN default; doc-currency = explicit persist step). **NEXT = the VOIDHAND M2 plan doc** (`writing-plans`; inputs = STATE-REVIEW §4 + the M2 carry-forwards above).

> **🔎 FULL STATE REVIEW (2026-06-10, Fable 5 ultracode — 21 agents, adversarially verified):** `memory/STATE-REVIEW-2026-06-10.md` is the live status + risk map. Headlines: master-plan reconciliation table (S0-S2-B1 ✅, VOIDHAND M1-of-8 in-flight, B3/B4+S3/S4 not started); architecture HOLDING (~84% of +6.9k LOC in new modules); M1 verified clean on every checkpoint; **1 BLOCKING** (EnemyProjectileSystem setState-per-frame GLI breach) + **10 HIGH confirmed** (raycast storm ~2,100/frame worst-case; AI bridge at render rate; mouse verbs double-fire eroding the player's own wall — undermines BASE-AS-ANVIL, pre-M3 blocker; FPV fuse-into-beast unmitigated; roar illegible; no-re-mesh gates miss Components.jsx + a 3rd `update_block` poster at `AdvancedGameFeatures.jsx:313` falsifying the spec seam-map; panel-key matrix 1/7 w/ confirmed Tab/E/C/B dual-modal asymmetry; M2-as-spec'd would gate a zero-physics phantom). New tasks **#68-#74**; SOTA-INITIATIVE.md bannered (was the last stale doc). **STEERING (Kevin): do the cheap #68 perf fixes BEFORE/WITH M2** (else siege-baseline noise swamps the M2 signal — recommended) **or M2 as-sequenced.** M2 plan doc inputs = STATE-REVIEW §4 (scenarios A-E, pin a numeric budget, dev-probe for touch-iPad, hurl-stub decision, levers-if-fail; bundle the #63 golem device check into the same session).

> **⏭️ S2-B2 VOIDHAND @ CLEAN BREAK after M1 (2026-06-09, all on `main`, pushed; design-of-record `docs/superpowers/specs/2026-06-09-crafty-s2b2-voidhand-design.md`).** Kevin APPROVED the 8-decision batch (recs as-is) + flagged the keybind conflict (`KeyG` = chest/trade → grab = **`KeyV`**, keymap-audited). **M1 BUILT + verified + adversarially reviewed (clean, 0 blockers):**
> - **Pure cores (TDD):** `src/game/kinetic.js` (meter twin of ferocity.js — KINETIC_MAX/GRAB_COST/kineticForKill/clampKinetic/canGrab; spent-per-grab) + `src/game/voidhand.js` (grab SM twin of beastTransform.js — IDLE→CHARGING→HELD→hurl/slam/drop; V to grab, re-press V to drop; cooldown+max-hold; references ZERO voxel/worker seams).
> - **Phantom + wiring:** `src/world/PhantomBlockSystem.jsx` (a 1×1×1 cube orbiting the player; kinematic RENDER write ~zero physics; self-nulls when !held → absent from all baselines; capture-frozen orbit) + the `grab` intent (KeyV) + transient store fields `voidhandHeld`/`heldPhantom` (NOT serialized) + the Components VOIDHAND SM block (transient reads, set() only on transitions) + `<PhantomBlockSystem/>` mounted.
> - **LOAD-BEARING no-re-mesh static gate GREEN:** `tests/gates/voidhand-noremesh-gates.test.js` (cloned from `beast-noremesh-gates`) — voidhand.js/kinetic.js/PhantomBlockSystem.jsx contain zero `update_block`/worker seams → a combat grab can never re-mesh.
> - **Death-edge fix** (proactive, from the WILDHEART review precedent): `damagePlayer` clears `voidhandHeld`+`heldPhantom` atomically (no 1-frame phantom over the death screen) + a store test.
> - **657 unit (67 files) · build clean · visual 13/13.** Commits: `4d01cb5` (cores) → `254606b` (wiring+gate) → `3d08f6c` (death-fix).
> - **RESUME = M2.** ⚠️ **NEW RULE (Kevin 2026-06-10, in `.agent/AGENTS.md` §Method):** EVERY milestone now starts by **authoring its own `superpowers:writing-plans` plan doc** in `docs/superpowers/plans/` (TDD red-first steps + gates) BEFORE building — no building-from-the-spec shortcuts (the VOIDHAND-M1 skip is the forbidden anti-pattern). So **M2 step 1 = write `docs/superpowers/plans/<date>-crafty-s2b2-m2-fps-gate.md`**, then build. M2 itself = **the real-iPad FPS gate BEFORE the verbs** (front-loaded perf — grab→orbit 3s→[hurl stub]→ delta-from-baseline budget; like the WILDHEART bull gate). Then M3 (HURL/SLAM impact + attack/cast re-skin + FPV playtest) → M4 (anvil 3× + kinetic meter wired + voidhand_grasp gate + autosave + HUD) → M5 elements → M6 pool-safety → M7 LOOK → M8 content — **each with its own plan doc first.** **M1-deferred (by design):** canGrab OPEN (M4 gates it); attack/cast fed `false` (M3); placeholder phantom color `#A9966E` (M3); look is M7. **Doc-currency (2026-06-10):** all superpowers specs/plans de-staled (24 plans SHIPPED-bannered; VOIDHAND/WILDHEART/S2-master spec headers current); **coherence-pillars** = ADOPTED/in-use (Kevin revised P5 + monetization→S4) but the destructive-CUT GATE is PARKED pending a 100%-on-must-NOT-cut calibration run. **Also still-open:** #63 S2-B1 tuning · #64 beast-capture reliability · #66 panel-flags→store · #32 vitest 3→4.

> **✅ S2-B1 DONE (2026-06-07).** The whole WILDHEART Aspect — mechanics + the third-person beast-transform LOOK — is MERGED to `main` (`458bbb5`, pushed; branch `s2b1-m7-thirdperson-beachhead` @ `9d2f55a` kept for the capture follow-up). 630 unit · build · visual 13/13. See CHANGELOG (2026-06-07 S2-B1-M7/M8 entry) for the full record. **NEXT options (Kevin to steer):** (a) **S2-B1 deferred tuning** — boss-kill ferocity, per-form melee range/arc + bull debris-shove, form-paced dodge/vault; (b) **beast-capture reliability** (in-world capture non-deterministic — paused-physics RB/render desync at unsettled player; needed to gate-baseline the 4 beast frames); (c) **S2-B2 = the next Aspect** (Voidhand / Soulbind / Elemancer per `SOTA-INITIATIVE.md`); (d) the deferred **vitest 3→4 bump** (#32, its own task on main); (e) the full **third-person control pivot** (likely ~S3 mobile). The look's daytime cool-element read + live in-world feel are worth a Kevin eyeball in playtest (the capture couldn't screenshot them this session).

> **🧭 DIRECTIONAL DECISION (Kevin, 2026-06-07) — Crafty is heading to THIRD-PERSON.** WILDHEART surfaced it: the game is currently FIRST-PERSON (camera-attached `StableMagicHands`, no player body — `character-closeup` renders a *zombie*), but the whole S2 Aspect thesis (4 visible power-fantasies: beast/squad/orbiting-mass/elemental) is structurally third-person, the S3 mobile/touch target forces a control rework (third-person+virtual-stick is native), it's an RPG (gear visibility), and every locked look-ref (Bayonetta/Hades/Genshin/LoL/Brawl Stars) is third-person. **Full pivot DEFERRED to a deliberate milestone (likely fused with the S3 mobile-control rework), decided AFTER Kevin sees the beachhead in-world. M7 = the no-regret first step (Option C hybrid).** Full reasoning in this turn's transcript + the plan doc.

> **⛔ SUPERSEDED (now MERGED to `main` `458bbb5` — see the Current-Task block above; this block is historical from before the merge):** ~~RESUME @ CLEAN BREAK~~ WILDHEART MECHANICS DONE (M0-M6+M3.5) + **THE LOOK** (M7a/b/c/d + M8 + review-fix + punchy glow + cool-rim) — all now on `main`. Historical detail of how the look was built:
> - **M7a transform-cam ✅** (`src/game/transformCam.js` pure + Components override; on roar, a ~1.2s 3rd-person cam pulls back, then returns to FPV).
> - **M7b/M7d beast avatar ✅** (`src/render/BeastAvatar.jsx` + `src/render/beastAvatarParts.js`): the ③·5 recipe = a crisp dark-ink `MobToonMaterial` body + element rim (`rimColor` added to MobToonMaterial) + a tiny white-hot bloom-core + a faint halo. **The first in-world capture REPRODUCED the loot-VFX BLOB → diagnosed+fixed (the §5a lesson: crispness = silhouette/contrast, NOT brightness — dark-ink body w/ element accents, not bright-element + heavy aura).** Capture hook `spawnBeastTransform(element)` (captureStudio:false, IN-WORLD) + per-beast cameras → `tests/visual/current/beast-{fire,ice,lightning,arcane}.png` (NOT baselined — Kevin ratifies).
> - **M8 ROSTER ✅ — 4 shapes Kevin-directed** (`fadde54`): **fire = a BIG rearing DRAGON** (horned head, huge bat-wings, claws, tail), **ice = a MASSIVE horned bull-brute**, lightning = an avian raptor, arcane = a blocky golem-construct.
> - **M7c CHOREOGRAPHY ✅** (`7b65476`): `src/game/beastMorph.js` (pure, 6 tests) + the 3-beat morph — beat1 anticipation charge-glow (a `beastCharging` store flag set by the Components SM) → beat2 a swap-masking BURST flash → beat3 the avatar pop (easeOutBack). CAPTURE-FROZEN to the SETTLED beast (deterministic).
> - **ADVERSARIAL REVIEW ✅ DONE** (2026-06-07, post-compact — 5-lens Workflow over the 686-line delta: 13 raised → **1 CONFIRMED MEDIUM, 12 refuted-as-correct-by-design**). The 1 fix (`2c17834`, TDD red→green): a cross-layer 1-frame race — `damagePlayer()`'s synchronous death-edge ran one frame ahead of the roar SM's own `!alive→cancel`, so dying mid-CHARGE left `beastCharging` true ~1 frame → the anticipation charge-glow flashed over the soft-death screen (I3). Fixed by clearing `beastCharging` atomically with `isAlive` (the sole `isAlive:false` site). The 12 refutations all confirm correct-by-design (null-gate, capture-freeze, transient persistence) — matched my own independent read. (My SM-level analysis alone missed the store-vs-SM 1-frame race; the ensemble caught it — value-of-adversarial-review.)
> - **630 unit (61 files) · build clean · the 16-state visual gate GREEN** (re-verified live post-compact; avatar self-gates on beastFormActive → null in every capture).
> - **GLOW DIAL ✅ LOCKED — Kevin picked PUNCHY** (`9b1bdeb`): baked `GLOW=1.7` const in BeastAvatar (render math + blob-guard caps unchanged → reproduces the picked render); the glow-dial scaffold removed (beastGlowMul/setBeastGlowMul, the App glowMul param, the capture glow-ladder loop). Discovery during the dial: the warm explore/dusk sky is low-contrast for a warm glow — only the **obsidian mood (dangerLevel=2) goes genuinely dark**; the ladder forced it (snap + reset) to give a judgeable comparison. The 4 roster `beast-*` frames now render at punchy. **LOOK NOW LOCKED: 4 shapes (Kevin-directed) + punchy glow.**
> - **COOL-RIM POLISH ✅ committed (`96c6459`) — DATA-VERIFIED, visually UNCONFIRMED this session.** Per-element `rim` multiplier (ice 1.7×, lightning 1.5×; fire/arcane 1.0× = unchanged) → a brighter grade-resistant element rim so the COOL beasts hold their cyan/yellow identity under the warm daytime sun (the explore grade saturates but doesn't hue-shift, so a brighter cyan edge stays cyan). `ELEMENT_COLOR.rim → parts.rimBoost → BeastAvatar rimStrength`. fire render byte-identical. 630 unit + build green; `beastAvatarParts` returns rimBoost correctly; dbg confirmed `active:true` in capture. **⚠️ BLOCKER: the in-world beast CAPTURE flaked badly this session** — instrumented dbg showed the player at **y:120 (unsettled, mid-air)**; paused-physics RB/render desync left the beast intermittently OFF-CAMERA (empty frames), plus one hang. PRE-EXISTING capture infra, orthogonal to the polish. Could not obtain a fresh confirmation frame. **DECISION PENDING (Kevin):** (a) proceed to cleanup+merge on the data-verification (eyeball in live playtest) + log capture-reliability as follow-up; or (b) fix the beast-capture reliability first (settle the player pre-shot) → clean frame → decide baselining → merge. **The 4 beast frames look NON-DETERMINISTIC (flaky) → likely NOT gate-baselineable; keep as review-only or drop.** Follow-up task: **beast-capture reliability** (settle/teleport the player to a stable pos in `spawnBeastTransform`; the RB three.js object desyncs from `rb.translation()` under paused physics).
> - **RESUME NEXT — Kevin-gated:** (1) **RATIFY the 4 punchy roster frames** (`tests/visual/current/beast-{fire,ice,lightning,arcane}.png`) — note: on the warm daytime explore grade the COOL elements (ice cyan, lightning yellow) read warm-tinted; they're true-colored in code + read true in cool light. (2) **FINAL CLEANUP: remove `FIRE_SHAPE_VARIANTS` + `beastShapeVariant`/`setBeastShapeVariant` + `spawnBeastTransform` + the capture roster loop** (all shape-showcase scaffold; shapes locked). (3) **re-baseline** the 4 `beast-*` into the gate (or drop them — Kevin decides if they're gate-tracked). (4) **merge M7+M8 → main** → then M5/M6-deferred tuning + the S2-B1 wrap. **Branch PUSHED @ `9b1bdeb`.** **Plan-of-record:** `docs/superpowers/plans/2026-06-07-crafty-s2b1-m7-thirdperson-beachhead.md`. **Kevin still-open:** the full third-person pivot timing (after the beachhead); the M5/M6 deferred tuning (boss-kill ferocity, range/arc, dodge/vault). **Possible polish (Kevin's call):** the ice/lightning cool elements read warm-muddied on the daytime grade — a stronger cool rim could hold their identity in warm light (or leave as-is, it's accurate world-lighting).

> **(historical M7 resume):** **M7 = THE LOOK via the third-person BEACHHEAD (Option C — hybrid transform-cam).** Plan-of-record: `docs/superpowers/plans/2026-06-07-crafty-s2b1-m7-thirdperson-beachhead.md`. Shape: on roar, a ~1.2s **third-person transform-cam** pulls back + plays the 3-beat morph (charge → BURST masks the collider swap → settle) showing the **full beast avatar** at the locked ③·5 glow (the clip-worthy moment), then snaps back to FPV beast gameplay. **Reuse (cheap):** beast avatar = procedural box-construction + `MobToonMaterial` (the mob render, `SimplifiedNPCSystem` MobMesh ~222) keyed off `BEAST_FORMS` proportions + the `SpellProjectileCore` 3-layer glow (`EnhancedMagicSystem.jsx:708-830`); camera = the R3F default (FPV head-lerp at `Components.jsx:497-499` is OVERRIDDEN during the window). **Phasing:** M7a transform-cam (pure `src/game/transformCam.js` camera-state machine + Components override; TDD; visual 13/13 unchanged — never fires in capture) → M7b beast avatar (LEAD = comet/fire) → M7c 3-beat choreography + BURST mask → M7d `spawnBeastTransform` capture (captureStudio:false, in-world, stable seed, frozen phase) + grayscale-judge → **Kevin judges the dial (NOT auto-merged)** → M8 replicate to the other 3 beasts. **VFX DISCIPLINE:** apply the PROVEN SpellProjectileCore recipe (cite it); judge IN-WORLD (real Caribbean ctx, grayscale), not a sky-studio card. **Invariant:** FPV combat aim/hit-reg UNTOUCHED (the full-pivot aim rework is the deferred S3 milestone); the camera+avatar+capture are all reused if the full pivot proceeds. **DEFERRED (Kevin):** boss-kill ferocity; per-form melee range/arc + bull debris-shove + behavior hooks; form-paced dodge/vault; re-baseline the M3/M4 provisional visuals at M7d. **Kevin flags:** M6 roar costs 2 talent points (intended gating); the full third-person pivot timing (after seeing the beachhead).

> **⏭️ M3.5/M4 RESUME (compaction-safe, 2026-06-07):** M0 look-lock ✅ · M1 collider-swap + no-permanent-beast ✅ (`6882e81`) · M2 bull physics gate ✅ (`d163afe`) · **M3 transform state-machine + roar verb ✅** (merge pending) — the roar is now a REAL `useFrame` intent consumer (`KeyR`→`setIntent('roar')`; pure SM `src/game/beastTransform.js`: hold-roar→anticipation charge→commit→14s duration→exit→cooldown; the store's `beastFormActive` is the single active-truth, the SM owns only charge+timers; `elementForSpell` picks the form: fireball→comet/iceball→bull/lightning→hawk/arcane→golem). **The roar WIRES the M1 swap to gameplay** (was dev/test-driven). An adversarial review caught a REAL B1 bug (a charge in-flight at death tunnelled the dead-window early-return + auto-fired on respawn if R held) → fixed (`6f9e8c8`: relocated the SM tick ABOVE the dead-window return so it cancels at the death edge; below the capture return so still capture-safe; + a B1 regression test). `canEnter=true` for now — M4 layers the ferocity threshold, M6 the wildheart_roar talent-unlock. 577 unit · build clean · visual 13/13. **NEXT = M3.5** (kill-event FAN-OUT: replace the single `onMobKill` slot with `subscribeMobKill`/`emitMobKill`, migrate QuestSystem onto it + a quest-kill REGRESSION test — the scaffold M4 ferocity-accrual + later Aspects reuse) → **M4** (Ferocity meter: store slice + day-only kill-accrual via the fan-out + dawn bleed + autosave-trigger + HUD `StatBar kind='ferocity'` w/ a NEW feral design token + wire it into the roar guard's `canEnter`) → M5 (combat+locomotion re-skin) → M6 (talent nodes incl. the roar-unlock) → M7-M8 (the LOOK, gated on the M0 lock). Premium VFX/beast mesh + the in-world transform capture state are M7 (reference-locked) — M3 is mechanic-only, no new baseline.

> **⏭️ (history) M3 RESUME (compaction-safe, 2026-06-07):** M0 look-lock ✅ · M1 transactional collider-swap + no-permanent-beast invariant ✅ (merge `6882e81`) · **M2 bull physics gate ✅** — real-Rapier integration tests (1000-cycle handle-stability against a live collider [the M1-review gap], all-4-forms KCC-compatible, real-collider restore) + an engine-side cost benchmark (`frontend/scripts/bench/bull-physics-bench.mjs`): bull 8.75× / golem 11.9× base per-sweep in a *pathological* 576-collider cluster, but one-entity/one-sweep-per-frame ≈ 5-7% worst-case frame budget + real density far sparser → **engine-side cost ACCEPTABLE, other 3 beasts GREENLIT.** GPU FPS = Kevin's real-device check (`memory/S2B1-M2-PERF.md` protocol + `collisionGroups`/dim-trim levers). 557 unit · build clean. **NEXT = M3** (the transform STATE MACHINE: roar-hold→anticipation→[burst masks swap]→ACTIVE→exit; the `roar` verb as a REAL `useFrame` intent consumer per the spec §3b — `setIntent('roar',…)` only + `getInput().roar` read transiently; element→form pick from `activeSpell`; enumerated modal guards; dodge-iframe preserve; a NEW deterministic `spawnBeastTransform` capture state with `captureStudio:false` + frozen phase). The roar wires the M1 swap to real gameplay (M1's `enter/exitBeastForm` are dev/test-driven until now). Then M3.5 (kill-event fan-out) → M4 (Ferocity meter + feral token) → M5 (combat+locomotion re-skin) → M6 (talent nodes) → M7-M8 (the LOOK, gated on the M0 lock).

> **🔒 LOOK LOCKED (Kevin 2026-06-07):** the WILDHEART look of record = **"Hades silhouette + Genshin radiance, fused" at glow-level ③·5** (crisp ink silhouette that NEVER blooms + back-aura + glowing core/accents = the shipped `SpellProjectileCore` recipe). Locked reference: `.superpowers/s2b1-wildheart-refs/mockup-b9.png`; the glow dial that proved it (①→③·5→④blob): `mockup-b7.png`. Recorded in spec §5. **M0 DONE.** The look is built + tuned in-world at M7; mechanics M1-M6 + M3.5 proceed now. **NOW BUILDING: S2-B1-M1** = the transactional collider hot-swap (`setShape` in-place, handle preserved) + `restoreBaseController()` on EVERY exit path (death-EDGE subscribe — `isAlive` true→false, NOT respawn; + loadWorldData imperative tail + timer/manual/error/unmount) + `BEAST_FORMS` table (`src/game/beasts.js`) + single-writer form authority (mirror `bossActive` value+getter) — PLAIN capsule (no beast art), behind the visual gate, dev/test-triggered (roar verb is M3). Gates: restore-invariant unit tests (enter→death/load/save/error/timer/unmount ⇒ base dims + base controller-config + `isBeastFormActive()` false) · 1000-cycle handle-stability stress · no-re-mesh static gate (`setWorldBlocks|terrain.worker|createChunk|setBlock|postMessage` = 0 in the beast module). Plan: `docs/superpowers/plans/2026-06-07-crafty-s2b1-m1-collider-swap.md`. **✅ M1 COMPLETE + MERGED (branch `s2b1-m1-collider-swap`):** T1 (`6df911c` BEAST_FORMS table) · T2 (`0c71ab1` store single-writer form-authority + the NO-PERMANENT-BEAST invariant, Rapier kept out of the store) · T3 (`9073331` Components.jsx Rapier collider hot-swap — `pendingFormSwapRef` enqueued by an `activeBeastForm` subscription, consumed in `useFrame` before `collider(0)`, atomic + isolation-safe + capture-safe). **An adversarial review caught a REAL B1 bug** (death flipped the STORE flag but the live COLLIDER only restored after manual respawn — gated behind the dead-window early-return) → fixed (`4b98b98`: EXIT/restore is now IMPERATIVE at the death/load edge via `restoreBaseCollider`; ENTER stays queued for swap+depenetration atomicity) + extracted testable `setColliderToForm`/`restoreBaseCollider` helpers (closes the review's "tests only checked the store, never the collider" gap). Gates: **552 unit · build clean · visual 13/13 · no-re-mesh static gate.** **NEXT = M2** (real-device bull-capsule FPS gate BEFORE the other 3 beasts; the 1000-cycle Rapier handle-stability stress lands here) → M3 (state machine + roar intent) → M3.5 (kill-event fan-out) → M4 (Ferocity meter + feral token) → M5 (combat+locomotion re-skin) → M6 (talent nodes) → M7-M8 (the LOOK, gated on the M0 lock). The roar trigger is M3, so M1's swap is dev/test-driven (`enter/exitBeastForm` store actions) for now.

> **⏭️ S2-B1 RESUME (compaction-safe, 2026-06-07): the WILDHEART design-of-record SPEC is written + reconciled at `docs/superpowers/specs/2026-06-07-crafty-s2b1-wildheart-design.md`.** Produced by an 11-agent design-workflow (5 code-seam mappers + 2 live-research lanes [Rapier transactional collider hot-swap · bloom-surviving morph VFX] → synth → 3 adversarial reviewers) then reconciled by me (v2). **The review caught 4 BLOCKING defects (all corrected in v2):** (1) death-restore wired to the respawn edge not the death edge (would leave a kid in beast-form through the soft-death screen — convergent R1+R3); (2) ferocity accrual stomping the already-owned single-writer `onMobKill` slot → fixed via a kill-event FAN-OUT scaffold (new milestone M3.5) + quest-kill regression test; (3) the `wildheart_roar` effect-less unlock node hard-crashing `foldTalentEffects` → fold-tolerance + node-shape contract; (4) `roar` claimed input-abstracted but wired imperatively → now a real `useFrame` intent consumer. Plus 12 major + 10 minor seam/persistence/atomicity/VFX corrections. **The spec is TWO-LAYER:** (A) mechanics M1-M6 + M3.5 are blind-buildable + TDD-gated; (B) the LOOK (M7-M8 morph VFX + 4 beast forms) is **reference-LOCK + in-world Kevin decision (VFX discipline), NOT blind-built**. **Milestones:** M0 reference-lock (Kevin) → M1 transactional swap + `restoreBaseController()` (DEATH-edge restore; plain capsule, behind the visual gate) → M2 bull real-device FPS gate (before the other 3 beasts) → M3 state machine + roar intent → M3.5 kill-event fan-out → M4 Ferocity meter + new feral token → M5 combat+locomotion re-skin → M6 signature talent nodes → M7 THE LOOK (gated on M0) → M8 replicate the 4 beasts. **NEXT once Kevin approves the design (+ optionally locks the look — only blocks M7/M8):** `superpowers:writing-plans` for the WILDHEART milestones → subagent-driven build, M1 first. **KEVIN DECISION BATCH in the spec §8** (reference-lock #1 is the genuine blocker for M7/M8; the rest — ferocity tuning, roster confirm incl. the ice→bull/fire→comet inversion flag, talent taxonomy, combat-differentiation depth, bull debris-shove, feral token color, roar keybinding — are reversible taste-tunables, proceeding on the spec's recommendations unless Kevin adjusts). Grounding: S2 spec `docs/superpowers/specs/2026-06-02-crafty-s2-game-design-design.md` §4 (S2-B1 = the LEAD Aspect). `main` @ `e508f1a`; `test:unit` 529 · `test:visual` 13/13 (no build yet — design phase only). **VISUAL reference-lock artifacts for Kevin (gitignored, `.superpowers/s2b1-wildheart-refs/`): `reference-board.png` (the 5 candidate game looks) + `mockup-b1..b5.png` (regen via `node gen.mjs && node render-mockups.mjs`) — board 1 = the 4 styles on one beast, b2 = the Lux identity (4 distinct beasts), b3/b4/b5 = the Lux+Bayonetta / Lux+Hades(rec) / Lux+Genshin blends. Key framing: Lux = the IDENTITY axis (how the 4 beasts differ, always needed), Bayonetta/Hades/Genshin = the RENDER-STYLE axis — they COMBINE, not conflict. `mockup-b6.png` = Hades silhouette + disciplined core-glow. `mockup-b7.png` = the GLOW DIAL (Kevin asked "can we fuse Genshin glow into b6?" — YES; the dial shows ①core-only=b6 → ③aura+rim+glow-accents=SWEET SPOT → ④full-body-bloom=b5 blob; the "problem" was only ever ④, glowing the whole body). `mockup-b8.png` = **the FUSED build target v2 (RECOMMENDED): all 4 beasts at level③ — crisp ink silhouette (never blobs) + Genshin back-aura/core/accent radiance** = b6 with the glow Kevin liked, dialed to the safe max (still how the shipped SpellProjectileCore works). AWAITING Kevin's pick: lock b8/level③ (or tune the dial more/less glow). If he picks the hybrid/Hades direction → `writing-plans` → build M1. Mechanics M1-M6+M3.5 don't wait on the look (only M7/M8 do).**

> **🎨 VFX/AESTHETIC DISCIPLINE — LOCKED (Kevin 2026-06-07, after the loot-VFX miss):** for ALL premium VFX / aesthetic work (S2-B Aspect + spell VFX, mob/biome/audio elevation), **LOCK A REFERENCE first + judge IN-WORLD (not an abstract sky-studio frame) BEFORE building.** Do NOT blind-tune brightness numbers against a misleading frame. (Process miss: I built premium loot-VFX without locking a reference/decide-how-to-judge → 3 attempts flat→blob→gem-hero, none premium; the strong global bloom turns additive glow into soft blobs + the sky-studio strips the world context a beam needs.) **LOOT-VFX premium polish = DEFERRED** (Kevin: park it, it's a small feature) to a focused reference-locked + in-world session; the WIP 5-layer attempt is preserved unmerged on branch `s2a-loot-vfx-premium` (main keeps the simple M3c loot). Backlog item in KEVIN-REVIEW-BATCH.

> **⏭️ S2-B RESUME (compaction-safe, 2026-06-03): ALL of S2-A DONE + merged + the pre-S2-B content audit DONE.** S2-A (signature-agnostic foundation) complete: progression/save (M2a) · build axis (M2b) · 4 Aspect trees (M2c) · single input authority (M2d) · day/night clock (M3a) · night siege+reward+currency (M3b) · loot juice (M3c) · perf tier-levers+recovery (M4a) · M4b visual-review artifacts (loot-showcase gate state + freeze + 3 forced-med/low review baselines awaiting Kevin's ratification). `main` @ `4905952`; `test:unit` 529 · `test:visual` 13/13. **Content audit** (`memory/PRE-S2B-CONTENT-AUDIT-2026-06-03.md`): mobs/biomes/audio are content-thin (template-swap / noise-tint / SFX-reuse); signature-fires healthy; coherence gate calibratable but NOT yet calibration-verified (must score 100% on must-NOT-cut before it governs cuts). **NEXT = S2-B Aspects** (sequential, deep-before-next: B1 Wildheart beast-transform → B2 Voidhand → B3 Soulbind capture-creatures → B4 Elemancer reactive-terrain). Per the master plan: a **music motif pass folds into each Aspect**; the **bestiary pass lands after B3 (Soulbind)**; **world-design/landmarks late-S2 or S3**. Author the S2-B (Wildheart) spec/plan via `superpowers:writing-plans` → subagent-driven. **PENDING KEVIN DECISIONS before/within S2-B (KEVIN-REVIEW-BATCH + the audit):** (1) bestiary timing (post-B3 vs pull silhouette/AI into B1); (2) world landmarks S2 vs S3; (3) named regions y/n; (4) dead axios cloud-save keep-S4-skeleton vs clean-now; (5) audio: approve per-Aspect motif + 3 cheap wins (player/enemy hit split · pad LFO · arpeggiator stinger); (6) med/low baseline ratification (promote to gate STATES?); (7) loot-beam punchiness (currently subtle — tunable in `lootJuice.js`); (8) deep-night-obsidian (default no); (9) siege/reward tuning; (10) music tooling (ElevenLabs, decide-later); (11) monetization model (S4). **Before the coherence gate governs any CUT: run its calibration (100% on must-NOT-cut).** **DEFERRED to S3:** real perf NUMBER + flipflops/onFallback/bounds tuning + weather-runtime-reactivity + Terrain.jsx pointer-lock reads + setIsPointerLocked→setInputActive rename + biome gen-systems. `challenge-memory` audit → now that S2 foundation is done, SURFACE it (Kevin's trigger was "when S2 is done"; S2-A+audit done, S2-B is the signature build — confirm with Kevin whether to run challenge-memory now or after S2-B).

> **⏭️ (history) S2-A-M4b RESUME: S2-A-M4a (perf-config wiring) DONE + merged to `main`**

> **⏭️ S2-A-M4b RESUME (compaction-safe, 2026-06-03): S2-A-M4a (perf-config wiring) DONE + merged to `main`** (branch `s2a-m4a-perf-config`; plan `docs/superpowers/plans/2026-06-03-crafty-s2a-m4-perf-gates.md`). M4a wired the 2 dead tier levers + tier recovery: `renderDistance`→chunk radius (was hardcoded `RENDER_DISTANCE=4`; high==4==legacy so capture byte-identical; low/med render fewer chunks), `weather`→`WeatherSystem` density, `onIncline`→`PerformanceMonitor` (low→med→high, fixes the one-way ratchet, capture-guarded). `test:unit` **529** · build clean · `test:visual` **12/12 no re-baseline**. Review=3 MINOR all inside `!isCaptureMode`+all S3: fixed the misleading `flipflops` comment; **deferred to S3** (honest, no overclaim): flipflops=3 too low + no `onFallback` (ratchet can re-emerge post-fallback) + weather mount-time-only. **NEXT = S2-A-M4b (forced-med/low visual baselines — HUMAN REVIEW):** add forced-med + forced-low capture variants of key in-world states (via the `setQualityTier` dev hook), GENERATE the baselines, then **render + eyeball them myself + surface to Kevin to RATIFY** before they become canonical (new baselines define "correct" → cannot auto-bless). Catches tier-specific render regressions the forced-high gate can't see. **THEN: pre-S2-B content-variety sub-audit** (Kevin greenlit) — mob/biome/**music** distinctness + signature-fires + coherence-gate calibration — BEFORE S2-B. **THEN S2-B Aspects** (Wildheart lead, deep-before-next). **S3 perf items (from M4a, logged in review-batch):** real perf NUMBER (real-device) + flipflops/`onFallback`/bounds tuning + weather-runtime-reactivity + the Terrain.jsx pointer-lock reads + the `setIsPointerLocked`→`setInputActive` rename. **Content-diversity passes NAMED in `SOTA-INITIATIVE.md`** (music→S2-B per-Aspect / mob→after S2-B3 / landscape→late-S2+S3). **Batched Kevin decisions:** siege/reward tuning · loot-juice + M4b baseline eyeballs · music tooling (ElevenLabs, decide-later). `challenge-memory` audit → when ALL of S2 done.

> **⏭️ (history) S2-A-M4 RESUME: S2-A-M3c (loot juice) DONE + merged to `main`** (branch `s2a-m3c-loot-juice`; plan `docs/superpowers/plans/2026-06-03-crafty-s2a-m3c-loot-juice.md`). **ALL of M3 (the stakes loop) is now done + merged: M3a day/night clock ✅ / M3b night-siege+reward+currency ✅ / M3c loot-juice ✅.** M3c: pure `src/game/lootJuice.js` `rarityBeam` (from locked `RARITY_FILL`) → drop-beam tiers by rarity (was fixed-look) + a rarity-tinted pickup-pop (pooled, no slop); fix-up stripped an rgba-alpha `THREE.Color` warning. My plan reality-grounding was stale (a beam+playPickup already existed via `679f2cd`) — implementer caught it via Goal-Frame + built the real deltas. `test:unit` **519** · build clean · `test:visual` **12/12 no re-baseline**. **EYEBALL GAP:** loot beam/pop look isn't gate-covered (no drops in capture) — offer Kevin a clip / dev showcase state. **NEXT = S2-A-M4 (perf + widen-the-gates):** wire the dead `renderDistance`/`weather` config (S1-audit found them unused) + add forced-med/low visual baselines + a tier-transition invariant (S3-flagged: the PerformanceMonitor is a one-way downgrade ratchet with no `onIncline`; real-device calibration is S3, but the gate-hardening is M4) + a perf number. Author via `superpowers:writing-plans` → subagent-driven; visual must hold. **THEN: pre-S2-B content-variety sub-audit** (Kevin greenlit) — content-variety (mob/biome/**music** distinctness) + signature-fires + coherence-gate calibration — runs BEFORE S2-B. **THEN S2-B Aspects** (Wildheart lead, deep-before-next). **Content-diversity passes now NAMED in `SOTA-INITIATIVE.md`** (Kevin agreed 2026-06-03): music→per-Aspect motif folded into S2-B · mob designs→after S2-B3 Soulbind · landscape→late-S2+S3. **Batched Kevin decisions open (KEVIN-REVIEW-BATCH):** siege/reward tuning · loot-juice look eyeball · **music treatment + tooling (ElevenLabs key available — Kevin asked; my answer in-thread, decide-later)**. **DEFERRED to S3 (touch):** `world/Terrain.jsx:253,556` raw pointer-lock reads + the `setIsPointerLocked`→`setInputActive` rename. `challenge-memory` audit → when ALL of S2 done.

> **⏭️ (history) S2-A-M3c RESUME: S2-A-M3b (night SIEGE + reward + currency) DONE + merged to `main`** (branch `s2a-m3b-night-siege`; plan `docs/superpowers/plans/2026-06-03-crafty-s2a-m3b-night-siege.md`). M3 decomposed → **M3a clock ✅ / M3b siege ✅ / M3c loot-juice NEXT**. M3b (Kevin: soft death / reward=all / proceed): **currency** (`coins` + `addCoins`, persisted, HUD readout); **escalating night siege** (pure `siegeParams(nightCount)`→hostileChance/maxMobs ramp+cap, wired into `SimplifiedNPCSystem`; `nightCount` lifted to the store as single SoT for survival-hook + spawn-loop); **survive-to-dawn reward** (`grantDawnReward` = scaling XP + coins + guaranteed rarity-climbing loot, once-per-dawn); **soft death LOCKED** by a characterization test. Review caught + I fixed 2 MAJORs: removed the night→`dangerLevel` bridge (no-op for mood + stomped the boss's obsidian → **boss is now SOLE `dangerLevel` writer**; night danger = siege ramp + dusk mood; obsidian = boss signature), and **persisted `nightCount`+`lastRewardedNight`** (siege/reward progression now durable; reward guard store-side). `test:unit` **507** · build clean · `test:visual` **12/12 no re-baseline**. **NEXT = S2-A-M3c (loot juice):** rarity-colored drop-beams (reuse the locked palette) + pickup pop/sound — grep confirms NO beam/pickup VFX exists today; capture-guard any world-space VFX so the 12 baselines hold. Author via `superpowers:writing-plans` → subagent-driven. Then M4 (perf + widen-the-gates: forced-med/low baselines + tier-transition invariant + wire dead `renderDistance`/`weather`). **Pre-S2-B sub-audit queued** (Kevin greenlit): content-variety (mob/biome/audio distinctness — see review-batch) + signature-fires + coherence-gate calibration — BEFORE S2-B. **Batched Kevin decisions open:** siege/reward tuning · deep-night-obsidian? (default no) · the 3 content-diversity passes (mob designs / landscape / music — Kevin asked when; folded as S2-B-adjacent/late-S2, surfaced by the pre-S2-B audit). **DEFERRED to S3 (touch):** `world/Terrain.jsx:253,556` raw pointer-lock reads + the `setIsPointerLocked`→`setInputActive` rename. `challenge-memory` audit → when ALL of S2 done.

> **⏭️ (history) S2-A-M3b RESUME: S2-A-M3a (day/night CLOCK) DONE + merged to `main`** (branch `s2a-m3a-daynight-clock`; plan `docs/superpowers/plans/2026-06-03-crafty-s2a-m3a-daynight-clock.md`). M3 (stakes loop) decomposed → **M3a clock ✅ / M3b siege / M3c loot-juice**. M3a: day↔night now AUTO-CYCLES (Kevin's "still day after the dragon" fixed) via `src/game/useDayNightClock.js` (coarse 1s `setInterval`, NOT useFrame — Game-Loop-Isolation safe; pauses in menus/click-to-play/death/capture via the pure `shouldAdvanceClock` decider reusing the M2d `active` SoT). Fixed a latent flip bug: `setGameTime` now flips `isDay` on half-cycle **CROSSING** (`crossedHalfCycle`), not brittle exact-`%600`-landing (a resumed save at `gameTime=437` never flipped before). On load, `isDay` is now DERIVED from `gameTime` (`isDayAtUnit`) so resumes are phase-consistent. Pure module `src/game/dayNight.js`. `test:unit` **478** · build clean · `test:visual` **12/12** (ticker paused in capture). 3-lens review APPROVE + my fix-up (wired `isDayAtUnit` on load + pure `shouldAdvanceClock`) — all re-verified by me. **KNOB (batched): 5-min full cycle (`GAME_UNITS_PER_SECOND=4`).** **NEXT = S2-A-M3b (night SIEGE + survive-to-dawn + reward):** `useSurvivalMode` is a STUB today (nightCount + toast; the night-danger interval body is EMPTY) — build escalating night spawn-ramp (per-night) tied into the `dangerLevel`→obsidian bridge (night=1, deep/boss=2) + a dawn-survival reward; **death=soft vs hard is a Kevin call (batched, default SOFT).** Then M3c (loot juice: rarity drop-beams + pickup feedback — grep confirms NONE today). Author via `superpowers:writing-plans` → subagent-driven; visual 12/12 must hold. Then M4 (perf + widen-the-gates). **DEFERRED to S3 (touch), logged in ROADMAP:** `world/Terrain.jsx:253,556` raw `document.pointerLockElement` reads (M2d) + the `setIsPointerLocked`→`setInputActive` prop-chain rename. **Pre-S2-B sub-audit queued** (Kevin greenlit): content-variety + signature-fires + coherence-gate calibration — BEFORE S2-B. `challenge-memory` audit → surface when ALL of S2 done.

> **⏭️ (history) S2-A-M3 RESUME: S2-A-M2d (single pointer-lock authority) DONE + merged to `main`** (branch `s2a-m2d-input-active-gate`; plan `docs/superpowers/plans/2026-06-03-crafty-s2a-m2d-input-active-gate.md`). Collapsed the DUAL pointer-lock authority onto `inputState.active` (single SoT) + a `useSyncExternalStore` reactive projection (`src/input/useActiveInput.js`); deleted InputManager's duplicate `pointerlockchange` listener + its `isPointerLocked` useState (now 0 raw `document.pointerLockElement` reads); Components.jsx is the SOLE authoritative listener. `test:unit` **451** · build clean · `test:visual` **12/12**. 4-lens adversarial review (spec/quality APPROVE, architecture MINOR, parity APPROVE) — all gates re-verified by me. **NEXT = S2-A-M3 (stakes loop):** the core day→build→**night-SIEGE**→survive-to-dawn loop — INCLUDING wiring the day/night clock tick Kevin asked about (the `setGameTime` auto-toggle EXISTS — flips `isDay` every 600 ticks — but nothing TICKS `gameTime` → permanent day; M3 owns making time advance) + escalating night danger (bridge into the existing `dangerLevel`→obsidian-mood already wired) + loot juice (rarity beams/pickup feedback). Author via `superpowers:writing-plans` → subagent-driven; visual 12/12 must hold. Then M4 (perf + widen-the-gates: forced-med/low baselines + tier-transition invariant + wire dead `renderDistance`/`weather`). **DEFERRED to S3 (touch), logged in ROADMAP:** `world/Terrain.jsx:253,556` block-highlight + block-place/break still read raw `document.pointerLockElement` (M2d reviewers caught the plan's "single SoT" overclaim — these are canvas-layer, pre-exist `main`, NOT a regression; migrating the CLICK gate to `getInput().active` needs optimistic-lock-window analysis = S3 work + rename the `setIsPointerLocked`→`setInputActive` prop chain coherently then). **Pre-S2-B sub-audit still queued** (Kevin greenlit): content-variety + signature-fires + the coherence-gate calibration run — BEFORE S2-B. **Pending Kevin decisions (review-batch):** minimal ambient day/night before M3? (M3 now subsumes the clock tick) · projectile-variety signature pass (scheduled after S2-B) · the 4-Aspect node taxonomy. `challenge-memory` audit → surface when ALL of S2 is done.

> **⏭️ (history) S2-A-M2d (was-RESUME): S2-A-M2c (A4 talent→4-Aspect-trees) DONE + merged to `main` (merge `849d1fa`).** The talent tree is now LIVE (was 10/11 inert): `src/game/talentTree.js` `ASPECT_TREES` (4 trees Voidhand/Wildheart/Soulbind/Elemancer, 12 foundational `+stat/rank` nodes) folded through `getEffectiveAttributes` (derive-never-bake) → reach combat dmg/crit/mitigation AND maxHealth/maxMana; single `TALENT_LIMITS`; `refundUnknownTalents` migration on load; panel renders 4 trees. 443 unit · 12/12 visual. **NEXT = S2-A-M2d (InputManager `active`-gate migration)** — the queued M2 scope item (grounded in the M2a recon's input-manager reader): `src/InputManager.jsx` (a LIVE hook, App.jsx:131) handles ONLY UI/menu hotkeys + pointer-lock (zero movement keys — those are in Components.jsx, M1-migrated). It holds its OWN `isPointerLocked` useState + **6 raw `document.pointerLockElement` reads** + a 2nd `pointerlockchange` listener — OUTSIDE the M1 `inputState.active` gate. Migration: make Components.jsx the SOLE `setActive` writer, DELETE InputManager's pointerlockchange listener, replace its 6 internal reads with `getInput().active`, BUT keep a thin reactive `isPointerLocked` shim for App's click-to-play render gate (`getInput().active` is non-reactive — can't drive React render). Preserve the optimistic-set + fail-locked behaviors consciously (behavior-parity, not mechanical). Extend `input-abstraction-gates.test.js` to assert ≤1 pointerLockElement read in InputManager. Author via `superpowers:writing-plans` → subagent-driven; visual 12/12 must hold. Then M3 (stakes loop: day→build→night-SIEGE + the day/night clock tick + loot juice) → M4 (perf + widen-the-gates). **Landed this session beyond the milestones:** quest+achievement persistence (`97f645a`), boss→obsidian bridge (`a428df7`). **Pending Kevin decisions (review-batch):** minimal ambient day/night cycle before M3? · projectile-variety signature pass before/after S2-B? · the 4-Aspect-tree node taxonomy (tune numbers/names). `challenge-memory` audit → surface when ALL of S2 is done.

> **⏭️ S2-A-M2b RESUME (compaction-safe, 2026-06-03): S2-A-M2b (build axis A7) DONE + merged to `main` (merge `f6665d0`).** M2 decomposition (adjusted flexibly): M2a (save core) ✅ / M2b (build axis A7) ✅ / **M2c (talent→4-Aspect-trees A4) NEXT** / M2d (InputManager `active`-gate migration). M2b closed the fight→loot→equip→**ALLOCATE**→fight-harder loop: shared pure `src/game/equipment.js` (`getItemSlot` + `getWeaponBaseDamage`) DRY'd the duplicated melee ladder + local getItemSlot; the Core Attributes panel gained a points banner + per-attribute "+" buttons (gated on `attributePoints>0` so the 0-point `inventory-open` visual fixture is byte-identical) wiring the existing `allocateAttribute` action — **level-up points are now spendable** (were accumulating unspent → the build axis dead-ended). `test:unit` **423** · build clean · `test:visual` **12/12**. Plan: `docs/superpowers/plans/2026-06-03-crafty-s2b-build-axis.md`. **Deferred from A7 (low-value hygiene):** loot→bucket routing + `addToInventory` dropped-rarity arg (works today — looted gear lands in `inventory.blocks` which the bag renders + equip reads); items.js↔EQUIPMENT_STATS dual-registry merge. **NEXT = S2-A-M2c (A4):** extract the inline 11-node `branches` array (`AdvancedGameFeatures.jsx:1104`, inside `SpellUpgradePanel`) → a module-level `ASPECT_TREES` data export grouped into the 4 Aspects (Voidhand/Wildheart/Soulbind/Elemancer); a data-driven effect table folded through `getEffectiveAttributes` (DERIVE not bake — mirror the M2a frost_shield fix; the `spendTalentPoint` dual-limits map collapses to the single `ASPECT_TREES` def); the panel renders the 4 trees. Per-Aspect SIGNATURE ability nodes are S2-B — A4 is the meaningful-stat-node scaffold (damage%/crit%/armor/maxMana/regen/cooldown). **The 4-tree node taxonomy + numbers are a TASTE call → batch to KEVIN-REVIEW-BATCH (proceed on best judgment; reversible data table).** Author via `superpowers:writing-plans` → subagent-driven build. Then M2d (InputManager `active`-gate migration), M3 (stakes loop: day→build→night-SIEGE + `dangerLevel`→obsidian bridge + loot juice), M4 (perf + widen-the-gates).

> **⏭️ (history) S2-A-M2a RESUME (2026-06-03): S2-A-M2a DONE + merged to `main` (merge `018f0dc`).** M2 was decomposed → **M2a (progression-persistence core + save consolidation — DONE)** / M2b / M2c. M2a delivered the A3 comprehensive save + the slop teardown Kevin authorized: a single canonical serializable **progression slice** (store-owned; persist BASE, derive effective-attrs/maxStats/talent-effects on read — never bake), **store = single source of truth for level/XP** (native `grantXP`; SimpleExperienceSystem → VFX-only), ONE `buildSaveData` serializer (deleted the dead axios `saveGame`/`loadGame` + the 4 duplicated payload literals), `loadWorldData` restores the **full slice + chests + position**, **local-first autosave** (new `src/game/{progression,saveSchema,worldSaves,autosave,invNormalize}.js` + `store.saveActiveWorld`; WorldManager consolidated −163 lines, cloud-axios marked S4-deferred), de-baked `frost_shield`, killed the GameSystems HP-ratchet. `test:unit` **406** · build clean · `test:visual` **12/12**. Plan: `docs/superpowers/plans/2026-06-03-crafty-s2a-m2a-progression-save-core.md`. **NEXT = S2-A-M2b:** **build axis A7** (allocate-attribute "+" UI button [the `allocateAttribute` store action EXISTS but is unwired] + weapon-dmg-ladder DRY [duplicated in Components.jsx + GamePanels.jsx] + loot→equip-bucket routing [`addToInventory` always writes `blocks`, ignores slot/rarity] — NOTE the equipment fold + the full GamePanels paper-doll UI + GearInspector ALREADY EXIST, A7 completes the player-facing loop + persistence already landed via M2a) **+ talent→4-Aspect-trees A4** (extract `ASPECT_TREES` module [Voidhand/Wildheart/Soulbind/Elemancer] from the inline 11-node array in `AdvancedGameFeatures.jsx:1104` + a data-driven effect table folded through `getEffectiveAttributes` [derive, don't bake — mirror the M2a frost_shield fix] + the 4-tree panel; the per-Aspect SIGNATURE ability nodes come in S2-B, A4 is the meaningful-stat-node scaffold — the node taxonomy/numbers are a TASTE call → batch to KEVIN-REVIEW-BATCH). Author the M2b plan via `superpowers:writing-plans` → subagent-driven build. **M2a carry-forward — ✅ RESOLVED 2026-06-03** (Kevin pull-forward, merge `97f645a`): quest progress + achievements now persist via a store `questState` mirror + load-resync tick + autosave trigger (QuestSystem's divergent local state mirrored → serialized → restored). "Comprehensive save" is now honest. 413 unit · 12/12 visual. **Process lesson reinforced this build:** a subagent FALSELY reported "6 failing tests" — VERIFY test state myself at every task boundary (subagent green AND red claims are both unreliable). Then M3 (stakes loop: day→build→night-SIEGE + `dangerLevel`→obsidian bridge + loot juice) → M4 (perf + widen-the-gates: forced-med/low baselines + tier-transition invariant + wire dead `renderDistance`/`weather` config). Then S2-B Aspects (Wildheart lead).

> **⏭️ (history) S2-A-M1 RESUME (2026-06-02): S2-A-M1 DONE + merged to `main`.** M1 (combat spine + input abstraction): input-intent module (`src/input/inputState.js`) · verbs routed through intents (pure refactor, visual 12/12 unchanged) · `src/combat/cone.js` + melee-hits-boss · boss-music key fix · ribbon-index fix. `test:unit` **360** · build clean · `test:visual` 12/12. **NEXT = S2-A-M2** (per the S1 audit `memory/REALITY-AUDIT-S1-2026-06-02.md`): the **comprehensive save** (level/XP/attributes/equipment/talents/**chests** all lost on reload + no live save trigger — A3) + wire the talent tree as **Aspect trees** (A4) + attribute/equipment build axis (A7). Author the M2 plan via `superpowers:writing-plans` → subagent-driven build. Then M3 (core loop + stakes: day→build→night-SIEGE + `dangerLevel` bridge + loot juice) → M4 (perf + gate hardening: wire dead `renderDistance`/`weather` config + forced-med/low baselines + tier-transition invariant). Then S2-B Aspects (Wildheart lead). **M1 nits DONE** (`7497b95`: dead `jumpRequested`/`dodgeRequested` refs removed; cone `angleRad<π` + reserved-intents docs; 360 unit · visual 12/12). **Still queued for M2 (scope, not a nit):** migrate `InputManager.jsx` (a 2nd input path that still reads `document.pointerLockElement` + holds its own input `useState`) onto the `inputState` module so it's the sole input authority. **Deferred:** vitest 3→4 security bump (task #32) + tier-calibration (S3, real-device profiling).

> **⏭️ S2-A-M1 BUILD RESUME (compaction-safe, 2026-06-02):** plan = `docs/superpowers/plans/2026-06-02-crafty-s2a-m1-combat-spine-input.md` (Combat spine + input abstraction). Build via **subagent-driven-development** (Opus implementer + spec + quality review per task; TDD red-first; sequential — all tasks share `Components.jsx`; NO Claude footer; fix-ups = NEW commits). **✅ T1 DONE** (`d3a7d6e`: input-intent module `src/input/inputState.js` + colocated test; `vitest.config.js` `include` extended to `src/**` so colocated pure-logic unit tests run — the convention for new combat/input pure modules; `tests/**` stays for gates/visual). **✅ T2 DONE** (`a4e3268`: routed movement/dodge/attack/cast through the intent module + centralized pointer-lock to one writer; static gate `tests/gates/input-abstraction-gates.test.js`; 343 unit · build clean · **visual 12/12 UNCHANGED = behavior parity proven**; minor cosmetic: `jumpRequested`/`dodgeRequested` refs now unused-but-kept → cleanup-eligible later). **✅ T3 DONE** (`2769192`: `src/combat/cone.js` pure helper + 8 tests; melee now hits BOTH mobs + boss; 351 unit · build clean · visual 12/12). **Next →** T4 boss-music key sync (`SoundManager.jsx:40` reads store `bossActive` VALUE but it's never written — `AdvancedGameFeatures.jsx:90` has a LOCAL `useState`; add store `bossActive`+`setBossActive`, make `isBossActive()` read it, call on boss spawn/despawn) → T5 ribbon-index fix (`Components.jsx` `indices[i*6+5]` bug) → T6 wrap (4-piece docs + merge to main + flush) → T3 pure cone helper + melee-hits-boss → T4 boss-music key sync → T5 ribbon-index fix → T6 wrap (docs + merge + flush). **Grounded anchors in the plan** (dodge/swing already EXIST — wire don't rebuild; boss outside ECS; boss-music local-vs-store key mismatch; ribbon `indices[i*6+5]` bug). Branch: `s2a-m1-combat-spine` off `main`. After M1: S2-A-M2 (save+build axis) → M3 (core loop+stakes) → M4 (perf+gate hardening), then S2-B Aspects (Wildheart lead). S1 audit ledger (folded into S2-A): `memory/REALITY-AUDIT-S1-2026-06-02.md`.

> **⏭️ S2 RESUME (compaction-safe, 2026-06-02): the S2 game-design SPEC is the design of record at `docs/superpowers/specs/2026-06-02-crafty-s2-game-design-design.md`.** Decisions locked WITH Kevin: (1) **Foundation-first** — build the signature-agnostic core loop **S2-A** before any signature (combat-feel fixes · dodge-roll + swing-cone + melee-hits-boss · **the progression-SAVE fix** · wire the talent tree as four Aspect trees · a day→build→night-SIEGE→survive-to-dawn stakes loop · rarity loot-beams · attribute/equipment build · input-abstraction · a real-device perf number). (2) The signature = a suite of **Aspects** built SEQUENTIALLY (each deep+shipped+fun before the next): **S2-B1 WILDHEART** (beast-transform — the LEAD, lowest-risk, zero re-mesh) → **S2-B2 VOIDHAND** (Kinetic/gravity-hand — combat-anchor + base-as-anvil) → **S2-B3 SOULBIND** (capture+fuse creatures — retention layer) → **S2-B4 ELEMANCER** (Elemental/reactive terrain — LAST, gated behind the S2-A perf number + overlay-first, the only re-mesh kill-risk). Mockups (gitignored): `.superpowers/s2-signature-mockups/board-1..7-*.png` + `matrix.png`. **⏭️ NEXT after Kevin approves the spec: `superpowers:writing-plans` for S2-A → subagent-driven build. (Parked schools/modes: TRAPSMITH [S4 social/virality], SKYTURN, PRISM CHORUS; folded: JUGGERNAUT→VOIDHAND, glyph-carving + sculpt-spells → sub-features.)**

> **(history) ALL OF S1 COMPLETE + studio mote-isolation closed + mob-outline regression FIXED; S2 design-analysis + mockups done.**

> **🔧 ALSO FIXED (2026-06-02, Kevin caught): mob/character ink-outline regression** — outlines appeared at spawn then vanished mid-session. Cause: `TIERS.low.charOutline` was false + `PerformanceMonitor.onDecline` ratchets the tier one-way to `low` under FPS pressure → outlines unmount permanently. Fix: `low.charOutline → true` (tier-independent signature outline); red-first gate in `character-render-gates.test.js`; unit 332, build clean (visual forces high → baselines unaffected). **S3 flags (in KEVIN-REVIEW-BATCH, need real-device profiling):** one-way downgrade ratchet (no onIncline); tier calibration (CORRECTED: `high` IS reachable on modern desktop Chrome — deviceMemory 16/32 per MDN; my earlier ≤8/unreachable claim was stale); Safari/FF→low; one-way ratchet (no onIncline); no forced-med/low baseline. Defer-S3 + profile. Full S1 audit → `memory/REALITY-AUDIT-S1-2026-06-02.md`.

> **⏭️ RESUME (compaction-safe, 2026-06-02): S1 COMPLETE; on `main` (`f386223` + the mote-isolation fix). NEXT = S2.** Kevin's calls this session: (a) **`challenge-memory` POSTPONED** — do NOT run it at the S1 boundary; surface it for Kevin's decision *when S2 is done*. (b) **Studio-fixture mote-isolation = FIXED** (was an open tunable): a dedicated `captureStudio` store flag (the 3 studio-card hooks SET it; `GameScene` gates `<LightMotes>` off; in-world hooks reset it) suppresses the warm motes in the 3 sky-studio gate frames (character/boss-closeup, spell-cast) while preserving them in all in-world frames; new gate `tests/gates/atmosphere-isolation-gates.test.js` (5 tests); 3 studio frames re-baselined mote-free; `test:unit` **331** · build clean · `test:visual` **12/12**. **ONE Kevin-tunable still open (low-pri, KEVIN-REVIEW-BATCH):** the magic-hour color band (premium-vs-candy; knobs `src/render/mood.js MOOD_GRADE` + `LightMotes.jsx uScale`). **⏭️ NOW = S2 (game design / core loop)** per `SOTA-INITIATIVE.md` §4/§7-equivalent: this is a DESIGN phase (HARD GATE per master plan: lock vision → spec → Kevin approves → then build; game-design has product/taste decisions → batch them for Kevin like the visual direction). Ground in `memory/REALITY-AUDIT-2026-05-30.md` + `MONETIZATION-VIRALITY-SCAN-2026-05-30.md` + the actual current core loop. (After S2: S3 engine — touch input [biggest gap] / de-monolith god-files / ECS / WebGPU-TSL; S4 multiplayer + monetization — surface the monetization plan to KEVIN-REVIEW-BATCH before any store/pricing.)

> **⏭️ S1-D RESUME (2026-06-02):** branch `s1d-signatures`. Plan: `docs/superpowers/plans/2026-06-02-crafty-s1d-signatures.md` (M1 spine → M2 cast-arc → M3 atmosphere → M4 mascot[Kevin]). **M1 DONE (`baca400`):** routed all 5 spell-impact sites through the existing GPU spark pool (was rendering 25-40 React-sphere slop/cast); **fixed a real 35ms main-thread busy-wait hitstop** (`SimplifiedNPCSystem.jsx:833`) → non-blocking `hitstopUntil` store flag (player loop clamps motion); camera-shake + non-blocking bloom-spike (driven via EffectComposerContext — `<Bloom>` ref crashes postprocessing@3.0.4, documented); deleted SpellTrail/SpellImpact/ImpactParticle slop → stretch-billboard trail + pooled shockwave ring + light-pop. 9 new gates (red-first) + `test:unit` 305 + build clean + `test:visual` 10/10 (spells not in capture states → no re-baseline). **NOT eyeballed: the actual spell-VFX LOOK** (no rendered cast frame). **NEXT = M2 (cast-arc):** FIRST add a deterministic `spell-cast` capture state (frozen mid-flight projectile + seeded spark burst) → eyeball M1's look + batch for Kevin; THEN telegraph→charge→release arc (rune-circle default, batch shape-vocabulary). Then M3 (atmosphere: light motes + god-rays-MED + per-mood LUT + height fog), M4 (mascot — render A/B/C → Kevin picks). **⏰ When S1-D done → remind Kevin: challenge-memory audit (S1 complete).**

> **⏰ KEVIN REMINDER (set 2026-06-02, per Kevin's request):** when the ENTIRE S1 is done (i.e. S1-D complete → before S2), **surface a recommendation to run a `challenge-memory` adversarial audit** of the native memory dir (the truth-audit ritual in `feedback_memory_hygiene.md` — distinct from Auto Dream; memory dir is now git-backed). Do NOT skip this at the S1-D→S2 boundary.

> **⚠️ S1-D SCOPE CORRECTED (verified 2026-06-02):** `<Atmosphere>` is ALREADY rendered unconditionally in gameplay (`GameScene.jsx:680`, not capture-gated) — "Atmosphere always-on" is already DONE; don't re-build it. **Real S1-D signature gaps:** (1) **Spell-VFX lead** — the combat-feel "wow" pass (fireball/ice/lightning/arcane impact, trails, screen-feel) on the existing EnhancedMagicSystem; (2) **Atmosphere ELEVATION** — push the always-on atmosphere toward the "wow" signature (god-rays/sky/grade richness at appropriate tiers); (3) **Mascot** — the title mascot is a placeholder wizard-hat (M3); a real signature CHARACTER is a HIGH-TASTE design decision → propose/mockup + batch for Kevin's visual direction (master plan: "explore vision WITH Kevin; lock vision before build"), do NOT autonomously finalize the mascot. Build the buildable (spell-VFX, atmosphere-elevation) autonomously + batch frames; mascot = Kevin's taste call.

> **⏭️ RESUME (compaction-safe, 2026-06-02): S1-C is DONE + MERGED to `main`. Next = S1-D.** Master plan = `SOTA-INITIATIVE.md` (repo ROOT): S1-C ✅ → **S1-D** → S2 → S3 → S4. **S1-C-M3 (icons + emoji-decouple) COMPLETE + MERGED** (plan `docs/superpowers/plans/2026-06-01-crafty-s1c-m3-icons-emoji.md`; contract `…-icon-registry-contract.md`): `src/` emoji 215→0; 19 new filled game-icons.net glyphs (CC BY 3.0) in `gameIcons.js`; `src/data/items.js` registry = single source for name/icon/rarity (dup `getItemRarity`/`getItemEmoji` killed); loot/recipe identity decoupled + save normalizer; **deliberate rarity FIX** (Golden Crown/Star Fragment→legendary etc., visible in inventory); Credits screen (CC BY); **zero-emoji hard gate GREEN** (both S1-C gates assert, 0 todos); 5-lens adversarial review → no BLOCKING, all minor findings fixed; `test:unit` **296/0todo** · `test:visual` **10/10**. **NEXT = S1-D (signatures)** per `SOTA-INITIATIVE.md`: (1) **Atmosphere always-on** (the `<Atmosphere>` gradient-sky/mood system from S1-B M2a → wire as the default, not capture-only), (2) **Spell-VFX lead** (the signature combat-feel pass — fireball/ice/lightning/arcane VFX polish), (3) **Mascot** phase-2 (replace the placeholder wizard-hat title mascot with the real signature character). Author the S1-D plan via `superpowers:writing-plans` → subagent-driven build. Then S2 (game design/core loop), S3 (engine: touch input [biggest gap], de-monolith god-files, ECS hardening, WebGPU/TSL), S4 (multiplayer + monetization — surface the monetization plan to KEVIN-REVIEW-BATCH before any store/pricing). Review/decide items batch in `docs/superpowers/KEVIN-REVIEW-BATCH.md`.

> **AUTONOMOUS MASTER-PLAN RUN (Kevin 2026-06-01): "keep building the entire plan, all approvals, batch reviews for the end, rigorous TDD, update docs + pre-compact-flush after every major phase, keep building even after compaction."** Master plan = `SOTA-INITIATIVE.md` (S1-C → S1-D → S2 → S3 → S4). Review/decide items accumulate in `docs/superpowers/KEVIN-REVIEW-BATCH.md`. **S1-C UI consolidation is COMPLETE** — one bold-flat language across the entire in-game UI (HUD/modals/quests/achievements/boss/pets/spells/NPC-trade); the 3 legacy languages (minecraft-bevel/glass/neon) are gone; the single-UI-language hard gate is GREEN. **RESUME POINTER (compaction-safe): next = S1-C-M3** — author `docs/superpowers/plans/2026-06-01-crafty-s1c-m3-icons-emoji.md` via `superpowers:writing-plans`, then subagent-driven build. M3 scope: (1) **emoji-DATA-decouple** — emoji are baked into data structures as match-keys (loot-table item names like `'🥩 Raw Porkchop'`, crafting-recipe patterns, `getItemRarity()` `includes('💎')`, duplicate `getItemEmoji()` mappers in SimplifiedNPCSystem + GamePanels, achievement/quest titles, damage-number emoji); decouple to stable item/spell/mob IDs + a single centralized id→game-icon mapper (the `Icon` primitive is already game-icons-capable from M1); (2) replace ALL remaining functional + decorative emoji with `Icon`/game-icons; (3) **flip the zero-emoji `it.todo` hard gate GREEN** in `tests/gates/static-gates.test.js`; (4) add the **game-icons.net CC BY 3.0 credits screen** (owed since M1). Then **S1-D** (signatures: Atmosphere always-on / Spell-VFX lead / Mascot), **S2** (game design/core loop), **S3** (engine: touch input [biggest gap], de-monolith god-files, ECS hardening, WebGPU/TSL), **S4** (multiplayer + monetization — surface the monetization plan to the review batch before any store/pricing wiring). **CAUTION for M3 (the trap M2 hit):** the emoji burn-down must grep ALL emoji incl. those baked into DATA (not just JSX), and the decouple must preserve the loot/recipe/rarity MATCH logic (the emoji are load-bearing match-keys today).

**S1-C-M2c status (2026-06-01): ✅ DONE + MERGED.** Neon surfaces → bold-flat; last `.game-panel` glass gone; **single-UI-language hard gate GREEN**; 3D entities untouched; `test:visual` 10/10, `test:unit` 91+1todo. Plan: `docs/superpowers/plans/2026-06-01-crafty-s1c-m2c-neon-and-gate.md`.

**S1-C-M2b status (2026-06-01): ✅ DONE + MERGED.** All 5 glass modals → bold-flat (Inventory mirrors showcase on real data; Crafting/Magic/Building/Settings); `.game-panel-item` glass removed; `inventory-open` capture state; handlers preserved; `test:visual` 9/9. Plan: `docs/superpowers/plans/2026-06-01-crafty-s1c-m2b-modals.md`.

**S1-C-M2a status (2026-06-01): ✅ DONE + MERGED.** HUD migrated to bold-flat (StatBars w/ icons, Slot hotbar, bold-flat spell chip + minimap/compass + XP/level), fake `MinecraftHealthHunger` + ❤/🍖 emoji + `.minecraft-*` bevel CSS removed (App.css 440→238), handlers preserved, `test:visual` 8/8 (explore-day/night/boss-obsidian re-baselined). Plan: `docs/superpowers/plans/2026-06-01-crafty-s1c-m2a-hud-consolidation.md`.

**S1-C-M1 status (2026-06-01): ✅ DONE + MERGED to `main`.** The LOCKED **bold-flat** token foundation + design language is built, wired, and tested. Delivered (plan: `docs/superpowers/plans/2026-06-01-crafty-s1c-m1-ui-foundation.md`): the **token→CSS-vars→Tailwind SoT chain** (`tokens.js` → `cssVars.js` `--ui-*` + `applyThemeVars()` → `tailwind.config.cjs`, parity-tested), **7 component primitives** (`src/ui/primitives/` Panel/Button/Slot/StatBar/Icon/Toast/Tooltip + `cn()` + barrel) + `LocaleToggle` + DEV `PrimitivesShowcase`, the **i18n layer** (`src/i18n/`, en default + lazy-CJK zh-CN toggle → English fetches zero CJK), self-hosted fonts (Lilita One + Space Grotesk eager; 得意黑 + zh-body lazy). Gate: `test:unit` 81+2todo · `npm run build` clean · `test:visual` **8/8** (incl. 2 new `primitives-showcase-{en,zh}`). Design language locked in `docs/superpowers/specs/2026-06-01-crafty-s1c-ui-system-brief.md` §9 (reference comps: `s1c-ui-reference/LOCKED-bold-flat-{en,zh}.png`).

**FIDELITY PASS DONE (2026-06-01, merged):** Kevin caught that the first showcase diverged from the locked `final-A` comp (flatter/darker, border-only rarity, generic icons). Fixed by re-valuing the token palette to the comp's exact lighter-slate ladder (so the ink borders + offset shadows pop), 2-tone game icons baked from the comp, rarity-FILLED Slot + SpellRing, and a full showcase rebuild mirroring `final-A` over the game scene — **now a faithful match in both locales (human-verified; the earlier ink-contrast concern is RESOLVED — root cause was a too-dark palette, not the token spec)**. **ALL tech-debt cleared (no punting):** real Alibaba PuHuiTi 3.0 woff2 (replaced Noto stand-in) · explore-night flake frozen-deterministic (3.84%→0.66%) · Toast `role=alert` · showcase DEV-gated out of prod. Gate now `test:unit` 89+2todo · `test:visual` 8/8 · build clean; final whole-branch review APPROVED.

**NEXT = S1-C-M2 (consolidate surfaces):** migrate HUD → modals → panels to the unified bold-flat primitives; kill the 3 clashing legacy languages (minecraft-bevel/glass/neon); migrate the ~40-50 UI-chrome hardcoded hex (`GamePanels.jsx` etc.) to tokens; flip the single-UI-language `it.todo` hard gate green. Then M3 (icon system + emoji-data-decouple, zero-emoji gate; the `Icon` primitive is already game-icons-capable — M3 formalizes + does the emoji-data refactor + credits screen for game-icons CC BY). Author the M2 plan via `superpowers:writing-plans` → subagent-driven build. **Residuals RESOLVED (2026-06-01):** explore-night capture is now deterministic — **~0.06% self-diff (from ~0.5%)** — by gating, in capture mode only, the compass chest-marker + a second per-frame mob spawner (`SimplifiedNPCSystem` `SpawnerSystem`) + the QuestSystem chest spawners + seeding grass-cloud particles + tightening the terrain-stable wait (gameplay byte-identical); also removed the stale `QuestSystem.jsx` debug log. All M1 residuals + tech-debt now clear. Exploration mockups + the `mockup.html` fidelity source in `.superpowers/s1c-mockups/` (gitignored).

---

### (archived) S1-B Render recipe — DONE + merged

**Status:** ✅ **M2b MERGED to `main`** (branch `s1b-m2b-character-render`, 2026-05-31) — the **character render language** (toon + fresnel rim + inverted-hull outlines; boss emissive telegraph preserved). Built autonomously via subagent-driven-development (Opus per-task; spec + quality reviews all APPROVED; every baseline self-gated visually). `test:unit` **40+2todo** · `npm run build` clean · `test:visual` **6/6** (menu / explore-day / explore-night / boss-obsidian / **character-closeup** / **boss-closeup**). M1 (render correctness) + M2a (mood/atmosphere + Caribbean art) merged earlier.

- **Spec (approved):** `docs/superpowers/specs/2026-05-30-crafty-visual-direction-design.md`.
- **M1 — DONE:** `docs/.../2026-05-31-crafty-s1b-render-recipe.md` (sRGB decode · N8AO · bloom · SMAA+grade · device tiers).
- **M2a — DONE:** `docs/.../2026-05-31-crafty-s1b-m2a-mood-atmosphere.md`. Delivered: `mood∈[0,2]` model (`src/render/mood.js` — moodRef / moodTarget / sampleMood) + store `dangerLevel`/`setDangerLevel` · **`<Atmosphere>`** (`src/render/Atmosphere.jsx`) = gradient SkyDome + mood-lerped lights/fog off `tokens.PALETTE` (replaced the inline Sky/fog/ternary-lights) · terrain `mood` uniform desaturation (gentle at dusk, strong at obsidian; luminance-preserving so dusk stays readable) · the **bright-Caribbean art pass**: **Neutral (Khronos PBR) tone mapping** (ACES was muting everything → grey "London"; switched via a `<ToneMapping>` composer EFFECT, since `gl.toneMapping` is overridden by the composer) · turquoise water texture · deeper saturated sky · bloom threshold→1.0 (kills the milky sky haze) · **sun-disc mesh + volumetric GodRays** (high-tier) · softer vignette · 4-state capture suite incl. `boss-obsidian`.
- **M2b — DONE:** `docs/.../2026-05-31-crafty-s1b-m2b-character-render.md`. Delivered: `src/render/characterStyle.js` (pure — memoized 2-band toon `DataTexture` + fresnel-rim `onBeforeCompile` + `OUTLINE`/`RIM`/`TOON` config + `flashableMaterial` allow-list) + `MobToonMaterial.jsx` · **mobs** toon body/head/legs + tier-gated rim + `<Outlines>` on body/head; **hit-flash traversal hardened** to a Standard/Toon allow-list (skips the outline ShaderMaterial whose `.color` uniform the old `name!=="eye"` guard would clobber) · **boss** outline on torso+neck ONLY, emissive telegraph BYTE-PRESERVED (no toon), capture freeze-gate + DEV-only `forceBossSpawn` · **chests + pets** outlined · tiers gained `charOutline`(med+)/`charRim`(high) · 2 new deterministic capture states `character-closeup` + `boss-closeup` (both 0.000%).
- **Look-tuning knobs (all in `src/render/characterStyle.js`, Kevin's taste call):** `TOON.shadow` 0.55 (2-band darkness), `RIM` `{color:#bfe2ff, power:2.5, strength:0.35}` (rim is subtle vs the sky backdrop — bump strength to taste), `OUTLINE` thickness in **screen-px** (mob 4 / boss 6 / prop 3; drei `screenspace=false` default), `OUTLINE.color` #0b0e14.
- **Pending look tweak (deferred):** obsidian boss state is dark (intentional cinematic; bump `MOOD_SCALARS.obsidian.ambientIntensity` if gameplay visibility needs it). Pre-existing `QuestSystem.jsx:664` DEBUG console.log to clean up. Gradient sky + god-rays (originally S1-D) were pulled forward into M2a.
- **Art-direction lessons (load-bearing for ALL future render work):** `~/.claude/projects/-Users-kz-Code/memory/feedback_stylized_render_tonemapping.md` — ACES mutes stylized-vivid looks (use **Neutral/AgX**); tone mapping is a composer **effect** under @r3f/postprocessing (not `gl.toneMapping`); **bloom threshold must track scene brightness** (raise exposure → raise threshold, else milky haze); when subjective art feedback won't converge via tweaks, pull REAL **reference images + extract the color/light DNA**, and after **2 failed tweaks STOP + diagnose** (the haze was found by hypothesis-driven isolation, not more tweaking).
- **Verify against:** the visual-regression gate (`npm run test:visual`, 6% threshold, **6 states**) — re-baseline per intended look change, forced `high` tier, **human-reviewed**. Static gates in `tests/gates/character-render-gates.test.js` (mob-AI capture freeze · boss emissive preserved · boss no-toon · boss freeze-before-move).
- **Capture-fixture determinism (load-bearing for any new capture state):** physics is paused → `getMobGroundLevel` raycast is null (place subjects at fixed Y, sky-studio); `performance.now()`/`state.clock` aren't frozen (gate anim useFrames on `isCaptureMode()`); ECS/store-list state LEAKS across states (spawn far apart + clear lists; screenshot before mutating); a competing writer can clobber your list (gate it `!isCaptureMode()`). See `~/.claude/projects/-Users-kz-Code/memory/feedback_visual_regression_fixture_isolation.md`.
- **Foundation (S1-A — DONE on `main`):** tokens `src/theme/tokens.js`, quality tiers `src/render/quality.js`, dev bridge + capture-determinism `src/devtest/`, the deterministic visual gate.

## Backlog (other streams)
- **S1-C** UI design system (one language; kill the 3 clashing languages + emoji — burn-down: 10 emoji-files, 255 hardcoded hex / 15 files).
- **S1-D** signatures (Atmosphere always-on / Spell-VFX lead / Mascot phase-2).
- **S2** game design + core loop · **S3** engine (touch input [biggest gap], de-monolith god-files, ECS hardening, WebGPU/TSL migration) · **S4** multiplayer + monetization (cosmetics+pass, NO gacha).
