# Active Plan

> **Method (session-archivist 4-piece):** this is the volatile current-task POINTER. It points at the detailed `superpowers` plan for the active work — it does NOT duplicate the TDD checklist. Ground truth = git `main` + `docs/superpowers/`.

## Current Task: S1-B Render recipe (Crafty → SOTA) — **M1 + M2a + M2b DONE; S1-C NEXT**

**Status:** ✅ **M2b MERGED to `main`** (branch `s1b-m2b-character-render`, 2026-05-31) — the **character render language** (toon + fresnel rim + inverted-hull outlines; boss emissive telegraph preserved). Built autonomously via subagent-driven-development (Opus per-task; spec + quality reviews all APPROVED; every baseline self-gated visually). `test:unit` **40+2todo** · `npm run build` clean · `test:visual` **6/6** (menu / explore-day / explore-night / boss-obsidian / **character-closeup** / **boss-closeup**). M1 (render correctness) + M2a (mood/atmosphere + Caribbean art) merged earlier.

- **Spec (approved):** `docs/superpowers/specs/2026-05-30-crafty-visual-direction-design.md`.
- **M1 — DONE:** `docs/.../2026-05-31-crafty-s1b-render-recipe.md` (sRGB decode · N8AO · bloom · SMAA+grade · device tiers).
- **M2a — DONE:** `docs/.../2026-05-31-crafty-s1b-m2a-mood-atmosphere.md`. Delivered: `mood∈[0,2]` model (`src/render/mood.js` — moodRef / moodTarget / sampleMood) + store `dangerLevel`/`setDangerLevel` · **`<Atmosphere>`** (`src/render/Atmosphere.jsx`) = gradient SkyDome + mood-lerped lights/fog off `tokens.PALETTE` (replaced the inline Sky/fog/ternary-lights) · terrain `mood` uniform desaturation (gentle at dusk, strong at obsidian; luminance-preserving so dusk stays readable) · the **bright-Caribbean art pass**: **Neutral (Khronos PBR) tone mapping** (ACES was muting everything → grey "London"; switched via a `<ToneMapping>` composer EFFECT, since `gl.toneMapping` is overridden by the composer) · turquoise water texture · deeper saturated sky · bloom threshold→1.0 (kills the milky sky haze) · **sun-disc mesh + volumetric GodRays** (high-tier) · softer vignette · 4-state capture suite incl. `boss-obsidian`.
- **M2b — DONE:** `docs/.../2026-05-31-crafty-s1b-m2b-character-render.md`. Delivered: `src/render/characterStyle.js` (pure — memoized 2-band toon `DataTexture` + fresnel-rim `onBeforeCompile` + `OUTLINE`/`RIM`/`TOON` config + `flashableMaterial` allow-list) + `MobToonMaterial.jsx` · **mobs** toon body/head/legs + tier-gated rim + `<Outlines>` on body/head; **hit-flash traversal hardened** to a Standard/Toon allow-list (skips the outline ShaderMaterial whose `.color` uniform the old `name!=="eye"` guard would clobber) · **boss** outline on torso+neck ONLY, emissive telegraph BYTE-PRESERVED (no toon), capture freeze-gate + DEV-only `forceBossSpawn` · **chests + pets** outlined · tiers gained `charOutline`(med+)/`charRim`(high) · 2 new deterministic capture states `character-closeup` + `boss-closeup` (both 0.000%).
- **Look-tuning knobs (all in `src/render/characterStyle.js`, Kevin's taste call):** `TOON.shadow` 0.55 (2-band darkness), `RIM` `{color:#bfe2ff, power:2.5, strength:0.35}` (rim is subtle vs the sky backdrop — bump strength to taste), `OUTLINE` thickness in **screen-px** (mob 4 / boss 6 / prop 3; drei `screenspace=false` default), `OUTLINE.color` #0b0e14.
- **Pending look tweak (deferred):** obsidian boss state is dark (intentional cinematic; bump `MOOD_SCALARS.obsidian.ambientIntensity` if gameplay visibility needs it). Pre-existing `QuestSystem.jsx:664` DEBUG console.log to clean up. Gradient sky + god-rays (originally S1-D) were pulled forward into M2a.
- **Art-direction lessons (load-bearing for ALL future render work):** `~/.claude/projects/-Users-kz-Code/memory/feedback_stylized_render_tonemapping.md` — ACES mutes stylized-vivid looks (use **Neutral/AgX**); tone mapping is a composer **effect** under @r3f/postprocessing (not `gl.toneMapping`); **bloom threshold must track scene brightness** (raise exposure → raise threshold, else milky haze); when subjective art feedback won't converge via tweaks, pull REAL **reference images + extract the color/light DNA**, and after **2 failed tweaks STOP + diagnose** (the haze was found by hypothesis-driven isolation, not more tweaking).
- **Verify against:** the visual-regression gate (`npm run test:visual`, 6% threshold, **6 states**) — re-baseline per intended look change, forced `high` tier, **human-reviewed**. Static gates in `tests/gates/character-render-gates.test.js` (mob-AI capture freeze · boss emissive preserved · boss no-toon · boss freeze-before-move).
- **Capture-fixture determinism (load-bearing for any new capture state):** physics is paused → `getMobGroundLevel` raycast is null (place subjects at fixed Y, sky-studio); `performance.now()`/`state.clock` aren't frozen (gate anim useFrames on `isCaptureMode()`); ECS/store-list state LEAKS across states (spawn far apart + clear lists; screenshot before mutating); a competing writer can clobber your list (gate it `!isCaptureMode()`). See `~/.claude/projects/-Users-kz-Code/memory/feedback_visual_regression_fixture_isolation.md`.
- **Foundation (S1-A — DONE on `main`):** tokens `src/theme/tokens.js`, quality tiers `src/render/quality.js`, dev bridge + capture-determinism `src/devtest/`, the deterministic visual gate.

## Backlog (other streams)
- **S1-C** UI design system (one language; kill the 3 clashing languages + emoji — burn-down: 10 emoji-files, 255 hardcoded hex / 15 files).
- **S1-D** signatures (Atmosphere always-on / Spell-VFX lead / Mascot phase-2).
- **S2** game design + core loop · **S3** engine (touch input [biggest gap], de-monolith god-files, ECS hardening, WebGPU/TSL migration) · **S4** multiplayer + monetization (cosmetics+pass, NO gacha).
